# Sessão 26-06-04_4 — CDX-308: mesas no SSO (Gate D)

- **Data:** 2026-06-04 · **Módulo:** `mesas` + `accounts` allowlist · **Gate:** D mesas
- **Objetivo:** integrar `mesas.artificiorpg.com` ao SSO central (`accounts.`), validar E2E browser e fechar 1º Gate D.
- **Spec:** `specs/004-mesas-sso-gate-d/{spec,plan,tasks}.md`
- **Dependências:** CDX-307 ✅ (design system real / D038) conforme pedido do mantenedor.

## Escopo
- `apps/accounts`: corrigir open-redirect (D037) em `/api/auth/google` e callback.
- `mesas`: integrar UI/auth quando houver app no monorepo.
- VM/deploy: somente com aprovação formal.

## Estado inicial
- `apps/mesas` **não existe** no monorepo em `C:\projetos\artificio`; existe repo legado `C:\projetos\mesas_rpg_artificio` e deploy vivo em `/opt/artificio/mesas`.
- CDX-307 deixou mudanças locais em `packages/ui`/`midias`; preservar sem sobrescrever.
- `accounts-api` está no ar; smoke atual `health=200`, `login=200`, `/me` sem cookie `401`.

## Plano
1. Corrigir D037 em `apps/accounts/src/app.ts`.
2. Validar build/test de `accounts`.
3. Investigar estrutura do `mesas` legado e decidir se CDX-308 exige importar `mesas` para `apps/mesas` antes de integrar.
4. Pedir aprovação antes de qualquer deploy/write na VM.

## Critério de fechamento
- Allowlist: `return=https://evil.com` cai em `PUBLIC_URL`; `return=https://*.artificiorpg.com/...` permitido.
- Mesas usa `@artificio/ui` e `@artificio/auth`.
- Backend mesas valida `artificio_session` com mesmo `JWT_SECRET`.
- Browser E2E real: login em mesas → accounts → Google → volta logado; rota privada OK logado; deslogado 401/redirect; logout limpa sessão; token/segredo não impresso.

## Log
- 2026-06-04 — Sessão aberta. Bloqueio potencial: `apps/mesas` ausente no monorepo; análise do legado necessária antes de implementar integração.
- 2026-06-04 — Spec 004 criada conforme `new-spec`/`add-module`. D037 implementado em `accounts`: `sanitizeReturnUrl` aceita só HTTPS com hostname terminando em `.artificiorpg.com`; `evil.com` e lookalike bloqueados. Validação: `pnpm --filter @artificio/accounts test` OK (6), `pnpm --filter @artificio/accounts build` OK.
- 2026-06-04 — CDX-308A aprovado pelo mantenedor: importar `C:\projetos\mesas_rpg_artificio` para `apps/mesas` como base do módulo G1. Escopo: local only; sem VM, sem deploy, sem `.env`, sem `.git`, sem `node_modules`, sem `dist/build/coverage`, sem dados/segredos. Após importar, validar árvore, ausência de segredos e build local possível.
- 2026-06-04 — CDX-308A executado. Criado `apps/mesas` com `frontend/`, `backend/`, `database/`, `scripts/`, docs operacionais, `package.json` orquestrador, `module.manifest.ts` e `CONTEXT.md`. `pnpm-workspace.yaml` inclui subpacotes `apps/*/{frontend,backend}`. Pacotes renomeados para `@artificio/mesas-frontend` e `@artificio/mesas-backend`. Ajuste mínimo para build: pin `express-rate-limit` 7.5.1 e cast local do rate limiter para `RequestHandler` Express 4.
- 2026-06-04 — Validação CDX-308A: `pnpm install` OK; `pnpm --filter @artificio/mesas build` OK; `pnpm -w turbo run build --filter=@artificio/mesas` OK; frontend tests OK (3 suites, 13 tests); backend tests OK com env dummy local não secreto (`DATABASE_URL` fake + `JWT_SECRET` fake; 13 suites, 104 tests). Scan: nenhum `.env` real importado; só `.env.example`; `node_modules/dist` são gerados/ignorados. VM/deploy não tocados.
- 2026-06-04 — CDX-308B iniciado. Escopo local only: `apps/mesas` frontend/backend + D037 accounts já local + turbo outputs. Sem VM/deploy/compose legado. Objetivo: mesas consumir `@artificio/ui` e `@artificio/auth`, aposentar login próprio no fluxo principal, backend validar cookie `artificio_session`, docs/env exemplo sem segredo.
- 2026-06-04 — CDX-308B frontend: `@artificio/mesas-frontend` consome `@artificio/ui` (`Header`, `Footer`, `styles.css`) e `@artificio/auth/client` (`useSession`, `redirectToLogin`). Fluxo `/login` e rotas protegidas redirecionam para accounts com `return=https://mesas.artificiorpg.com/...`. Contexto legado `useAuth` mantido como adapter para reduzir blast radius, alimentado pelo SSO central; não usa token local. `SiteHeader/SiteFooter` ficam legados sem uso no shell.
- 2026-06-04 — CDX-308B backend: `authMiddleware` agora delega para `requireAuth` de `@artificio/auth`, lendo cookie `artificio_session`/Bearer; mapeia sessão SSO para `req.user` legado (`userId`, `role`, email/name/avatar) e tenta resolver usuário local por `google_id`/email para preservar FKs/roles `gm/admin`. OAuth Google local em `/api/v1/auth/google` virou redirect compatível para accounts; callback/logout locais retornam 410. `csrfProtection` usa `artificio_session`.
- 2026-06-04 — CDX-308B suporte: `@artificio/auth` ganhou export ESM client-only (`@artificio/auth/client`) e build CJS server-only para backend CommonJS legado; contrato JWT/cookie não alterado. Turbo outputs ajustados para `frontend/dist/**` e `backend/dist/**`.
- 2026-06-04 — CDX-308B env/docs: `apps/mesas/backend/.env.example` documenta `ACCOUNTS_URL` e `JWT_SECRET=mesmo_jwt_secret_do_accounts` (placeholder, sem valor); remove OAuth Google/JWT local do exemplo. `apps/mesas/frontend/.env.example` inclui `VITE_ACCOUNTS_URL`.
- 2026-06-04 — CDX-308B validação local: `pnpm -w turbo run build --filter=@artificio/mesas` OK, sem warning de Turbo output. `pnpm --filter @artificio/accounts test` OK (6/6, allowlist `evil.com` bloqueado). `pnpm --filter @artificio/auth test` OK (3/3). `pnpm --filter @artificio/mesas-frontend test` OK (4 suites, 15 tests; inclui redirect SSO). `pnpm --filter @artificio/mesas-backend test -- --runInBand` OK com env dummy local (14 suites, 106 tests; inclui 401 sem cookie e 200 com `artificio_session`). `rg` sem resíduos `am_session`, `auth/google`, `getGoogleLoginUrl`, `JWT_EXPIRES`, `GOOGLE_CALLBACK` no escopo mesas.
- 2026-06-04 — Browser local: usuário ativou Chrome/Computer, mas ferramentas não ficaram callable nesta sessão (`tool_search` só expôs `node_repl`; Playwright ausente no repo). Tentativa com stubs localhost abortou por `Cannot find module 'playwright'`. Evidência de fluxo local coberta por testes unit/integration locais; E2E browser real fica para CDX-308C/deploy com ferramenta disponível/aprovação.
- 2026-06-04 — CDX-308C iniciado após "pode seguir". Primeiro passo: preflight read-only local/VM e montagem do bloco de aprovação formal. Write/deploy na VM só após aprovação por ação com comandos.
- 2026-06-04 — CDX-308C preflight read-only: VM `faren` ok, containers `accounts`, `mesas`, `glossario`, `cloudflared` up na `artificio_net`; `/opt/artificio/mesas/.env` existe. `JWT_SECRET` de `mesas` e `accounts` existem mas **não batem**; `VITE_ACCOUNTS_URL` e `ACCOUNTS_URL` ausentes em mesas. Deploy precisa sincronizar `JWT_SECRET` de mesas com accounts sem imprimir segredo. Docker local indisponível no Windows (`docker` não reconhecido), então compose config local não validado; build/test TS local OK.
- 2026-06-04 — CDX-308C Docker local preparado: Dockerfiles de `apps/mesas/{frontend,backend}` migrados para contexto monorepo/pnpm/turbo; `docker-compose.prod.yml`/`beta.yml` ajustados para `context: ../..`, `apps/mesas/.../Dockerfile`, `VITE_ACCOUNTS_URL`/`ACCOUNTS_URL`, rede `artificio_net`. Validação local: `pnpm -w turbo run build --filter=@artificio/mesas` OK; frontend tests 15/15; backend tests 106/106.
- 2026-06-04 — CDX-308C aprovação formal recebida (`sim`) para o bloco apresentado: empacotar `apps/mesas` + `packages`, enviar para `faren`, backup de `/opt/artificio/mesas`, sincronizar `JWT_SECRET` com accounts sem imprimir segredo, rebuild/up de `mesas-api`, `mesas-app`, `mesas-cron`, smoke HTTP. Escopo não inclui redeploy de `accounts`/D037 nem DNS/WP.
- 2026-06-04 — CDX-308C primeira tentativa interrompida antes de `rsync`/Docker: bundle enviado e backup `/opt/artificio/_deploy_backups/mesas-20260604-222801.tgz` criado, mas extração em `/tmp/artificio-mesas-deploy` falhou. Diagnóstico read-only: tar criado no Windows registrou diretórios como `dr-xr-xr-x`, então GNU tar sem write nos diretórios não abriu arquivos filhos. Containers antigos `mesas-*` permaneceram `Up`; prod não trocado.
- 2026-06-04 — CDX-308C segunda tentativa: bundle com permissão normal enviado; `/tmp` velho limpo com `chmod -R u+w`; `rsync` e sync de `.env` executaram sem imprimir segredo; Docker build falhou antes de substituir containers. Falhas: `frontend/Dockerfile` fazia `COPY nginx.conf` apesar do contexto monorepo; imagens `node:25.9.0-alpine` não tinham `corepack`. Containers antigos continuaram `Up`; `https://mesas.artificiorpg.com/` ainda 200.
- 2026-06-04 — CDX-308C correção local de Dockerfiles: `COPY apps/mesas/frontend/nginx.conf ...`; `corepack` trocado por `npm install -g pnpm@10.12.1` no frontend/backend. Validação local: `pnpm -w turbo run build --filter=@artificio/mesas` OK.
- 2026-06-04 — Mantenedor apontou workflows legados de `C:\projetos\mesas_rpg_artificio` como guia. Leitura read-only confirmou padrões a incorporar: lock por arquivo (`flock`) para deploy serial; build/test antes de SSH; validação de env obrigatório; `docker compose build --no-cache`/remover cache em vez de restart simples; health por `docker inspect` (`healthy`, não só `Up`); smoke de rotas críticas; rollback trap; snapshot DB só quando houver migration. Para CDX-308C atual (sem migration), próxima retomada deve usar lock + bundle/backup + `build --no-cache --pull` + `up -d --force-recreate` apenas de `mesas-api`, `mesas-app`, `mesas-cron` + health/smoke.
- 2026-06-04 — Auditoria pós-crash do Codex: nenhum deploy novo com padrão legado foi concluído. Local: bundles `222801`, `223048` e `223646`; `223646` está inválido/incompleto (`gzip decompression failed`) e não chegou à VM. VM: só bundles `222801`/`223048`; backups `222801`/`223048`; `/opt/artificio/mesas` foi trocado para layout monorepo na tentativa `223048`; `.env` foi atualizado sem imprimir segredo; `JWT_SECRET` de mesas agora bate com accounts e `ACCOUNTS_URL`/`VITE_ACCOUNTS_URL` presentes. Dockerfiles na VM ainda são a versão quebrada (`corepack` + `COPY nginx.conf`), pois o fix local não foi reenviado. Containers/imagens continuam os antigos (`mesas-*` criados há ~11h) e saudáveis; smoke atual: interno `200`, home pública `200`, privado sem cookie `401`, `/api/v1/auth/google` `302` ainda para Google legado. Conclusão: prod está no ar, mas CDX-308C não implantou SSO novo; próximo passo é reenviar bundle corrigido e rebuildar com no-cache/health.
- 2026-06-04 — CDX-308C aprovado novamente após auditoria (`pode seguir`). Bundle `231005` válido enviado; build Docker no-cache passou, mas `mesas-api` novo falhou runtime por `@artificio/auth/dist-cjs` ausente na imagem. Correção local: backend Dockerfile copia `packages/{config,auth}/dist*` do builder para runtime. Bundle `231509` enviado; API falhou de novo por resolução PNPM workspace (`Cannot find module 'jsonwebtoken'` ao carregar `packages/auth/dist-cjs`). Correção local final: `ENV NODE_PATH=/repo/apps/mesas/backend/node_modules` no runtime backend.
- 2026-06-04 — CDX-308C deploy técnico concluído com bundle `232010`: `mesas-api` e `mesas-cron` rebuildados no-cache; `mesas-api` healthy; `mesas-cron` recriado; `mesas-app` já estava healthy do bundle anterior. Smoke: `internal_api=200`, `public_home=200`, `private_no_cookie=401`, `/api/v1/auth/google` `302` para `https://accounts.artificiorpg.com/login?return=https%3A%2F%2Fmesas.artificiorpg.com%2F`; `JWT_SECRET` mesas=accounts confirmado sem imprimir segredo; `ACCOUNTS_URL`/`VITE_ACCOUNTS_URL` presentes. Logs API sem segredo (`cloudinary` mostra apenas `set`). T7 marcado OK. Falta T8: E2E browser real Google/login/logout e allowlist prod; D037 allowlist está testado local, mas accounts ainda precisa redeploy p/ validar em prod.
- 2026-06-08 — **Gate D mesas FECHADO.** Retomada validou read-only/HTTP: `https://mesas.artificiorpg.com/` 200; `/api/v1/me/options` sem cookie 401; `/api/v1/auth/google` 302 para `accounts.artificiorpg.com/login?return=https%3A%2F%2Fmesas.artificiorpg.com%2F`; `accounts /api/auth/google?return=https://evil.com/x` guarda `state.returnUrl=https://accounts.artificiorpg.com`; `return=https://mesas.artificiorpg.com/perfil` preservado no `state`. Browser chegou em `accounts/login?return=https://mesas.artificiorpg.com/perfil`; mantenedor confirmou que a parte de login real está funcionando e autorizou fechar. T8 marcado OK; nenhum segredo impresso; sem VM/write/deploy nesta retomada.

## Fechamento

**Status:** concluída (Gate D `mesas` ✅). Próximo módulo/bloco fica liberado pelo Gate D, respeitando aprovações por ação para commit/deploy/VM.
