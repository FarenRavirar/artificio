# Tasks — 087

## Fase 0 — Gate, T0 e rechecagem de decisões (bloqueante, antes de qualquer código)

- [x] T0.0 — Ler `AGENTS.md` inteiro (T0 pétreo, obrigatório toda sessão, não é opcional mesmo se já lido em sessão anterior) e confirmar explicitamente: **Gate D está ativo e cobre esta spec** (`AGENTS.md` §Gates do Programa — Downloads é o projeto ativo do gate D; pré-condição do gate D é "projeto atual passou smoke", ou seja, nenhuma alteração desta spec pode ser dada como concluída sem smoke real, não só local — reforçado em T6.0). Se o estado do gate mudou desde a redação desta spec, reconfirmar contra `.specify/memory/project-state.md` atual (não assumido da memória desta spec). · feito quando: leitura confirmada e estado do gate D checado.
- [x] T0.1 — Rechecar as 6 decisões já respondidas pelo mantenedor em 2026-07-26 (`plan.md` §Decisões): rota `/catalogo` (ambas válidas), busca no Header compartilhado, prateleiras horizontais, Bayesian average + corte de elegibilidade pra popularidade, sort formal + prateleira pros dois, Bayesian average pro rating. Se a sessão de implementação não é a mesma que decidiu (retomada em chat novo), reler `spec.md`/`plan.md` inteiros antes de agir — não presumir, confirmar cada decisão contra o texto atual da spec. · feito quando: as 6 decisões confirmadas como lidas e entendidas, nenhuma re-perguntada ao mantenedor sem motivo novo.
- [x] T0.2 — Ler esta spec inteira (`spec.md`/`plan.md`/`tasks.md`) antes de tocar código; se a sessão que implementa não é a mesma que escreveu a spec, tratar como T0 de retomada (`AGENTS.md` §Leitura Mínima de Retomada). · feito quando: agente confirma leitura na sessão.
- [x] T0.3 — Checar `specs/backlog.md` linha `BL-087-DOWNLOADS-HOME-CATALOGO` e qualquer débito (`D-*`) aberto relacionado a `apps/downloads` que possa colidir com esta spec (ex. `BL-DOWNLOADS-BETA-VOLUME-MISMATCH`, se ainda aberto). · feito quando: colisões conhecidas listadas ou confirmado que não há nenhuma.
- [x] T0.4 — Confirmar com o mantenedor a aprovação nominal de escrita em `packages/ui` (pacote compartilhado, decisão 2 já fixa a busca no Header — mas commit exige aprovação própria conforme `AGENTS.md` §Autorização, separada da decisão de design). · feito quando: aprovação nominal registrada antes do primeiro commit que toca `packages/ui`.

### Evidência da Fase 0 — 2026-07-26

- T0.0: `AGENTS.md` lido integralmente nesta sessão. Gate D continua aplicável ao Downloads; fechamento exige smoke beta real, não apenas validação local.
- T0.1: seis decisões reconfirmadas no texto vigente: `/` e `/catalogo` usam o mesmo componente; busca no `Header` compartilhado; prateleiras horizontais; popularidade por Bayesian average com corte `download_count >= 1`; `rating`/`trending` como sort e prateleira; rating por Bayesian average sem piso fixo.
- T0.2: `spec.md`, `plan.md` e `tasks.md` lidos integralmente antes de código.
- T0.3: `BL-087-DOWNLOADS-HOME-CATALOGO` continua pronto para desenvolvimento. Colisão direta registrada na época: `BL-DOWNLOADS-BETA-VOLUME-MISMATCH` mantinha Downloads beta fora do ar e bloqueava T6.2/T6.0. **Superado em 2026-07-26 (ver Evidência da Fase 1): débito fechado, beta verificado no ar, T6.2/T6.0 desbloqueados.** Débitos abertos 084–086 foram rechecados: `D-084-09`, `D-085-04`, `D-086-01`, `D-086-02` e `D-086-04` não colidem diretamente com arquivos/contratos planejados nesta fase; permanecem fora desta implementação conforme decisões já registradas.
- T0.4: autorização nominal concedida pelo mantenedor em 2026-07-26 para escrita local em `packages/ui` no escopo da busca do `Header` da spec 087. Commit/push continuam exigindo autorizações próprias por ação.
- Registro documental desta execução restrito a `specs/087-downloads-home-catalogo-unificado/`, por ordem do mantenedor.

## Fase 1 — Pesquisa visual e direção de design (skill frontend-design)

- [x] T1.1 — Rodar a skill `frontend-design` com o brief: catálogo gratuito de material de RPG em português, PWYW/grátis, sem carrinho/preço, link sempre externo pro autor/fonte original, layout de vitrine já decidido como prateleiras horizontais roláveis (decisão 3, não reabrir). Gerar token system (cores dentro dos limites de `packages/ui`, tipografia) e proposta visual das prateleiras/pills/card de estrelas. · feito quando: proposta registrada na sessão com justificativa (por que não é clone de DMs Guild, o que foi adaptado).
- [x] T1.2 — Confirmar com o mantenedor a direção visual escolhida antes de implementar em código definitivo (screenshot/mock ou HTML estático de protótipo é aceitável). · feito quando: aprovação registrada.

### Evidência da Fase 1 — 2026-07-26

- T1.1: skill `frontend-design` executada 2 vezes. **1ª execução rejeitada pelo mantenedor** ("não parece que tá respeitando a nav compartilhada que já existe" + baixa confiança na direção): entregou layout genérico sem tipografia própria, protótipo isolado sem `Header`/`Footer` reais de `packages/ui`, e "signature" que era ausência (`sem selo comercial`), não presença. **2ª execução aceita**, após verificar no schema real que `material_type` é taxonomia canônica (UUID validado contra Central, `routes/materials.ts:76`), `credits`/`creator_slug` são campos de primeira classe (`types/material.ts`) e a maioria dos itens não tem `cover_image_url`. Direção completa registrada em `plan.md` §Direção de design (Fase 1).
- Justificativa anti-clone (exigência do Requisito 9): DMs Guild/DriveThruRPG tratam o editor como letra miúda sob o preço, porque o produto é retenção + venda. Downloads é acervo doado cujo propósito é o usuário SAIR pro site do autor — a direção inverte a hierarquia e promove o crédito do autor a eyebrow no topo do card. Estrutura de informação (prateleira horizontal, pill+popover) vem das referências; a pele visual e a hierarquia do card não.
- Assinatura descartada no meio do caminho (registro de decisão): "lombada" colorida por `material_type` na borda esquerda do card foi proposta e **cortada** ao verificar o schema — codificava a mesma informação do badge `[Aventura]` que continua no card, ou seja, decoração redundante disfarçada de estrutura.
- T1.2: direção aprovada nominalmente pelo mantenedor em 2026-07-26 ("autorizado"), com a assinatura (crédito do autor promovido ao topo do card em Oswald caixa-alta) como decisão que carrega o resto. Protótipo HTML foi dispensado explicitamente pelo mantenedor antes da aprovação.
- Backlog (`AGENTS.md` §Conclusão de Tarefas): nada a atualizar em `specs/backlog.md` — Fase 1 é decisão de design, não gerou débito novo nem mudou status de `BL-087-DOWNLOADS-HOME-CATALOGO` (segue em desenvolvimento). `BL-DOWNLOADS-BETA-VOLUME-MISMATCH` continua aberto e segue bloqueando T6.2/T6.0, sem mudança nesta fase.
- Nenhum arquivo de código tocado nesta fase (design/decisão apenas). Implementação visual é T4.1, dependente da Fase 2.

#### Desbloqueio de `BL-DOWNLOADS-BETA-VOLUME-MISMATCH` (2026-07-26, mesma sessão)

Investigado e **fechado** — a premissa do débito não valia mais. Registrado aqui porque destrava T6.2/T6.0 desta spec.

- Estado real verificado (inspeção read-only na VM, `AGENTS.md` §Autorização permite sem aprovação): `downloads-beta-app`/`-api`/`-db` todos `Up (healthy)`; `GET https://downloadsbeta.artificiorpg.com/api/v1/materials` → HTTP 200 com material real; front → HTTP 200. O débito descrevia API unhealthy com `42P01`; beta se recuperou em deploy posterior a 2026-07-20 sem ninguém fechar a linha do backlog.
- Pergunta central do débito ("qual dos 2 volumes tem os dados reais") respondida com medição, não achismo: ativo `downloads-beta_pgdata_downloads_beta` (65MB, banco `downloads`, **103 materiais / 28 migrations**) vs. órfão `downloads_pgdata_downloads_beta` (47MB, **0 materiais / 19 migrations**, schema parcial — resíduo do deploy que o guard `MAX_AUTO_PENDING` bloqueou em 2026-07-12). Órfão inspecionado por container Postgres temporário read-only, encerrado após a leitura, sem tocar o volume ativo.
- Órfão removido com aprovação nominal do mantenedor (`docker volume rm downloads_pgdata_downloads_beta`). Smoke pós-remoção: 3 containers healthy, 103 materiais íntegros, API/front 200.
- Correção de contrato do débito: o banco chama-se `downloads`, não `downloads_beta` como o texto original afirmava.
- `specs/backlog.md` atualizado (linha movida para `fechado`, com evidência e comandos).

## Fase 1B — Backend: métricas reais pras prateleiras (view_count, Bayesian average, corte de elegibilidade)

Pré-requisito da Fase 2 (prateleiras dependem desses dados existirem na API). Pode rodar em paralelo com Fase 1 (design), não com Fase 2.

- [x] T1B.0 — Rechecagem T1 antes de tocar backend/migration: reler `AGENTS.md` §Migrations (checklist pétreo completo) SE T1B.5 concluir que precisa de migration nova. Reler também `AGENTS.md` §Erros Conhecidos (`.specify/memory/errors.md`, buscar `E009`-`E017` por relevância a Postgres/migration/deploy do Downloads) antes de qualquer decisão de índice/coluna nova. · feito quando: confirmado que T1B não precisa de migration (registrar por quê) OU checklist pétreo relido e aplicado linha a linha na migration criada.
- [x] T1B.1 — Implementar gravação de `view_count` em `GET /api/v1/materials/:slug` (débito real achado nesta spec: coluna existe desde migration_008/spec 070, nunca foi incrementada — confirmar com `rtk rg "view_count" apps/downloads/backend/src` que segue não incrementada antes de codar, pra não duplicar se alguém já mexeu). Mesmo padrão `onConflict...doUpdateSet` de `routes/downloads.ts:48-54`. Dedup: no máximo 1 incremento por (sessão/IP, material, dia) — critério observável obrigatório (spec.md Requisito 13), mecanismo concreto livre. · feito quando: acessar a ficha do material incrementa `download_metric_daily.view_count` do dia uma única vez por origem, confirmado via teste automatizado.
- [x] T1B.2 — Implementar Bayesian average pra rating: `avg_rating = (v/(v+m))×R + (m/(v+m))×C`, onde `R`=média bruta do item, `v`=`rating_count` do item, `C`=média geral de `download_rating.score` do catálogo, `m`=constante de confiança (default proposto 5, nomeada/comentada no código). `GET /api/v1/materials` expõe `avg_rating` (ajustado) e `rating_count` (bruto, pra exibir "N avaliações" real). Material com `rating_count=0` retorna `avg_rating: null` (nunca entra na fórmula, nunca aparece como "0 estrelas"). · feito quando: material com 1 avaliação isolada de 5 estrelas NÃO aparece no topo absoluto — fica próximo da média geral do catálogo; material com muitas avaliações reais reflete a própria média com precisão crescente.
- [x] T1B.3 — Implementar corte de elegibilidade + Bayesian average pra popularidade: filtrar `WHERE download_count >= 1` (na janela móvel) ANTES de qualquer cálculo — material só visualizado (`download_count=0`) fica com `popularity_score: null`, nunca elegível pra `sort=trending`/prateleira "Mais visitados", mesmo com `view_count` alto. Dentro do universo elegível: `popularity_score = (v/(v+m))×R + (m/(v+m))×C`, `R`=taxa de conversão do item (`download_count/(view_count+download_count)`) na janela de 30 dias, `v`=`view_count+download_count` do item na janela, `C`=taxa de conversão geral do catálogo elegível na mesma janela, `m`=constante de confiança (default proposto 20, nomeada/comentada). · feito quando: teste com fixture de material só-visualizado (muitas views, zero downloads) confirma AUSÊNCIA TOTAL em `sort=trending` (não presença no fim da lista); material com poucos dados mas 1+ download aparece próximo da taxa de conversão média do catálogo.
- [x] T1B.4 — `SORT_OPTIONS` ganha `'rating'` e `'trending'` (decisão 5, já fixa). Cobrir paginação normalmente para os 2 novos valores. · feito quando: `GET /api/v1/materials?sort=rating` e `?sort=trending` retornam resultado ordenado corretamente, paginação funcional.
- [x] T1B.5 — Avaliar necessidade de migration/índice novo pra performance do agregado (ver `plan.md` §Riscos — calcular Bayesian average on-the-fly em toda listagem pode ficar caro). Se necessário: seguir checklist pétreo completo de `AGENTS.md` §Migrations (header 5 campos, idempotência, `online-safe`). Se não necessário agora: registrar como TODO/débito explícito, não decidir silenciosamente que "está bom assim". · feito quando: decisão registrada com justificativa (medição real de tempo de query, não achismo).
- [x] T1B.6 — Testes de backend cobrindo T1B.1-T1B.4: view incrementando com dedup, Bayesian average de rating (item novo ancorado na média, item com volume reflete próprio desempenho), Bayesian average de popularidade, **corte de elegibilidade (caso crítico: material com view_count alto e download_count=0 nunca aparece em sort=trending)**. **`GET /materials`/`GET /materials/:slug` não têm nenhum teste hoje (`materials.test.ts` não existe, confirmado — achado da revisão fase-a-fase, 2026-07-27)** — além dos casos novos, esta task cobre também o comportamento PRÉ-EXISTENTE da rota que fica exposto a regressão silenciosa pelo novo join/subquery: paginação, facetas combinadas com filtro, `sort=popular`/`name`/`relevance` (comportamento atual, sem tocar), enriquecimento de taxonomia (`enrichMaterialsWithTaxonomy`). Sem isso, "nenhum teste existente quebrado" (critério de aceite) não tem o que verificar nessa rota especificamente. · feito quando: `rtk pnpm --filter downloads-backend test` verde, cobrindo tanto os casos novos (rating/popularidade/elegibilidade) quanto uma suíte mínima de regressão do comportamento pré-existente da rota.

### Evidência da Fase 1B — 2026-07-26

Arquivos: `services/materialMetrics.ts` (novo), `services/materialMetrics.test.ts` (novo), `database/migration_029_download_material_view.sql` (nova), `routes/materials.ts`, `db/types.ts`, `routes/materials.list.test.ts`.

- **T1B.0** — `AGENTS.md` §Migrations relido integralmente (T1B.5 concluiu que migration É necessária, ver abaixo) e §Erros Conhecidos rechecado: `E009`-`E017` consultados. Relevantes aplicados: `E011` (header de 5 campos — copiado do vizinho verde `migration_028`), `E010` (`online-safe` não pode ter DDL destrutivo — a 029 só cria tabela/índice, nenhum `DROP`), `E012` (guard `MAX_AUTO_PENDING=5` — esta spec adiciona 1 migration, sem risco de acúmulo), `E016`/`E017` (Dockerfile: **nenhuma dependency `@artificio/*` nova foi adicionada**, então o gap de `COPY --from=builder` não se aplica).
- **T1B.1** — `view_count` confirmado sem incremento antes de codar (`rtk rg "view_count" apps/downloads/backend/src` → só `db/types.ts:330` e `routes/admin.ts:131`, nenhuma escrita). Incremento agora em `GET /materials/:slug` via `registerMaterialView`. **Dedup exigiu tabela nova**: `download_metric_daily` agrega por `(material_id, metric_date)` e não tem onde registrar qual origem já contou — sem isso, todo refresh incrementaria. `download_material_view` usa PK composta `(material_id, view_hash, view_date)` como mecanismo de dedup (mesmo padrão de `download_user_material_download`). `view_hash` é digest SHA-256 de `(data, ip, user-agent)` com sal diário — **nunca IP em claro**, porque visualização é anônima e guardar IP identificável seria coleta desnecessária de dado. Gravação é fail-soft: métrica nunca derruba a ficha pública.
- **T1B.2** — Bayesian average de rating em `loadRatingAggregates`. `avg_rating` sai ajustado, `rating_count` sai cru (o card mostra "12 avaliações" real). `RATING_CONFIDENCE_M = 5`, nomeada e comentada. Material com `rating_count = 0` → `avg_rating: null`, nunca 0.
- **T1B.3** — Corte de elegibilidade + Bayesian de popularidade em `loadPopularityScores`/`loadTrendingOrder`. O corte é `HAVING sum(download_count) >= 1` aplicado **antes** do cálculo — material só-visualizado não recebe score e não entra na lista de IDs elegíveis, então **desaparece** de `sort=trending` em vez de ir pro fim. `C` (ancora) é calculada só sobre o universo elegível, pra não diluir a média com material que nunca converteu. `POPULARITY_CONFIDENCE_M = 20`, `POPULARITY_WINDOW_DAYS = 30`.
- **T1B.4** — `SORT_OPTIONS` ganhou `'rating'` e `'trending'`. Como ordenam por métrica calculada (não coluna), a ordem é resolvida sobre o catálogo inteiro e reproduzida no SQL com `array_position`, preservando `limit`/`offset` normais — ordenar só a página daria ordem incoerente entre páginas.
- **T1B.5** — **Migration necessária, decisão registrada com motivo**: (a) a dedup de view não tinha onde morar (acima); (b) o agregado de popularidade varre `download_metric_daily` filtrando por `metric_date` e agrupando por `material_id` a cada listagem — daí `idx_download_metric_daily_date_material`; (c) `idx_download_rating_material` cobre o agregado de rating no mesmo request. `migration_029_download_material_view.sql`: `@class: online-safe`, header de 5 campos, idempotente (`CREATE TABLE/INDEX IF NOT EXISTS`), sem DDL destrutivo.
- **T1B.6** — `rtk pnpm --filter downloads-backend test` → **287 testes, 42 arquivos, todos verdes**. Cobertura nova: 6 casos de comportamento da fórmula (`materialMetrics.test.ts` — ancoragem de item novo, convergência com volume, `null` sem volume, ancora nos dois sentidos, popularidade como taxa de conversão, relação entre as constantes) e 9 casos de contrato de rota, incluindo o **caso crítico** (`sort=trending` restringe aos IDs elegíveis, material só-visualizado ausente) e regressão do pré-existente (paginação, sorts antigos, `sort` inválido → 400, taxonomia, agregação 1×/página e não N+1).
- **Correção de premissa da própria task (achado):** T1B.6 afirmava que "`GET /materials` não tem nenhum teste hoje (`materials.test.ts` não existe, confirmado)". **Falso** — a suíte existe em `routes/materials.list.test.ts` com 13 casos desde a spec 073. A suíte foi **estendida**, não recriada. (O agente chegou a repetir o erro nesta sessão ao concluir ausência a partir de `rtk find`/`Glob` retornando vazio; confirmado depois por leitura direta.)
- **Bug real encontrado e corrigido nos testes:** ao cobrir `sort=popular` pela primeira vez, o stub de query builder estourou com `.as is not a function` — a subquery agregada de `sort=popular` (código pré-existente, `materials.ts:196`) nunca tinha sido exercitada por teste nenhum. Stub corrigido com `.as`/`.having`; comportamento do código de produção não foi alterado.
- **Erro de processo cometido e revertido:** o agente editou `docs/api/openapi/downloads.openapi.yaml` à mão pra documentar os `sort` novos e o efeito colateral de `view_count`. O arquivo é **auto-gerado** (`scripts/api/generate-openapi.ts`) e `AGENTS.md` proíbe edição manual de `docs/api/openapi/**`; a regeneração descartou as edições, estado final limpo. Documentar semântica de campo ali exige mudar o gerador, não o YAML — registrado como observação, não débito (o inventário já capturou a mudança).
- `pnpm verify:api` → **verde**, `downloads: breaking=0 non-breaking=1` (aditivo, como esperado). `docs/api/generated/api-inventory.generated.json` e `api-map.generated.md` regenerados.
- `npx tsc --noEmit` e `npx eslint` nos arquivos tocados → limpos.
- **Não executado nesta fase (fica pra T5.2/T6.2):** a migration 029 **não foi aplicada em nenhum banco** — nem local, nem beta. Aplicação real acontece no deploy, e o smoke beta (T6.2) é que confirma o `view_count` incrementando de fato contra Postgres real. Os testes cobrem a lógica, não a migration aplicada.
- Backlog: nada a atualizar em `specs/backlog.md` por esta fase — nenhum débito novo criado, nenhum fechado (o fechamento de `BL-DOWNLOADS-BETA-VOLUME-MISMATCH` foi registrado na Fase 1).

## Fase 2 — Componente de vitrine (prateleiras)

- [x] T2.1 — Criar `MaterialShelf.tsx` (nome provisório) — prateleira horizontal rolável (decisão 3, layout fixo) que recebe título + `sort`/critério e renderiza `MaterialCard` em lista com scroll lateral, link "Ver tudo" que aplica o `sort` como filtro/entra em modo resultado. · feito quando: componente renderiza com dado real da API local/beta, scroll horizontal funcional, testado isoladamente.
- [x] T2.2 — Detectar "modo vitrine vs. modo resultado" no componente de página unificado (`isBrowsing = !q && !materialType && !systemId && !editionId`). · feito quando: alternar entre os dois modos funciona navegando com/sem query params.
- [x] T2.3 — Pills de categoria (tipo/sistema/edição facets) visíveis no modo vitrine, cada uma abrindo popover com o conteúdo de `CatalogFilterSidebar` (reaproveitado, não reescrito) — aplicando filtro ao selecionar. · feito quando: clicar numa pill abre popover, selecionar opção leva pro modo resultado filtrado.
- [x] T2.4 — 3 prateleiras da home usando `MaterialShelf`: "Recém adicionados" (`sort=recent`), "Mais visitados" (`sort=trending`, Fase 1B — nunca inclui material com `download_count=0`), "Mais bem avaliados" (`sort=rating`, Fase 1B — Bayesian average, sem piso fixo). Prateleira vazia (nenhum material elegível) não renderiza em branco. · feito quando: as 3 prateleiras aparecem com dado real de beta/local; prateleira "Mais visitados" comprovadamente nunca mostra material só-visualizado (teste manual com fixture, além do teste automatizado de T1B.6).
- [x] T2.5 — `MaterialCard.tsx` ganha exibição de estrelas+contagem (`avg_rating` ajustado pras estrelas, `rating_count` bruto pra contagem) quando `rating_count > 0` (nada quando `rating_count = 0`). **Cuidado explícito com o alvo de clique único do card** (`MaterialCard.tsx:46-51` usa `before:absolute before:inset-0` no `<Link>` pra cobrir o card inteiro, decisão documentada em comentário existente) — o bloco de estrelas fica dentro dessa área clicável, não pode virar link/botão separado que rouba o clique do card (a menos que a intenção seja abrir avaliação, o que é fora de escopo desta spec). · feito quando: card renderiza `4.5 ★ (12 avaliações)` com dado real, sem nada quando `rating_count: 0`; clicar em qualquer ponto do card (incluindo sobre as estrelas) continua navegando pra ficha do material, não abre nada separado.
- [x] T2.6 — Dropdown de ordenação do modo resultado ganha "Mais visitados"/"Mais bem avaliados" (decisão 5, sort formal) ao lado das opções já existentes (Relevância/Recentes/Populares/Nome). · feito quando: selecionar essas opções no modo resultado reordena a lista corretamente, com paginação funcional.

### Evidência da Fase 2 — 2026-07-26

Validação: **207 testes / 43 arquivos verdes** (`npx vitest run`), `npx tsc --noEmit` limpo, `npx eslint` limpo nos arquivos tocados, `npx vite build` verde.

**Arquivos novos:** `MaterialRating.tsx`, `MaterialShelf.tsx`, `CatalogShowcase.tsx`, `FilterControls.tsx`, `FilterPills.tsx` (+ 3 arquivos de teste).
**Modificados:** `MaterialCard.tsx`, `CatalogFilterSidebar.tsx`, `CatalogoPage.tsx`, `useMaterialsCatalog.ts`, `types/material.ts`, `MaterialCard.test.tsx`, `CatalogoPage.test.tsx`.

**T2.1/T2.4 — prateleiras.** `MaterialShelf` com `scroll-snap-type: x proximity` (não `mandatory`, conforme plan.md), card 220px/240px≥lg, último card `scroll-snap-align: end`, sem setas e sem `scroll-behavior: smooth` forçado. Prateleira sem item devolve `null` (Requisito 16) — testado. `CatalogShowcase` existe porque cada prateleira precisa do próprio `useMaterialsCatalog`, e hook não pode ser chamado dentro de `.map()` sem virar chamada condicional.

**T2.2 — modo vitrine.** `isBrowsing = !q && !materialType && !systemId && !editionId`. **`sort` deliberadamente fora do cálculo:** se entrasse, clicar em "Ver tudo" numa prateleira cairia em modo resultado e pareceria uma busca vazia. Coberto por teste próprio. No modo vitrine a listagem principal é desabilitada (`enabled: false` no `useMaterialsCatalog`) — sem fetch ocioso cujo resultado ninguém renderiza.

**T2.3 — pills.** `FilterControls` foi **extraído** de `CatalogFilterSidebar.tsx` para arquivo próprio, com prop `groups` que recorta quais facetas exibir; a sidebar/drawer passou a importá-lo. Composição, não reescrita — sidebar e pill não podem divergir em comportamento. Popover com Escape + clique-fora, pill ativa mostra valor no lugar do rótulo.

**T2.5 — estrelas.** `MaterialRating` usa 5 `<span>`, nunca `<button>` — teste explícito garante que o card continua com **1 único link e nenhum botão** mesmo com estrelas presentes (o `before:absolute` do `<Link>` não pode ser roubado). Meia-estrela por gradiente, `rating_count === 0` some com o bloco inteiro.

**T2.6 — sorts.** `trending`/`rating` no `SORT_OPTIONS` e no dropdown, com os mesmos rótulos das prateleiras ("Mais visitados"/"Mais bem avaliados") para que quem chega por "Ver tudo" reconheça de onde veio. Dropdown só aparece no modo resultado: na vitrine cada prateleira já é um critério de ordenação.

#### Achados e desvios registrados

1. **Bug real corrigido (ponto flutuante nas estrelas).** `4.4 - 4` em IEEE 754 dá `0.40000000000000036`, que vazava como `linear-gradient(..., 40.000000000000036%, ...)` pro CSS. Pego por teste que assertava `40%`. Corrigido **no componente** (arredondamento da porcentagem para 1 casa), não no teste — o teste estava certo.

2. **Dois testes existentes de `MaterialCard` foram reescritos, não só ajustados.** A direção de design aprovada muda o contrato do crédito: sai de `"Por <credits>"` na 3ª linha, some quando `null`; entra como eyebrow acima do título, sem prefixo, e **nunca ausente** (`null` vira "Acervo Artifício"). O teste `'não mostra linha de autores quando credits ausente'` afirmava o comportamento **oposto** ao aprovado, então virou `'assume a autoria do acervo quando credits ausente'`. Não é teste "consertado para passar" — é contrato mudado por decisão registrada em `plan.md` §Assinatura.

3. **4 testes de `CatalogoPage` passaram a navegar com query param.** Eles renderizavam `/catalogo` sem filtro, o que agora é modo vitrine; sem isso testariam uma tela que não é mais a que eles descrevem. Passaram a entrar por `?q=...`/`?material_type=...`. As asserções de `toHaveBeenLastCalledWith` ganharam `expect.anything()` porque o hook agora recebe 2 argumentos.

4. **T3.5 (extensão do schema Zod) foi executada aqui, antecipada da Fase 3.** `avg_rating`/`rating_count`/`popularity_score` são pré-requisito técnico de T2.5 — sem eles o card não tem o que renderizar. Marcada como feita na Fase 3.

#### Pendências reais (não fechadas nesta fase)

- **T2.1/T2.4 pedem "dado real da API local/beta"; a validação aqui é com mock.** A lógica está coberta por teste automatizado, mas prateleira renderizando dado real de Postgres só se confirma no smoke de T6.2 — que também depende da `migration_029` (ainda não aplicada em banco nenhum).
- **T2.4 pede teste manual com fixture** provando que "Mais visitados" nunca mostra material só-visualizado. O corte de elegibilidade é garantido em SQL e testado no backend (T1B.6); a verificação manual ponta-a-ponta continua pendente pra T6.2.

### Evidência da rodada de review da PR #214 — 2026-07-26

Achados de Codex e CodeRabbit na PR #214, verificados **contra o código real** antes de qualquer correção. Diretriz do mantenedor para as decisões em aberto: *"as mais escalonáveis e uniformes... tem que ser homogêneo o máximo possível... não quero nada simples só por ser simples"*.

#### Corrigidos

1. **`trust proxy` ausente no backend do Downloads (P1).** `apps/downloads/frontend/nginx.conf:28-29` envia `X-Real-IP`/`X-Forwarded-For`, mas `server.ts` nunca configurou `trust proxy` — `req.ip` era o IP do nginx, e a dedup de view por `(origem, material, dia)` colapsaria quase tudo num hash só, corrompendo o ranking `trending`. Corrigido com a **mesma linha dos outros 5 apps** (`process.env.TRUSTED_PROXY_CIDR || '172.18.0.0/16'`) em vez de solução local: `mesas`, `site`, `links`, `glossario` e `accounts` já usavam esse padrão; Downloads era o único fora. CIDR restrito, nunca `true` — com `trust proxy: true` qualquer cliente forjaria `X-Forwarded-For` e escaparia da dedup.

2. **`sort` explícito não abria o modo resultado (P1) — bug introduzido na Fase 2.** `isBrowsing` ignorava `sort` de propósito, mas `?sort=trending|rating|recent` é exatamente o destino dos links "Ver tudo" das prateleiras: o link voltava para a vitrine com as mesmas três prateleiras, sem nunca abrir a lista paginada que promete. O comentário que justificava o comportamento antigo estava **invertido** e foi reescrito, não apagado. Testado por `searchParams.has('sort')`, não pelo valor, porque o default é `'recent'` e comparar valor não distinguiria `/catalogo` de `/catalogo?sort=recent`.

3. **Dedup e contador de view fora de transação (P2).** Se o insert de dedup commitasse e o upsert de `download_metric_daily` falhasse, a view ficava marcada como contada sem nunca ter entrado no contador — e todo retry no mesmo dia colidia na PK, perdendo a contagem em definitivo. Os dois writes agora estão em `db.transaction()`.

4. **`purgeStaleViewDedup` sem chamador (P2).** A tabela crescia sem limite apesar da retenção curta declarada na migration. Em vez de inventar mecanismo novo, **reusa o padrão já existente no próprio app**: `node-cron` (já era dependency) + advisory lock + fail-soft, espelhando `scraperScheduler.ts`, em `services/metricsScheduler.ts`, ligado no boot junto dos outros dois schedulers. Lock com chave distinta da do scraper (o purge não deve esperar o scraper, que leva minutos por fonte) e horário 04:30, fora do 03:00/04:00 já ocupados. Retenção virou constante nomeada `VIEW_DEDUP_RETENTION_DAYS` em vez do `- 2` mágico. Cobertura nova em `metricsScheduler.test.ts` (3 testes), incluindo liberação do lock quando o expurgo falha.

5. **Busca prometia mais do que entregava (P2).** O placeholder dizia "título, autor ou sistema" mas `materials.ts` só cobria `title`/`summary`. Escolha do mantenedor: **ampliar o backend**, não encolher o texto. Autor sai de `download_creator` via `EXISTS`, cobrindo os **dois** lados do `creator_id` (scraper grava `download_creator.id`, humano grava `user_id` do SSO — sem os dois, a busca por autor perderia metade do acervo). Sistema **não tem tabela local**: a taxonomia vive no Catálogo Central e chega por HTTP, então o novo `matchTaxonomyIdsByName` (em `catalogClient.ts`, onde o conhecimento de taxonomia já mora, para ser reusável) resolve os IDs em memória sobre o snapshot já cacheado — sem chamada extra de rede — e casa contra `name`, `name_pt`, `slug` **e aliases** (é o vocabulário que o público digita: "dnd", "5e"). Fail-soft: Central fora do ar degrada a busca para título/autor em vez de derrubar a listagem. `verify:api`: `breaking=0 non-breaking=1`.

6. **Cor de marca hardcoded na pill (P1) — resolvido na raiz, em `packages/ui`.** A pill usava `rgba(255,87,34,0.10)` cru, mas o problema era maior: o próprio `styles.css` já repetia `rgba(255,87,34,0.14)` em `.artificio-badge-brand`. Com aprovação nominal do mantenedor para mexer em pacote compartilhado, criada a tripla `--state-brand-bg/-line/-fg` seguindo a convenção `--state-*` já documentada (success/warning/danger/info), com `--state-brand-fg` clareando no tema escuro para AA, igual aos demais. O cru existente foi refatorado para o token. Impacto verificado: `.artificio-badge-brand` não tem nenhum consumidor fora do próprio `styles.css`, e todos os tokens novos são aditivos — nenhum token existente mudou de valor.

7. **Alvo de toque abaixo de 44px (P1).** `h-9` = 36px. `min-h-11` aplicado à pill **e a cada botão interno** — a altura do container não vira área clicável do filho, então abrir e remover filtro continuariam abaixo do mínimo. Correção histórica preservada; não reabre T4.2, posteriormente dispensada pelo mantenedor.

8. **Falha de prateleira virava silêncio (CodeRabbit).** `data?.items ?? []` transformava erro em lista vazia, e prateleira vazia não renderiza (Requisito 16) — com o backend fora do ar a vitrine ficava muda, como se o acervo estivesse vazio. `CatalogShowcase` agora agrega o `isError` das prateleiras e mostra aviso **só quando todas falham** (uma fora do ar entre três não merece alarme, as outras já mostram acervo). 3 testes novos.

9. **Eyebrow de crédito podia renderizar em branco.** `material.credits ?? 'Acervo Artifício'` não pegava `""`/`"   "`, que chegam de scraper e de formulário. Trocado por `?.trim() ||`.

10. **Nitpicks aplicados:** `registerMaterialView` sem `await` na ficha pública (dois writes fora do caminho da resposta, com `.catch` como rede de segurança); `fieldset`/`legend` no lugar de `section`/`h2` em `FilterControls` (o título vira nome acessível do grupo de rádios, e some `<h2>` solto dentro de popover); foco devolvido à pill ao fechar o popover (Escape e seleção), com refs por gatilho; pill sem rótulo resolvido mostra `…` em vez do UUID cru; `Intl.NumberFormat('pt-BR')` no lugar de `toFixed(1).replace('.', ',')`; utilitários `snap-proximity`/`snap-start`/`snap-end` no lugar de estilo inline em `MaterialShelf`. 4 testes novos em `FilterPills.test.tsx` (clique-fora, foco restaurado, rótulo pendente).

11. **Âncoras de catálogo cacheadas (CodeRabbit).** O `C` da fórmula Bayesiana era recalculado a cada request — e a de popularidade **duas vezes no mesmo request** de `sort=trending`, porque `loadPopularityScores` e `loadTrendingOrder` repetiam a mesma agregação de 30 dias. Consolidadas em `loadRatingAnchor`/`loadPopularityAnchor` com TTL de 30s, mesmo padrão e mesmo TTL do `facetsCache` já existente em `routes/materials.ts`. Isso resolve de uma vez os dois achados (cache + duplicação). `invalidateCatalogAnchorCache` exportada porque cache é estado de módulo e vazaria entre testes. Cobertura nova em `materialMetricsCache.test.ts` (3 testes), incluindo que o score não muda com a âncora cacheada.

#### Descartado, com motivo

- **`unnest(...) WITH ORDINALITY` no lugar de `array_position` (nitpick).** Sugestão legítima em SQL — `array_position` varre o array por linha. **Não aplicada**: a suíte da listagem roda 100% sobre mock de Kysely, então a forma nova passaria no teste sem nunca ter sido executada contra Postgres. Cheguei a implementar e os dois testes de `rankedIds` quebraram (`innerJoin` ausente no stub), o que expôs justamente o ponto: o teste não prova o SQL. Trocar SQL que roda em produção por SQL não exercitado, por ganho teórico num ranking hoje pequeno, é risco sem retorno. O motivo ficou registrado em comentário no código, com a condição de reavaliação (teste de integração com banco real, ou custo aparecendo em medição).

#### Validação

`rtk tsc` limpo (backend e frontend) · `rtk vitest` **296 backend** + **215 frontend** verdes · `rtk pnpm run lint` verde repo-wide (23 tasks) · `rtk pnpm run build` verde · `rtk pnpm verify:api` `breaking=0 non-breaking=1`.

**Falha de processo desta sessão:** rodei `npx tsc`/`npx vitest` crus no início da rodada, apesar de `rtk` ser obrigatório (`AGENTS.md` §T0). O mantenedor cobrou duas vezes. Corrigido no meio da sessão e fixado em memória persistente — a regra antiga cobria busca (`rtk rg`/`rtk read`), não execução.

#### Pendências (inalteradas)

As duas pendências da Fase 2 continuam abertas e dependem de T6.2: `migration_029` não aplicada em banco nenhum, e validação ponta-a-ponta das prateleiras com dado real.

### Evidência da 2ª rodada de review da PR #214 — 2026-07-26

Achados de Codex (3 P2) e Sonar (10), verificados contra o código real. Dois exigiram decisão do mantenedor (registradas abaixo, não inferidas).

#### Corrigidos

1. **`TRUSTED_PROXY_CIDR` não chegava ao container (Codex P2).** O fix da 1ª rodada ficou **pela metade**: `app.set('trust proxy', ...)` foi adicionado, mas a variável não era injetada em `docker-compose.beta.yml`/`prod.yml` — funcionava só porque o default coincide com a rede real, e voltaria a quebrar se `artificio_net` mudasse de CIDR. Confirmado por busca: `site`, `links`, `glossario`, `mesas` e `accounts` já injetavam nos dois composes; downloads era o único fora. Injetada nos dois, no mesmo formato dos demais.

2. **Advisory lock podia liberar em conexão diferente (Codex P2).** Com `pg.Pool`, `pg_try_advisory_lock`, o trabalho e o `pg_advisory_unlock` podem cair em sessões distintas — nesse caso o unlock não libera nada e o job fica travado até a conexão original morrer. **`scraperScheduler.ts` tinha o mesmo bug** (o padrão foi copiado dele). Decisão do mantenedor: corrigir os dois com helper compartilhado. Criado `services/advisoryLock.ts` com `pg_try_advisory_xact_lock` — o lock é preso à transação e o Postgres o libera sozinho no commit/rollback, sem unlock explícito que possa vazar. `purgeStaleViewDedup` passou a aceitar o executor, para o DELETE rodar **dentro** da transação que segura o lock (senão o lock seria decorativo). Suíte própria (`advisoryLock.test.ts`, 4 testes) incluindo asserção de que usa a variante `xact`, não a de sessão.

3. **Hash da origem da view era reversível (Codex P2).** O achado mais sério da rodada: SHA-256 de `data:IP:user-agent` com sal público e espaço IPv4 enumerável é quebrável por força bruta offline — e o comentário no código **afirmava** que a tabela "não permite reconstruir histórico de navegação de ninguém", o que era **falso**. Decisão do mantenedor: HMAC com segredo do servidor. `VIEW_HASH_SECRET` obrigatório, sem default (cair numa chave fixa reintroduziria a reversibilidade em silêncio; falhar no boot é melhor). O `throw` está dentro do `try` fail-soft de `registerMaterialView`, então a ficha pública não cai — perde a métrica e loga. Registrado em `.env.example` com `openssl rand -hex 32` e injetado nos dois composes com `:?`. Cobertura nova em `viewOriginHash.test.ts` (6 testes), incluindo que o digest depende do segredo e que nunca contém o IP em claro.

4. **Sonar (nits aplicados):** `<output>` no lugar de `role="status"` (elemento nativo, mesma semântica de live region — os testes que buscam por role seguem passando, o role é implícito); optional chain em `FilterControls` (2 ocorrências); texto de label envolvido em `<span>` nas 6 opções de rádio, removendo a ambiguidade de espaçamento JSX sem mudar render (o espaço vem do `gap` do flex).

#### Descartados, com motivo

- **Sonar Blocker "Add at least one assertion" (`materials.list.test.ts:439`).** Falso positivo: o teste valida por `.expect(200)`/`.expect(400)` do supertest, que **é** asserção real e lança em falha. Sonar não reconhece o padrão. Não vou adicionar `expect` decorativo só para satisfazer a regra.
- **Sonar "Complete the task associated to this TODO" (`materialMetrics.ts:356`).** Falso positivo: não existe nenhum `TODO` no arquivo (`rtk rg "TODO"` retorna vazio). Provável leitura de linha deslocada.
- **Sonar "hardcoded IP 172.18.0.0/16" (`server.ts:60`).** É o default da rede docker interna, idêntico ao usado pelos outros 5 apps, e sobrescrevível por env — agora inclusive injetado explicitamente no compose. Marcar como safe no painel.
- **Sonar "Replace these 3 tests with a single Parameterized one" (`CatalogoPage.test.tsx:95`).** Os três testam comportamentos distintos (vitrine sem parâmetro, sort explícito, sort default explícito). Parametrizar juntaria casos que falham por razões diferentes e tornaria o diagnóstico pior.

#### Validação

`rtk tsc` limpo (backend e frontend) · `rtk vitest` **307 backend** (+11) + **215 frontend** verdes · `rtk pnpm run lint` verde repo-wide (23 tasks) · `rtk pnpm run build` verde · `rtk pnpm verify:api` `breaking=0 non-breaking=1`.

#### Pendência nova de deploy ✅ resolvida (2026-07-26)

**`VIEW_HASH_SECRET`** injetado no `.env.beta` da VM (`/opt/artificio-beta/apps/downloads/.env.beta`). Backup em `.env.beta.bak-20260726`. Composes agora sobem sem travar no `:?`. **Prod** continua sem `.env.prod` (Downloads nunca foi deployado em prod) — quando chegar a hora, repetir o mesmo procedimento.

### Evidência da 3ª rodada de review da PR #214 — 2026-07-26

Três achados (Codex P1 + P2, CodeRabbit Major). Todos procedem; nenhum descartado.

1. **Regressão que eu introduzi na 2ª rodada (Codex P1).** Ao migrar o `scraperScheduler` para o helper de lock, movi o `INSERT` de `download_scraper_run` para **dentro** da transação. Mas `executeScraperRun` (`routes/scraper.ts`) usa a conexão **global**: a run ficava sem commit enquanto o pipeline rodava noutra conexão, que não a enxergava — log com FK quebraria, contadores e `status` não achariam a linha, e a run ficaria presa em `running` para sempre, sem auditoria. A rota manual (`POST /run`) sempre gravou fora de transação; foi o padrão que quebrei. Run volta a ser gravada com `db` global. **O lock não perde propósito**: ele existe para impedir duas execuções concorrentes do agendador, não para tornar a escrita da run atômica. A doc de `advisoryLock.ts` foi corrigida — dizia "todo trabalho tem que usar a transação", o que passou a ser falso; agora distingue os dois casos (expurgo **precisa** da transação, scraper **não pode** usá-la) e alerta sobre transação longa. O teste do scraper passou a injetar uma transação **inerte que estoura se usada**, então a regressão não volta em silêncio.

2. **Desempate determinístico nos rankings (Codex P2).** `.sort((a, b) => b.score - a.score)` devolve `0` no empate — comum com poucas avaliações ou métricas iguais — preservando a ordem arbitrária do `GROUP BY` sem `ORDER BY`, que o Postgres não garante estável entre queries. Como a paginação fatia essa lista, páginas diferentes podiam repetir ou omitir o mesmo material. Comparator `byScoreThenId` compartilhado pelos dois rankings (a regra é a mesma; duplicar seria convite a divergirem). 3 testes novos em `rankingOrder.test.ts`, incluindo o caso decisivo: mesma ordem final mesmo quando o banco devolve as linhas embaralhadas.

3. **Contraste AA do token de marca (CodeRabbit Major).** Confirmado por cálculo, não por confiança no bot: `--artificio-brand-deep` (#e64a19) sobre o fill de marca em superfície clara dá **3.32:1**, abaixo do mínimo AA de 4.5:1 para texto normal. **Não** adotei a sugestão literal de usar `--fg`: daria 16:1, mas apagaria o tom de marca que é a razão de o token existir. Usei um laranja mais fechado (#b3350f) — **5.19:1** sobre superfície branca e **4.66:1** sobre `--surface-subtle` (pior caso do tema claro), ambos acima do mínimo, mantendo a identidade. Tema escuro conferido e preservado: #ffab91 dá **6.77:1**.

#### Validação

`rtk tsc` limpo · `rtk vitest` **310 backend** (+3) + **215 frontend** verdes · `rtk pnpm run lint` verde repo-wide (23 tasks) · `rtk pnpm verify:api` `breaking=0 non-breaking=1`.

#### Pendência de deploy ✅ resolvida (2026-07-26)

`VIEW_HASH_SECRET` injetado no `.env.beta`. Backup criado. T6.2 desbloqueado deste lado.

## Fase 3 — Busca no Header compartilhado (packages/ui) e unificação de rota

- [ ] T3.0 — Rechecagem antes de editar `packages/ui`: confirmar aprovação nominal de commit já obtida (T0.4) — decisão de design (T0.1) e autorização de commit são ações diferentes (`AGENTS.md` §Autorização: "por ação, não por sessão"), não presumir que uma cobre a outra. · feito quando: aprovação nominal de commit confirmada separadamente antes do primeiro `git add`/`git commit` em `packages/ui`.
- [x] T3.1 — `App.tsx`: rotas `/` e `/catalogo` apontam pro mesmo componente unificado (decisão 1, já fixa — ambas rotas continuam válidas). · feito quando: as duas URLs levam à mesma experiência sem erro 404/duplicação de layout.
- [x] T3.2 — Remover `HomePage.tsx`/`HomePage.test.tsx` (decisão 1). · feito quando: nenhum import quebrado, `rtk tsc` limpo.
- [x] T3.3 — Implementar input de busca real no `Header` de `packages/ui` (decisão 2, já fixa): prop nova aditiva (ex. `searchValue`/`onSearchChange`/`searchPlaceholder`), sem quebrar consumidores que não passam a prop. Preservar debounce 300ms e escrita em `?q=` (reaproveitar lógica já existente de `CatalogoPage.tsx` — quem escreve na URL é a página consumidora, não o `Header` em si). `AppShell.tsx` de Downloads liga o `Header` ao estado da página. · feito quando: digitar busca no Header sem sair da tela inicial retorna resultado real e atualiza URL do Downloads; outros módulos que renderizam `Header` sem passar a prop nova continuam funcionando exatamente como antes.
- [ ] T3.4 — Smoke visual de TODOS os consumidores REAIS de `Header` (confirmado por grep, 2026-07-27: `accounts`, mesas, glossario, site, links, downloads — `esferas`/`srd` não têm frontend implementado, fora do smoke por não existirem) rodando cada um localmente ou build, confirmando que a mudança de layout do Header não quebrou nada mesmo em quem não usa a busca nova. **`accounts` (SSO/login) é prioridade de risco** — regressão ali afeta a porta de entrada do portal inteiro, testar primeiro. · feito quando: evidência (screenshot ou descrição) registrada por módulo real, nenhuma quebra visual identificada.
- [x] T3.5 — Estender `apps/downloads/frontend/src/types/material.ts`: `materialSchema` (Zod) ganha `avg_rating`/`rating_count`/`popularity_score` (nullable conforme contrato do backend, Fase 1B); `SORT_OPTIONS`/`SORT_LABELS` em `types/material.ts`/`CatalogoPage.tsx` ganham `'rating'`/`'trending'`. Sem isso os campos novos chegam como `unknown`/`any` no frontend, violando a normalização pétrea de `AGENTS.md` §Regras Gerais de Código. · feito quando: `materialListResponseSchema.parse()` valida a resposta real da API com os campos novos sem erro; `SORT_LABELS` cobre os 2 valores novos no dropdown.

### Evidência parcial da Fase 3 — 2026-07-26

- **T3.0 permanece aberto:** escrita local em `packages/ui` foi autorizada na Fase 0; não houve autorização desta ação de commit. Nenhum `git add`, commit ou push executado.
- **T3.1/T3.2:** `/` e `/catalogo` renderizam `CatalogoPage`; `HomePage.tsx` e seu teste foram removidos. `App.routes.test.tsx` prova as duas rotas contra o mesmo componente. TypeScript do frontend limpo.
- **T3.3:** `Header` ganhou contrato controlado aditivo (`searchValue`, `onSearchChange`, `searchPlaceholder`, `searchLabel`). O contrato legado `showSearch` + `onSearch` permanece. Downloads concentra estado, debounce de 300 ms e escrita de `q`/`page=1` no `AppShell`; busca iniciada fora do catálogo navega para `/catalogo`. O chip ativo de `q`, ausente na implementação anterior, foi incluído e pode limpar a busca.
- **T3.4 parcial:** builds verdes, na ordem de risco, para `accounts`, `downloads`, `mesas`, `glossario`, `site` e `links`. O contrato sem busca e o botão legado estão cobertos por teste do `Header`. Smoke visual não foi marcado como concluído: o Browser interno não está disponível neste ambiente; o runtime ofereceu apenas Chrome com perfil real, que não foi usado porque exige autorização nominal do mantenedor. Nenhum servidor auxiliar ficou rodando.
- **T3.5:** já executada e validada na Fase 2, como registrado acima; não exigiu nova alteração nesta branch.
- **Testes:** `packages/ui` 42/42 verdes; `downloads-frontend` 216/216 verdes. Casos novos cobrem busca controlada, fallback legado, consumidores sem busca, debounce/URL nas duas origens e unificação de rotas.
- **Qualidade:** `rtk tsc` limpo em `packages/ui` e `downloads-frontend`; lint limpo nos dois; `rtk pnpm verify:api` verde (`breaking=0`, `downloads non-breaking=1`). O lint OpenAPI manteve 3 avisos preexistentes de caminhos ambíguos em `mesas`/`glossario`; decisão do mantenedor sobre corrigir agora ou registrar débito continua pendente.

### Reexecução da Fase 3 em sessão nova — 2026-07-26

Sessão nova, contexto zerado: a Fase 3 foi refeita do zero contra o código real, não aceita a partir da evidência anterior. Toda a validação abaixo é execução nova, via `rtk`.

- **T3.1/T3.2 reconferidos no código:** `App.tsx` aponta `/` e `/catalogo` para `CatalogoPage`; `HomePage.tsx`/`HomePage.test.tsx` continuam removidos; `App.routes.test.tsx` cobre as duas rotas.
- **T3.3 reconferido no código:** `HeaderProps` expõe `searchValue`/`onSearchChange`/`searchPlaceholder`/`searchLabel`, com `hasEmbeddedSearch = showSearch && Boolean(onSearchChange)` — consumidor que não passa `onSearchChange` cai no botão-lupa legado, sem mudança de comportamento. `AppShell.tsx` detém estado, debounce de 300 ms, `page=1` e navegação para `/catalogo` quando a busca começa fora do catálogo. `styles.css` usa só tokens semânticos (`--surface-subtle`, `--line`, `--fg`, `--fg-muted`, `--artificio-focus`) e reserva linha própria para a busca no breakpoint mobile.
- **Bug corrigido nesta reexecução (`MaterialShelf.tsx`):** o ajuste da Fase 4 que trocou `Ver tudo` por `Ver tudo →` colocou a seta dentro do nome acessível do link e quebrou `MaterialShelf.test.tsx` (`getByRole('link', { name: 'Ver tudo' })`). Corrigido na causa: a seta virou `<span aria-hidden="true">`, mantendo a direção visual aprovada na Fase 1 sem poluir o nome acessível. Confirmado pelo mantenedor manter a correção. Nenhuma asserção de teste foi afrouxada.
- **Validação nova executada:** `rtk` + `tsc --noEmit` limpo em `packages/ui` e `downloads-frontend`; `rtk pnpm --filter @artificio/ui test` **42/42**; `rtk pnpm --filter downloads-frontend test` **216/216** (antes da correção: 215/216, 1 falha em `MaterialShelf.test.tsx`); `rtk pnpm run lint` **23/23 tasks**; `rtk pnpm run build` **23/23 tasks** — cobre os 6 consumidores reais de `Header` (`accounts`, `downloads`, `mesas`, `glossario`, `site`, `links`); `rtk pnpm verify:api` verde (`breaking=0`, `downloads non-breaking=1`), sem drift em `docs/api/generated/*`.
- **T3.4 permanece ABERTO por decisão do mantenedor (2026-07-26):** o smoke *visual* real não foi executado. Browser interno indisponível neste ambiente e o Chrome com perfil real não foi usado (exige autorização nominal, não concedida). O que existe é evidência de build/teste, não inspeção visual. A task fica explicitamente pendente — não marcada como feita. Nenhum servidor auxiliar ficou rodando.
- **T3.0 permanece ABERTO:** nenhum `git add`, `git commit` ou `git push` executado nesta sessão. Autorização de commit não foi pedida nem concedida.

## Fase 4 — Polish visual, responsividade e SEO

- [x] T4.0 — Auditar o estado real antes do handoff: confrontar `plan.md` §Direção de design com os componentes atuais e separar o que a Fase 2 antecipou do que ainda falta. Não reimplementar capa/eyebrow/rating/prateleiras/pills já existentes. · feito quando: matriz de estado/lacunas registrada em `plan.md` §Handoff de implementação — Fase 4.
- [ ] T4.1 — Fechar a direção visual aprovada da Fase 1, sem redesenhar: conferir `MaterialCard`, `MaterialRating`, `MaterialShelf`, `CatalogShowcase`, `FilterPills`, `SystemChainBadge` e `CatalogoPage` contra tipografia, espaçamento e cor do `plan.md`. Estados obrigatórios: com/sem capa; crédito vazio; título longo; cenário ausente; zero avaliações; cadeia de sistema longa; vitrine e resultado; claro e escuro. Remover qualquer cor literal/Tailwind cromática restante em favor dos tokens semânticos de `packages/ui`. Confirmar “Ver tudo →”, badges 11px/600, 40px entre prateleiras, contagem total e vazio orientativo. · feito quando: inspeção visual nos dois temas confirma a proposta aprovada, sem divergência nova; testes dos componentes atualizados.
- [x] T4.2 — **Trabalho dedicado de acessibilidade dispensado pelo mantenedor**, reconfirmado em 2026-07-26 ao iniciar esta fase. Não rodar auditoria/checklist AA, teclado, leitor de tela, contraste ou `prefers-reduced-motion`; não criar task substituta escondendo o mesmo escopo. Preservar proteções existentes sem expandi-las. · feito quando: `spec.md`, `plan.md` e esta task não exigem acessibilidade como gate da Fase 4.
- [ ] T4.3 — Validar responsividade real em 320px, 375px, 768px e desktop ≥1024px. Em mobile: busca do `Header` em linha própria sem overflow; pills quebram linha e popover fica dentro da viewport; prateleira rola por toque/trackpad e mantém cards 220px com pista visual do próximo; sort usa largura disponível; drawer de `CatalogFilterSidebar` abre, aplica filtro e fecha; grid/paginação não estouram horizontalmente. Em desktop: cards 240px ≥1024px, sidebar preservada e Header sem colisão entre nav/busca/sessão. · feito quando: browser/devtools real executado em cada largura, resultado descrito por viewport e nenhuma quebra visual aberta.
- [ ] T4.4 — Implementar canonical route-aware sem dependência nova depois da decisão do mantenedor sobre o alvo (`/` ou `/catalogo`). **Não** adicionar canonical estática em `index.html`: o fallback SPA a vazaria para ficha/painel/gestão. Montar componente/hook somente em `CatalogoPage`, garantir um único `link[rel="canonical"]` absoluto para `/`, `/catalogo` e todas as query strings, e remover no cleanup. Testar criação, unicidade, alvo e cleanup em JSDOM. Validar no DOM renderizado — `view-source`/`curl` não executam React e não servem para esta implementação. · feito quando: as duas rotas e seus filtros apontam para o mesmo alvo decidido, rotas alheias não herdam a tag e teste automatizado prova o ciclo completo.

### Evidência de preparação da Fase 4 — 2026-07-26

- Auditoria confirmou que a maior parte de T4.1 foi antecipada na Fase 2; inventário detalhado e lacunas estão em `plan.md` §Handoff de implementação — Fase 4.
- Ordem do mantenedor aplicada: acessibilidade saiu como frente/gate; nenhuma proteção existente deve ser desfeita, mas não há trabalho novo de AA nesta fase.
- Lacuna de T4.4 corrigida documentalmente: canonical estática em `index.html` contaminaria todas as rotas do SPA; manipulação React não aparece em `view-source`. Google aceita canonical injetada por JavaScript durante renderização, desde que exista uma única tag consistente (fonte oficial registrada no `plan.md`).
- Decisão pendente, não inferida: alvo canônico único `/` ou `/catalogo`.
- Ajustes preparatórios locais já presentes e mantidos por ordem do mantenedor: “Ver tudo →”, gesto/overscroll da prateleira, contenção mobile do popover, tipografia/token dos badges, token de erro, sort mobile e contagem de resultados. T4.1/T4.3 continuam abertas porque não houve validação visual nem rodada completa de testes após esses ajustes.

### Auditoria da Fase 4 contra `spec.md`/`plan.md` — 2026-07-26 (sessão nova)

Auditoria feita contra o **código real**, não contra o `tasks.md`: `AGENTS.md` §Erros que não podem se repetir manda tratar código como verdade material quando doc e código divergem.

**Conforme à direção aprovada (verificado arquivo a arquivo):** Requisito 8 (contrato do card preservado: título em 2 linhas sem truncamento cego, cenário, badges, `SystemChainBadge`, alvo de clique único); eyebrow de crédito acima do título em Oswald com fallback “Acervo Artifício” cobrindo `""`/`"   "`; eyebrow como `<p>`, nunca link; capa sangrada com a mesma silhueta com e sem imagem; Requisito 17 e as 3 travas das estrelas (`<span>` nunca `<button>`, meia-estrela por gradiente, bloco inteiro some com `rating_count === 0`); espaçamentos base 4 do card e **40px entre prateleiras** (`gap-10`); trilho com snap `proximity`, 220/240px, último item em `snap-end`, sem setas; Requisito 16 (prateleira sem item elegível não renderiza, coberto por teste); copy sem hype e vazio orientativo; contagem total com singular tratado; Requisito 9a nos componentes desta spec (só tokens semânticos); badges 11px/600; popover das pills contido no mobile consumindo `--state-brand-*`. Todos os componentes da fase têm arquivo de teste próprio.

**Desvio real encontrado e CORRIGIDO nesta sessão (`SystemChainBadge.tsx`):** o badge usava `--artificio-brand` (#ff5722) como **cor de texto**. O token cru de marca é acento/preenchimento, não texto: a própria Fase 2 desta branch criou `--state-brand-fg` (#b3350f claro / #ffab91 escuro) porque `--artificio-brand-deep` (#e64a19) dava 3.32:1 sobre o fill de marca — e #ff5722 é mais claro ainda que o tom já rejeitado. O ajuste da Fase 4 tocou exatamente essa linha (trocou `border-white/10` e `--admin-surface` por tokens) e deixou a cor de texto no token errado, com o token correto disponível ao lado, criado na mesma branch. Não é frente de acessibilidade (dispensada pelo Requisito 10) — é consistência do design system quebrada dentro da própria branch. Corrigido para `--state-brand-fg` por autorização do mantenedor, com comentário inline registrando a razão para não se perder num refactor futuro.

**Contradição interna do `plan.md` (documental, não é erro do código):** §Tipografia afirma “Oswald aparece em exatamente 2 lugares — título de prateleira e crédito. Nada mais”, mas a tabela imediatamente acima lista um terceiro papel em Oswald (“Rating numérico | Oswald | 13 / 600 / tabular”). O código tem os 3 usos e seguiu a tabela; a frase da regra é que está desatualizada. Registrado aqui em vez de “corrigido” silenciosamente porque mudar a direção aprovada da Fase 1 exigiria nova aprovação.

**Validação executada após a correção:** `tsc --noEmit` limpo em `downloads-frontend`; `rtk pnpm --filter downloads-frontend test` **216/216**; `rtk pnpm run lint` **23/23**; `rtk pnpm run build` **23/23**.

**Continua ABERTO na Fase 4:** T4.1/T4.3 (nenhuma inspeção visual ou de viewport real foi executada — mesmo bloqueio de browser da Fase 3) e T4.4 (canonical **não existe no código**; `rtk rg "canonical"` no frontend só retorna comentários do importador). T4.4 é critério de aceite da spec e segue bloqueado pela decisão não tomada do alvo canônico (`/` ou `/catalogo`).

## Fase 5 — Testes e validação

- [ ] T5.1 — Atualizar/criar testes: `CatalogoPage.test.tsx` (ou nome novo) cobrindo modo vitrine e modo resultado, novos sorts; `AppShell.test.tsx` e teste de `Header.tsx` (packages/ui) cobrindo a busca embutida. · feito quando: `rtk pnpm --filter downloads-frontend test` e teste de `packages/ui` verdes.
- [ ] T5.2 — Lint e build (frontend + backend + packages/ui) + `pnpm verify:api` (contrato de `GET /materials`/`GET /materials/:slug` mudou, Fase 1B). · feito quando: `rtk lint`, `pnpm run build` (downloads-frontend, downloads-backend, packages/ui, e os 6 módulos consumidores de Header) e `pnpm verify:api` verdes, `docs/api/generated/*` regenerado revisado.
- [ ] T5.3 — Smoke manual local (dev server) cobrindo: carregar home com prateleiras, buscar termo real pelo Header, aplicar filtro via pill, paginar, voltar ao modo vitrine limpando filtro, ordenar por Mais visitados/Mais bem avaliados no modo resultado, mobile, tema claro e escuro. · feito quando: cada passo executado e resultado registrado (não é "deveria funcionar" — é execução real).

## Fase 6 — Deploy e fechamento

- [ ] T6.0 — Rechecagem de gate antes de declarar a spec fechada: gate D exige "projeto atual passou smoke" — reconfirmar que T6.2 (smoke beta real) rodou de fato, não só T5.3 (smoke local). `AGENTS.md` §Conclusão de Tarefas proíbe fechar com "local"/"parcial"/"validado em dist local" contando como concluído. · feito quando: smoke beta real (T6.2) confirmado antes de qualquer atualização de status para "fechado" em T6.3.
- [ ] T6.1 — Commit/PR (autorização nominal por ação, conforme `AGENTS.md` — já confirmada em T0.4/T3.0 pra `packages/ui`, mas commit em si continua exigindo autorização própria a cada vez). · feito quando: PR aberta contra `dev`, ready for review.
- [ ] T6.2 — Deploy beta (autorização nominal) + smoke real em `downloadsbeta.artificiorpg.com`, incluindo teste manual do corte de elegibilidade (material real com views e sem downloads, confirmar ausência na prateleira). · feito quando: prateleiras/busca/filtro/ordenação confirmados funcionando em beta real, não só local.
- [ ] T6.3 — Atualizar `specs/backlog.md` com o fechamento (ou débito residual, se houver) e `.specify/memory/project-state.md` se mudar estado operacional do gate D. · feito quando: backlog refletindo o estado real pós-implementação.
- [ ] T6.4 — Promoção pra prod (autorização nominal separada, `workflow_dispatch` manual) — fora desta rodada salvo pedido explícito. · feito quando: `gh run list --workflow=deploy.yml --branch=main` confirma deploy real, não só merge de Git.
