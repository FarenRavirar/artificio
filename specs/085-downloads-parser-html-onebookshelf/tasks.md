# Tasks — Spec 085 (Downloads: parser HTML determinístico OneBookShelf)

## Fase 0 — Fixture real (bloqueante, sem código) — ✅ CONCLUÍDA 2026-07-24
- [x] T0.1 — Mantenedor colou HTML real de produto DMs Guild (via DevTools, pós-Angular). Salvo em `apps/downloads/backend/test/fixtures/dms-guild-product-1.html` (158KB).
- [x] T0.2 — Mantenedor colou HTML real de 1 produto DriveThruRPG. Salvo em `apps/downloads/backend/test/fixtures/drivethrurpg-product-1.html` (136KB).
- [x] **T0.2b — Achado real, correção de escopo:** o 3º arquivo colado (`drivethrurpg-product-2.html`, 144KB) NÃO é DriveThruRPG — confirmado via `canonical`/`og:url`/`<title>` que é produto real de `storytellersvault.com` (White Wolf/Vampire), marca irmã na mesma plataforma OneBookShelf mas domínio/enum diferente (`DownloadSourcePlatform` não tem `storytellersvault`). Mantido como fixture de TESTE NEGATIVO (confirma que o parser rejeita corretamente, requisito 8a) — renomear para `storytellersvault-product-1.html` na Fase 1 pra não confundir com fixture positivo de `drivethrurpg`.
- [x] T0.3 — Confirmado via grep nos 3 arquivos: todos contêm `<script type="application/ld+json">` com `@type: "Product"`, `"Brand"`, `"Offer"` — mesmo shape Schema.org nas 3 plataformas (DMs Guild, DriveThruRPG e StorytellersVault usam a mesma stack OneBookShelf, JSON-LD idêntico em estrutura — por isso a checagem de sanidade sozinha via JSON-LD não basta, precisa confirmar domínio do `canonical` também, requisito 8/8a).
- [x] **Achado real confirmado e CORRIGIDO após 2ª leitura mais cuidadosa (não suposição) — ver requisito 4 revisado da spec:** primeira leitura assumiu que o texto "Pague quanto quiser" era sinal de PWYW no produto — **falso positivo**: esse texto aparece **idêntico nos 3 fixtures** (inclusive nos 2 produtos grátis fixos), porque é link de categoria do MENU de navegação lateral, não sinal do produto específico. Sinal real e confiável, achado na 2ª investigação: tag `<obs-product-format-pwyw-options>` (com múltiplos botões `$X.00` e texto "Apoie o criador ao pagar mais do que o preço sugerido") aparece **só** no fixture PWYW real (Lutador, JSON-LD `price:4`); os 2 fixtures grátis fixos não têm essa tag, só `<p class="cy-prc">$0.00</p>`. `isFreeOrPwyw` extraído com **confiança alta** via presença/ausência dessa tag — não precisa ficar sempre marcado como baixa confiança (correção da decisão original do requisito 4, agora com sinal estrutural real em vez de heurística de texto solto).
- [x] **Achado real, revisão de esforço alto (2026-07-24):** `ScrapedItem.isFreeOrPwyw` (`types.ts:10`) é `boolean` obrigatório e `ingestItemSchema` (`scraper.ts:131`) exige `z.boolean()` — não há "estado incerto" nesse shape. Corrigido: `/parse-html` devolve shape de PREVIEW próprio (`extractedPriceValue`/`priceSignal` além dos campos de `ScrapedItem`), não o `ScrapedItem` direto — ver requisito 3 revisado e Fase 1 abaixo.
- [x] **Achado real, revisão de esforço alto (2026-07-24):** `priceRecheckJob.ts:20` exclui `dms_guild`/`drivethrurpg` da re-checagem de preço pós-publicação (mesmo WAF) — material catalogado por esta spec fica permanentemente sem essa rede de segurança. Documentado como requisito 15 + risco na spec, sem tentar resolver aqui (fora de escopo).

## Fase 1 — Parser puro (testável isolado)
- [x] T1.0 — Renomear fixture `drivethrurpg-product-2.html` → `storytellersvault-product-1.html` (achado T0.2b) antes de escrever qualquer teste, pra não confundir fixture positivo com negativo. Confirmado: `apps/downloads/backend/test/fixtures/storytellersvault-product-1.html` já existe com nome correto.
- [x] T1.1 — `services/scrapers/onebookshelfHtmlParser.ts`: função `parseOneBookShelfHtml(html, sourcePlatform)` criada, extrai JSON-LD via regex ancorada + `JSON.parse`, mapeia `name`→title, `description`→description, `brand.name`→publisherName. Retorna shape de PREVIEW próprio (`OneBookShelfParsePreview`), não `ScrapedItem` direto. Pure function, sem persistência.
- [x] T1.2 — Checagem de sanidade via `OneBookShelfParseError` tipado (`code`: `html_too_large`/`missing_json_ld`/`invalid_json_ld`/`missing_canonical`/`domain_mismatch`). Domínio do canonical validado via `new URL(sourceUrl).hostname` contra `SOURCE_DOMAIN[sourcePlatform]`. Testado (`onebookshelfHtmlParser.test.ts`, 9/9 verde) contra os 4 casos, incluindo fixture `storytellersvault-product-1.html` como negativo (2 variantes: declarado como `drivethrurpg` e como `dms_guild`).
- [x] T1.3 — Sinais complementares extraídos via regex isolada (sem lib DOM, mesmo padrão `itchIoParser.ts`): `canonical`→sourceUrl, `og:image`→coverImageUrl (regex aceita `content` antes ou depois de `property`, mesma cautela do parser itch.io), `html lang`→sourceLanguageHint (`pt`/`not_pt`/`null`). Confirmado por teste contra os 2 fixtures válidos reais (DMs Guild: `pt`, `4` USD, PWYW; DriveThruRPG: `pt`, `0` USD, grátis fixo).
- [x] T1.4 — Preço/`isFreeOrPwyw` implementado em `resolvePriceSignal()`: `extractedPriceValue` vem de `offers.price` (JSON-LD, único sinal necessário nos 3 fixtures reais — fallback `<p class="cy-prc">` não foi preciso, JSON-LD sempre presente). Tag `obs-product-format-pwyw-options` presente → `pwyw_tag_present`/`true`; ausente + preço `0` → `zero_price_no_pwyw_tag`/`true`; ausente + preço > 0 → `nonzero_price_no_pwyw_tag`/`null` (sugestão omitida, não `false`). Testado: fixture DMs Guild real (`pwyw_tag_present`), fixture DriveThruRPG real (`zero_price_no_pwyw_tag`), caso sintético preço=15 sem tag (`nonzero_price_no_pwyw_tag`, `isFreeOrPwyw: null`).
- [x] T1.5 — `oneBookShelfParsePreviewSchema` (Zod `.strict()`) valida todo retorno de `parseOneBookShelfHtml` via `.parse()` antes de devolver ao caller — não é checagem passiva, é gate ativo (lança se schema não bater). Teste confirma que campo extra injetado no preview é rejeitado pelo schema.
- [x] T1.6 — Suíte `onebookshelfHtmlParser.test.ts`: 10/10 verde, sem mock de HTML — fixtures reais de `test/fixtures/`. Cobre: extração completa dos 2 fixtures válidos, rejeição de domínio incompatível (fixture StorytellersVault, 2 variantes), JSON-LD ausente/malformado, canonical ausente, HTML acima do limite, schema fechado rejeitando campo extra. `tsc --noEmit` limpo.

## Fase 2 — Rota
- [x] T2.1 — `POST /api/v1/admin/scraper/parse-html` criada em `routes/scraper.ts`: `writeRateLimiter` + `authMiddleware` + `requireRole('admin')` (mesma ordem das rotas vizinhas). `parseHtmlBodySchema` valida `source_platform` contra `PARSE_HTML_SOURCE_PLATFORMS = ['dms_guild', 'drivethrurpg']` (allowlist própria, não reaproveita `IMPLEMENTED_SOURCE_PLATFORMS`). Handler chama `parseOneBookShelfHtml`, mapeia `OneBookShelfParseError` → 422, sucesso → 200 `{ preview }`. Suíte `scraper.test.ts` existente (9/9) segue verde — rota nova não quebrou nada. `tsc --noEmit` limpo. 401/403 cobertos pelo mesmo `authMiddleware`/`requireRole` já usado (e já testado) nas outras rotas do arquivo; teste dedicado da rota nova fica pra T2.4.
- [x] T2.2 — Tamanho checado via `parseHtmlBodySchema` (`z.string().min(1).max(MAX_HTML_LENGTH)`, `MAX_HTML_LENGTH = 1_000_000` em `onebookshelfHtmlParser.ts`) dentro do `safeParse` do handler — não middleware dedicado, `server.ts:67` (`express.json({ limit: '4mb' })`) cobre o transporte. **Achado real ao testar:** o `app()` de teste em `scraper.test.ts` usava `express.json()` sem limite (default 100kb do body-parser), diferente do `server.ts` real (4mb) — payload de teste >100kb estourava 413 no body-parser antes de chegar no handler, mascarando o 422 esperado do schema. Corrigido replicando o limite real (`{ limit: '4mb' }`) no `app()` de teste. Teste `422 quando html excede o limite de tamanho` confirma o comportamento correto do handler (10/10 verde).
- [x] T2.3 — `parseOneBookShelfHtml` chamada no handler (já feito em T2.1), `OneBookShelfParseError` → 422 com `error`+`code`, sucesso → 200 `{ preview }`. Confirmado: error handler global (`server.ts:106`, `console.error('[Global Error]', err)`) loga só o objeto `HttpError` (`type`/`status`/`statusCode`/`message`), nunca `req.body` — nenhuma exclusão extra necessária. Teste `nunca loga o conteúdo do html recebido` injeta marcador único no HTML, espiona `console.log`/`console.error` durante a chamada (branch de erro 422), confirma ausência do marcador na saída capturada (11/11 verde).
- [x] T2.4 — Testes de rota adicionados em `scraper.test.ts` (suíte completa do arquivo: 16/16 verde, `tsc --noEmit` limpo): 200 com preview real (fixture DMs Guild, PWYW confirmado), 200 com preview real (fixture DriveThruRPG, grátis fixo confirmado), 422 domínio incompatível (fixture `storytellersvault-product-1.html` declarado como `drivethrurpg`, `code: 'domain_mismatch'`, requisito 8a), 422 `source_platform` ausente, 422 `html` ausente, 422 payload grande demais (T2.2). **401/403 não testado nesta rota especificamente** — `authMiddleware`/`requireRole` são mockados como pass-through (`next()` direto) em todo `scraper.test.ts`, mesmo padrão já usado nas outras 4 rotas do arquivo (nenhuma delas testa 401/403 isoladamente); comportamento de auth é responsabilidade do middleware compartilhado, sem teste dedicado no repo hoje — registrado aqui como lacuna pré-existente, não introduzida por esta task.

## Fase 3 — Dedupe por similaridade (adaptado de `tableDuplicateDetection.ts`)
- [x] T3.1 — Confirmado sem migration necessária: `pg_trgm` já habilitada (`apps/downloads/database/migration_009_download_material_indexes.sql:12`, `CREATE EXTENSION IF NOT EXISTS pg_trgm`), com índice GIN trigram já criado em `download_material.title` (`idx_download_material_title_trgm`) e `summary` (`idx_download_material_summary_trgm`) — spec 070. **Achado real:** `download_material_metadata` citada no `plan.md` não existe como tabela separada; `title`/`summary`/`description` vivem direto em `download_material` (`db/types.ts:26-51`). Dedupe consulta `download_material` diretamente, sem tabela auxiliar.
- [x] T3.2 — `services/scrapers/onebookshelfDuplicateCheck.ts` criado: `findDuplicateCandidates(title)` consulta `download_material` via kysely query builder (`similarity(lower(title), lower($1))`, threshold `0.75` reaproveitado de `tableDuplicateDetection.ts`, `editorial_state in ('draft','in_review','published')`), retorna até 10 candidatos ordenados por similaridade desc. Nunca decide sozinho. Testado (`onebookshelfDuplicateCheck.test.ts`, 3/3 verde): candidato encontrado, nenhum candidato, título vazio não consulta o banco. `tsc --noEmit` limpo.
- [x] T3.3 — Rota `/parse-html` chama `findDuplicateCandidates(preview.title)` após parse bem-sucedido, resposta 200 inclui `{ preview, duplicateCandidates }` (`[]` se nenhum candidato). Teste dedicado confirma candidato retornado (similaridade 0.95) não impede 200 com preview completo. Suíte inteira do backend `downloads` afetada por esta fase: 48/48 verde (`scraper.test.ts` 17, `onebookshelfHtmlParser.test.ts` 10, `onebookshelfDuplicateCheck.test.ts` 3, + scrapers/scheduler pré-existentes intactos). `tsc --noEmit` limpo.

## Fase 4 — Auditoria mínima de extração (adaptado de `discord_parse_cases`, sem aprendizado)
- [x] **T4.1 — Decisão técnica já resolvida (revisão de esforço alto, 2026-07-24): tabela nova mínima, não reaproveitar schema da spec 084.** Checado contra código real (`db/types.ts:198-229`): `DownloadScraperRunTable.trigger_kind` é `DownloadScraperTriggerKind = 'manual' | 'cron' | 'local_ingest'` — enum **fechado**, adicionar `'html_paste'` exigiria migration de qualquer forma (não é "sem schema novo" como a opção (b) original sugeria). Além disso `download_scraper_run`/`download_scraper_item_log` modelam conceitualmente "uma run que processa N items e cria/rejeita material" (`items_found`/`items_created`/`outcome: 'created'|'skipped_*'`) — não cabe "parse individual que só gerou preview, ninguém confirmou nada ainda, pode nem virar material". Forçar esse conceito no schema existente é abuso de modelo. **Tabela nova mínima:** `download_scraper_parse_log` (migration nova, `apps/downloads/database/`, seguindo checklist de 5 campos header do `AGENTS.md`) com: `id`, `source_platform` (mesmo enum `DownloadSourcePlatform`), `admin_user_id`, `fields_extracted` (jsonb — quais campos vieram preenchidos vs. vazios), `price_signal` (mesmo enum do requisito 3 da spec), `parse_case_id` como PK própria pra linkar depois (T4.3), `created_at`. **Sem `run_id`** — não pertence a uma run do scraper automático. **Migration criada:** `apps/downloads/database/migration_024_download_scraper_parse_log.sql` (header 5 campos, `CREATE TABLE IF NOT EXISTS`, idempotente), com `confirmed_material_id UUID NULL REFERENCES download_material(id) ON DELETE SET NULL` adicionado (não estava na decisão original) pra T4.3 não precisar de 2ª migration. Tabela+tipos TS (`DownloadScraperParseLogTable`, `db/types.ts`) registrados em `Database`. `tsc --noEmit` limpo.
- [x] T4.2 — Rota `/parse-html` grava em `download_scraper_parse_log` após parse+dedupe bem-sucedidos: `fields_extracted` (objeto de booleans — preenchido/vazio por campo, não o valor em si), `price_signal`, `admin_user_id` (`req.user!.userId`, mesmo padrão `!` já usado em todas as outras rotas do backend pós-`requireRole`), `source_platform`. Resposta 200 ganha `parse_case_id` (prepara T4.3). **Nunca grava HTML bruto** — `fields_extracted` é só booleans (~200 bytes), não texto extraído. Teste dedicado confirma via `insertInto` spy: `admin_user_id`/`source_platform`/`price_signal` corretos, marcador do HTML (`obs-product-format-pwyw-options`) ausente do payload gravado, tamanho do JSON gravado <1000 bytes (fixture real tem 158KB). Suíte `scraper.test.ts`: 18/18 verde, `tsc --noEmit` limpo.
- [x] T4.3 — `ingestItemSchema` ganhou `parse_case_id: z.uuid().optional()`. Handler `/ingest` extrai `parse_case_id` por item (mapa `sourceUrl → parse_case_id`, removido antes de montar o `ScrapedItem[]` que o pipeline consome — pipeline não muda), roda `runScraperIngest` normalmente. **Após** o pipeline completar, consulta `download_scraper_item_log` por `run_id` (filtrando `material_id IS NOT NULL`) e faz `UPDATE download_scraper_parse_log SET confirmed_material_id = ...` por `source_url` casado — best-effort, mesmo princípio do `logItem` em `scraperIngest.ts` (link é observabilidade, nunca muda o resultado do ingest). Achado confirmado: sem mapeamento item→material_id no retorno de `runScraperIngest` (só agregados), a fonte de verdade pro link é `download_scraper_item_log` (já grava `source_url`+`material_id` por item, spec 084) — evitou reescrever pipeline de produção só por causa deste link. Teste `linka parse_case_id ao material criado` cobre o fluxo completo, teste `não quebra quando item não tem parse_case_id` confirma retrocompatibilidade total (nem consulta `download_scraper_item_log` quando não há `parse_case_id` no payload). Suíte completa do backend `downloads`: **34 arquivos, 183 testes, 100% verde**; `tsc --noEmit` sem erros.

## Fase 5 — Frontend (painel `/gestao`)
- [ ] T5.1 — UI nova (local exato a definir durante implementação — provável extensão de alguma página de gestão de materiais existente, ou nova): campo de texto para colar HTML + botão "Analisar" → chama `/parse-html` → mostra preview dos campos extraídos, incluindo `extractedPriceValue`/`priceSignal` sempre visíveis (requisito 3/4 — nunca só um `isFreeOrPwyw` boolean sem contexto) e candidatos de duplicata (Fase 3) exibidos como alerta não-bloqueante. Quando `priceSignal: 'nonzero_price_no_pwyw_tag'`, UI bloqueia visualmente o botão de confirmar até o admin editar manualmente (produto parece pago, fora do critério D119). Feito quando: componente renderiza preview e permite editar cada campo antes de confirmar.
- [ ] T5.2 — Confirmação do preview chama `POST /admin/scraper/ingest` já existente (reaproveita fluxo, sem rota nova de publicação). Feito quando: fluxo completo colar→preview→editar→confirmar→publicado testado manualmente em dev local.
- [ ] T5.3 — Teste de componente (render + interação básica), seguindo padrão de cobertura já usado no restante do frontend `downloads`.

## Fase 6 — Validação final
- [ ] T6.1 — `pnpm verify:api` (rota nova documentada em OpenAPI gerado), lint, build, test (backend + frontend) verdes.
- [ ] T6.2 — Smoke manual real em beta: colar HTML real de 1 produto de cada fonte (pode reaproveitar as URLs de exemplo já discutidas: DMs Guild filtro pt/pwyw), confirmar preview, publicar via `/ingest`, conferir item aparece corretamente em `GET /materials` e na página pública do material. Incluir teste de duplicata: colar o mesmo HTML 2x, confirmar que o segundo aponta candidato de duplicata no preview.
- [ ] T6.3 — Atualizar `specs/backlog.md` (entrada nova ou fechamento, conforme status ao final) e `.specify/memory/project-state.md` no fechamento.

## Débitos/riscos desta spec
- [ ] D-085-01 — Risco jurídico residual aceito conscientemente: HTML de produto passa pela memória do request (RAM) mesmo nunca sendo persistido — mantenedor decidiu que isso é tolerável, mas não é "zero risco", registrar como decisão consciente, não como resolvido.
- [ ] D-085-02 — Volume de uso real ainda desconhecido no momento da decisão desta spec (mantenedor respondeu "não sei ainda, vou descobrir usando") — se o uso real for baixo, revisitar se o investimento se pagou; não é motivo para não implementar agora, é item de acompanhamento pós-implementação.
- [ ] D-085-03 — Se o layout de DMs Guild/DriveThruRPG mudar e o parser passar a falhar sistematicamente (JSON-LD sumir ou mudar de shape), não há detecção automática nesta spec — só será percebido quando um admin colar HTML de novo e o resultado vier estranho/com erro. Ver critério de aceite de 422 explícito como mitigação parcial (falha visível é melhor que campo errado silencioso, mas não é alarme proativo).
