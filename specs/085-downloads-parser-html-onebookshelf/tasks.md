# Tasks — Spec 085 (Downloads: parser HTML determinístico OneBookShelf)

## Fase 0 — Fixture real (bloqueante, sem código) — ✅ CONCLUÍDA 2026-07-24
- [x] T0.1 — Mantenedor colou HTML real de produto DMs Guild (via DevTools, pós-Angular). Salvo em `apps/downloads/backend/test/fixtures/dms-guild-product-1.html` (158KB).
- [x] T0.2 — Mantenedor colou HTML real de 1 produto DriveThruRPG. Salvo em `apps/downloads/backend/test/fixtures/drivethrurpg-product-1.html` (136KB).
- [x] **T0.2b — Achado real, correção de escopo:** o 3º arquivo colado (`drivethrurpg-product-2.html`, 144KB) NÃO é DriveThruRPG — confirmado via `canonical`/`og:url`/`<title>` que é produto real de `storytellersvault.com` (White Wolf/Vampire), marca irmã na mesma plataforma OneBookShelf mas domínio/enum diferente (`DownloadSourcePlatform` não tem `storytellersvault`). Mantido como fixture de TESTE NEGATIVO (confirma que o parser rejeita corretamente, requisito 8a) — renomear para `storytellersvault-product-1.html` na Fase 1 pra não confundir com fixture positivo de `drivethrurpg`.
- [x] T0.3 — Confirmado via grep nos 3 arquivos: todos contêm `<script type="application/ld+json">` com `@type: "Product"`, `"Brand"`, `"Offer"` — mesmo shape Schema.org nas 3 plataformas (DMs Guild, DriveThruRPG e StorytellersVault usam a mesma stack OneBookShelf, JSON-LD idêntico em estrutura — por isso a checagem de sanidade sozinha via JSON-LD não basta, precisa confirmar domínio do `canonical` também, requisito 8/8a).
- [x] **Achado real confirmado e CORRIGIDO após 2ª leitura mais cuidadosa (não suposição) — ver requisito 4 revisado da spec:** primeira leitura assumiu que o texto "Pague quanto quiser" era sinal de PWYW no produto — **falso positivo**: esse texto aparece **idêntico nos 3 fixtures** (inclusive nos 2 produtos grátis fixos), porque é link de categoria do MENU de navegação lateral, não sinal do produto específico. Sinal real e confiável, achado na 2ª investigação: tag `<obs-product-format-pwyw-options>` (com múltiplos botões `$X.00` e texto "Apoie o criador ao pagar mais do que o preço sugerido") aparece **só** no fixture PWYW real (Lutador, JSON-LD `price:4`); os 2 fixtures grátis fixos não têm essa tag, só `<p class="cy-prc">$0.00</p>`. `isFreeOrPwyw` extraído com **confiança alta** via presença/ausência dessa tag — não precisa ficar sempre marcado como baixa confiança (correção da decisão original do requisito 4, agora com sinal estrutural real em vez de heurística de texto solto).
- [x] **Achado real, revisão de esforço alto (2026-07-24):** `ScrapedItem.isFreeOrPwyw` (`types.ts:10`) é `boolean` obrigatório e `ingestItemSchema` (`scraper.ts:131`) exige `z.boolean()` — não há "estado incerto" nesse shape. Corrigido: `/parse-html` devolve shape de PREVIEW próprio (`extractedPriceValue`/`priceSignal` além dos campos de `ScrapedItem`), não o `ScrapedItem` direto — ver requisito 3 revisado e Fase 1 abaixo.
- [x] **Achado real, revisão de esforço alto (2026-07-24):** `priceRecheckJob.ts:20` exclui `dms_guild`/`drivethrurpg` da re-checagem de preço pós-publicação (mesmo WAF) — material catalogado por esta spec fica permanentemente sem essa rede de segurança. Documentado como requisito 15 + risco na spec, sem tentar resolver aqui (fora de escopo).

## Fase 1 — Parser puro (testável isolado)
- [ ] T1.0 — Renomear fixture `drivethrurpg-product-2.html` → `storytellersvault-product-1.html` (achado T0.2b) antes de escrever qualquer teste, pra não confundir fixture positivo com negativo.
- [ ] T1.1 — `services/scrapers/onebookshelfHtmlParser.ts`: função `parseOneBookShelfHtml(html: string, sourcePlatform: 'dms_guild' | 'drivethrurpg')` — extrai bloco JSON-LD via regex ancorada, `JSON.parse`, mapeia `name`→title, `description`→description, `brand.name`→publisherName. Retorna o shape de PREVIEW (ver `plan.md` "Shape de preview"), não `ScrapedItem` direto. Feito quando: função existe, tipada, sem persistir nada (pure function).
- [ ] T1.2 — Checagem de sanidade de entrada: rejeita (retorna erro tipado, não lança exceção genérica) quando falta `<script type="application/ld+json" @type=Product>`, quando falta `<link rel="canonical">`, quando o DOMÍNIO do `canonical` não bate com o `source_platform` declarado (ex.: `source_platform: 'drivethrurpg'` mas `canonical` é `storytellersvault.com` → rejeitar, requisito 8a), ou quando o HTML é maior que o limite de tamanho definido (ex. 2MB). Feito quando: teste unitário cobre os 4 casos, incluindo o fixture `storytellersvault-product-1.html` (T0.2b) como teste negativo de domínio incompatível.
- [ ] T1.3 — Extração dos sinais complementares fora do JSON-LD (regex de tag/bloco isolado, sem lib DOM — mesmo padrão de `itchIoParser.ts`): `<link rel="canonical">`→sourceUrl, `<meta property="og:image">`→coverImageUrl, `<html lang="...">`→sourceLanguageHint. Feito quando: teste confirma extração correta contra os 2 fixtures válidos (DMs Guild, DriveThruRPG).
- [ ] T1.4 — Preço/`isFreeOrPwyw` via sinal estrutural confiável (achado real, ver T0): extrai preço numérico bruto (`extractedPriceValue`) de `offers.price` (JSON-LD) OU `<p class="cy-prc">` (fallback DOM); presença de `<obs-product-format-pwyw-options>` no HTML → sugestão `isFreeOrPwyw: true`, `priceSignal: 'pwyw_tag_present'`; ausência + preço extraído = `0` → sugestão `true`, `priceSignal: 'zero_price_no_pwyw_tag'`; ausência + preço > 0 → sugestão **omitida** (não `false` silencioso — ver requisito 3, o pipeline de `/ingest` trataria `false` como erro genérico), `priceSignal: 'nonzero_price_no_pwyw_tag'`, preview sinaliza "produto pago, fora do critério D119". Feito quando: teste cobre os 2 fixtures válidos reais (1 PWYW confirmado `pwyw_tag_present`, 1 grátis fixo confirmado `zero_price_no_pwyw_tag`) e 1 caso sintético de preço pago (`nonzero_price_no_pwyw_tag`, sem sugestão).
- [ ] T1.5 — Schema de saída (preview) Zod fechado (`.strict()` ou equivalente) — rejeita silenciosamente qualquer chave extra que o parser tente devolver além do shape esperado (`ScrapedItem` fields + `extractedPriceValue` + `priceSignal`). Feito quando: teste que tenta forçar campo extra confirma que ele nunca aparece na saída.
- [ ] T1.6 — Testes unitários do parser contra os 2 fixtures válidos reais de T0 (DMs Guild + DriveThruRPG): extração bem-sucedida de cada campo (title/description/coverImageUrl/sourceUrl/publisherName/sourceLanguageHint/extractedPriceValue/priceSignal), campo obrigatório ausente simulado (fixture editado/truncado) → erro tipado, fixture `storytellersvault-product-1.html` → erro tipado de domínio incompatível (T1.2). Feito quando: suíte verde, sem mock do HTML (fixture real).

## Fase 2 — Rota
- [ ] T2.1 — `POST /api/v1/admin/scraper/parse-html` em `routes/scraper.ts`: `authMiddleware` + `requireRole('admin')` + `writeRateLimiter` (mesmo padrão das rotas vizinhas). Valida `source_platform` contra allowlist `['dms_guild', 'drivethrurpg']` (não reaproveita `IMPLEMENTED_SOURCE_PLATFORMS` do scraper automático — escopo intencionalmente menor). Feito quando: rota registrada, 401/403 para não-admin confirmado por teste.
- [ ] T2.2 — Checagem de tamanho **dentro do handler** (`if (html.length > LIMIT) return 422`), não middleware de body-parser dedicado — achado real (revisão de esforço alto): `server.ts:67` já tem `express.json({ limit: '4mb' })` global, cobre os 3 fixtures reais (133-155KB) com folga; reconfigurar limite por rota em Express não é trivial sem afetar outras rotas do mesmo router. `LIMIT` calibrado bem acima do maior fixture real (ex.: 1MB). Feito quando: teste confirma 422 para payload acima do `LIMIT` do handler (não depende do limite global do Express ser atingido).
- [ ] T2.3 — Chama `parseOneBookShelfHtml`, mapeia erro tipado → 422 com mensagem clara; sucesso → 200 com preview. **Confirma que `html` do body nunca é logado** (nenhum `console.log`/`console.error` imprime o body inteiro; se algum error handler global loga `req.body`, adicionar exclusão explícita para esta rota). Feito quando: teste de integração roda a rota com HTML de teste e faz grep na saída de log capturada, confirmando ausência do conteúdo do HTML.
- [ ] T2.4 — Testes de rota: sucesso com os 2 fixtures válidos reais (DMs Guild, DriveThruRPG), 422 domínio incompatível (fixture `storytellersvault-product-1.html`, requisito 8a), 422 campo ausente, 422 payload grande demais, 401/403 sem auth/sem role admin.

## Fase 3 — Dedupe por similaridade (adaptado de `tableDuplicateDetection.ts`)
- [ ] T3.1 — Confirmar se extensão `pg_trgm` já está habilitada no banco `downloads` (checar migrations existentes) — se não, migration nova habilitando (`CREATE EXTENSION IF NOT EXISTS pg_trgm`), seguindo checklist de migration do `AGENTS.md` (header 5 campos, idempotência). Feito quando: extensão confirmada ativa via `SELECT * FROM pg_extension WHERE extname='pg_trgm'`.
- [ ] T3.2 — `services/scrapers/onebookshelfDuplicateCheck.ts` (novo): dado título/descrição extraídos, consulta `download_material` (+ `download_material_metadata` se necessário) via `similarity()` do Postgres, retorna candidatos acima de um threshold (calibrar, `mesas` usa 0.75) com id/título/slug do material existente. Nunca decide sozinho — só retorna candidatos pro preview. Feito quando: função testada com fixtures de título similar/diferente.
- [ ] T3.3 — Resposta do `/parse-html` inclui `duplicateCandidates: []` (vazio se nenhum candidato) — endpoint continua 200 mesmo com candidato encontrado, nunca bloqueia. Feito quando: teste confirma que candidato encontrado não impede o preview de ser retornado.

## Fase 4 — Auditoria mínima de extração (adaptado de `discord_parse_cases`, sem aprendizado)
- [x] **T4.1 — Decisão técnica já resolvida (revisão de esforço alto, 2026-07-24): tabela nova mínima, não reaproveitar schema da spec 084.** Checado contra código real (`db/types.ts:198-229`): `DownloadScraperRunTable.trigger_kind` é `DownloadScraperTriggerKind = 'manual' | 'cron' | 'local_ingest'` — enum **fechado**, adicionar `'html_paste'` exigiria migration de qualquer forma (não é "sem schema novo" como a opção (b) original sugeria). Além disso `download_scraper_run`/`download_scraper_item_log` modelam conceitualmente "uma run que processa N items e cria/rejeita material" (`items_found`/`items_created`/`outcome: 'created'|'skipped_*'`) — não cabe "parse individual que só gerou preview, ninguém confirmou nada ainda, pode nem virar material". Forçar esse conceito no schema existente é abuso de modelo. **Tabela nova mínima:** `download_scraper_parse_log` (migration nova, `apps/downloads/database/`, seguindo checklist de 5 campos header do `AGENTS.md`) com: `id`, `source_platform` (mesmo enum `DownloadSourcePlatform`), `admin_user_id`, `fields_extracted` (jsonb — quais campos vieram preenchidos vs. vazios), `price_signal` (mesmo enum do requisito 3 da spec), `parse_case_id` como PK própria pra linkar depois (T4.3), `created_at`. **Sem `run_id`** — não pertence a uma run do scraper automático.
- [ ] T4.2 — Cada chamada bem-sucedida ao `/parse-html` grava na tabela nova: `source_platform`, `fields_extracted` (quais campos vieram preenchidos vs. vazios/baixa-confiança), `price_signal`, timestamp, `admin_user_id` (de `req.user.userId`, já disponível via `authMiddleware` — confirmado em `middleware/auth.ts:47`, sem trabalho extra de auth). **Nunca grava o HTML bruto** (mesma trava do requisito 6 da spec). Feito quando: teste confirma que o registro não contém a string do HTML de entrada em nenhum campo.
- [ ] T4.3 — Quando o admin confirma via `/ingest` um item que veio de `/parse-html`, linkar o resultado final ao registro de auditoria (`parse_case_id` do preview reenviado no payload do `/ingest`, mesmo padrão do `parseCaseId` que `mesas`/`parseTextForPreview` já usa) — permite comparar depois "o que o parser sugeriu" vs "o que foi publicado de fato". Achado: isso exige um campo novo opcional no `ingestBodySchema`/`ingestItemSchema` (`scraper.ts:127-140`) pra aceitar o `parse_case_id` de volta — mudança pequena na rota `/ingest` já existente, não só na rota nova. Feito quando: teste cobre o link sendo gravado corretamente no fluxo completo parse→confirma→ingest, e teste confirma que `/ingest` continua funcionando normalmente sem `parse_case_id` (uso direto do Modo 3 manual, sem ter passado pelo `/parse-html`).

## Fase 5 — Frontend (painel `/gestao`)
- [ ] T5.1 — UI nova (local exato a definir durante implementação — provável extensão de alguma página de gestão de materiais existente, ou nova): campo de texto para colar HTML + botão "Analisar" → chama `/parse-html` → mostra preview dos campos extraídos, incluindo `extractedPriceValue`/`priceSignal` sempre visíveis (requisito 3/4 — nunca só um `isFreeOrPwyw` boolean sem contexto) e candidatos de duplicata (Fase 3) exibidos como alerta não-bloqueante. Quando `priceSignal: 'nonzero_price_no_pwyw_tag'`, UI bloqueia visualmente o botão de confirmar até o admin editar manualmente (produto parece pago, fora do critério D119). Feito quando: componente renderiza preview e permite editar cada campo antes de confirmar.
- [ ] T5.2 — Confirmação do preview chama `POST /admin/scraper/ingest` já existente (reaproveita fluxo, sem rota nova de publicação). Feito quando: fluxo completo colar→preview→editar→confirmar→publicado testado manualmente em dev local.
- [ ] T5.3 — Teste de componente (render + interação básica), seguindo padrão de cobertura já usado no restante do frontend `downloads`.

## Fase 6 — Validação final
- [ ] T6.1 — `pnpm verify:api` (rota nova documentada em OpenAPI gerado), lint, build, test (backend + frontend) verdes.
- [ ] T6.2 — Smoke manual real em beta: colar HTML real de 1 produto de cada fonte (pode reaproveitar as URLs de exemplo já discutidas: DMs Guild filtro pt/pwyw), confirmar preview, publicar via `/ingest`, conferir item aparece corretamente em `GET /materials` e na página pública do material. Incluir teste de duplicata: colar o mesmo HTML 2x, confirmar que o segundo aponta candidato de duplicata no preview.
- [ ] T6.3 — Atualizar `specs/backlog.md` (entrada nova ou fechamento, conforme status ao final) e `.specify/memory/project-state.md` no fechamento.

## Débitos/riscos desta spec
- [ ] D-085-01 — Risco jurídico residual aceito conscientemente: HTML de produto passa pela memória do request (RAM) mesmo nunca sendo persistido — mantenedor decidiu que isso é tolerável, mas não é "zero risco", registrar como decisão consciente, não como resolvido.
- [ ] D-085-02 — Volume de uso real ainda desconhecido no momento da decisão desta spec (mantenedor respondeu "não sei ainda, vou descobrir usando") — se o uso real for baixo, revisitar se o investimento se pagou; não é motivo para não implementar agora, é item de acompanhamento pós-implementação.
- [ ] D-085-03 — Se o layout de DMs Guild/DriveThruRPG mudar e o parser passar a falhar sistematicamente (JSON-LD sumir ou mudar de shape), não há detecção automática nesta spec — só será percebido quando um admin colar HTML de novo e o resultado vier estranho/com erro. Ver critério de aceite de 422 explícito como mitigação parcial (falha visível é melhor que campo errado silencioso, mas não é alarme proativo).
