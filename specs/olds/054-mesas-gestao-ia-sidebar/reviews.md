# 054 — Reviews

Só reviews externos (mantenedor, bots, PRs, checks). Achados internos → `debitos.md`.

| ID | Origem | Data | Achado | Veredito | Notas |
|---|---|---|---|---|---|
| 001 | chatgpt-codex-connector (PR bot) | 2026-06-27 | P1: Inbox drafts sync via API errada — `DiscordDraftReviewTable` usa `discordSyncApi` default que dá 422 para drafts sem `discord_message_id`. | ✅ CORRIGIDO | `DiscordDraftReviewTable` ganhou prop `inboxApi?: DraftApiOperations`. `ModeracaoSection` injeta adapter `inboxDraftApi` (wrapper de `inboxApi` → `DraftApiOperations`). `resolveApi(draft)` roteia sync/reparse/get por origem (FK `discord_message_id`). Commit local. |
| 002 | chatgpt-codex-connector (PR bot) | 2026-06-27 | P2: Deep links `/gestao/moderacao/rascunhos` e `/gestao/moderacao/mensagens` não casam com rota. | ✅ CORRIGIDO | Rota `App.tsx` mudada para `moderacao/:sub?`. `ModeracaoSection` lê `useParams().sub` para inicializar `subTab` + `useEffect` sincroniza. Deep links funcionam; refresh preserva aba. |
| 003 | chatgpt-codex-connector (PR bot) | 2026-06-27 | P2: Troca de filtro em `ComunidadeSection` não dispara refetch — `mounted.current` guard bloqueia. | ✅ CORRIGIDO | `mounted.current` removido; substituído por padrão `active` flag com `setTimeout`. Fetch dispara a cada mudança de `filter`. |
| 004 | coderabbitai (PR bot) | 2026-06-27 | Minor: `adminEnrichment.ts:458` mensagem 500 ainda diz "hidratação". | ✅ CORRIGIDO | Texto alterado para "Erro durante o enriquecimento". |
| 005 | coderabbitai (PR bot) | 2026-06-27 | Major: Deep links do `DashboardSection`/`IntegracoesSection` para `/gestao/moderacao/rascunhos`/`mensagens` não encontram rota. | ✅ CORRIGIDO | Ver 002. Mesma correção de `moderacao/:sub?`. |
| 006 | coderabbitai (PR bot) | 2026-06-27 | Major: `AdminMain` renderiza header sticky vazio quando sem props. | ✅ CORRIGIDO | Header condicional: `hasHeader` booleano derivado de `groupEyebrow \|\| breadcrumbPath?.length \|\| groupLabel \|\| actions`. Faixa sticky só renderiza se `hasHeader`. |
| 007 | coderabbitai (PR bot) | 2026-06-27 | Major: Mesmo bug de `mounted.current` em `ComunidadeSection` (duplicata do 003). | ✅ CORRIGIDO | Ver 003. |
| 008 | coderabbitai (PR bot) | 2026-06-27 | Major: `maybePublishPendingDrafts` usa `.length`/`.map` sem `Array.isArray`. | ✅ CORRIGIDO | Parâmetro mudado para `unknown`; `Array.isArray` explícito no início + `as Array<{...}>` após validação. `handleResolved` também ajustado (`pending_drafts?: unknown`). |
| 009 | coderabbitai (PR bot) | 2026-06-27 | Major: `ConteudoSection.fetchAllTables` põe payload bruto como `AdminTableRow[]`. | ✅ CORRIGIDO | Normalização linha a linha: `Array.isArray` → loop `for (const item of raw)` → validação de `id` string, `title`/`status`/`created_at` com fallback seguro, `is_covil` boolean. |
| 010 | coderabbitai (PR bot) | 2026-06-27 | Minor: `ConteudoSection` e `PlatformsPage` têm seletores de `kind` independentes — divergem. | ⏸️ DÉBITO | `PlatformsPage` usa estado interno de `kind`; `ConteudoSection` passa `key={platformKind}` forçando remount. Melhoria futura: centralizar `kind` (lift state). Registrado como `DEB-054-10` em `debitos.md`. |
| 011 | coderabbitai (PR bot) | 2026-06-27 | Minor: `DashboardSection` usa `getDrafts({ limit: 1 })` como contagem real. | ✅ CORRIGIDO | Substituído por booleano `temPendenciaRascunhos`. Rótulo "Há rascunhos a revisar" sem número falso. Backend não expõe `total`; débito registrado (`DEB-054-05`). |
| 012 | coderabbitai (PR bot) | 2026-06-27 | Major: `GestaoLayout` conta no máximo 1 draft na sidebar (mesmo `limit: 1`). | ✅ CORRIGIDO | `pendenciaCount` agora é `> 0 ? total : undefined` (some quando zero). Sugestões contadas corretamente; rascunhos contribuem +1 se ≥1. Comentário documenta limitação do `limit:1`. |
| 013 | coderabbitai (PR bot) | 2026-06-27 | Major: `IntegracoesSection` `ModeracaoLinks` apontam para rotas inexistentes. | ✅ CORRIGIDO | Ver 002. Links em `ModeracaoLinks` (`/gestao/moderacao/mensagens`, `/gestao/moderacao/rascunhos`) agora funcionam com `moderacao/:sub?`. |
| 014 | coderabbitai (PR bot) | 2026-06-27 | Major: `computePayloadDiff` compara objeto inteiro (ruidoso) e `null !== undefined`. | ✅ CORRIGIDO | Comparação restrita ao nível `table` (campos editáveis). `null`/`undefined` tratados como equivalentes (normalizados para `null` antes da comparação). Função `isRecord` auxiliar extraída. |
| 015 | coderabbitai (PR bot) | 2026-06-27 | Minor: Subnav de `SistemaSection` sem `aria-pressed`/`aria-selected`. | ✅ CORRIGIDO | `SistemaSection`: `role="tablist"` + `role="tab"` + `aria-selected`. Demais Sections (`ComunidadeSection`, `ModeracaoSection`, `ConteudoSection`, `DashboardSection`, `IntegracoesSection`): `aria-pressed` nos botões de subnav/filtro. |
| 016 | coderabbitai (PR bot) | 2026-06-27 | Major: `onBeforeSync` não aplicado no sync em lote ("Sincronizar todos prontos"). | ✅ CORRIGIDO | `handleSyncReady` bifurcado: se `onBeforeSync` existe, itera drafts `ready`, aplica `onBeforeSync` + `resolveApi(draft).syncDraft(draft.id)` draft a draft; senão usa `syncReadyAction` em lote como antes. |
| 017 | coderabbitai (PR bot) | 2026-06-27 | Minor: `MAPA_DE_API.md` referencia `DiscordSyncPanel.tsx` (deletado). | 📝 NOTA | Spec 055 tornou `MAPA_DE_API.md` legado/não-canônico (`docs/api/README.md` proíbe agente usar mapa manual como fonte primária). Atualização do mapa é opcional/moot. Nenhuma ação nesta spec. |
| 018 | coderabbitai (PR bot) | 2026-06-27 | Minor: Sessão `26-06-27_1` sem `## Plano` e `## Critério de conclusão` explícitos. | ✅ CORRIGIDO | Sessão reescrita com plano, critério de conclusão e registro de fixes da fase final. |
| 019 | coderabbitai (PR bot) | 2026-06-27 | Minor: `plan.md` Fase 2 marcada "IMPLEMENTADA" mas descrição ainda tem IA antiga. | 📝 NOTA | `plan.md` é snapshot histórico da fase de planejamento. `tasks.md` é a verdade material do que foi implementado. Não reescrever `plan.md` — o veredito "IMPLEMENTADA" significa que o código está conforme o `tasks.md`, não que a descrição do `plan.md` foi atualizada. |
| 020 | coderabbitai (PR bot) | 2026-06-27 | Minor: `tasks.md` T4.1 contagem de pendências marcada como concluída mas código usa `limit:1`. | ✅ CORRIGIDO | `tasks.md` T4.1 atualizado: contagem de rascunhos mudou para booleano "há pendências". `DEB-054-05` registrado para expor `total` no backend futuramente. |
| 021 | LSP (pré-existente) | 2026-06-27 | LSP Error: `GestaoPage.test.tsx` acusa falta de tipos `jest-dom`. | ✅ MOOT | Arquivo `GestaoPage.test.tsx` **não existe no disco** (deletado em T3.4). LSP com cache dessincronizado — falso positivo. Nenhuma ação. |
| 022 | LSP (pré-existente / fantasma) | 2026-06-27 | LSP Error: `adminHydration.test.ts` não encontra `vi`/`describe`/etc. | ✅ MOOT | Arquivo `adminHydration.test.ts` **não existe no disco** (renomeado para `adminEnrichment.test.ts` no DEB-054-02). Cache do language server dessincronizado. Falso positivo. |
| 023 | SonarCloud | 2026-06-27 | Duplicated Lines: 4.2% global (2 arquivos). | 📝 REGISTRADO | `DEB-054-03` (ConteudoSection `extractErrorMessage` duplicado) e `DEB-054-04` (adminEnrichment `resolveFkBySlug` duplicado) já registrados em `debitos.md`. |
| 024 | SonarCloud | 2026-06-27 | `ConteudoSection.tsx` 16.5% duplicação (blocos `extractErrorMessage`). | 📝 REGISTRADO | Ver 023 → `DEB-054-03`. |
| 025 | SonarCloud | 2026-06-27 | `adminEnrichment.ts` 13.8% duplicação (blocos `resolveFkBySlug`). | 📝 REGISTRADO | Ver 023 → `DEB-054-04`. |
| 026 | chatgpt-codex-connector (PR bot) | 2026-06-28 | P2: `tasks.md` TZ.3 manda fazer deploy beta (`--ref dev`) **antes** do merge do PR. `_deploy-module.yml` hard-coded `git reset --hard origin/dev` → deploy beta pré-merge = fumaça falsa sobre código velho. | ✅ CORRIGIDO | Ordem corrigida: merge PR em `dev` **primeiro** (passo 5), depois deploy beta (passo 6). Smoke agora enxerga o código mergeado. |
| 027 | coderabbitai (PR bot) | 2026-06-28 | Minor: `project-state.md` "Próximo passo" com numeração quebrada (1-4 novos + 2.,5.,6. antigos) e fluxo 054 com ordem errada (smoke beta → PR → dev → merge). | ✅ CORRIGIDO | Lista renumerada 1–17 sequencial. Fluxo 054 corrigido: commit → push → PR → CI → merge em dev → deploy beta → smoke → promote → prod. |
| 028 | coderabbitai (PR bot) | 2026-06-28 | Minor: Sessão menciona mudança em `.claude.json` mas arquivo ausente do inventário "Arquivos modificados". | ✅ CORRIGIDO | `.claude.json` adicionado à tabela "Arquivos de doc modificados". `project-state.md` também incluído (estava fora). |
