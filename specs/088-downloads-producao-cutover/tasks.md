# Tasks — 088

**Ordem entre fases é rígida.** Cada fase é pré-requisito da seguinte, e inverter duas produz falha silenciosa — não erro visível. Especificamente: aplicar migrations antes de atualizar o clone aplicaria só 20 dos 31 arquivos sem acusar nada; subir o banco antes de fixar o `.env` congela a senha errada no volume (E009). Não pular, não paralelizar.

**Todo o código (Fases 0–2) vem antes do cutover (Fases 4–6).** O cutover transforma o acervo de beta em conteúdo público. Publicar primeiro significaria expor um catálogo com 80% de cards em placeholder cinza, 100% dos materiais sem sistema e autoria falsa nos materiais sem crédito — na primeira indexação do domínio. Corrigir depois é caro de um jeito que corrigir antes não é.

**Aprovação é por ação.** Cada comando de escrita na VM, cada deploy, cada commit e cada push exigem autorização nominal própria do mantenedor, mesmo dentro de uma fase já iniciada. Autorizar o `.env` não autoriza subir o banco.

---

## Fase 0 — Canonical route-aware (T4.4 herdada da spec 087)

> **Entregue e mergeada em `dev`** — PR #217 (commits `be570be`, `a453cec`, `d10832f`), junto com a Fase 1. Os checkboxes ficaram por marcar até 2026-07-27; conferidos um a um contra o código antes de fechar, incluindo as travas de não-alteração (`MaterialRating.tsx` e `RedirectDestinationPage.tsx` intactos em relação a `origin/dev`).

- [x] T0.0a — Ler `AGENTS.md` inteiro (T0 pétreo — obrigatório toda sessão/toda fase nova, mesmo se já lido antes nesta mesma sessão) antes de agir nesta fase. · feito quando: leitura confirmada, e a regra pétrea relevante identificada — aqui, §Regras Gerais de Código (normalização, comentário de decisão preservado) e §Regras de Produto e SEO (SEO inegociável).
- [x] T0.0b — Usar `rtk` no lugar de comando cru equivalente durante toda a fase (`rtk git status/diff/log`, `rtk rg`, `rtk read`, `rtk pnpm`, `rtk tsc`, `rtk lint`, `rtk vitest` — ver `AGENTS.md` §rtk pra lista completa). · feito quando: nenhum comando cru rodado onde `rtk` cobria o caso.
- [x] T0.0c — Comunicação com o mantenedor nesta fase em português, caveman ultra (`AGENTS.md` — regra de comunicação do projeto). · feito quando: mensagens da fase seguem o registro.
- [x] T0.1 — Criar `src/hooks/useCanonicalUrl.ts`: hook sem dependência nova que monta a URL absoluta a partir de `VITE_PUBLIC_SITE_URL`, remove qualquer `link[rel="canonical"]` pré-existente antes de inserir a sua, e remove a própria no cleanup do efeito. Alvo é **parâmetro fixo**, nunca derivado da rota ou da query string atual. · feito quando: o arquivo existe, tipa sem erro no LSP e `rtk tsc` passa.
- [x] T0.2 — Criar `src/hooks/useCanonicalUrl.test.ts` cobrindo os cinco comportamentos: (a) tag criada com `href` absoluto correto; (b) **uma única** tag após múltiplos rerenders; (c) tag pré-existente no `head` é substituída, não duplicada; (d) alvo permanece a raiz mesmo com query string na URL; (e) tag removida no unmount. · feito quando: `rtk vitest` roda os cinco casos verdes.
- [x] T0.3 — Chamar o hook em `CatalogoPage.tsx` com alvo fixo `/`, conforme decisão do mantenedor (2026-07-26). Comentário inline explicando **por que** o alvo é fixo e não route-aware: `/` e `/catalogo` servem o mesmo conteúdo e query strings são recortes da mesma listagem, então consolidar tudo na raiz é o que preserva o sinal de indexação. · feito quando: uma única chamada cobre as duas rotas (`CatalogoPage` já é o componente de ambas desde a spec 087) e o comentário cita a origem da decisão.
- [x] T0.4 — Estender `CatalogoPage.test.tsx`: asserção de que a tag existe com o alvo certo ao renderizar o catálogo, **e** teste que prova que uma rota alheia (ficha de material ou painel) **não** herda a tag. · feito quando: ambos os casos passam e o teste de rota alheia falharia se alguém movesse a canonical para `index.html`.
- [x] T0.5 — Validar: `rtk vitest apps/downloads/frontend`, `rtk lint`, `rtk tsc`. · feito quando: os três verdes, com saída registrada na sessão. **`curl` e `view-source` não validam canonical** — não executam React e não veriam a tag.

---

## Fase 1 — Capa, crédito, acesso e avaliação

- [x] T1.0a — Ler `AGENTS.md` inteiro (T0 pétreo — obrigatório toda sessão/toda fase nova, mesmo se já lido antes nesta mesma sessão) antes de agir nesta fase. · feito quando: leitura confirmada, com atenção a §Regras de Produto e SEO (design vem de `packages/ui`, não divergir por app) e §Regras Gerais de Código (comentário de decisão preservado).
- [x] T1.0b — Usar `rtk` no lugar de comando cru equivalente durante toda a fase. · feito quando: nenhum comando cru rodado onde `rtk` cobria o caso.
- [x] T1.0c — Comunicação com o mantenedor nesta fase em português, caveman ultra (`AGENTS.md` — regra de comunicação do projeto). · feito quando: mensagens da fase seguem o registro.
- [x] T1.1 — Criar `src/components/MaterialCover.tsx`: **regra única de exibição de capa**, consumida por todo ponto que mostra capa. `object-contain` em vez de `object-cover` (contém, não corta), **piso e teto verticais**, largura derivada da altura e centralizada, sem ultrapassar a largura do contêiner. · feito quando: capa vertical não perde topo nem base, capa mais alta que o teto reduz proporcionalmente sem cortar, capa mais baixa que o piso não é esticada, e capa horizontal/quadrada cai na mesma regra sem caso especial.
- [x] T1.2 — Criar `src/components/CoverPlaceholder.tsx`: placeholder desenhado em **CSS/SVG inline**, sem requisição de rede, sem arquivo de imagem novo, sem dependência nova. Um placeholder que falha ao carregar seria pior que o retângulo cinza atual — ele É o tratamento de falha da capa. · feito quando: o componente existe, usa **apenas tokens semânticos** (`--surface-*`, `--line`, `--fg-muted`), vira com o tema claro/escuro sozinho sem cor crua, e ocupa altura dentro da mesma faixa piso–teto de T1.1.
- [x] T1.3 — Fazer o placeholder **variar por `material_type`**, usando o campo já presente no payload da listagem — sem consulta nova ao backend. · feito quando: uma prateleira de materiais sem capa não é uma fileira de retângulos idênticos, e tipo desconhecido cai num default sem quebrar.
- [x] T1.4 — Marcar o placeholder como **decorativo** (`aria-hidden`): não anuncia "sem capa" nem compete com o título do material na navegação por leitor de tela. · feito quando: o placeholder não tem nome acessível, verificado em teste.
- [x] T1.4a — Substituir em `MaterialCard.tsx` o `h-32 w-full object-cover` (altura fixa que **recorta**) e o `<div>` cinza "Sem capa" pelo `MaterialCover` + `CoverPlaceholder`. · feito quando: card com capa e card sem capa mantêm silhueta compatível na prateleira — propriedade existente (capa sangra até as bordas) que não pode regredir.
- [x] T1.4b — Aplicar a **mesma** `MaterialCover` em `MaterialPage.tsx:126`, que hoje usa `w-full object-cover` sem trava de altura. Resolve também a incoerência atual de o placeholder da ficha já usar `aspect-[3/4]` enquanto a imagem real ao lado não tem trava alguma. · feito quando: os **dois** pontos de render de capa (inventário verificado: `MaterialCard.tsx:52` e `MaterialPage.tsx:126`) consomem a mesma regra, sem implementação duplicada — duas cópias divergem no primeiro ajuste.
- [x] T1.5 — Remover o fallback `'Acervo Artifício'` de `MaterialCard.tsx:41`. Exibir **publicante (editora) primeiro, autor depois** — decisão do mantenedor (2026-07-26) — cada um com rótulo que identifica o que é. **Editora não é autoria e não serve de fallback dela**: exibir a editora sob rótulo de autor repetiria o mesmo erro com outro nome. Sem nenhum dos dois, o eyebrow **não é renderizado**. Manter o `.trim()`: os campos vêm de scraper e de formulário, então `""` e `"   "` chegam até aqui e passariam por um null-check ingênuo (achado de review da PR #214). Comentário inline registrando por que o Artifício não assume autoria de material de terceiro (D107/D119) e por que editora e autor são campos distintos. · feito quando: a string `'Acervo Artifício'` não existe mais, os dois campos aparecem rotulados, e o comentário registra a decisão e sua origem.
- [x] T1.6 — Garantir que o card **sem nenhum dos dois** permanece coerente: sem colapso de layout, sem título encostando na borda da capa, sem salto de alinhamento entre cards vizinhos na prateleira. · feito quando: verificado em teste e no DOM renderizado. Altura variável entre cards é **custo aceito** da decisão — o que não se aceita é layout quebrado.
- [x] T1.7 — **Inverter** o teste `MaterialCard.test.tsx:81-83`, que hoje exige `expect(screen.getByText('Acervo Artifício')).toBeInTheDocument()`: passa a provar a **ausência** do eyebrow quando não há editora nem autor. Somar casos: só editora, só autor, e ambos — provando que os rótulos os distinguem. · feito quando: o teste novo quebraria se alguém reintroduzisse o fallback, ou se editora passasse a ser exibida como autoria.
- [x] T1.8 — Somar testes: placeholder aparece quando não há capa (e não o texto "Sem capa"), placeholder varia por tipo, `onError` na capa real cai no placeholder em vez de imagem quebrada, placeholder não tem nome acessível, capa vertical não é cortada, altura fica entre piso e teto. · feito quando: `rtk vitest apps/downloads/frontend` verde com todos os casos.
- [x] T1.9 — Trocar em `MaterialPage.tsx` o `<button onClick={handleAccess}>Acessar material</button>` por âncora nativa: `<a href={`/ir/${material.destination_id}`} target="_blank" rel="noopener noreferrer" onClick={handleAccess}>`. **`window.open` não serve** — chamado depois do `await` da API perde o gesto do usuário e é bloqueado por popup blocker; navegação nativa nunca é bloqueada. · feito quando: o elemento é âncora, abre em nova aba, e `Ctrl+clique`/botão do meio funcionam.
- [x] T1.10 — Manter `rel="noopener noreferrer"` com comentário inline explicando **por que**: sem `noopener`, a página de destino recebe `window.opener` e pode manipular a aba de origem; sem `noreferrer`, o `Referer` entrega a URL de origem ao terceiro. · feito quando: o atributo está presente e o comentário registra a razão de segurança, não só o quê.
- [x] T1.11 — `handleAccess` perde o `navigate` final mas **mantém** `trackEvent('download_cta_click', …)` e `registerDownload.mutate(…)`, disparando antes da navegação como hoje. Trocar o elemento não pode custar a instrumentação de funil. · feito quando: teste prova que o evento dispara no clique da âncora.
- [x] T1.12 — Confirmar que o desenho **fail-closed** da rota `/ir/:destinationId` permanece intacto e que o `destination_id` continua **opaco**: o `href` aponta para a rota interna, não para a URL externa. A URL real não é pré-resolvida nem embutida no HTML da ficha — é o que faz o link sobreviver a troca futura de slug (DEB-073-02) e mantém o critério da spec 073 (T4.1). · feito quando: `RedirectDestinationPage` não foi alterada e a URL externa não aparece no DOM da ficha.
- [x] T1.13 — Confirmar que material **sem destino** configurado continua mostrando o aviso de indisponibilidade, sem link clicável. · feito quando: verificado em teste — o caminho degradado não pode virar âncora quebrada.
- [x] T1.14 — Substituir em `RatingSection.tsx` o `<select>` de 1 a 5 por **cinco estrelas clicáveis**. · feito quando: nenhum `<select>` de nota permanece no componente.
- [x] T1.15 — Garantir que as estrelas de entrada são **pelo menos tão acessíveis quanto o `<select>` que substituem** — trocar controle nativo por glifos é exatamente onde acessibilidade se perde: focáveis por `Tab`, acionáveis por `Enter`/`Espaço`, foco visível, **nome acessível por estrela** comunicando o valor ("3 de 5 estrelas"), estado selecionado exposto a tecnologia assistiva, e alvo de toque de 44px sem colisão visual entre as estrelas. · feito quando: todos os pontos verificados em teste, incluindo operação só por teclado.
- [x] T1.16 — Estado de seleção visível **sem depender de cor isolada**: estrela escolhida e não escolhida diferem em preenchimento, não só em matiz. · feito quando: a distinção sobrevive em escala de cinza.
- [x] T1.17 — Refletir no controle a **nota já enviada** pelo usuário ao carregar a ficha, em vez do `useState(5)` fixo de hoje — a pessoa vê o que avaliou antes e reavalia a partir desse estado. · feito quando: ficha de material já avaliado abre com a nota correta selecionada.
- [x] T1.18 — Preservar o guard de permissão: `403` do backend (conta sem download registrado do material) continua exibindo a explicação visível. A mudança é de controle de entrada, **não** de regra de negócio. · feito quando: teste do caminho `403` continua passando.
- [x] T1.19 — Confirmar que `MaterialRating.tsx` **não foi alterado**: as estrelas somente-leitura do card seguem `<span>`, nunca `<button>`. O card inteiro é alvo de clique único (`before:absolute before:inset-0` no `<Link>`) e qualquer elemento focável ali roubaria o clique — trava explícita da spec 087. Só o controle de **entrada** da `RatingSection` vira interativo. · feito quando: `rtk git diff` não mostra alteração em `MaterialRating.tsx`.

---

## Fase 2 — Parser: autoria, editora, sistema e tipo

> **Estado (2026-07-27): todo o trabalho de código está feito.** Nada mais é executável nesta fase sem acesso a ambiente. O que resta são 6 itens, nenhum deles trabalho de codificação pendente:
> - **4 bloqueadas por banco/ambiente** (T2.1, T2.10, T2.10b, T2.11) — exigem limpar o acervo de beta, aplicar `migration_016` (site) + `migration_030`/`migration_031` (downloads) e disparar o scraper, tudo escrita que depende de autorização nominal.
> - **2 adiadas por decisão do mantenedor** (T2.5, T2.6) — itch.io fica para depois, por falta de DOM real da página de jogo.
>
> Entregue: hint de tipo ponta a ponta (contrato, 2 schemas `.strict()`, resolução por item, taxonomia semeada, coluna de hint bruto **e fila de triagem**), autoria/editora com regra por fonte, e 3 defeitos do parser OneBookShelf. Backend 349/349 verde, `tsc` limpo.
>
> **Segunda rodada (2026-07-27), após review da PR #218 pelo Codex:** dois achados procedentes, corrigidos em T2.13/T2.14/T2.15 e um convertido em bloqueio de ambiente (T2.10b). O P1 é o mais relevante: o critério de aceite de T2.10 era **inalcançável** como escrito, porque o dedupe descarta duplicata antes de qualquer resolução.

**As quatro lacunas têm uma causa só.** A extração rica vive nos `platformOverrides`, invocados **apenas** por `genericHtmlParser` (importação manual por URL). O pipeline automático (`scraperIngest`) nunca passa por eles, e os adapters de descoberta emitem só o mínimo. Todo o acervo veio pelo caminho automático — **zero materiais de OneBookShelf**, a única plataforma com override rico. Não é que a extração falhe: ela não é chamada nesse caminho. Corrigir uma vez beneficia os quatro campos.

- [x] T2.0a — Ler `AGENTS.md` inteiro (T0 pétreo — obrigatório toda sessão/toda fase nova, mesmo se já lido antes nesta mesma sessão) antes de agir nesta fase. · feito quando: leitura confirmada, com atenção a §Regras Gerais de Código (normalização obrigatória: todo dado externo é `unknown` até passar por normalizador tipado) — scraper lê HTML de terceiro, que é dado hostil por definição.
- [x] T2.0b — Usar `rtk` no lugar de comando cru equivalente durante toda a fase. · feito quando: nenhum comando cru rodado onde `rtk` cobria o caso.
- [x] T2.0c — Comunicação com o mantenedor nesta fase em português, caveman ultra (`AGENTS.md` — regra de comunicação do projeto). · feito quando: mensagens da fase seguem o registro.
- [ ] ⛔ **BLOQUEADA — exige acesso ao banco de beta (autorização nominal do mantenedor).** T2.1 — Registrar a baseline completa no banco de beta **antes** de qualquer alteração: (a) `select count(*) total, count(system_id) com_sistema, count(edition_id) com_edicao, count(raw_system_hint) hint_bruto from download_material;` (b) `select material_type, count(*) from download_material group by 1 order by 2 desc;` (c) `select count(*) total, count(credits) com_credito, count(publisher_name) com_publicante from download_material_metadata;` (d) `select source_platform, count(*) from download_material group by 1 order by 2 desc;`. · feito quando: as quatro saídas estão na sessão. Baseline conhecida (2026-07-26): `103|0|0|0`; uma linha só `Aventura|103`; `103|0|87`; e `opera_rpg 77 / itch_io 15 / grimorios_e_dados 7 / dms_guild 4`.
- [x] T2.2 — Confirmar a **causa arquitetural comum** antes de corrigir: os `platformOverrides` (único lugar que lê `details.get('authors')`, `details.get('artists')`, `details.get('ruleSystem')`) são invocados **apenas** por `genericHtmlParser`, ou seja, só na importação manual por URL. O `scraperIngest` nunca passa por eles, e o acervo tem **zero materiais de OneBookShelf** — a única plataforma com override rico. · feito quando: a sessão registra a tabela dos dois caminhos de ingestão e a evidência de origem dos 103, deixando claro que a extração não falha, **não é chamada**.
- [x] T2.2a — Confirmar o detalhe diagnóstico do sistema: `raw_system_hint` zerado prova que o problema **não** é o matcher falhando (falha de casamento preencheria esse campo), e sim `systemHint` nunca chegando ao ingest. · feito quando: registrado, com a tabela de quais scrapers emitem o campo.
- [x] T2.2b — Extrair **autoria** (`authorsCredits`/`artistsCredits`) nos cinco adapters, a partir de fixture real. Onde a plataforma já tem override, **reaproveitar** a lógica em vez de reescrever em paralelo — duas implementações do mesmo parsing divergem no primeiro ajuste. · feito quando: teste por adapter prova extração de fixture real e `null` explícito quando a fonte não expõe autoria.
- [x] T2.2c — Manter `publisher_name` (**editora**) como campo próprio, sem se misturar a autoria em ponto algum do pipeline. Editora é quem publicou; `credits` é quem escreveu ou ilustrou — um **não** é fallback do outro. · feito quando: verificado que nenhum caminho de código copia um campo no outro.
- [x] T2.3 — **O parser de OneBookShelf já existe e extrai tudo** — `applyOneBookShelfOverride` (`platformOverrides/onebookshelf.ts`), validado em 2026-07-26 contra 4 DOMs reais do mantenedor sem nenhuma linha nova (ver T2.3b). Não há parser a escrever nem a corrigir para sistema/autoria. O que falta em `driveThruRpgScraper.ts` é **fiação**: `blockedSourceScraper` nunca invoca o override. · feito quando: decidido com o mantenedor se a fiação entra nesta fase, já que o 403 impede a coleta de qualquer forma.
- [x] T2.3a — **Achado da Fase 2 (2026-07-26), corrige o pressuposto de T2.3/T2.4:** `driveThruRpgScraper.ts` e `dmsGuildScraper.ts` não têm parser próprio — delegam a `blockedSourceScraper.ts` (`attemptAllModesAndFail`, retorno `Promise<never>`), que lança em todos os desfechos. O bloqueio é atual: `GET` com User-Agent de browser nas duas listagens deu **403 em ambas** em 2026-07-26. O gargalo é **obter o HTML**, não interpretá-lo. · feito quando: registrado na sessão, e nenhum fechamento da fase apresenta essas duas fontes como "corrigidas" ou atribui a elas métrica de T2.10. Detalhe em `spec.md` 42a/42b/43a/43b.
- [x] T2.3b — **Evidência da validação de parser** (2026-07-26): 4 DOMs reais (DriveThruRPG e DMs Guild × grátis e PWYW) processados por `applyOneBookShelfOverride`. Extraiu nos quatro: `systemHint`, autoria, artistas, formato, tamanho, método de criação. Sinal de preço correto nos quatro (`pwyw_tag_present` × `zero_price_no_pwyw_tag`), via marcador estrutural `obs-product-format-pwyw-options`. Confirmado que `price` do JSON-LD **não** serve de critério: nos PWYW traz o preço sugerido (`0.99`, `2`), não zero — filtrar por `price > 0` descartaria todo o acervo PWYW. · feito quando: evidência registrada — **já registrada aqui e em `spec.md` 43a**.
- [x] T2.3c — **Defeitos do parser existente corrigidos** (decisão do mantenedor: corrigir tudo nesta fase), em `platformOverrides/onebookshelf.ts`: (i) `N/A`/`N / D` viravam texto literal em vez de `null` — corrigido em `nullableText`, com âncora de string inteira pra preservar nome legítimo que só comece assim ("N/A Studios"); (ii) `tags`/`sourceFilters` saíam vazios em toda página em inglês — `FILTER_FACETS` ganhou os facets `en` (`genre`, `productType`, …) e o `tema` do DMs Guild, que também faltava; (iii) efeito colateral de (i): tile sem valor colapsava pra um parágrafo e `paragraphs.at(-1)` devolvia o rótulo como valor — corrigido exigindo 2 posições distintas. **O `pageCount` reportado como defeito não existe** — era erro de encoding na sondagem (ver `spec.md` 43e). · feito: 4 testes novos em `onebookshelf.test.ts`, suíte de scrapers 47/47 verde. Detalhe em `spec.md` 43c/43d/43e.
- [x] T2.4 — Idem para `dmsGuildScraper.ts` — mesma plataforma OneBookShelf, mesmo parser já pronto, mesma trava de acesso. · feito quando: mesmo critério de T2.3, com as mesmas ressalvas de T2.3a.
- [ ] ⏸ **ADIADA por decisão do mantenedor (2026-07-27) — não bloqueia o fechamento da Fase 2.** T2.5 — itch.io fica para depois. Bloqueio técnico: o critério de aceite exige fixture real, e o parser (`itchIoParser.ts`) hoje lê só `og:image`, `og:description` e o link da conta — a seção "More information" da página de jogo, que poderia trazer sistema, **não foi observada** em DOM real. As fixtures do teste são trechos mínimos e não a incluem. Escrever regex contra estrutura não vista é exatamente o que o requisito 43 proíbe ("não de suposição sobre o HTML dela"). · feito quando: DOM real de página de jogo do itch.io disponível → extração + teste de fixture; ou constatado que a fonte não expõe sistema → `null` explícito registrado como constatação (T2.10a).
- [ ] ⏸ **ADIADA junto com T2.5.** T2.6 — `grimoriosEDadosScraper.ts`: mesma plataforma, mesmo `itchIoParser.ts` compartilhado, mesmo adiamento.
- [x] T2.7 — **`operaRpgScraper.ts`: a fonte não publica sistema — constatação, não falha de extração.** A listagem expõe exatamente `<b>Título</b><br/><small>por Autor · Descrição</small>`; não há campo de sistema em ponto algum. Emitir `null` explícito é o comportamento correto e é o que já ocorre. Derivar hint do título ou da descrição é proibido pelos requisitos 44/56 — vínculo errado é pior que vínculo ausente. · feito: verificado no parser e na fixture real; nenhuma alteração necessária.
- [x] T2.8 — **Nunca derivar hint de título ou descrição.** Fonte que não expõe sistema de forma confiável emite `null` explícito. Hint inventado alimenta o matcher com ruído e produz vínculo errado — que afirma ao usuário um sistema que o material não é, e contamina filtro e badge. Vínculo errado é **pior** que vínculo ausente. · feito quando: nenhum dos cinco scrapers usa heurística sobre texto livre, verificado por revisão do diff.
- [x] T2.9 — **Cumprido no que a trava protege, com uma exceção prevista pela própria spec.** `resolveSystemHint` e `matchSystemNameExact` estão **intactos** — o matcher continua igualdade exata normalizada, fuzzy segue reservado à triagem admin. `scraperIngest.ts` **foi** alterado, mas apenas no eixo de TIPO de material, que é o que T2.9d/T2.9e mandam fazer: resolução por item, `raw_material_type_hint`, e default neutro no lugar de `'aventura'`. Nenhuma linha do caminho de sistema foi tocada. A redação original ("não foram alterados") vale para o casamento de sistema; ela não podia proibir a correção de tipo que a mesma fase exige no arquivo. · feito: verificado por `rtk git diff` — o diff em `scraperIngest.ts` não encosta em `resolveSystemHint`, `matchSystemNameExact` nem no bloco de `system_id`/`edition_id`/`raw_system_hint`.
  *(Redação original de T2.9, preservada para rastreio — não é task aberta: "Confirmar que `scraperIngest.ts`, `resolveSystemHint` e `matchSystemNameExact` não foram alterados. O matcher permanece igualdade exata normalizada; fuzzy segue reservado à triagem admin com humano decidindo (decisão da spec 086, T4.5). Esta spec conserta a alimentação, não afrouxa o casamento.")*
- [x] T2.9a — Confirmar a causa raiz do tipo de material antes de corrigir: a classificação **nunca existiu**. `ScrapedItem` (`scrapers/types.ts`) **não tem campo de tipo**, e `DEFAULT_MATERIAL_TYPE_SLUG = 'aventura'` (`scraperIngest.ts:22`) é resolvido **uma vez por execução** (linha 309, fora do laço) e aplicado a todos os itens. Não é regressão — é lacuna desde a origem do pipeline (`ef9efd6`). · feito quando: a sessão registra o diagnóstico com as referências de linha.
- [x] T2.9b — **`ScrapedItem.materialTypeHint` adicionado**, mesmo desenho do `systemHint`: opcional, `null` explícito. Propagado por toda a cadeia — inclusive os **dois schemas Zod `.strict()`** (`genericHtmlParser.ts`, `routes/scraper.ts`), que sem a declaração **rejeitariam** o campo em silêncio, exatamente a armadilha que o comentário do `systemHint` já documentava. · feito: `tsc` limpo, nenhum adapter quebrado.
- [x] T2.9c — **OneBookShelf emite o hint** pelo facet `tipoDeProduto` (pt) / `productType` (en), usando a **folha** do caminho hierárquico ("Opções para personagens" > "Classe/Arquétipo" → `Classe/Arquétipo`). Coberto por 2 testes de fixture real: extração, e `null` explícito quando o facet não existe. **OPERA RPG e itch.io não emitem** — ver T2.5/T2.6/T2.7: OPERA RPG não expõe tipo, itch.io adiado por falta de DOM real.
- [x] T2.9d — **Resolução movida para dentro do laço**, via `getCatalogMaterialTypeBySlug` (já casa slug **ou** alias, normalização `pt-BR`). Taxonomia central povoada em `apps/site/db/migrations/016` — a tabela tinha **um único tipo** (`aventura`, seed mínimo da spec 086), o que faria todo hint legítimo cair em não-casado. Hint não resolvido preserva o bruto em `raw_material_type_hint` (`migration_030`, simétrica a `raw_system_hint`, com índice parcial). · feito: 3 testes cobrindo casou / não casou / sem hint.
- [x] T2.9e — **Default deixou de ser `'aventura'`**: agora é `nao-classificado`, tipo neutro com slug próprio. Isso torna "caiu no default" **distinguível** de classificação real — consulta pelo slug lista exatamente o que falta triar, o que `aventura` tornava impossível. · feito: teste prova que item sem hint nunca cai em "Aventura".
- [x] T2.9f — **Nenhuma heurística sobre texto livre.** O hint vem de facet estruturado; onde ele não existe, o valor é `null` (teste dedicado). O scraper **só lê** a taxonomia central — a escrita segue exclusiva da triagem admin, e hint sem correspondente vira `raw_material_type_hint`, não linha nova no catálogo. · feito: verificado por revisão do diff.
- [ ] ⛔ **BLOQUEADA — exige LIMPAR o acervo de beta (ver T2.10b), aplicar `migration_016` (site) + `migration_030`/`migration_031` (downloads) e disparar o scraper em beta; tudo escrita em ambiente, autorização nominal.** T2.10 — Reprocessar o scraper em beta e comparar contra a baseline completa de T2.1. **Todos os números importam juntos:** `com_sistema > 0` prova casamento de sistema; `hint_bruto > 0` prova que o caminho de não-casamento passou a ser exercitado (código que existe, está testado e nunca rodou de fato); `com_credito > 0` prova que autoria passou a ser extraída; `material_type` deixa de ser uma linha só com 100% do acervo; `total` estável prova que não duplicou. **`com_publicante` deve CAIR, e isso é sucesso, não regressão:** dos 87 da baseline, 77 vinham de `opera_rpg` gravando o autor no campo da editora (requisito 40a). Depois da correção, esses 77 migram para `com_credito` e `publisher_name` fica `null` — a fonte não publica editora. Esperado: `com_credito` sobe de 0 para ~77+, `com_publicante` cai de 87 para ~10. Se `com_publicante` continuar em 87, a correção **não** entrou. · feito quando: todos os critérios batem, com saída do banco registrada. **Escopo real da prova: as três fontes acessíveis** (`opera_rpg`, `itch_io`, `grimorios_e_dados` — 99 dos 103 materiais). DriveThruRPG e DMs Guild não entram em nenhum número aqui enquanto o 403 persistir (T2.3a); ausência de material novo dessas duas é o resultado esperado, não falha de extração.
- [x] T2.10a — **Veredito explícito por fonte, quanto a AUTORIA** (o reprocessamento de T2.10 confirma os números; o veredito de fonte é apurável no código e está fechado):
  - `opera_rpg` — **extrai**. A listagem publica `"por Fulano"`; o valor era gravado em `publisherName` (papel errado) e passou a ir para `authorsCredits`. Evidência: `operaRpgScraper.ts` + teste `nunca grava o autor como se fosse a editora`.
  - `itch_io` / `grimorios_e_dados` — **a fonte não distingue os papéis**. A página expõe um único nome, o da conta que hospeda, que no modelo da plataforma é a publicadora. Vai para `publisher_name`; `credits` fica `null` por decisão registrada em `spec.md` 40b — não é falha de extração. Evidência: `itchIoParser.ts` + teste `não inventa autoria a partir do nome da conta publicadora`.
  - `dms_guild` / `drivethrurpg` (OneBookShelf) — **extrai**, e já extraía: `authors` e `artists` em linhas separadas da tabela de detalhes. Comprovado contra 4 DOMs reais (T2.3b): `Thiago Gomes`, `Octavius Knorr +4`, `Pedro H Ewald`, `Felix Klaus`. O que impede o dado de chegar ao acervo é o 403 (T2.3a), não a extração.
- [ ] ⛔ **BLOQUEADA — escrita destrutiva no banco de beta, exige autorização nominal própria e formato "APROVAÇÃO NECESSÁRIA".** T2.10b — **Limpar o acervo de beta ANTES do reprocessamento.** Achado de review da PR #218 (Codex, P1): `processItem` retorna `skipped_duplicate` **antes** de qualquer resolução, então reprocessar sobre os 103 existentes descartaria todos e **nenhum número de T2.10 mudaria** — o critério de aceite, como escrito, era inalcançável. O pipeline só cria; nunca atualizou material existente. Decisão do mantenedor (2026-07-27): limpar, já que o acervo de beta é descartável e produção nasce vazia; **não** introduzir refresh no ingest (sobrescreveria edição manual do admin, que é o produto da triagem). Detalhe em `spec.md` 57a. · feito quando: `select count(*) from download_material` em beta retorna 0, com `pg_dump` prévio registrado, e T2.10 roda sobre banco limpo.
- [ ] ⛔ **BLOQUEADA — só verificável durante o reprocessamento (T2.10).** T2.11 — **Critério invertido pela decisão de T2.10b:** com o acervo limpo antes de reprocessar, a baseline vira zero e "total estável" deixa de significar coisa alguma. O que se vigia agora é **recoleta completa**: esperado ~99 das três fontes acessíveis (`opera_rpg` 77, `itch_io` 15, `grimorios_e_dados` 7); `dms_guild` não volta enquanto o 403 persistir (T2.3a), e essa ausência é resultado esperado. `total` bem abaixo de 99 significa falha de coleta — **parar** e investigar antes de seguir, sem tratar como sucesso parcial. `total` acima do esperado ainda merece checagem de dedupe: URL de origem que mudou geraria material duplicado no acervo já limpo. · feito quando: contagem por fonte confere com o esperado, ou a divergência é investigada e reportada ao mantenedor antes de seguir.
- [x] T2.12 — Confirmar que nenhum vínculo foi escrito diretamente no catálogo central pelo scraper. Só a triagem admin faz isso (requisito 8 da spec 086), e essa fronteira não muda. · feito quando: verificado que o caminho de escrita continua exclusivo da triagem.

> **Achados de review da PR #218 (Codex, 2026-07-27) — T2.13/T2.14.** Dois defeitos reais na entrega da Fase 2, ambos confirmados contra o código. O P1 invalidava o critério de aceite de T2.10 (virou T2.10b, acima); o P2 deixava a `migration_030` sem consumidor. Decisão do mantenedor: corrigir os dois agora, nenhum vira débito.
>
> **`specs/backlog.md`: nada a acrescentar nesta rodada.** Os dois achados foram corrigidos no mesmo turno, não adiados — não há pendência a registrar. DEB-088-01 (falha do proxy `rtk`), DEB-088-02 (403 em DriveThru/DMs Guild) e DEB-088-03 (itch.io adiado) seguem abertos e inalterados; nenhum deles é tocado por esta correção.

- [x] T2.13 — **Fila de triagem para o hint de TIPO** (achado P2). `raw_material_type_hint` era gravado e **nenhum consumidor administrativo lia a coluna** — dado invisível, sem caminho de resolução, enquanto `raw_system_hint` já tinha fila desde a spec 086. Entregue: `migration_031_download_material_type_suggestion.sql` (tabela + índice parcial de `pending` + índice único parcial contra pending duplicada, espelhando `migration_027`), `openMaterialTypeSuggestion` no `scraperIngest` e `routes/materialTypeSuggestionsAdmin.ts` (`GET /`, `GET /:id/candidates`, `POST /:id/resolve` sob advisory lock). Detalhe em `spec.md` 56c. · feito: 15 testes de rota + 3 de ingest (abre fila quando não casa, não abre quando casa, não abre sem hint).
- [x] T2.14 — **Contrato de resolução adaptado à taxonomia PLANA**, não copiado da de sistema. `catalog_material_types` é lista, não árvore: as resoluções são `merge_existing` / `create_type` / `reject` — sem `create_child` (não há pai) e sem `create_alias` separado (apontar para um tipo existente **já** registra o alias, senão o mesmo `raw_value` volta à fila em todo reprocessamento). Escrita no catálogo central via `createCatalogMaterialType`/`addCatalogMaterialTypeAlias`, chamadas **só** pela rota admin — o scraper continua apenas lendo (requisitos 48/56). · feito: teste prova que `create_child` é rejeitado com 400, e que `reject` não escreve no catálogo nem toca no material.
- [x] T2.15 — **Atribuição do itch.io emitida como `null` explícito**, não campo omitido (achado de review, inline). O comentário do parser afirmava "o campo é `null` explícito, não uma cópia do outro", mas o `yield` **omitia** `authorsCredits`/`artistsCredits`, deixando `undefined` — que lê como "não extraído ainda" em vez de "a fonte não afirma isso" (requisito 38). Teste passou a asseverar `toBeNull()` direto, sem o `?? null` que mascarava a omissão. · feito: `itchIoScraper.test.ts` verde com a asserção estrita.

---

## Fase 3 — Promoção do código para `main`

> **Estado (2026-07-27): validação completa verde; o resto aguarda autorização nominal.**
>
> **Descoberta que muda o desenho original desta fase.** O plano supunha uma única PR levando canonical + capa + crédito + scrapers de uma vez. Não foi assim: as **Fases 0 e 1 já foram mergeadas em `dev`** pela PR #217 (`be570be`, `a453cec`, `d10832f`) antes da Fase 2 começar. Logo T3.1/T3.2 cobrem **apenas o trabalho da Fase 2** — parser, taxonomia e as duas migrations —, e a promoção de T3.3 carrega os dois conjuntos juntos. A redação original ("commitar canonical, capa, crédito e scrapers") descreve o total que chega a `main`, não o conteúdo de um único commit.
>
> **Duas migrations entram por esta fase**, contrariando o `plan.md`, que dizia "nenhuma migration nova é escrita nesta spec" (já corrigido lá): `apps/downloads/database/migration_030` e `apps/site/db/migrations/016`. A segunda vive no banco do **Site** e não é aplicada pelo runner do Downloads — ver T5.0, criada para essa dependência cruzada.

- [x] T3.0a — `AGENTS.md` lido; §PR, Commit e Push identificada como a regra pétrea desta fase (branch→dev→main, fast-forward, `--amend` proibido, autorização **por ação**).
- [x] T3.0b — `rtk` usado onde cobria o caso. **Exceção registrada:** `rtk lint` falhou com `ESLint output (JSON parse failed: EOF while parsing a value at line 1 column 0)` e caiu para `pnpm run lint`; `rtk pnpm run build` idem. Não é ausência de ferramenta — é falha do proxy nesses dois comandos, com fallback nomeado. Ver DEB-088-01.
- [x] T3.0c — Comunicação em português, caveman ultra, ao longo da fase.
- [x] T3.0d — **Validação completa verde, com saída registrada (2026-07-27, antes de qualquer commit):**
  - `pnpm verify:api` → `✅ verify:api concluído`, `downloads: breaking=0 non-breaking=1 unclassified=0` (aditivo: `publisher_name`, `materialTypeHint`), demais apps `0/0/0`. Rodado **antes** de montar o commit, então os artefatos regenerados entram nele.
  - `pnpm run lint` → `Tasks: 23 successful, 23 total`.
  - `pnpm run build` → `Tasks: 23 successful, 23 total`.
  - `vitest` backend downloads → **331 passed** (49 arquivos).
  - `vitest` frontend downloads → **255 passed** (46 arquivos).
- [x] T3.1 — **Commit `cb59f45`** (autorizado pelo mantenedor em 2026-07-27), 22 arquivos, +676/-119, na branch `feat/088-fase23-parser` partida de `origin/dev` atualizado (`49f5331`). Cobre **apenas a Fase 2** — Fases 0 e 1 já haviam entrado pela PR #217. `verify:api` rodado antes do `git add` (T3.0d), então os artefatos gerados entraram no commit; o hook de pre-commit reexecutou e não produziu diff novo.
- [x] T3.2 — **PR #218** aberta contra `dev`, ready for review (https://github.com/FarenRavirar/artificio/pull/218). Corpo registra os três pontos de atenção para o merge: as duas migrations (contagem 29→30 e a dependência cruzada da `016` no banco do Site), o fato de a Fase 2 ainda não ter rodado em beta, e o 403 de DriveThruRPG/DMs Guild. Agente encerrou a ação após abrir — sem acompanhar checks, sem `gh pr view`/`gh run watch`, sem tocar em bots de review.
- [ ] 🔒 **AGUARDA MERGE DA PR PELO MANTENEDOR + AUTORIZAÇÃO NOMINAL PRÓPRIA.** T3.3 — Promover `dev → main` por **fast-forward**, sem merge commit e sem squash. · feito quando: `rtk git log origin/main -1` mostra o mesmo commit que `origin/dev` tinha na promoção, e o histórico é linear.
- [x] T3.4 — **Registrado: promover `main` NÃO coloca nada em produção.** `promote-prod-fast-forward.yml` move apenas o ponteiro Git — não builda, não sobe container, não dispara `deploy.yml`. Depois da promoção, produção continua exatamente como está (**502**, sem container de Downloads no ar), e assim permanece até a Fase 6 disparar `workflow_dispatch` explícito em `--ref main` com `env=prod` e confirmar `conclusion: success`. Nenhuma mensagem ao mantenedor pode afirmar "em produção" antes disso — é violação explícita de `AGENTS.md` (§Deploy e Infra de CI/CD, trava pétrea). · feito: registrado aqui e em `plan.md`.

---

## Fase 4 — Preparar a VM (clone e `.env`)

- [ ] T4.0a — Ler `AGENTS.md` inteiro (T0 pétreo — obrigatório toda sessão/toda fase nova, mesmo se já lido antes nesta mesma sessão) antes de agir nesta fase. · feito quando: leitura confirmada, com atenção a §Autorização (escrita na VM exige aprovação nominal por ação) e §VM, Banco e Infra.
- [ ] T4.0b — Usar `rtk` no lugar de comando cru equivalente durante toda a fase. · feito quando: nenhum comando cru rodado onde `rtk` cobria o caso.
- [ ] T4.0c — Comunicação com o mantenedor nesta fase em português, caveman ultra (`AGENTS.md` — regra de comunicação do projeto). · feito quando: mensagens da fase seguem o registro.
- [ ] T4.1 — Inspecionar a VM read-only antes de qualquer escrita: memória e disco livres, contagem de containers, e confirmação de que `downloads-beta-*` segue saudável. · feito quando: há folga registrada para três containers novos (um deles Postgres) sem degradar os 25 já rodando.
- [ ] T4.2 — Atualizar o clone de produção `/opt/artificio` para `origin/main`. **Exige autorização nominal (escrita na VM).** · feito quando: `ls /opt/artificio/apps/downloads/database/migration_*.sql | wc -l` retorna **31** na VM. **Bloqueante para a Fase 5** — com 20 arquivos em disco, a aplicação criaria schema incompleto sem erro aparente.
- [ ] T4.3 — Montar a lista completa de chaves exigidas por `docker-compose.prod.yml`, separando as seis obrigatórias marcadas com `:?` (`DATABASE_URL`, `JWT_SECRET`, `VIEW_HASH_SECRET`, `SERVICE_SECRET`, `RESEND_API_KEY`, `CATALOG_INTERNAL_TOKEN`) das opcionais com default. · feito quando: a lista existe e cada `:?` está identificado — ausência de qualquer uma delas aborta o boot do container.
- [ ] T4.4 — Criar `/opt/artificio/apps/downloads/.env` com `chmod 600`. **Exige autorização nominal (escrita na VM).** `VIEW_HASH_SECRET` e `POSTGRES_PASSWORD` **distintos dos de beta** — compartilhar o primeiro permitiria correlacionar visualizações entre ambientes. Nenhum valor é impresso em log, colado em PR ou ecoado em saída de comando. · feito quando: o arquivo existe com permissão correta e a conferência de presença de chaves foi feita **sem imprimir valores** (só nomes).
- [ ] T4.5 — Registrar que a senha do Postgres precisa estar **definitiva** neste ponto (E009): `POSTGRES_PASSWORD` só grava em `pg_authid` na primeira init do volume; trocar depois no `.env` não reescreve nada e produz `28P01` em loop. · feito quando: a sessão registra que a senha está fixada antes da Fase 5, e que testar por `psql -h 127.0.0.1` engana (localhost é `trust`) — validação sempre pela rede Docker.

---

## Fase 5 — Banco de produção e as 31 migrations

- [ ] T5.0a — Ler `AGENTS.md` inteiro (T0 pétreo — obrigatório toda sessão/toda fase nova, mesmo se já lido antes nesta mesma sessão) antes de agir nesta fase. · feito quando: leitura confirmada, com §Migrations (trava pétrea) lida por inteiro, incluindo o item 4 sobre `MAX_AUTO_PENDING`.
- [ ] T5.0b — Usar `rtk` no lugar de comando cru equivalente durante toda a fase. · feito quando: nenhum comando cru rodado onde `rtk` cobria o caso.
- [ ] T5.0c — Comunicação com o mantenedor nesta fase em português, caveman ultra (`AGENTS.md` — regra de comunicação do projeto). · feito quando: mensagens da fase seguem o registro.
- [ ] T5.0 — **Confirmar que `apps/site/db/migrations/016_catalog_material_types_seed.sql` está aplicada no banco do SITE antes de qualquer coisa do Downloads.** Dependência cruzada criada na Fase 2 (requisito 55a): a taxonomia central vive no banco do Site e é lida por HTTP pelo Downloads. Ela **não** entra na contagem das 31 nem no runner do Downloads — vai pela esteira própria do site (migração no entrypoint do container). Sem ela, o ingest falha com `catalog_material_type_not_found: nao-classificado` e todo material cai em erro, com o scraper aparentando estar quebrado quando o problema é a taxonomia ausente. · feito quando: `select slug from catalog_material_types order by slug` no banco do Site lista os sete (`aventura`, `cenario`, `ficha`, `mapa`, `nao-classificado`, `regras`, `suplemento`).
- [ ] T5.1 — Subir **apenas** `downloads-db`, sem `downloads-api` nem `downloads-app`. **Exige autorização nominal (escrita na VM).** · feito quando: container `healthy`, volume `pgdata_downloads_prod` criado, banco `downloads` vazio, e o volume do beta (`downloads-beta_pgdata_downloads_beta`) **intacto**.
- [ ] T5.2 — `pg_dump` do banco de produção antes de aplicar qualquer migration, guardado fora do container. Mesmo com banco vazio — é o único rollback manual se algo falhar no meio. · feito quando: o arquivo existe fora do container e seu caminho está registrado na sessão.
- [ ] T5.3 — Aplicar as 31 migrations pelo **script oficial** `scripts/deploy/apply_required_migrations.sh`, em **execução única**, com `MAX_AUTO_PENDING` elevado ao total pendente e `MIGRATIONS_DIR=apps/downloads/database`. **Exige autorização nominal (escrita no banco).** **Fatiar em lotes é proibido** — o script compara o conjunto inteiro de uma vez. **`psql` avulso é proibido** — geraria drift e bloquearia o próximo deploy automático. · feito quando: o script conclui sem erro e sem migration parcialmente aplicada.
- [ ] T5.4 — Verificar a aplicação **no banco**, não no log do script: `SELECT count(*) FROM schema_migrations` no banco de produção retorna **31**, idêntico ao beta. · feito quando: a contagem bate. Contar arquivos em disco não prova aplicação; ler `schema_migrations` prova.
- [ ] T5.5 — Confirmar ausência de drift: nenhuma migration registrada no banco que não exista em disco, e vice-versa. Se houve qualquer intervenção fora do script oficial, reconciliar com `reconcile_migrations.sh --mark-applied` antes de fechar a fase. · feito quando: banco e disco batem, e a fase não fecha com drift pendente.
- [ ] T5.6 — Confirmar que o guard `MAX_AUTO_PENDING=5` continua **inalterado** no script versionado. A elevação foi por variável de ambiente numa execução única, não mudança permanente de código — a proteção segue valendo para todos os módulos. · feito quando: `rtk git diff` não mostra alteração em `scripts/deploy/`.

---

## Fase 6 — Deploy de produção e smoke

- [ ] T6.0a — Ler `AGENTS.md` inteiro (T0 pétreo — obrigatório toda sessão/toda fase nova, mesmo se já lido antes nesta mesma sessão) antes de agir nesta fase. · feito quando: leitura confirmada, com atenção à trava pétrea de que `promote` **nunca** dispara deploy de produção.
- [ ] T6.0b — Usar `rtk` no lugar de comando cru equivalente durante toda a fase. · feito quando: nenhum comando cru rodado onde `rtk` cobria o caso.
- [ ] T6.0c — Comunicação com o mantenedor nesta fase em português, caveman ultra (`AGENTS.md` — regra de comunicação do projeto). · feito quando: mensagens da fase seguem o registro.
- [ ] T6.1 — Registrar a baseline **antes** do deploy: `https://downloads.artificiorpg.com/` e `/api/v1/health` retornam **502** hoje. · feito quando: os dois códigos estão registrados na sessão — a transição 502 → 200 é a prova material do cutover.
- [ ] T6.2 — Disparar `deploy.yml` por `workflow_dispatch` manual em `--ref main` com `module=downloads`, `mode=deploy`, `env=prod`. **Exige autorização nominal (deploy).** · feito quando: o run foi disparado. Com as migrations já aplicadas na Fase 5, o deploy encontra zero pendências e não bate no guard.
- [ ] T6.3 — Confirmar o resultado do run: `conclusion: success`. · feito quando: confirmado por `gh run list --workflow=deploy.yml --branch=main`. Run falho **reabre a fase** — rollback automático devolve produção ao 502, sem dano.
- [ ] T6.4 — Confirmar os três containers `Up ... (healthy)`: `downloads-app`, `downloads-api`, `downloads-db`. · feito quando: `docker ps` comprova os três, e o beta segue saudável em paralelo.
- [ ] T6.5 — Smoke das três `critical_routes` de produção por HTTP real contra o hostname público: `/api/v1/health` → 200, `/` → 200, `/api/v1/materials/mine` sem cookie → 401. · feito quando: os três códigos batem. O **401 importa tanto quanto os 200** — prova que a rota privada está protegida e não vaza sem cookie.
- [ ] T6.6 — Confirmar que nenhuma rota pública responde 5xx e que o 502 desapareceu. · feito quando: verificado por `curl`, com saída registrada.
- [ ] T6.7 — Smoke de login real: acessar produção deslogado, autenticar via `accounts.artificiorpg.com`, confirmar que a sessão do cookie `.artificiorpg.com` é aceita e que `/api/v1/materials/mine` passa de 401 para 200 com cookie. · feito quando: o fluxo completa. **Único ponto que precisa de browser** — sem browser disponível, vira débito nomeado e registrado, **nunca** conclusão presumida.
- [ ] T6.8 — Verificar o canonical no DOM renderizado em produção: exatamente uma `<link rel="canonical">` apontando para a raiz absoluta, presente em `/` e `/catalogo` e nas query strings, ausente em ficha e painel. · feito quando: verificado no DOM. **`view-source` e `curl` não servem** — não executam React.
- [ ] T6.9 — Verificar em produção que nenhum card renderiza "Acervo Artifício" nem o placeholder cinza "Sem capa". · feito quando: confirmado no DOM renderizado, fechando os requisitos 13 e 14 dos critérios de aceite.
- [ ] T6.10 — Confirmar que `CATALOG_INTERNAL_TOKEN` de produção está correto: a resolução de sistemas contra `site-prod-app` funciona. · feito quando: verificado — token errado **degrada silenciosamente** a resolução sem derrubar o app, então ausência de erro no log não é evidência suficiente.

---

## Fase 7 — Fechamento

- [ ] T7.0a — Ler `AGENTS.md` inteiro (T0 pétreo — obrigatório toda sessão/toda fase nova, mesmo se já lido antes nesta mesma sessão) antes de agir nesta fase. · feito quando: leitura confirmada, com §Conclusão de Tarefas lida por inteiro.
- [ ] T7.0b — Usar `rtk` no lugar de comando cru equivalente durante toda a fase. · feito quando: nenhum comando cru rodado onde `rtk` cobria o caso.
- [ ] T7.0c — Comunicação com o mantenedor nesta fase em português, caveman ultra (`AGENTS.md` — regra de comunicação do projeto). · feito quando: mensagens da fase seguem o registro.
- [ ] T7.1 — Fechar os débitos da **spec 087** que esta spec resolve: **T4.4** (canonical, resolvida na Fase 0) e **T6.4** (promoção para produção, resolvida nas Fases 3–6). · feito quando: `specs/087-downloads-home-catalogo-unificado/tasks.md` reflete o fechamento com referência a esta spec, sem marcar como feito nada que não tenha evidência.
- [ ] T7.2 — Registrar os débitos da **spec 087** que **continuam abertos**: T3.4/T4.1/T4.3 (smoke visual e de viewport) e T5.3 (smoke manual local). Este cutover **não** os desbloqueia. · feito quando: seguem nomeados como abertos, sem serem absorvidos numa conclusão falsa.
- [ ] T7.3 — Registrar o estado de `auto_deploy_on_push` do Downloads: permanece `false` (dispatch-only). Habilitá-lo é decisão posterior do mantenedor, com evidência de estabilidade — não entra nesta spec. · feito quando: registrado como decisão pendente explícita, não como omissão.
- [ ] T7.4 — Registrar o descompasso de `packages/ui`: o Downloads recebe o Header da spec 087 neste deploy; mesas, glossário e site só recebem no próprio. Benigno pelo contrato aditivo (`hasEmbeddedSearch` exige `onSearchChange`, que nenhum consumidor atual passa). · feito quando: registrado, para não virar surpresa em investigação futura.
- [ ] T7.5 — Registrar como débito próprio a **extração de capa faltante nos scrapers**: 80% dos materiais não têm `cover_image_url`. Esta spec definiu o padrão de exibição na ausência (placeholder desenhado), não a busca da imagem. Corrigir a extração exige fixture real por plataforma e é spec separada. · feito quando: débito registrado em `specs/backlog.md` com a evidência (`21 de 103`).
- [ ] T7.6 — Atualizar `specs/backlog.md` e `.specify/memory/project-state.md` com o estado real do Gate D e o resultado do cutover. · feito quando: ambos refletem evidência, e nenhuma afirmação de "em produção" existe sem o run de deploy confirmado que a sustente.
- [ ] T7.7 — Atualizar a sessão em `sessoes/` com a evidência de cada fase e o estado final. · feito quando: a sessão contém as saídas de comando que sustentam cada critério de aceite, e o índice em `sessoes/index.md` foi atualizado.
