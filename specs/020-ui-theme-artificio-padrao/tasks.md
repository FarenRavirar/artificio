# Tasks — 020

- [x] T1 — Registrar decisao de marca/paleta · **D064** (2026-06-12): laranja canonico `#FF5722` supera `#FF9457` do D040; navy `#020740` mantido. Evidencia pixel em `sessoes/26-06-12_6`.
- [x] T2 — Mapear tokens atuais por consumidor · tabela em `sessoes/26-06-12_6_ui-theme-020-tokens.md` (tokens.ts/preset.js/glossario/mesas/accounts/target).
- [x] T3 — Definir contrato de tokens · **FEITO** em `token-contract.md`: fonte canonica `tokens.ts`, saidas `styles.css`/`tailwind-preset.js`, papeis existentes, aliases temporarios por app e regra de paridade. Lacunas futuras (semanticos/spacing/shadow/dark estruturado) ficam em T14/primitives/recipes.
- [x] T4 — Definir checklist dark readiness · **FEITO** em `dark-readiness-checklist.md` (sessao `26-06-12_7`): criterio objetivo e simetrico — contraste AA, states, forms/select/validacao, modais/drawers/toasts, header/footer/toggle, mobile, troca sem flash, evidencia + builds. Gate de fechamento explicito.
- [x] T5 — Planejar consolidacao de `artificio_theme` · **FEITO** em `theme-consolidation.md`: fonte canonica `packages/ui/src/theme.tsx`, inventario por app, excecoes (`mesas` default-dark, `site`/inline zero-flash), migracao de `accounts`/`site`, rollback e validacao. Observacao: comportamento D067 ja esta correto; remocao de duplicacao de codigo fica em T14/fatia futura.
- [x] T6 — Especificar header/nav/actions · **FEITO** em `header-nav-actions.md`: contrato atual do `Header` (`navItems`, `moduleNav`, `userMenu`, `actions`, `sessionOverride`), fronteira de dados por app, inventario mesas/glossario/site/accounts, API futura `HeaderAction` proposta e rollback/validacao. Mantem fetch/changelog/notificacao/feedback fora de `packages/ui`.
- [x] T7 — Especificar primitives minimas · **FEITO** em `primitives-form-state.md`: `Button`, `Field`, `TextInput`, `Textarea`, `Select`, `Badge`, `Panel`, `Toolbar`, `FilterPanel`, states, `Modal`, `Drawer` e `HeaderAction` com fronteira, variantes, fora de escopo, rollout e validacao. Implementacao fica em T14/fatia propria.
- [x] T8 — Especificar recipes de pagina · **FEITO** em `page-recipes.md`: `PublicSearchPage`, `CatalogPage`, `AdminWorkspacePage`, `AuthPage`, `EditorialPage` e `DetailPage` descritos como composicao, com exemplos vivos, slots, fronteiras, fora de escopo e validacao. Sem layout forcado; implementacao runtime fica em T14/fatia futura se necessaria.
- [x] T9 — Definir caminho Astro/zero-JS · **FEITO** em `astro-zero-js.md`: site publico fica Astro SSG/static, shell `.astro`, CSS/tokens via `@artificio/ui/styles.css`, scripts vanilla permitidos (tema, login link/sessao, TOC, Pagefind lazy, GA4), sem React/auth client no publico. Build `@artificio/site` verde (`output: static`, 45 paginas); `_astro` sem JS de shell, JS apenas `/pagefind/*`. B2/T11 seguem para export/paridade static oficial.
- [x] T10 — Planejar rollout piloto · **FEITO** em `rollout-pilots.md`: ordem accounts → glossario → mesas → site → site-admin, com estado atual, regra de entrada, validacao, criterio de saida e rollback por consumidor. Separa plano fechado de residuais B2/B6/B7/T11/T14.
- [x] T11 — Resolver paridade brand/static shell · **FEITO** em 2026-06-15: `@artificio/ui/static` data-only exporta `faviconV2`, logos e `defaultNavItems`; `apps/site` usa esse subpath em `Base.astro` e `content.ts`, sem barrel React/auth e sem espelho local de nav/logo.
- [x] T12 — Atualizar backlog de debitos · feito: `D-UX2` aponta para B6/B7; `D-MARCA2` foi movido para fechado/superado por D064; residuais estao em `specs/backlog.md`.
- [x] T13 — Implementar Fase B em SDD proprio/continuidade autorizada · **feito** (`sessoes/26-06-12_6`, commits `a48a518`+`d9c6d8a`): tokens.ts/styles.css/tailwind-preset.js com laranja canonico `#FF5722` + paridade travada por `scripts/check-token-parity.mjs`; consumidores puxam `var(--artificio-brand)` (zero hex/rgba laranja duplicado em codigo vivo); turbo build 13/13; betas site/mesas/glossario verificados com `#FF5722`.
- [~] T14 — Implementar Fase C/D/E/F por fatias · **parcial** (feito quando cada consumidor migrado tiver build, smoke visual e contraste registrados): Fase C/D (accounts/glossario/mesas/site consumindo tokens) feita junto da fonte unica; B10b/B11 fechados localmente. **lua/sol (R4) dois pilotos implementados e promovidos**: **glossário +dark** (D065: `[data-theme]`+remap, navy `#1a2744`→`#020740`) e **mesas +light** (D066/D067: `[data-theme="light"]`+remap das ~1873 utilities brancas/hexes, default-dark sem cookie e cookie compartilhado so por escolha explicita). Ambos: toggle no Header via `actions` + `ThemeIcon`/`setTheme`, `variant` reativo, boot, zero-flash; build/smoke publico/AA principal registrados. Residual: telas auth-gated com dados (E2E mantenedor). Falta: primitives (E), recipes runtime (F).

## Corte de escopo recomendado para primeira PR futura

- Tokens e paridade `tokens.ts`/CSS vars/Tailwind.
- Accounts consumindo `@artificio/ui/theme`.
- Nenhuma migracao grande de layout.
- `ThemeToggle` ainda opt-in por app.

## Backlog derivado

- [x] B1 — Teste de paridade entre `tokens.ts`, `styles.css` e `tailwind-preset.js` · `packages/ui/scripts/check-token-parity.mjs`.
- [x] B2 — Export static-friendly para site Astro, incluindo brand/logo/header/footer parity · **FEITO** em 2026-06-15: subpath `@artificio/ui/static` criado e consumido pelo site; `SiteHeader.astro`/`SiteFooter.astro` seguem Astro/static, CSS segue `@artificio/ui/styles.css`, sem React island.
- [~] B3 — HeaderAction/changelog/notification shell em `@artificio/ui` · **revisado em `backlog-b2-b7-review.md`**. Slot/classes/actions existem e `header-nav-actions.md` define fronteira/API proposta; nao fecha porque falta `HeaderAction` visual-only em `packages/ui`.
- [~] B4 — Primitives de formulario e estado · **revisado em `backlog-b2-b7-review.md`**. Contrato fechado em `primitives-form-state.md`; implementacao em `packages/ui` pendente (shared code = SDD Completo + builds/smokes de consumidores). Variantes semanticas dependem de B11.
- [x] B5 — Recipes de pagina documentadas · **revisado em `backlog-b2-b7-review.md`**. `page-recipes.md` fecha o contrato documental; implementacao runtime em `packages/ui` nao faz parte deste debito.
- [x] B6 — Dark readiness glossario · **FECHADO por evidência registrada em `sessoes/26-06-13_1_ui-theme-020-b6b7-e2e-prod.md`**. Variante dark implementada/promovida; AA medido nos estados principais (hero 13.9 / muted 5.2 / input 13.9 / modal 8.84 / empty 5.18); telas com dados/admin/AddTermModal/forms/selects/validação/mobile aprovadas pelo mantenedor em prod.
- [x] B7 — Light readiness mesas · **FECHADO 2026-06-15 por validação do mantenedor.** Reaberto em 2026-06-13 por `/perfil` light + P2 hero banner custom; publicado em prod via PR #27/#28. Validação final do mantenedor confirmou `/perfil` light com dados + mestre com `banner_url` custom + smoke anti-regressão em prod.
- [x] B8 — **Logo responsiva do Header sem deformar no mobile** (`packages/ui`) · corrigido. Mantenedor esclareceu em 2026-06-13: "dinamismo" era só manter tamanho/proporção conforme a largura mobile do usuário, sem esticar/espremer. Fix já feito em `.artificio-brand-logo` com `object-fit:contain` + `flex-shrink:0`; não há débito de animação/micro-interação.
- [x] B9 — **Causa-raiz corrigida (D067): tema compartilhado, cookie só em escolha explícita.** `accounts` (`main.tsx`) e `site` (`Base.astro`) não gravam mais o cookie no boot (a partir do OS-pref) — só no toggle. Cookie virou opt-in confiável. mesas voltou a HONRAR o cookie (`resolveMesasTheme = cookie ? cookie : 'dark'`), removido o marcador `mesas_theme` do D066. Tema agora persiste cross-módulo (escolha num vale em todos), com mesas default-dark quando ninguém escolheu. Validado local (build + propagação). Sem commit/deploy.
- [x] B10a — **`navy` na trava de paridade** · gap do `token-contract.md` corrigido (2026-06-13): `navy` (`#1B2A4A`) virou cross-app com D065/D066 mas estava fora de `check-token-parity.mjs`. Adicionado (compara `tokens.ts` x `styles.css`; sem alias no preset). `node check-token-parity.mjs` → 11 papéis OK.
- [x] B10b — **Tokenizar superfícies dark/light ad-hoc dos pilotos** (planejamento em `token-contract.md` §Planejamento) · **FEITO** (2026-06-13, SDD Completo autorizado). Parte 1: `dark*`/`light*` estruturados existem em `tokens.ts` + `styles.css` (`--artificio-dark-*`/`--artificio-light-*`) + `check-token-parity.mjs` (11 papéis novos, 2-way tokens×styles como `navy`; sem alias no preset). Parte 2: `apps/glossario/frontend/src/index.css` consome `--artificio-dark-*`; `apps/mesas/frontend/src/index.css` e `pages/MestrePage.css` consomem `--artificio-light-*` nas superfícies/ink dos remaps. Valores ficaram iguais aos hexes aprovados dos pilotos, então sem mudança visual esperada. Validação: `node packages/ui/scripts/check-token-parity.mjs` OK (31 papéis), `pnpm --filter=@artificio/glossario-frontend build` OK, `pnpm --filter=@artificio/mesas-frontend build` OK, `pnpm turbo run build` 13/13 OK, `git diff --check` OK.
- [x] B11 — **Tokens semânticos canônicos (`success`/`warning`/`danger`/`info`) em `packages/ui` + paridade** · **FEITO** (2026-06-13, SDD Completo autorizado). 4 papéis + variante `*Text` (acento ancora no mesas `--success-strong`/`--warn-strong`/`--danger`/`--info`; `*Text` escurecido p/ AA sobre claro — lição do bug B7, "variante light/dark deriva do token" da `primitives-form-state.md`): `success #10B981`/`successText #15803D`, `warning #F59E0B`/`warningText #A16207`, `danger #EF4444`/`dangerText #B91C1C`, `info #38BDF8`/`infoText #1D4ED8`. Em `tokens.ts` + `styles.css` (`--artificio-{role}`/`-text`) + `preset` (`artificio.{role}`/`-text`) + `check-token-parity.mjs` (8 papéis novos, 3-way). Build 13/13 verde; parity OK. Desbloqueia variantes coloridas de `Button`/`Badge`/`Panel`/`State` (B4).
- [ ] B12 — **Limpeza do `@import` de fontes (Google Fonts) no shell público** · `packages/ui/src/styles.css:2` tem `@import url("https://fonts.googleapis.com/...Oswald+Inter")`. **Dois problemas:** (1) **ordem de bundle** — no build do site (`apps/site/src/styles/global.css` importa o styles.css), o `@import` cai depois de regras → aviso `@import rules must precede all rules` (não quebra T9: build verde, `output: static`, 45 páginas + Pagefind); (2) **perf/privacidade** — é fetch externo de terceiro **render-blocking** no caminho crítico do público (Google Fonts). **Fix futuro:** self-host das fontes (Oswald/Inter) OU `<link rel="preconnect">` + `<link rel="stylesheet">` no lugar do `@import`, removendo a dependência externa e a ordem inválida de uma vez. Documentado em `astro-zero-js.md` §Validação e `brand-static-shell.md` §Fora de escopo. Shared CSS = SDD Completo (toca `packages/ui`).
- [~] B13 — **Unificar nav/footer entre todos os módulos, incluindo glossário** · débito criado em 2026-06-13 por pedido do mantenedor. **Fatia 1 FEITA + PROD (PR #28, `25be29a`, 2026-06-13, estratégia A = fonte única de DADOS):** `packages/ui` exporta `./brand`+`./modules` data-only; `apps/site` consome dados de marca/nav da fonte única; `apps/glossario` brandHref WP-raiz→home próprio. **B2 fechou em 2026-06-15 o subpath `@artificio/ui/static` para Astro.** Residual B13: markup Astro de `SiteHeader`/`SiteFooter` segue espelhado e accounts sem nav/footer compartilhado, tratar só se houver duplicação real que justifique.
