# Tasks — Spec 062

## F1 — Mesas

- [x] S1 — Localizar schema/migrations/tipos de sistemas e edições.
- [x] S2 — Mapear CRUD/admin, APIs e validações.
- [x] S3 — Mapear consumidores frontend/backend e relações de domínio.
- [x] S4 — Registrar dados-semente, IDs, aliases, hierarquia e constraints.

## F2 — Glossário

- [x] G1 — Localizar schema/migrations/tipos de sistemas e edições.
- [x] G2 — Mapear CRUD/admin, APIs e validações.
- [x] G3 — Mapear consumidores frontend/backend e relações de domínio.
- [x] G4 — Registrar dados-semente, IDs, aliases, hierarquia e constraints.

## F3 — Comparação

- [x] C1 — Matriz campo/status/ID/semântica.
- [x] C2 — Mapa de consumidores e contratos API.
- [x] C3 — Duplicatas, conflitos, lacunas e compatibilidade.

## F4 — Arquitetura

- [x] A1 — Comparar ownership/localização/serviço/API/cache/projeções.
- [x] A2 — Threat model, disponibilidade e blast radius.
- [x] A3 — Propor decisão recomendada e alternativas.

## F5 — Decisões

- [x] D1 — Definir superfícies de administração: site/sidebar como principal; mesas/glossário/downloads também podem administrar pelo serviço central.
- [x] D2 — Produzir recomendação fundamentada sobre owner/host, API×projeção, cenários e sugestões.
- [x] D3 — Obter decisões do mantenedor para o pacote arquitetural.

## F6–F8 — Saída

- [x] O1 — Modelo canônico conceitual.
- [x] O1a — Contrato de capacidades administrativas compartilhadas e integração da sidebar do site.
- [x] O1b — Definir fronteira semântica e tratamento de cenários.
- [x] O1c — Definir API central integral, sem projeções locais.
- [x] O2 — Plano de migração/rollout/rollback.
- [x] O2a — Planejar reescrita do subsistema de sistemas/edições do glossário e remoção segura do CRUD local.
- [x] O2b — Definir prova de integridade dos termos: contagem, mapeamento, órfãos zero e rollback.
- [x] O2c — Plano de UUIDs legados e validação dos 1.265 nós do Mesas.
- [x] O2d — Compatibilidade temporária das APIs e payloads históricos.
- [x] O2e — Disponibilidade, falhas, propagação e separação beta/prod.
- [x] O3 — Testes, gates e critérios de aceite conceituais.
- [x] O4 — Organizar duas etapas na própria 062: investigação e código.
- [x] O5 — Atualizar backlog, specs README, project-state e sessão.

## Etapa II — código

- Decisão operacional atual (2026-07-10): fundação central nasce dentro de `apps/site` e o primeiro ambiente real é `beta.artificiorpg.com`. Prod/promote ficam fora desta rodada até aprovação nominal futura.
- [x] I0 — Detalhar fases executáveis, critérios de aceite, arquivos, migrations, APIs, testes, deploy e rollback. **Detalhado em `plan.md`/`tasks.md`; I0a/I0b executados localmente e em beta conforme subitens.**
- [ ] I0a — Unificar consumo frontend do catálogo em `apps/mesas` (independe do serviço central; ver detalhe em `plan.md`). Só beta nesta rodada, sem promote/prod.
  - **GATE (corrigido 2026-07-09 — sem mais risco de duplicação, mas ordem por disciplina): nenhuma task I0a.0+ inicia antes de I0b.6 estar ✅ fechado.** Modelo corrigido: cada nó guarda `name`/`name_pt`/`aliases` PRÓPRIOS e completos, sem composição de pai+filho em lugar nenhum — 573 nós hoje têm `name` sujo (contaminado com o prefixo do sistema/edição pai, herança do import antigo), a correção deles é reescrever o campo pra virar o nome próprio definitivo, não gerar um "sufixo" que o frontend recompõe depois. `useSystemsCatalog()`/`SystemPicker` (I0a.1-2) devem exibir a árvore de verdade e ler `name` de cada nó como valor final — se começarem antes de I0b.3, só vão exibir os 573 nomes ainda sujos (feio, mas não quebra nem duplica).
  - [x] I0b.1 — Backup (`pg_dump`) de `mesas_rpg` prod e beta antes de qualquer escrita; salvar em `C:\projetos\artificiobackup`. **Fechado 2026-07-09:** dumps read-only salvos em `C:\projetos\artificiobackup\spec-062\mesas_prod_20260709-132037.sql` (5.060.121 bytes, SHA256 `227D1F9B3EF4A0848950BAD155AA392CDDBF95EB5C747BAD9A9856D3A43AD803`) e `mesas_beta_20260709-132037.sql` (5.200.707 bytes, SHA256 `E5048F0767050CBCB32AF78B3AE271B0847AFB62CAF996AA573082E775E8223D`).
  - [x] I0b.2 — Rodar em beta a mesma query de auditoria (contaminados vs irregulares, incluindo `subsystem`) usada em prod; não assumir mesma proporção. **Fechado 2026-07-09:** prod confirmou 688 `system`, 392 `edition`, 187 `variant`, 579 filhos, 573 contaminados e 6 irregulares. Beta confirmou 697 `system`, 402 `edition`, 189 `variant`, 1 `subsystem`, 592 filhos, 577 contaminados e **15 irregulares**. **D109:** prod é a fonte real/principal para curadoria; beta/dev é ambiente de ensaio/deploy até a centralização.
  - [x] I0b.3 — Migration `manual-risk` com os 5 campos de header, executada pela **transação atômica única do runner oficial** (`apply_required_migrations.sh` envolve arquivo + registro em `schema_migrations` em `BEGIN`/`COMMIT`; sem transação interna na migration) para não deixar estado misto visível a leituras concorrentes: reescreve `name` dos nós contaminados (edition+variant cujo `name` = `parent.name + ' ' + resto`) para conter só o nome próprio definitivo (o "resto"), removendo o prefixo herdado do import antigo. **Arquivo criado localmente em 2026-07-09:** `apps/mesas/database/migration_142_fix_system_child_names.sql`, com `manual-risk`, `requires-backup: true`, predicado literal `left(...) = parent.name || ' '` (sem `LIKE`/wildcards), guard contra nome vazio e conferência `candidate_count == updated_count`. **Validação:** header/guard verdes; predicado literal revalidado read-only em prod `573|0|6` e beta `577|0|15`. **Beta/dev aplicado 2026-07-09:** runner oficial em `/opt/artificio-beta`, `COMPOSE_PROJECT=mesas-beta`, backup VM-local `/tmp/artificio-mesas-beta-i0b3-pre-20260709-143715.dump` SHA256 `3eb7ffc055ca27616033c2449d7f6f9f5d1341b4efd454692233475e5e13b85a`; `577` nomes corrigidos; `schema_migrations=1`; health beta OK. **Prod aplicado 2026-07-09:** runner oficial em `/opt/artificio`, `COMPOSE_PROJECT=mesas`, backup VM-local `/tmp/artificio-mesas-prod-i0b3-pre-20260709-162521.dump` SHA256 `44d947913ec88505a5db7bdff9fccab27ad7126c153510425ec2114ac16dee75`; `573` nomes corrigidos; `schema_migrations=1`; health prod OK. Pós-detector ficou beta `1|0|591` e prod `1|0|578` por falso positivo legítimo `Time` → `Time Again`.
  - [x] I0b.4 — Fila de revisão manual com prod como fonte principal (D109). **Fechado 2026-07-09; D110:** `Advanced Dungeons & Dragons` é edição de `Dungeons & Dragons` e fica como nome próprio legítimo; `Vampire` é nome inglês e `Vampiro` nome português do mesmo sistema, portanto as edições `Vampire 1e`, `Vampire 2e`, `Vampire 5e` e `Vampire Anniversary` exigem correção localizada/manual orientada por nomes localizados (não update automático por prefixo PT); `Anniversary` tem PT `Aniversário`; `Dungeons & Dragons 3.5e` e `Dungeons & Dragons 3.0` são edições irmãs de `Dungeons & Dragons`, não relação edição→variante. Beta/dev continua só fila operacional de ensaio.
  - [x] I0b.5 — `useSystemsCatalog()` expõe a árvore de verdade (nó com filhos, cada nível com `name`/`name_pt`/`aliases` próprios). `SystemPicker` mostra a árvore navegável; quando precisa de 1 linha de texto (chip, resultado de busca), concatena os nomes próprios do caminho selecionado (`sistema.name + edicao.name + variante.name`) — isso é formatação de exibição de uma seleção já resolvida, não um campo derivado nem regra de "sufixo do pai". Deploy deste passo pode ser feito a qualquer momento em relação a I0b.3 (sem risco de duplicação, ver gate acima), mas segue a ordem por disciplina: beta primeiro, prod só após aprovação de promote+deploy separada. **Fechado local 2026-07-09:** evidência consolidada em I0a.1/I0a.2: `useSystemsCatalog()` preserva `children[]`, `name`/`name_pt`/`aliases[]` próprios, `slug`, `node_type`, logo/site e contadores; `SystemPicker` renderiza árvore navegável e monta caminho apenas em render; testes direcionados, lint e build verdes.
  - [x] I0b.6 — Validar build+lint+smoke visual do catálogo pós-migration em beta antes de qualquer promote/prod desta parte. Só depois deste ✅ as tasks I0a.0+ abaixo podem iniciar. **Fechado 2026-07-09:** `pnpm --filter @artificio/mesas lint` ✅; `pnpm --filter @artificio/mesas build` ✅; beta DB/API `schema_142=1`, residual esperado `Time -> Time Again`, `/catalogo` HTTP 200, `/api/v1/systems?view=tree` HTTP 200, payload `roots=697`, `dnd_5e=true`, `dnd_5e_2024=true`, `dnd_contaminated_string=false`, DOM com `#root` e assets. Playwright visual real tentado, mas Chromium local ausente; sem sessão Chrome real.
  - [x] I0a.0 — Remover rota órfã `/api/v1/profile/me/systems*` do backend (`profile.ts`) e apagar `useProfile.ts` do frontend (hook morto, 0 referências confirmadas via grep+Serena). Manter só `/api/v1/profile/systems*` (rota ativa real). Aprovado pelo mantenedor em 2026-07-09. **Fechado 2026-07-09:** removidos `POST /profile/me/systems` e `DELETE /profile/me/systems/:id`; mantidos `POST/DELETE /profile/systems*`; `useProfile.ts` apagado; tipos extraídos para `types/profileTypes.ts`; grep sem uso legado; smoke beta sem sessão da rota mantida retorna 401 (auth guard, não 404); `pnpm --filter @artificio/mesas lint` ✅; `pnpm --filter @artificio/mesas build` ✅; `pnpm verify:api` ✅ com 2 breaking changes intencionais (rotas órfãs removidas).
  - [x] I0a.1 — Criar `useSystemsCatalog()`: único fetch de `GET /api/v1/systems?view=tree`, cache com TTL + `forceRefresh` manual (mesmo padrão de `draftFormUtils.ts`/`loadSystems` e do cache backend `systemsTreeCache`), normalização de payload (`unknown` → tipado), loading/error padronizados. Retorna a árvore (nós com `children[]`, cada nó com `name`/`name_pt`/`slug`/`node_type`/`aliases[]` próprios intactos, sem transformação, mais `logo_filename`, `website_url`, `children_count`, `tables_count`, `aliases_count`) + uma view flat (todos os nós numa lista, para busca). Deve ampliar `apps/mesas/frontend/src/types/systems.ts` para refletir o payload real do backend e preservar explicitamente as exibições/capacidades de **Slug**, **Tipo**, **Aliases (0)**, edições, variantes, **Logo** e **Website Oficial**; `Logo`/`Website Oficial` continuam campos de nó raiz (`node_type = system`) no admin, não viram campos derivados. **Não expõe `pathLabel`, "label composto" nem qualquer concatenação pai+filho — isso é proibido no hook** (ver `plan.md`, regra de nome próprio por nó). Montar o texto do caminho completo é responsabilidade do `SystemPicker` (I0a.2), a partir do caminho selecionado, nunca do hook. **Fechado 2026-07-09:** `types/systems.ts` ampliado; `hooks/useSystemsCatalog.ts` criado com cache TTL 5 min, `forceRefresh`, `invalidateSystemsCatalogCache`, normalização Zod, árvore + flat + loading/error; teste cobre preservação de slug/tipo/aliases/filhos/logo/site/contadores, cache/forceRefresh e loading→dados→erro simulado. **Review PR #143:** contadores `children_count`/`tables_count`/`aliases_count` agora usam `z.coerce.number()` para aceitar `COUNT(DISTINCT ...)` serializado como string pelo `pg` (`int8`) e normalizar para número; `forceRefresh()` não relança erro após gravar `state.error`, evitando rejection não tratada em retry de UI; erro de payload inesperado agora usa `TypeError`; testes cobrem payload com contadores string e retry com erro resolvendo `undefined`. Validação: `pnpm --filter @artificio/mesas-frontend test -- src/hooks/useSystemsCatalog.test.ts` ✅ (suite frontend verde), `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.2 — Criar `SystemPicker` v1 (modo `single` e `multi`). **Layout vinculante: seguir exatamente a seção "Referência visual aprovada do SystemPicker" no topo de `plan.md` e o mockup `sessoes/assets/062-systempicker-arvore-mock.html`** — árvore navegável com chevron/indentação por nível, nome+nome PT+aliases por nó, destaque de seleção só na linha do nó escolhido, bloco de confirmação com caminho completo abaixo. Não é lista plana nem grid de 3 colunas. Busca por nome/alias/name_pt filtra a árvore mantendo hierarquia (ancestrais de um match ficam visíveis mesmo sem match direto). **Princípio de design (correção do mantenedor, 2026-07-09): é 1 componente único para todos os contextos/permissões — não variantes separadas por tela.** O que muda por `role` são as ações disponíveis ao lado de cada resultado, não o layout: usuário comum (mestre) vê só "Sugerir" quando não encontra; admin vê "Sugerir" **e** "Criar agora" (grava direto no catálogo, sem fila de aprovação) lado a lado; nó já existente e selecionado ganha um botão de editar inline (lápis) só para admin, ausente para o mestre. `SystemPicker` recebe a `role` do usuário autenticado e decide as ações a renderizar — não recebe uma prop de "modo visual" por tela. Nota: este componente será redesenhado (não substituído) em I0a.13 para suportar lote — v1 aqui cobre só seleção de nó já existente ou sugestão avulsa de 1 nó, igual ao `SystemSuggestionModal` atual. **Fechado 2026-07-09:** criado `components/SystemPicker.tsx` com `single`/`multi`, árvore navegável, busca hierárquica, confirmação por caminho renderizado, ações `Sugerir`/`Criar agora`/editar por `role`, sem lista plana/grid 3 colunas. **Review PR #143:** input de busca recebeu `aria-label={searchPlaceholder}` para nome acessível programático; props marcadas como `Readonly`; ternário aninhado do label expandir/recolher extraído. Teste `SystemPicker.test.tsx` cobre render da árvore, filtro mantendo ancestrais, seleção single, ações admin para busca sem resultado e remoção multi. Validação: `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx` ✅ (suite frontend verde), `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.3 — Migrar `StepSystem.tsx` (criar mesa) de `SystemTreeSelector` para `SystemPicker` single. **Fechado 2026-07-09:** `StepSystem.tsx` agora usa `SystemPicker` em `mode="single"`/`role="user"`, mantém `SystemSuggestionModal` para sugestão e seleciona o sistema criado no sucesso. Removido estado local de busca antigo do passo; `SystemTreeSelector` deixa de ser usado neste fluxo. Validação: `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.4 — Migrar `CatalogoPage.tsx` de `SystemAutocomplete` + fetch local para `useSystemsCatalog()` + `SystemPicker` single. **Fechado 2026-07-09:** `CatalogoPage.tsx` usa `useSystemsCatalog()` para árvore/cache/erro/retry e `SystemPicker` single nos filtros desktop e mobile; mapas id→slug/id→nome agora vêm da view `flat`; removidos `SystemAutocomplete`, `fetch('/api/v1/systems?view=tree')`, `systemsTreeReloadKey` e `setSystemsTree`. Filtro continua gravando slug em `filters.system` e mantendo `trackFilterSistema`. **Review PR #143:** bloco duplicado loading/erro/`SystemPicker` de desktop/mobile extraído para `CatalogSystemFilter` local, preservando `idPrefix` e cor de loading por contexto; empty state/cards extraídos para reduzir complexidade e remover ternários sinalizados pelo Sonar. Validação: grep sem legado na página, `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx src/hooks/useSystemsCatalog.test.ts` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.5 — Migrar `UserSystemsSelector.tsx` (perfil: favoritos + sistemas que mestra) de `fetch` cru para `useSystemsCatalog()` + `SystemPicker` multi. **Fechado 2026-07-09:** `UserSystemsSelector.tsx` agora usa `useSystemsCatalog()` para árvore/loading/error/retry e `SystemPicker` em `mode="multi"`/`role="user"`; removidos `fetch` cru, `VITE_API_URL`, `SystemTreeSelector`, estado local de busca/árvore/loading, `console.log` e `showError`. `onSelectionChange` reconcilia ids adicionados/removidos chamando `onAdd`/`onRemove`, preservando contrato com `ProfileEditPage`. Validação: grep sem legado no componente, `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx src/hooks/useSystemsCatalog.test.ts` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.6 — Migrar `OnboardingPage.tsx` de `SystemTreeSelector` para `useSystemsCatalog()` + `SystemPicker` multi. **Fechado 2026-07-09:** `OnboardingPage.tsx` agora usa `useSystemsCatalog()` para árvore/cache/loading/error/retry e `SystemPicker` em `mode="multi"`/`role="user"`; `/api/v1/me/options` continua carregando só tags/plataformas do onboarding, sem depender de `systems_tree`; resumo da confirmação usa a view `flat` do hook para id→nome. Validação: grep sem legado na página, `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx src/hooks/useSystemsCatalog.test.ts` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.7 — Migrar `SystemSuggestionModal.tsx` de `authGet` própria para `useSystemsCatalog()` (elimina reimplementação nº 6 de fetch de árvore). **Fechado 2026-07-09:** `SystemSuggestionModal.tsx` agora usa `useSystemsCatalog()` para árvore/loading/error/retry do select de sistema pai; removidos `authGet('/api/v1/systems?view=tree')`, `systemsTree` local, `setSystemsTree`, `systemsLoading`/`systemsError` locais e tipo local duplicado de nó. Criação/sugestão continua usando `authPost` e preserva callback `onSuccess`. Validação: grep sem fetch próprio no modal, `pnpm --filter @artificio/mesas-frontend test -- src/test/suggestionModals.test.tsx src/hooks/useSystemsCatalog.test.ts` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.8 — Restringir `SystemTreeSelector` (grid 3 colunas) ao admin de catálogo; remover consumo por telas de usuário final. **Nota (revisão cruzada, 2026-07-09): confirmado que o editor de draft Discord (`DraftEditorTab.tsx`) NÃO consome `SystemTreeSelector` — usa componente próprio `SystemSearchSelect.tsx` (busca flat simples). Esta task não afeta nem depende de I0a.9; sem risco de remover acesso do staff antes da hora.** **Fechado 2026-07-09:** grep confirmou nenhum import/uso real de `SystemTreeSelector` em `apps/mesas/frontend/src`; o admin de catálogo já usa `CatalogTree`/`CatalogTreeNode`, não o grid legado. Como não havia consumidor admin restante, o arquivo `components/SystemTreeSelector.tsx` foi removido para impedir regressão em tela final; comentário obsoleto em `SystemAutocomplete.tsx` também foi limpo. Validação: `rg "from ['\"].*SystemTreeSelector|<SystemTreeSelector" apps/mesas/frontend/src` sem matches, `Test-Path apps/mesas/frontend/src/components/SystemTreeSelector.tsx` → `False`, `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx src/hooks/useSystemsCatalog.test.ts src/test/suggestionModals.test.tsx` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - [x] I0a.9 — Migrar fluxo de **moderação de drafts Discord** (`draftFormUtils.ts`/`loadSystems` + `SystemSearchSelect.tsx` no `DraftEditorTab.tsx`, usado só em `ModeracaoSection.tsx` — confirmado via grep, não é tela do mestre) para `useSystemsCatalog()` + `SystemPicker`; redesenho visual do mesmo padrão das outras telas. Correção de escopo (2026-07-09): esta tela mostra resultado do parser que já rodou no backend (`parseDiscordAnnouncement.ts`, na ingestão), revisor confirma/corrige — não é o mestre comum, e não confundir com I0a.3 (mestre, sem parser, busca/seleção manual pura). **Fechado 2026-07-09:** `useDraftForm.ts` usa `useSystemsCatalog()` para árvore/flat/loading/error/retry; `DraftEditorTab.tsx` trocou `SystemSearchSelect` por `SystemPicker` single/admin, mantendo botão de refresh e modal de adicionar sistema; `DiscordDraftPreview.tsx` repassa `systemsError`; `draftFormUtils.ts` perdeu o loader/cache/schema duplicado de sistemas; `SystemSearchSelect.tsx` removido. **Review PR #143:** termo digitado no `SystemPicker` agora é propagado para `SystemSuggestionModal.initialName`, evitando redigitar ao sugerir/criar sistema no editor de draft; teste cobre prefill via botão "Criar agora". Validação: grep sem `SystemSearchSelect`/`loadSystems` no fluxo Discord, testes direcionados ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅.
  - **Fim do PR 1** (I0b.1-6 + I0a.0-9): fechar, validar smoke em beta, só então abrir PR 2 com as tasks abaixo (decisão da revisão cruzada 2026-07-09 — evita bot de review analisar `SystemPicker` sendo escrito e reescrito na mesma janela de diff).
  - [x] I0a.10 — Migration em `system_suggestions` (tabela distinta de `systems`, sem relação com a migration de I0b.3): campo/estrutura de agrupamento (`batch_id` ou equivalente) para permitir cadeia sistema→edição→variante numa submissão só. Header com os 5 campos obrigatórios (`@class`, `@requires-backup`, `@author`, `@created`, `@description`). Linhas antigas continuam válidas (cadeia de tamanho 1). **Fechado local 2026-07-09:** criada `migration_143_system_suggestions_batch.sql` (`online-safe`, `requires-backup:false`) com `batch_id`, `batch_index`, `parent_suggestion_index`, checks de índice e `resolution_type` ampliado para `create_chain`; tipos Kysely e hydrate config atualizados.
  - [x] I0a.11 — `POST /api/v1/system-suggestions` aceita array de nós em lote (cada nó referencia índice do pai na mesma lista quando o pai também é novo, ou `parent_id` real quando o pai já existe). **Fechado local 2026-07-09:** rota aceita payload legado de nó único, `{ nodes: [...] }` ou array direto, limita lote a 1-3 nós, preserva limite de 5 pendentes, cria `batch_id` único quando há cadeia e retorna lista quando lote.
  - [x] I0a.12 — Novo `resolution_type: create_chain` em `POST /api/v1/admin/system-suggestions/:id/resolve`: aprova a cadeia inteira numa transação, criando cada nível na ordem e resolvendo `parent_id` do nível anterior com o `id` real recém-criado. **Fechado local 2026-07-09:** `systemSuggestionsAdmin.ts` aceita `create_chain`, busca sugestões pendentes do mesmo `batch_id`, cria nós com helper transacional comum e marca todas como aprovadas; sugestão antiga sem lote vira cadeia de 1 nó.
  - [x] I0a.13 — Redesenhar `SystemPicker` v1 (de I0a.2) para formulário único de até 3 linhas (sistema/edição/variante), cada linha detectando sozinha se reaproveita nó existente ou marca criação, envio em lote via I0a.11. Depende de I0a.10-12 (schema+API) estarem prontos antes; é revisão do mesmo arquivo criado em I0a.2, não um componente novo — planejar como 1 PR de continuação, não retrabalho paralelo. **Fechado local 2026-07-09:** `SystemSuggestionModal` ganhou formulário de cadeia sistema/edição/variante para usuário comum e envia lote via I0a.11; `SystemPicker` troca ação vazia para "Sugerir cadeia"; admin mantém criação direta legada.
  - [x] I0a.14 — Redesenhar `SystemSuggestionResolutionDrawer.tsx` (admin) para aprovar/rejeitar cadeia completa via `create_chain`, mantendo suporte a sugestões antigas de nó único. **Fechado local 2026-07-09:** drawer ganhou opção "Criar cadeia completa", preview/aviso de transação e payload `resolution_type:create_chain`, mantendo `create_alias`, `create_child`, `create_system`, `merge_existing` e `reject`.
  - [ ] I0a.15 — Build + lint verdes; smoke visual de todos os fluxos migrados (criar mesa, catálogo, perfil, onboarding, sugestão em cascata, resolução admin, editor Discord); deploy beta apenas (`gh workflow run deploy.yml --ref dev -f module=mesas -f mode=deploy -f env=beta`), sem promote/prod. **Parcial local 2026-07-09:** testes direcionados, lint, build e `pnpm verify:api` verdes; deploy beta fica pendente de PR/merge em `dev` + aprovação nominal separada do workflow.
- [x] I1 — Fundação do serviço/banco/API central dentro do `artificiorpg.com` (implementação local no app `site`; primeiro alvo de deploy: `beta.artificiorpg.com`; sem prod). **Fechado local 2026-07-10:** criada migration `apps/site/db/migrations/006_catalog_foundation.sql` com tabelas centrais `catalog_versions`, `catalog_nodes`, `catalog_aliases`, `catalog_redirects`, `catalog_suggestions` e `catalog_audit_events`; criado repo `apps/site/db/repo/catalog.ts` com UUID canônico, slug/path, árvore ativa, snapshot com `catalog_version`/checksum, resolução por id/path e create/update admin com auditoria/versionamento; criado `apps/site/server/catalog-api.ts` com API pública read-only `/api/catalog/v1/systems`, `/api/catalog/v1/nodes/:idOrSlug`, `/api/catalog/v1/resolve?q=...` e API admin `/api/admin/v1/catalog/*`; `server.ts` plugado. Nenhum consumidor (`mesas`/`glossario`/`downloads`) conectado ainda; não há novo módulo/hostname/manifesto, a fundação pega carona no deploy do `site`. Validação: `pnpm --filter @artificio/site exec tsc --noEmit` ✅; `pnpm --filter @artificio/site test` ✅; `pnpm --filter @artificio/site build` ✅; `pnpm verify:api` ✅ (warnings legados de paths ambíguos; `site` com 10 non-breaking); migration aplicada em PGlite limpo com `SITE_PGDATA=apps/site/.pgdata-i1` ✅; smoke repo criou sistema+edição, gerou árvore e resolveu `dungeons-e-dragons/5e` ✅. Pendente operacional: commit/push/PR e deploy beta do `site` quando autorizado.
- [x] I2 — Modelo completo e importação do catálogo Mesas para o serviço central em beta: extrair árvore material do Mesas, importar para `catalog_nodes`/aliases com UUIDs canônicos por ambiente, gerar mapa legado→canônico, snapshot/checksum/contagens e relatório de divergências; sem conectar consumidores ainda. **Fechado local 2026-07-10:** criada migration `apps/site/db/migrations/007_catalog_legacy_mappings.sql` com `catalog_legacy_mappings` (`source_app`, `source_environment`, `source_table`, `legacy_id`, `canonical_id`, checksum e payload fonte); criado importador idempotente `apps/site/scripts/import-mesas-catalog.ts`, aceitando `MESAS_DATABASE_URL` para fonte Mesas real ou `MESAS_CATALOG_JSON` para fixture/export, com `CATALOG_SOURCE_ENV=beta|prod|local`, relatório JSON e `CATALOG_IMPORT_DRY_RUN=true`; criado fixture `apps/site/scripts/fixtures/mesas-catalog-sample.json`. O importador ordena pais antes de filhos, preserva `node_type`, `slug`, `path_slug`, `name`, `name_pt`, `description`, `logo_filename`→`logo_media_id`, `website_url`→`official_website_url`, aliases e gera mapa legado→canônico por ambiente. Validação: `pnpm --filter @artificio/site exec tsc --noEmit` ✅; `pnpm --filter @artificio/site test` ✅; `pnpm --filter @artificio/site build` ✅; `pnpm verify:api` ✅; migrations 006+007 aplicadas em PGlite limpo ✅; import sample criou 3 nós/2 aliases/3 mappings, checksum `65abaca6f2579991edc91939048a063ee3c199b21d34323f1e8bcfffe518dcb5`, relatório `sessoes/assets/062-i2-sample-import-report.json` ✅; rerun idempotente `created=0 updated=0 unchanged=3`, relatório `sessoes/assets/062-i2-sample-import-report-rerun.json` ✅. Pendente operacional: executar contra `mesas-beta-db`/`site-beta-db` após commit+PR+deploy beta do `site`, com env real e aprovação nominal.
- [x] I3 — Gestão principal na sidebar do Site. **Fechado local 2026-07-10:** `site-admin` ganhou entrada "Sistemas" na sidebar e rota `/admin/catalogo-sistemas`; criado `apps/site-admin/src/pages/CatalogSystemsPage.tsx` consumindo `/api/admin/v1/catalog/snapshot`, `POST /catalog/nodes` e `PUT /catalog/nodes/:id`; `api.ts` ganhou tipos `CatalogNode`, `CatalogSnapshot`, `CatalogNodeInput` e métodos `getCatalogSnapshot`, `createCatalogNode`, `updateCatalogNode`; UI permite buscar árvore por nome/slug/path/alias, ver versão/checksum/contagem, selecionar nó, criar sistema raiz, criar filho com tipo sugerido, editar nome, nome PT, slug, aliases, logo e Website Oficial. CSS local adicionado em `site-admin/src/styles.css`. Validação: `pnpm --filter @artificio/site-admin typecheck` ✅; `pnpm --filter @artificio/site-admin build` ✅; `pnpm verify:api` ✅ (site-admin agora com 32 chamadas/19 endpoints; warnings legados de paths ambíguos permanecem). Pendente operacional: smoke visual real em `beta.artificiorpg.com/admin/catalogo-sistemas` após commit/PR/deploy beta do `site`.
- [x] I4 — Migrar Mesas para leitura/escrita integral no serviço central. **Fechado local 2026-07-10:** `apps/mesas/backend/src/routes/systems.ts` virou fachada compatível que lê `GET /api/catalog/v1/systems` e escreve `POST|PUT /api/admin/v1/catalog/nodes`; criado `apps/mesas/backend/src/services/catalogClient.ts` com cache transitório, adaptação do shape legado (`slug`, `path_slug`, aliases, logo/site e contadores locais de mesas) e token interno; `DELETE /admin/:id` virou arquivamento central (`status=rejected`) após bloquear filhos/mesas, preservando UUID. `apps/site/server/server.ts` agora aceita `CATALOG_INTERNAL_TOKEN` só na API admin do catálogo para chamadas server-to-server de apps consumidores. Pendente operacional: configurar `CATALOG_API_URL` e `CATALOG_INTERNAL_TOKEN` em beta, deploy site+mesas beta e smoke `/api/v1/systems?view=tree`/CRUD admin contra o catálogo central importado.
- [x] I5 — Migrar Glossário para leitura/escrita integral no serviço central. **Fechado local 2026-07-10:** `apps/glossario/backend/src/services/catalogClient.ts` criado; `/api/systems*` virou fachada sobre `GET /api/catalog/v1/systems` e `POST|PUT /api/admin/v1/catalog/nodes`; deletes legados viram arquivamento central com bloqueio por termos/cenários/edições; `termController`, `scenarioController`, `exportController` e import lookup deixam de fazer join/lookup em `systems`/`editions` locais e hidratam nomes por snapshot central; `mergeUsers` não toca mais `public.systems`; script `pnpm --filter @artificio/glossario-backend catalog:migrate-refs` criado para mapear/aplicar referências locais `terms/scenarios` para UUIDs centrais, dry-run por padrão e apply só com `GLOSSARIO_CATALOG_MIGRATION_APPLY=true`. Pendente operacional: configurar env central em beta, rodar dry-run do mapeamento com relatório visível, aplicar só após aprovação nominal, e smoke do Glossário beta.
- [ ] I5a — Mapear manualmente sistemas/edições do Glossário e migrar referências. **Critérios de fechamento:** (1) dry-run do script `catalog:migrate-refs` executado e relatório aprovado pelo mantenedor; (2) aplicação autorizada nominalmente com `GLOSSARIO_CATALOG_MIGRATION_APPLY=true`; (3) smoke test do Glossário beta concluído (consulta, CRUD de termo/cenário, exportação) confirmando que referências estão resolvidas contra UUIDs centrais sem joins nas tabelas legadas `systems`/`editions`.
- [x] I6 — Administração contextual pelos projetos consumidores. **Fechado local 2026-07-10:** adotada a rota 1, sem criar pacote compartilhado ainda. `apps/mesas/frontend/src/components/SystemSuggestionModal.tsx` agora permite que admin, dentro do fluxo contextual do Mesas/Discord, crie nó central com nome, nome PT, descrição, tipo, pai, **aliases**, **Logo** e **Website Oficial**, usando o endpoint central já existente via fachada Mesas (`POST /api/v1/systems/admin`). O submit envia `aliases`, `logo_filename` e `website_url`; labels novos são associados a controles; teste cobre payload admin. Usuário comum continua no fluxo de sugestão/moderação em cadeia.
  - **Achado do mantenedor (2026-07-10, PR #144 pós-deploy):** fluxo de sugestão de sistema no editor de draft Discord (`SystemSuggestionModal.tsx`, `apps/mesas/frontend`) é contraprodutivo para admin — permite criar só nome/nome PT/descrição/tipo/pai, sem aliases, sem URL oficial, sem logo, sem criar edição/variante encadeada na mesma ação. Admin precisa sair pro `site-admin` (`CatalogSystemsPage.tsx`) pra completar o cadastro, ida e volta desnecessária quando já está revisando o draft.
  - **Diretriz do mantenedor: não duplicar formulário/lógica.** `apps/mesas/frontend` e `apps/site-admin` são apps separados sem pacote de UI compartilhado — não existe hoje um `packages/*` de catálogo para reusar componente entre eles. Duas rotas possíveis, a decidir nesta task quando for implementada:
    1. Ampliar `SystemSuggestionModal.tsx` (mesas) com os campos que o backend **já aceita** e ignora hoje (`aliases`, `logo_filename`, `website_url` — confirmado em `apps/mesas/backend/src/routes/systems.ts:72-95`, que já repassa pro `catalogClient.createCatalogNode`, a mesma fonte central usada por `CatalogSystemsPage.tsx`). Isso não duplica lógica de API/persistência (já é uma só, central), só duplica campos de formulário React entre os dois apps.
    2. Extrair um `packages/catalog-ui` (ou equivalente) com o form completo de nó de catálogo, consumido por `site-admin` e `mesas-frontend`. Elimina duplicação de verdade, mas é escopo SDD Completo (mudança em pacote compartilhado, trava de AGENTS.md) — maior que o ganho imediato.
  - Ação recomendada quando esta task for priorizada: rota 1 primeiro (SDD Lite, escopo só `apps/mesas/frontend`, sem tocar `site-admin`); avaliar rota 2 só se o padrão se repetir num terceiro consumidor (glossário/downloads via I6 mais amplo).
  - Criar filho encadeado (sistema→edição→variante) direto no modal de admin também é gap, mas menor prioridade que os campos ausentes acima — já existe suporte de lote no backend para sugestão comum (`I0a.10-13`), falta só o caminho admin direto (hoje só cria 1 nó por vez via `POST /api/v1/systems/admin`).
  - Validação I6: `pnpm --filter @artificio/mesas-frontend test -- src/test/suggestionModals.test.tsx src/components/SystemPicker.test.tsx` ✅; `pnpm --filter @artificio/mesas lint` ✅; `pnpm --filter @artificio/mesas build` ✅.
- [x] I7 — Compatibilidade, observabilidade, beta, rollback e produção. **Fechado local/parcial 2026-07-10:** adicionada observabilidade/readiness sem acoplar health Docker à dependência central: `GET /api/catalog/v1/health` no Site retorna `ok`, `catalog_version`, `nodes_count` e `checksum`; `GET /api/v1/systems/health` no Mesas e `GET /api/systems/health` no Glossário validam a conexão com o catálogo central e retornam 503 acionável quando indisponível. Isso preserva compatibilidade dos endpoints legados e cria smoke explícito para beta. Validação: `pnpm --filter @artificio/site exec tsc --noEmit` ✅; `pnpm --filter @artificio/mesas-backend build` ✅; `pnpm --filter @artificio/glossario-backend build` ✅; `pnpm --filter @artificio/site test` ✅; `pnpm verify:api` ✅ (3 warnings legados de paths ambíguos). **Pendente operacional:** commit/PR/merge, configurar env beta, deploy beta, import real Mesas→Site beta, dry-run/apply controlado Glossário, smoke real e ensaio de rollback beta. Prod fora até aprovação futura.

## Bloqueio

- Etapa I concluída e aprovada; zero pergunta arquitetural bloqueante.
- I0/I1 já têm implementação local. Próximos bloqueios são operacionais: commit/push/PR/deploy beta exigem aprovação nominal por ação; prod fica fora até nova decisão.
