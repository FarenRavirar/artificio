# Backlog Geral de Specs e Debitos

> Mapa vivo das pendencias acionaveis. Atualizar ao abrir/fechar specs. Nao substituir `project-state.md`; este arquivo organiza o que falta por prioridade e origem.

## LEIA PRIMEIRO — regras de manutencao

- Ao criar spec: adicionar linha nesta tabela se houver debito novo.
- Ao fechar: registrar evidencia e mudar status/remover da lista ativa.
- Se uma pendencia precisar aprovacao nominal (`commit`, `push`, deploy, VM write, segredo), marcar `bloqueado`.
- Nao listar checklist historico ja superado; se a fonte antiga divergir, preferir `project-state.md` + sessao mais recente.
- Toda linha deve ter origem rastreavel (`specs/...`, `sessoes/...`, decisao `D###` ou erro `E###`).
- Task aberta em `tasks.md` nao precisa virar linha propria se estiver coberta por item agregado claro; registrar essa cobertura na sessao/spec da auditoria.
- Se decisao posterior mudou o caminho, mover o item antigo para fechado/absorvido ou atualizar o proximo passo para nao orientar agente errado.
- Este arquivo tem dois tipos de item:
  - `BL-*`: backlog/spec/fatia planejada.
  - `D-*`: debito nomeado vindo de sessoes/reviews.

## Especiais — Governanca / Dev-Tools

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| BL-084-DOWNLOADS-SCRAPER | 🟢 fechado (PRs #193-#197 mergeadas em `dev`, deploy beta healthy, 2026-07-24) | `specs/084-downloads-scraper-catalogo-terceiros/` (spec/plan/tasks), D118, PRs #193 (feature)/#194 (Fase 10)/#195-#197 (fixes de Dockerfile) | `apps/downloads/backend` + schema novo + `patchright`/`camoufox`/`franc-min` | FECHADO com débito residual D-084-09 (decisão nominal do mantenedor, 2026-07-24): cron 04:00 ainda não teve disparo real desde deploy, Modo 3 (Chrome logado) nunca executado contra fonte real, `/sobre-e-uso` não visto renderizado — os 3 cobertos por teste automatizado, nenhum bloqueia catálogo funcionar. 5 adapters (itch.io/Grimórios & Dados/OPERA RPG/DriveThruRPG/DMs Guild), 159/159 testes backend + 8/8 frontend, deploy beta confirmado (`downloads-beta-app/api/db` healthy, `/api/v1/health` 200). | — |
| BL-085-DOWNLOADS-PARSER-HTML | 🟡 PR #201 mergeado em `dev`, deploy beta `success` (2026-07-25); falta smoke funcional real | `specs/085-downloads-parser-html-onebookshelf/` (spec/plan/tasks), emenda de escopo E1-E7 (spec.md, 2026-07-24), PR #201 (`e1a41b5`), `.specify/memory/errors.md` E012 (achado do deploy manual-risk) | `apps/downloads/backend` (parser genérico + registry de plataformas em banco) + `apps/downloads/frontend` (UI `/gestao/materiais/importar` + `/gestao/plataformas`) | Registry de plataformas em banco (`download_scraper_platform`, migration_025), parser genérico JSON-LD Schema.org (normaliza `@graph`/array/`@type` array/`offers` array) + overrides em código, CRUD de cadastro sem deploy, sidebar/rota `/gestao/plataformas` admin-only. 2 rounds de fix de review (CodeRabbit/Codex/Sonar) antes do merge. Deploy beta: 1ª tentativa bloqueada pelo guard `manual-risk` da migration_025 (rollback automático limpo), aplicada manualmente via SSH (mesmo padrão E012), 2ª tentativa `success` (`gh run 30146119480`). **T9.2 ainda falta:** smoke funcional real (colar HTML de cada fonte, publicar, checar duplicata, cadastrar plataforma nova pela UI ponta a ponta) — só a infra (deploy+migration) foi validada até aqui. | Smoke funcional real em beta (T9.2), autorização nominal por ação conforme necessário (ex.: promote pra prod depois). |
| D-085-04 | 🟡 aberto (registrado, não corrigido por decisão do mantenedor 2026-07-25) | `.specify/memory/errors.md` E012 (achado desta sessão) | `.github/workflows/deploy.yml`, `.github/workflows/_deploy-module.yml` | Nenhum workflow do repo expõe `ALLOW_MANUAL_MIGRATIONS`/`REQUIRE_PROD_BACKUP_FOR_MANUAL` como input de `workflow_dispatch` — único caminho pra aplicar migration `manual-risk` é SSH manual na VM (mesmo padrão do E012, script oficial). `break-glass-deploy-prod.yml` não cobre esse caso (só `mesas`/prod). | Adicionar os 2 inputs ao `workflow_dispatch` de `deploy.yml`/`_deploy-module.yml`, propagando pro `apply_required_migrations.sh` — elimina o SSH manual pra esse cenário. Mudança de infra/CI, pede spec/PR própria. |
| BL-086-DOWNLOADS-METADATA-RICA | 🟡 spec pronta pra desenvolvimento (2026-07-25), nada implementado | `specs/086-downloads-metadata-rica-onebookshelf/` (spec/plan/tasks reescritos), `sessoes/2026-07-25-downloads-086-metadata-rica.md`, referências reais do mantenedor em `C:\projetos\artificio\temp\*.png` + `exemplo_de_codigo.txt` (fora do git) | `apps/downloads/backend` (3 extratores + sanitização rica + 2 migrations + taxonomia/fila de sugestão) + `apps/downloads/frontend` (card, ficha, filtro, editor TipTap, gestão) + `packages/ui/src/admin/` (kit administrativo compartilhado, aditivo) | Fase 0 fechada: 18 decisões do mantenedor registradas. 9 gaps confirmados por evidência (arquivo:linha), incluindo 4 que a versão anterior da spec não tinha: `isbn` esquecido (12º campo), `data-codeid` não exclusivo da tabela (6× `commentText`/`customerName` em fixture — regex ingênua captura comentário de cliente), `filters` é árvore com facet tipado (não lista plana), descrição do JSON-LD truncada em ~500 chars vs. HTML rico completo em `obs-product-description`. Também: `GET /material-metadata/:id` já é público e faz `selectAll()` (reduz escopo da ficha), `GET /materials` sem join (card precisa), scraper nunca preenche `system_id`/`edition_id`. 11 fases + 2 de kit (5B/5C), um PR por fase. | Fase 1 (extração backend). Pendências pontuais dentro das fases: lib de sanitização (T2.1, perguntar antes de instalar), onde o editor aparece (T9.3), se o `Footer` de `packages/ui` aceita link extra (T10.1). `packages/ui` é pacote compartilhado — aprovação nominal antes do commit das fases 5B/5C. |
| D-086-01 | 🔴 URGENTE — aberto — **débito futuro registrado por decisão do mantenedor (2026-07-25), reclassificado urgente (2026-07-25)** | `specs/086-downloads-metadata-rica-onebookshelf/` (requisito 17, Fase 5B/5C), `sessoes/2026-07-25-downloads-086-metadata-rica.md` | `apps/mesas/frontend/src/features/admin/` (12 peças) + `apps/mesas/frontend/src/index.css` (tokens `--admin-*`) | **Migrar `apps/mesas` para o kit administrativo compartilhado** (`packages/ui/src/admin/`, criado na spec 086). Hoje o mesas mantém a cópia local (`AdminSidebar`, `AdminMain`, `AdminWorkspaceLayout`, `Breadcrumb`, `PageHeader`, `SectionCard`, `MetricCard`, `StatusPill`, `AdminTable`, `bulkActions`, `tabButtonClass`, `cn`) e os tokens `--admin-rail`/`--admin-surface`/`--admin-canvas`/`--admin-hover`/`--fg-low`/`--fg-faint`/`--shadow-card` definidos em `index.css` (dark 32-34/72, light 204-206/227). A spec 086 **proíbe** tocar `apps/mesas` (trava verificada por `git diff` em cada PR) — o kit é criado e validado no Downloads primeiro, por decisão explícita do mantenedor. Consequência aceita e deliberada: duas cópias do kit coexistem até esta migração. **Reclassificado urgente:** enquanto durar, todo fix/decisão de UX no shell administrativo precisa ser replicado manualmente nos dois lugares (mesas local + `packages/ui/src/admin/`) ou diverge silenciosamente — risco cresce a cada PR que toca gestão em qualquer um dos dois apps. **Divergência adicional (2026-07-25, D-086-05 fechado):** `packages/ui/src/admin/admin.css` renomeou todos os tokens genéricos pro prefixo `--admin-` (`--fg*`→`--admin-fg*`, `--border*`→`--admin-border*`, `--success`/`--warn`/`--danger*`/`--info`→`--admin-*`, `--shadow-card`→`--admin-shadow-card`, `--surface-input`→`--admin-surface-input`); `apps/mesas/frontend/src/index.css` continua com os nomes antigos sem prefixo. Migração terá que também renomear referências no mesas, não só trocar import. | Depois de o mantenedor aprovar o kit rodando no Downloads (smoke visual comparativo com `https://mesas.artificiorpg.com/gestao`, T5C.8): trocar os imports do mesas para `@artificio/ui/admin`, apagar `features/admin/components/ui/` e as peças de shell locais, remover os tokens `--admin-*` duplicados de `index.css` (passa a importar `@artificio/ui/admin/admin.css`), rodar build/lint/test do mesas + smoke visual de `/gestao`. Toca app em produção com gestão ativa — pede spec/PR própria e aprovação nominal. Adaptar a fiação de router/ícones ao contrato desacoplado do kit (`LinkComponent`/`currentHref`/`icon: ReactNode`, `AdminTable` com facetas controladas). |
| D-086-02 | 🟡 aberto (achado de investigação, 2026-07-25) | `specs/086-downloads-metadata-rica-onebookshelf/` (§Riscos), descoberta via `artificio-api-governance` | `docs/api/openapi/**` + `apps/downloads/backend/src/routes/scraper.ts` | `POST /api/v1/admin/scraper/parse-html` **não aparece no bundle OpenAPI** — `artificio-api-governance` não retorna a rota em nenhuma busca (`downloads` + `scraper parse`/`metadata`). Rota existe e funciona em produção (spec 085), mas está invisível pra descoberta de API, contra a regra de `AGENTS.md` que proíbe usar memória de chat como fonte primária de rota. | Documentar a rota no OpenAPI e rodar `pnpm verify:api`/`pnpm api:bundle` pra regenerar `docs/api/generated/*`. Verificar se outras rotas de `scraper.ts` têm o mesmo problema antes de fechar. |
| D-086-03 | 🟢 fechado na própria sessão (2026-07-25) | `sessoes/2026-07-25-downloads-086-metadata-rica.md`, decisões D097/D099 | `apps/downloads/backend/src/services/catalogClient.ts` | Comentário do arquivo afirmava "Downloads consome (nunca escreve) […] escrita de sistema/edição continua proibida aqui […] (D097)" — **leitura errada de D097**. D097 ("catálogo central, administração distribuída") e D099 dizem o oposto: sistemas/edições podem ser administrados a partir de mesas, glossário **ou downloads**, e "todos leem e escrevem integralmente nele"; o que D097 reserva ao admin do site é a gestão *principal/completa*, não a exclusividade de escrita. Comentário errado teria feito o próximo agente descartar a frente de taxonomia da spec 086 como proibida. | Corrigido: comentário reescrito citando D097/D099, o modelo de sugestão + triagem do mesas e o contrato de referência (`POST /api/v1/system-suggestions` + 5 rotas admin). `rtk tsc` do backend verde após a edição. Commit pendente de autorização nominal. |
| D-086-05 | 🟢 fechado na própria sessão (2026-07-25) | PR fase 5B (review Codex, comentário em `packages/ui/src/admin/admin.css:1-28`), `sessoes/2026-07-25-downloads-086-metadata-rica.md` | `packages/ui/src/admin/admin.css` (tokens `--fg*`/`--border*`/`--success`/`--warn`/`--danger*`/`--info`/`--shadow-card`/`--surface-input`) + 9 arquivos consumidores em `packages/ui/src/admin/` (`SectionCard`, `AdminWorkspaceLayout`, `AdminTable`, `AdminMain`, `AdminSidebar`, `MetricCard`, `StatusPill`, `PageHeader`, `Breadcrumb`) | `admin.css` define variáveis genéricas (`--fg`, `--border`, `--danger` etc.) direto em `:root`, sem prefixo `--admin-`, ao lado dos tokens já namespaced (`--admin-rail`/`--admin-surface`/`--admin-canvas`/`--admin-hover`). Comentário do arquivo ("arquivo isolado evita mudar consumidores que ainda não importam") está incorreto: por estar em `:root` (não escopado), importar este CSS **pisa em variáveis globais** de qualquer app que já tenha `--fg`/`--border`/etc. definidos com outro valor — não é isolado. Investigação confirmou 9 arquivos do próprio kit consomem essas variáveis, então renomear é mudança abrangente (não pontual). | Prefixar todas variáveis genéricas com `--admin-` em `admin.css` (ambos blocos `:root` e `:root[data-theme="light"]`), atualizar os 9 consumidores pra nova nomenclatura, corrigir o comentário de isolamento pra refletir o risco real. Overlap com D-086-01 (migração do mesas pro kit compartilhado) — resolver isso antes ou junto evita renomear duas vezes. |
| D-086-04 | 🔴 URGENTE — aberto (achado de review PR #204, Codex, confirmado e registrado em 2026-07-25) | PR #204 (review Codex, comentário em `apps/downloads/backend/src/routes/systemSuggestionsAdmin.ts:68-206`), `sessoes/2026-07-25-downloads-086-metadata-rica.md` | `apps/downloads/backend/src/routes/systemSuggestionsAdmin.ts` (`withSuggestionLock`, `resolveCreateSystem`/`resolveCreateChild`/`resolveMergeExisting`/`resolveCreateAlias`) **e o mesmo padrão em** `apps/mesas/backend/src/routes/systemSuggestionsAdmin.ts` | `withSuggestionLock` abre `db.transaction()` + `pg_advisory_xact_lock(hashtext(id))` e, **dentro** dessa transação Postgres local, chama `createCatalogNode`/`addCatalogNodeAlias` — POST/PUT HTTP reais para `apps/site` (catálogo central), outro serviço, outro banco, sem transação distribuída entre os dois. Enquanto a chamada de rede está em voo (pode levar segundos, pode falhar, pode dar timeout), a transação Postgres do Downloads fica aberta segurando: (a) o advisory lock (esperado, é o propósito), e (b) uma conexão do pool de Postgres do Downloads — se `apps/site` ficar lento/instável, cada `/resolve` prende uma conexão do pool pelo tempo inteiro da chamada HTTP, degradando outras rotas do Downloads que competem pelo mesmo pool. Caso mais grave: `resolveCreateSystem` com `edition_name` faz **duas** chamadas HTTP sequenciais (sistema, depois edição) dentro da mesma transação — dobra a janela de exposição. Mitigação já aplicada nesta sessão (compensação: se o 2º POST falhar, arquiva o 1º node via `archiveCatalogNode` antes de relançar) resolve o node órfão no catálogo central, mas **não resolve o problema de escala/latência da conexão presa**. **Confirmado que é o mesmo padrão já em produção no `apps/mesas`** (`withSuggestionLock`/`performApprove`/`resolveCreateSystem` etc. do mesas fazem exatamente a mesma coisa, mesmo comentário de trade-off aceito: "aceitável: fluxo admin de baixa frequência, nao dado de usuario publico") — Downloads replicou o desenho de propósito (decisão do mantenedor, spec 086: "isso tem que ser comum"). Por isso a correção **não pode ser feita só no Downloads** sem criar assimetria entre os dois apps que resolvem o mesmo tipo de sugestão do mesmo jeito hoje. | Resolver nos dois apps ao mesmo tempo, em spec própria: tirar as chamadas HTTP de dentro de `db.transaction()` — abrir a transação só para os writes locais rápidos (updates de `download_system_suggestion`/`download_material` ou `system_suggestions` no mesas), fazer as chamadas ao catálogo central antes/depois da transação, mantendo a compensação (archive em falha parcial) e decidindo se o advisory lock ainda cobre a janela toda ou só a parte local. Reavaliar `packages/catalog-matching` como candidato a também hospedar essa lógica compartilhada, já que a extração do kit administrativo (D-086-01) está tocando o mesmo par de apps. |
| BL-082-DOWNLOADS-FECHAMENTO-REAL | 🟢 fechado (PRs #188/#190 mergeadas em `dev`, 2026-07-23) | `specs/082-downloads-fechamento-real/`, PR #188 (auditoria diff), PR #190 (F2 fechamento) | `apps/downloads/backend`, `apps/downloads/frontend`, `apps/downloads/database/` | F2 concluída: criação/submissão de material com UI, storage MVP, migração de tema, gestão admin. Build/lint/test/verify:api verdes. Auditoria beta real executada (2026-07-23). | — |
| BL-078-MESAS-SISTEMAS-HIDRATACAO | ✅ deployado em beta/prod (PRs #163/#164/#165/#166; migrations pendentes) | `specs/078-mesas-sistemas-central-hidratacao/`, D114, D115, D116, `sessoes/26-07-15_1_mesas_078-sistemas-central-hidratacao.md`, PRs #163 (`d9aa9b2`), #164 (`1b7aef5`), #165 (`9d0c76e`), #166 (`8d8ff6a`) mergeados e deployados | `apps/mesas` backend/frontend/DB + `apps/site` admin + migrations 146-150 | Código deployado em beta e prod. Fases 1-6: provider único read/write, hidratador Central→Beta set-based, hidratação Mesas sem sistemas, parser/onboarding migrados, UX compartilhada. 542 testes backend verdes, lint/build/verify:api limpos. **Migrations NÃO aplicadas em bancos reais** (project-state confirma "bancos reais intactos"). Migration 150 é `manual-risk` (exige backup antes de apply real). | Aplicar migrations 146-150 em beta (ensaio) → validar smoke → backup prod → aplicar em prod. Migration 150 exige `ALLOW_MANUAL_MIGRATIONS=true` + backup. |
| BL-077-MESAS-DEDUPE-ATIVAS | ✅ deployado em beta/prod (PRs #159/#162; smoke runtime pendente) | `specs/077-mesas-dedupe-mesas-ativas/`, `sessoes/26-07-14_1_mesas_077-dedupe-mesas-ativas.md`, PR #159 (`c0965e6`: dedupe + OG scrape + 4 fixes), PR #162 (`e27f2c2`: classificar hierarquia legada) | `apps/mesas` backend + frontend + API/admin + migration | Código deployado em beta e prod. Migration 145, scanner mesa×mesa/draft×mesa, 3 rotas admin, badge e aba Duplicatas implementados. Produção read-only: 31 ativas; teste exploratório achou 3 pares fortes. Lint/build/API/tests verdes. **Falta:** smoke UI real (botão "Checar duplicatas" + validar 3 pares e links). Migration pode já ter sido aplicada no deploy (verificar `schema_migrations`). | Verificar se migration 145 foi aplicada em beta/prod (read-only `SELECT * FROM schema_migrations WHERE migration_name LIKE '%145%'`); se sim, executar smoke UI. Se não, aplicar + smoke. |
| SPEC-044 | fechado (concluido 2026-06-22) | `specs/044-opencode-ecosystem/`, `sessoes/26-06-22_7_opencode-ecosystem.md` | `opencode.json`, `AGENTS.md`, `CLAUDE.md`, `ast-grep`, MCP/plugins | 10 ferramentas/configs ativas. OpenCode + Claude Code 100%. Smoke 4/4 verde. 2 debitos implementados (Serena, CBM). 2 rejeitados (Sleev, hooks Claude). | — |
| SPEC-045 | aberto (investigacao concluida; T1 ✅) | `specs/045-debitos-pendentes-deploy/`, `sessoes/26-06-22_8_debitos-pendentes-deploy.md`, PR #85 | accounts, links, `.gitignore`, backlog | Investigou 5 debitos "Local": **3 ja resolvidos** (BL-BUILD-CACHE-PRUNE-ALL, BL-LINKS-NAV-CROSSAPP, BL-033-SECRET-BLOCK), 2 reais (BL-ACCOUNTS-PORT deploy, BL-LINKS-GROUP-LOGOS 12/13). T1 (BL-033 gitignore) ✅ PR #85. T5 reconciliacao backlog ✅. | T2 deploy accounts + T3 canal logo + T4 smoke links (aprovacao nominal) |
| SPEC-046 | aberto (auditoria em andamento) | `specs/046-arquiteture-docs-sync/`, `sessoes/26-06-22_5_arquiteture-docs-sync.md` | `.specify/arquiteture.md`, docs Serena | Auditoria secao por secao do `arquiteture.md` contra realidade 2026-06-22. §1 corrigido, §2 auditado, §3-§9 pendentes. 4 debitos registrados (module.manifest ausente, nav apps inexistentes, site-admin isolado, metadata verificacao). | Continuar auditoria §3 (SSO/Auth). |
| BL-055-API-GOVERNANCE | 🟢 fechado | `specs/055-api-governance-executavel/`, `sessoes/26-06-27_2_api-governance-spec.md`, `sessoes/26-06-27_3_api-governance-055_investigacao-fase1.md` | governança/API/CI, `docs/api`, `scripts/api`, apps Express e consumidores | FECHADO 2026-06-30: fluxo executável implementado em modo estrito. Inventário ~331 rotas, OpenAPI validado, scan de consumidores, allowlist vazia (`api:check --strict` exit 0), diff bloqueante, `api-governance` required check no CI. `MAPA_DE_API.md` manual deprecado. O que não foi feito foi descartado (mantenedor). | — |
| BL-ECOSYSTEM-SERENA | local (instalado, configurado) | `specs/044-opencode-ecosystem/debitos.md` DEB-044-01 | `Serena MCP` (oraios/serena) | v1.5.3 instalado via uv, 549 arquivos TS indexados, MCP config no opencode.json, `.serena/` gitignored. Falta: smoke test com agente real. | Reiniciar OpenCode → testar ferramentas Serena em 1 sessao → avaliar. |
| BL-ECOSYSTEM-CODEBASE-MEMORY | local (instalado, indexado) | `specs/044-opencode-ecosystem/debitos.md` DEB-044-02 | `codebase-memory-mcp` (DeusData) | v0.8.1, npm global. 10.6k nos, 18.1k edges em 4.1s. MCP manual no opencode.json. Bloco instrucoes no AGENTS.md (marcadores HTML). OpenCode nao auto-detectado. Falta: smoke test com agente real. | Reiniciar OpenCode → testar MCP tools → avaliar. |

## P0 — Seguranca / Infra / Deploy

> **Guarda-chuva `BL-INFRA-WORKFLOWS-026`** (spec 026) consolida a direcao dos itens de
> esteira abaixo em fatias F1-F9 (`specs/026-infra-workflows-audit/tasks.md`). Os itens
> seguem rastreaveis individualmente; a 026 so organiza ordem e risco.

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| D-AIKIDO-CSP-ARTIFICIORPG | aberto | Aikido (scan externo, achado 2026-07-12) | `artificiorpg.com` (WordPress raiz de produção, Gate C intocável) | Header `Content-Security-Policy` ausente em `https://artificiorpg.com`, risco reportado 91 (XSS/injeção de dados). Não investigado se é config nativa do WP, plugin de segurança ausente, ou proxy/Cloudflare na frente — sem diagnóstico ainda. | Diagnosticar onde o header deveria ser setado (WP/plugin vs. Cloudflare Transform Rules) antes de mexer — WP de produção é intocável sem aprovação nominal (Gate C); avaliar CSP via Cloudflare (não toca WP) como caminho preferencial. |
| BL-INFRA-WORKFLOWS-026 | fechado (auditoria concluida) | `specs/026-infra-workflows-audit/`, `sessoes/26-06-16_10`, `sessoes/26-06-16_11`, `sessoes/26-06-16_14` | `.github/workflows/*`, esteira CI/CD | FECHADO 2026-06-20: auditoria 100% concluida. F1/F2/F3/F4/F5/F10/F-SEC fechados. F11 confirmado implementado (cache pnpm/turbo em `_deploy-module.yml`). Subsistentes viram itens individuais no backlog: F6=`BL-ACCOUNTS-PORT`, F7=`BL-MESAS-AUTO-ARCHIVE-CF`, F12=`BL-INFRA-GHCR-F12`. | — |
| BL-INFRA-CACHE-CAP-F10 | fechado | `specs/026-infra-workflows-audit/` F10, `sessoes/26-06-16_10`, commits `06a5ded`/`a727ab2`, run `27633842040` | build cache Docker VM | FECHADO: `docker builder prune -f` total pos-deploy em `_deploy-module.yml` + `deploy-accounts.yml` + `docker-cleanup.yml`. Cap de imagem descartado (no-op), scripts removidos. Provado em prod-beta: deploy mesas baixou build cache de 20.89GB reclaimable -> 0B (`system df`), images 28->10GB | — |
| BL-ACCOUNTS-PORT | local (spec 035/045, aguarda deploy) | `specs/023`, `docs/agents/infra-map.md`, `specs/035-infra-small-debts/`, `specs/045-debitos-pendentes-deploy/` T2 | `apps/accounts/docker-compose.yml`, `docker-compose.prod.yml` | T1-T4 LOCAL CONCLUIDO 2026-06-20 (reconfirmado spec 045 §4): `expose: ["3000"]` em ambos composes. Falta T5: deploy prod com smoke health/login/me (aprovação nominal). | dispatch `module=accounts mode=deploy`, smoke, fechar (spec 045 T2) |
| BL-CDX-310 | fechado | `specs/026` F5, `sessoes/26-06-16_14`, PR #45 (`0b4ec43`), deploy run `27656716758` | CI/CD accounts, `apps/accounts/docker-compose.prod.yml` | FECHADO EM PROD: accounts deploya via `_deploy-module` do clone git (configfile `/opt/artificio/apps/accounts/docker-compose.prod.yml`, snowflake/tarball aposentado), healthcheck `healthy`, project/volume `accounts_accounts_pgdata` preservados, smoke `/health`=200/`/login`=200/`/api/auth/me`=401, snapshot+rollback armados. `deploy-accounts.yml` deletado. Fix seguranca: `POSTGRES_PASSWORD` sem default `admin` | — (rollback futuro = revert F5 em main + re-dispatch) |
| BL-INFRA-CI-EFFICIENCY-F11 | fechado | `specs/026` F11, `specs/033-infra-toolchain-update/`, `_deploy-module.yml` | `_deploy-module.yml` CI | FECHADO 2026-06-20: cache pnpm (`cache: pnpm`) + turbo (`actions/cache@v4`, `.turbo`, `--cache-dir=.turbo`) confirmado presente em `_deploy-module.yml`. O item "nao em main" era true na epoca (2026-06-17); spec 033 promoveu tudo. Auto-deploy gating absorvido pelo `build-matrix` do F4. | — |
| BL-INFRA-GHCR-F12 | aberto | `specs/026` F12, D008 | build/deploy + GHCR | VM builda `--no-cache` (lento); alvo = build em CI -> push GHCR -> VM `pull`; reconcilia F10 | SDD Completo proprio; registry/auth/compose refs |
| BL-MIGRATION-GUARD-ACCUMULATION-E012 | aberto | deploy prod mesas `run 28844372359` (2026-07-07, rollback automatico), E012 (`errors.md`), spec 058 Fase 11/PR #129 | `scripts/deploy/apply_required_migrations.sh` (`MAX_AUTO_PENDING=5`), `promote-prod-fast-forward.yml` | Guard barrou deploy prod com "9 > 5 migrations pendentes" (migrations 133-141, specs 057/058, todas online-safe) — fluxo real de promote `dev->main` esporadico sempre acumula >5 quando varias specs seguidas mexem em `apps/mesas/database/` sem promote no meio. Resolvido manualmente desta vez (script oficial via SSH com `MAX_AUTO_PENDING` ajustado pro total real, sem SQL solto). Guard em si continua correto/saudavel — o gap e de processo, nao de codigo. | Avaliar (a) promover `dev->main` com mais frequencia, ou (b) step opcional pos-fast-forward em `promote-prod-fast-forward.yml` que aplica migrations pendentes antes do proximo deploy de codigo tentar rodar, ou (c) alerta quando `dev` diverge de `main` por N commits com migration nova. SDD Lite, planejamento antes de programar. |
| BL-058-T11-10-FIELD-AUDIT | aberto | `specs/058-mesas-parser-learning-deepseek/tasks.md` Fase 11, T11.10 | `apps/mesas/backend/src/discord/parseDiscordAnnouncement.ts`, `apps/mesas/backend/src/discord/types.ts` | Auditoria campo-a-campo de `DiscordTableDraftTable` confirmou 3 gaps (`experience_level`/`table_level`/`scenario_id`, T11.7-T11.9, ja fechados) mas foi feita so lendo o objeto de retorno do parser, sem validar CADA campo individualmente contra os 3 datasets reais. Pode haver mais campos `null` hardcoded sem tentativa de extracao. | Rodar auditoria campo-a-campo completa (extraido com logica real / `null` deliberado com motivo documentado / gap com task propria) contra os 3 datasets validos; registrar em `debitos.md` ou tasks.md da spec 058. |
| BL-MESAS-AI-AUDIT-NO-TEST-COVERAGE | aberto | E013 (`errors.md`, 2026-07-07), achado do mantenedor testando draft real pos-deploy spec 058 Fase 11 | `apps/mesas/backend/src/discord/llmAssist.ts` (`auditDiscordDraftCompleteness` v2), `apps/mesas/backend/src/routes/discord/drafts.ts` (`runCompletenessAudit`, `/audit-field/:field`) | Corrigidos 2 furos estruturais da auditoria de completude por IA (nao detectava contato faltando por exclusao de privacidade; prompt so pegava campo vazio, nao campo errado) + endpoint novo por-campo — mas sem teste automatizado cobrindo o payload real mandado ao DeepSeek nem o parsing do `issue_type`. Validacao foi so build+testes existentes (nenhum mockava a chamada LLM real) + inspecao de codigo. | Escrever teste unitario pra `runCompletenessAudit`/`auditDiscordDraftCompleteness` mockando fetch do DeepSeek, cobrindo: contato ausente vira candidate local mesmo com LLM mockado retornando vazio; `issue_type=incorrect` sobrevive ao schema Zod; endpoint por-campo filtra certo. |
| BL-059-MESAS-COPIAR-ANUNCIO-OG | 🟢 fechado (PR #140 mergeada, deploy beta+prod confirmados 2026-07-09) | `specs/059-mesas-copiar-anuncio-whatsapp-og/`, `sessoes/26-07-08_1_mesas_copiar-anuncio-whatsapp-og.md` | `apps/mesas` pagina publica, painel do mestre, gestao, OG Express/API | Formatter WhatsApp e botoes de copiar anuncio em `/mesas/:slug`, `/painel` e `/gestao` implementados; OG de `/mesas/:slug` usa banner/imagem da mesa; `age_rating` exposto e tipado em `TableDetail`. Faixa etaria, comissionada e gestao decididos. SDD Completo. Deploy beta + prod validados pelo mantenedor. | — |
| BL-061-DOWNLOADS-DEFINICAO | 🟢 fechado (definição de produto) | `specs/061-downloads-definicao-produto/`, `sessoes/26-07-08_4_downloads_061-definicao-produto.md`, D089–D111 | Futuro `apps/downloads`; sistemas compartilhados, produto, taxonomia, UX, auth, storage, infra e deploy | Hub gratuito. F0–F7 concluídas (2026-07-11): produto, referências, políticas, UX/wireframes, técnica/segurança, infra e as 10 perguntas bloqueantes resolvidas (D111). Mapa de 7 specs filhas (Downloads A–G) proposto e estrutura criada. Zero código ainda. | Autorização nominal para iniciar `specs/070-downloads-a-schema-api-ownership` (Downloads-A: schema/API/ownership), primeira da fila A→{B,C}→D→E→F→G. |
| BL-070-076-DOWNLOADS-SPECS-FILHAS | 🟡 em execução (070 mergeada; 071-076 locais; 082/083/084 em branches próprios) | `specs/070-downloads-a-schema-api-ownership/` a `specs/076-downloads-g-infra-deploy/`, `specs/082-downloads-fechamento-real/`, `specs/083-downloads-rejeicao-motivo-legal-email/`, `specs/084-downloads-scraper-catalogo-terceiros/` | 7 specs filhas + 3 specs adicionais (082/083/084) decompostas da 061 | 070 (schema/API/ownership) **mergeada** via PR #151. 082 (fechamento real) **mergeada** via PRs #188/#190. 083 (rejeição/email) **mergeada** via PR #192. 084 (scraper) implementada localmente (Fases 1-9, branch `fix/084-*`). 071-076 permanecem locais na branch `feat/070-downloads-schema-api`. | Autorização nominal para commit/PR das specs restantes (071-076, 084). |
| BL-062-SISTEMAS-CATALOGO-CANONICO | ✅ deployado em beta/prod (PRs #143/#145/#149); I0a/I0b em andamento | `specs/062-sistemas-catalogo-canonico/`, `sessoes/26-07-08_5_sistemas_062-fonte-unica-investigacao.md`, D096–D099, D109, D110 | `apps/mesas`, `apps/glossario`, futuro `apps/downloads`, `site` admin, serviço compartilhado | Etapa I aprovada. Serviço independente dentro do Site; leitura/escrita integral central; Mesas principal; Glossário manual; cenários fora; merge sem apagar/arquivar. I0b.1-I0b.2 fechadas; I0b.3 fechada em beta/dev e prod; I0b.4 fechada; I0b.6 fechada. I0a.0 fechada: rotas órfãs `/profile/me/systems*` e hook morto `useProfile.ts` removidos, rota ativa `/profile/systems*` mantida, lint/build/verify:api verdes. | Próximo: I0a.1 criar `useSystemsCatalog()` como fonte única de fetch da árvore. |
| BL-API-DIFF-PATH-REMOVE-BLANK | aberto | Spec 062 I0a.0, `pnpm verify:api`, `docs/api/generated/api-diff.generated.md` | `scripts/api/diff-api.ts` / governança API | `api:diff` detecta corretamente 2 breaking changes em `mesas` após remover rotas órfãs, mas o relatório renderiza `path.remove` com `Path`/`Method` vazios. O ref local `dev` foi alinhado com `origin/dev` e o problema persistiu; não é só o caso antigo de branch local stale. | Corrigir normalização/renderização de diferenças `path.remove` no `scripts/api/diff-api.ts`, garantindo path/method no relatório; adicionar caso de teste com remoção de path OpenAPI. |
| BL-MESAS-TABLEREPO-EMPTY-CONTACTS-NO-TEST | aberto | CodeRabbit, PR #131 (nitpick trivial) | `apps/mesas/backend/src/repositories/tableRepository.ts` (`createTableWithRelations`, guard `contacts.length > 0`) | Guard que evita insert vazio em `table_contacts` (fix do bug de sync 500, E013/PR #131) nao tem teste cobrindo o cenario `contacts=[]`. Exige teste de integracao com DB real (Kysely transaction) ou mock de `db.transaction()` — fora do escopo rapido do fix original. | Escrever teste (integracao com DB de teste, ou mock de `trx.insertInto`) garantindo que `createTableWithRelations` com `contacts=[]` NAO chama `trx.insertInto('table_contacts')`. |
| BL-MESAS-OPENAPI-AUDIT-STATUS-CODES | aberto | Codex, PR #131 (nitpick trivial) | `docs/api/openapi/mesas.openapi.yaml` (overlay), `apps/mesas/backend/src/routes/discord/drafts.ts` (`runCompletenessAudit`) | `POST /audit-completeness` e `/audit-field/:field` podem responder 404 (draft nao encontrado), 422 (texto original indisponivel), 423 (kill switch IA ligado) e 502 (DeepSeek indisponivel) — OpenAPI so documenta os genericos 400/401/403/500. Endpoint irmao `/automation/auto-approval/guard` no mesmo arquivo ja documenta 423 explicitamente via overlay. | Adicionar os status codes reais ao overlay OpenAPI do endpoint (mesmo padrao ja usado em `/automation/auto-approval/guard`), rodar `pnpm api:generate-openapi` + `pnpm verify:api`. |
| BL-INFRA-DEFAULT-BRANCH | fechado | `sessoes/26-06-16_14`, D073 | default branch + branch protection | FECHADO: default trocado `main`->`dev` (mantenedor, UI). Auditoria provou que esteira referencia branch por NOME (nao "default") -> flip baixo risco. Branch protection aplicada via `gh api`: `main` (block force-push/delete; SEM require-PR/checks/linear p/ nao travar ff-promote, `dev` tem merge commits) + `dev` (require-PR 0-approvals + required check `lint + build + test` [unico sem path-filter; `build-matrix` fora pois travaria PR so-doc] + block force-push/delete). `scorecard.yml` trigger `main`->`dev` (analise read-only). **Crons de PROD (`docker-cleanup`/`mesas-auto-archive`) ganharam job `guard`** que pula a acao se o workflow divergir de `origin/main` (preserva gate dev->prod; achado de revisao PR #48). D073 (decisions/AGENTS): fim do doc-only-ff, tudo em `dev` via PR | smoke residual: confirmar promote ff `dev->main` sob protecao + 1 run de cron guardado |
| BL-CI-ESLINT-FLAT-CONFIG | fechado | `specs/037-lint-frontends-cleanup/`, `sessoes/26-06-16_11_security-ci.md`, PR #40 | `packages/auth`, `packages/content` + todos os 16 pacotes | FECHADO 2026-06-22: spec 037 criou `eslint.config.js` flat em todos os pacotes (16/16). `continue-on-error` removido do step Lint no ci.yml — lint é gate obrigatório. Comentário no ci.yml: "Lint agora e gate obrigatorio (sem continue-on-error)". | — |
| BL-INFRA-SEC-SCAN | fechado | `sessoes/26-06-16_11_security-ci.md`, PR #40 `chore/026-security-ci` (MERGEADO) | `.github/dependabot.yml` + 7 workflows (ci.yml, codeql.yml, dependency-review.yml, osv-scanner.yml, secret-scan.yml, scorecard.yml, semgrep.yml) | FECHADO 2026-06-20: PR #40 mergeado. 7 workflows de seguranca nativos presentes e ativos. Settings habilitados pelo mantenedor: Dependency graph + Secret scanning + Push protection. | — |
| BL-DEP-MESAS-LEGACY-SCRIPTS | fechado (spec 050 T12) | `specs/019` FSU-012/B4, `specs/035` R0/T6, `specs/050-infra-migration-guard-online-safe/` Fase C | deploy mesas | limpar/arquivar scripts legados `apps/mesas/scripts/deploy/*` depois que `_deploy-module` for a fonte unica. Spec 050 absorve R0a-e/T6a-d (reconfirmar órfãos + decisão A/B + remover + fechar). **Fechado 2026-06-24:** 6 arquivos removidos (escopo A), builds verdes, R0a-e/T6a-d marcados na spec 035. | seguir spec 050 Fase C (T8-T14); deletar = aprovação nominal |
| BL-MESAS-MIGRATION-GUARD-FALSE-POSITIVE | fechado (spec 050) | deploy prod mesas `run 28125222995` (2026-06-24, rollback automático), `specs/050-infra-migration-guard-online-safe/` | `scripts/deploy/lib_migrations.sh:59` (guard canônico) + cópia órfã `apps/mesas/scripts/deploy/lib_migrations.sh:61` | Guard `validate_sql_against_class` usa `\bDROP\b` largo demais → barra `DROP NOT NULL`/`DROP CONSTRAINT`/`DROP DEFAULT` (não destrutivos). `migration_128` (online-safe legítima) bloqueada no 1º apply em prod → deploy abortado. Beta passou porque 128/129 já em `schema_migrations`. **Fechado 2026-06-24:** regex estreito aplicado no guard canônico + cópia órfã sincronizada + teste shell (28/28) plugado no CI `_lint-shell.yml`. Erro E010 registrado. Follow-up: re-deploy prod mesas gated. | spec 050 Fases A/B completadas; re-deploy prod = aprovação nominal |
| DEB-050-01 | fechado (spec 050 T26, 2026-06-24) | SonarCloud PR #95 (2026-06-24), `specs/050-.../debitos.md` | `scripts/deploy/{reconcile_migrations,test_migration_guard,test_migration_reconcile}.sh` | Smells: `[`→`[[`, erros sem `>&2`, literais repetidos. **Fechado:** 3 arquivos corrigidos; ShellCheck + testes (29/29, 9/9) verdes. | — |
| DEB-050-03 | fechado (spec 050 T27, 2026-06-24) | ChatGPT Codex PR #95 (2026-06-24), confirmado no código `scripts/deploy/reconcile_migrations.sh:83-91` | `scripts/deploy/reconcile_migrations.sh` | **Correção (Major):** `--mark-applied` parseava DB args posicionais antes do `--force`. **Fechado:** `--force` extraído de qualquer posição; teste cobre prod-force sem DB args. | — |
| DEB-050-04 | fechado (spec 050 T28, 2026-06-24) | revisão PR #95 (2026-06-24), confirmado em `scripts/deploy/lib_migrations.sh:62` | `scripts/deploy/lib_migrations.sh` | **Correção (Major):** strip de comentário de bloco era line-based. **Fechado:** pipeline `perl -0777 -pe 's{/\*.*?\*/}{}gs'` + `sed 's/--.*//'`; teste multiline passa (29/29). | — |
| DEB-050-05 | fechado (spec 050 T29, 2026-06-24) | revisão PR #95 (2026-06-24), confirmado em `scripts/deploy/reconcile_migrations.sh:72-78` | `scripts/deploy/reconcile_migrations.sh` | **Confiabilidade (Major):** `cmd_list` engolia erros (`|| true` + process substitution). **Fechado:** erros propagados; `MOCK_QUERY_FAIL` stub + teste cobre (9/9). | — |
| DEB-050-06 | fechado (spec 050 T30, 2026-06-24) | verificação pós-T28 (2026-06-24), `scripts/deploy/lib_migrations.sh:63` | `scripts/deploy/lib_migrations.sh` | **Low (rebaixado):** `perl -0777` no strip → fail-open se perl ausente. **Fechado:** match consolidado em perl com checagem de exit (cobre perl ausente E quebrado, fail-closed). | — |
| DEB-050-07 | fechado (spec 050 T31, 2026-06-24) | CodeRabbit + 2 revisores PR #95 (2026-06-24) | `scripts/deploy/lib_migrations.sh`, `test_migration_guard.sh` | **Segurança (Major):** guard era denylist de tipos de objeto → `DROP POLICY/DOMAIN/FOREIGN TABLE/PUBLICATION/SERVER/AGGREGATE/OWNED` escapavam como online-safe. **Fechado:** invertido para **allowlist** (bloqueia qualquer DROP exceto NOT NULL/CONSTRAINT/DEFAULT/IDENTITY/EXPRESSION) via perl negative-lookahead; erros em `>&2`; +7 casos de teste (36/36); 128/129 PASS. | — |
| DEB-050-08 | fechado (spec 050 T32, 2026-06-25) | CodeRabbit PR #95 (2026-06-25), PoC confirmado no código `scripts/deploy/lib_migrations.sh` | `scripts/deploy/lib_migrations.sh`, `test_migration_guard.sh` | **Segurança (Major / Data Integrity):** strip naive `s{--[^\n]*}{}g` engolia `--` dentro de string literal → escondia destrutivo seguinte (`INSERT ... VALUES('--'); DROP TABLE x;` passava online-safe). **Fechado:** tokenizer perl de 1 passada ignora conteúdo de comentário (`/* */`, `--`) E string literal (`'...'`, `"..."`); dollar-quote fica como código (fail-closed). +3 casos de teste (39/39); 128/129 PASS. Pendente commit/push autorizado. | — |
| DEB-050-09 | fechado (spec 050, 2026-06-25) | SonarCloud PR #95 (2026-06-25) | `scripts/deploy/lib_migrations.sh` | **Code Smell (Major):** `[`→`[[` em `validate_sql_against_class` (L55/L108/L111). **Fechado:** 3 `[` da função → `[[`; resto do arquivo fica p/ DEB-050-02. 39/39, ShellCheck limpo. Pendente commit/push autorizado. | — |
| DEB-050-02 | fechado (spec 051 F3, 2026-06-25) | `specs/051-ui-changelog-nav-active/` | `scripts/deploy/*.sh` (8 arquivos) | Convergir estilo dos scripts antigos (`[`→`[[`, `>&2`) p/ alinhar com os novos. **Fechado:** `lib_migrations` (8×`[`→`[[` + 4×`>&2`), `apply_required_migrations` (11×`[`→`[[` + 6×`>&2`), `test_migration_lock` (1×`[`→`[[` + 2×`>&2`), `validate_branch_invariant` (2×`>&2`), `test_branch_invariant` (2×`>&2`). `reconcile_migrations`/`test_migration_guard`/`test_migration_reconcile` já convertidos (T26). `bash -n` 8/8 OK, self-tests: guard 39/39 ✅, reconcile 9/9 ✅, lock ❌ (flock ausente no Windows; OK em CI Linux). | — |
| BL-051-UI-CHANGELOG-NAV | ✅ **ENTREGUE EM PROD (2026-06-26)** | mantenedor; `specs/051-ui-changelog-nav-active/` | `packages/ui` (`ChangelogModal`/`Nav`/primitivas/`styles.css`) + `packages/config` (MODULE_ORIGINS) + apps consumidores + `scripts/deploy/*` | Spec 051 completa: **F0** varredura; **F4** wrappers changelog colapsados; **F5** ConfirmDialog/FileDropzone→`packages/ui` (rollout site-admin DEB-051-01); **F1** changelog corrigido (scroll lock + **DEB-051-02** `@source packages/ui` no Tailwind — causa real); **F2** marcador nav hostname-based; **F3** scripts deploy (DEB-050-02); **F6** cancelada (nulo); **E** origins centralizados. Reviews CI: DEB-051-CODEQL-01/RABBIT-04/05 corrigidos, RABBIT-06 descartado (falso-positivo). **PR #96 + #97 merged; promote dev→main ff; deploy prod 4/4 (glossario/site/links/mesas) ✅; smoke visual do mantenedor OK.** mesas-prod destravado (049+050+051). | — |
| BL-051-CONFIRMDIALOG-SITEADMIN-ROLLOUT | **fechado (DEB-051-01, 2026-06-26)** | Onda A da spec 051 (F5.2a), `f0-discovery.md` §6 | `apps/site-admin` | `site-admin` não depende de `@artificio/ui` (ausente no `package.json`). `ConfirmProvider`/`useConfirm` extraídos p/ `packages/ui` mas os 4 `window.confirm` crus do site-admin (`PostsList`, `PagesList`, `FeedbackPage`, `MediaLibrary`) não foram migrados. **Fechamento:** dep `@artificio/ui` adicionada, CSS importado, `<ConfirmProvider>` no root, 4 `window.confirm` → `await confirm({...})`. 8 arquivos, +50/-5. Lint 15/15 ✅, build ✅. | — |
| DEB-051-RABBIT-04 | **fechado (2026-06-26)** | CodeRabbit PR #96 | `scripts/deploy/apply_required_migrations.sh` | `CLASS` validado só com `-z`; valor não vazio mas inválido (typo no header) passa e migration é auto-aplicada. **Fechado:** allowlist fail-closed `online-safe\|manual-risk` em `load_header_vars` (domínio real do header; bot citou classes inexistentes). shellcheck ✅, guard 39/39 ✅. Ver reviews.md §11. | — |
| DEB-051-CODEQL-01 | **fechado (2026-06-26)** | CodeQL `js/polynomial-redos` PR #96 | `packages/ui/src/Nav.tsx` | Regex `replace(/\/+$/, "")` em `normalizeHref` fallback → ReDoS polinomial (check de CI **bloqueante**, falhou no PR #96). Risco prático nulo (input interno), mas gate bloqueia. **Fechado:** strip de barras finais sem regex de repetição (loop `charCodeAt`). build+lint ✅. Ver reviews.md §10. | — |
| BL-MIGRATION-RECONCILE-TOOL | fechado (spec 050 Fase E) | `specs/050-infra-migration-guard-online-safe/` R8/R9, achado ao remover órfãos (T12) | `scripts/deploy/` (canônico) | Ao remover `apps/mesas/scripts/deploy/reconcile_migrations.sh` (órfão), a única ferramenta de recuperação de drift de migration (`--list`/`--mark-applied`) sumiu. Canônico só **detecta** drift (`DRIFT ERROR`), não recupera. **Fechado 2026-06-24:** port canônico parametrizado (`scripts/deploy/reconcile_migrations.sh`) com gates R9 (prod-force, version validation, idempotência) + teste shell 7/7 no `_lint-shell.yml`. | Nenhuma — Fase E completa |
| BL-BETA-HYDRATE | **fechado (2026-06-29)** | `specs/005`, `sessoes/26-06-29_1_mesas_enrich-prod-db-url.md` | mesas beta | `POST /sync/enrich` (ex-`/sync/hydrate`) dava 500: `.env.beta` sem `PROD_DB_URL`. **Fechado:** role read-only `mesas_ro` criada no DB prod (CONNECT+USAGE+SELECT all tables + default privileges), `PROD_DB_URL` no `.env.beta` apontando p/ a role `mesas_ro` (host `mesas-db`, DB `mesas_rpg`), `mesas-beta-api` recriado. Validado: health 200, `mesas_ro` lê (15 users) e write negado (`permission denied`), beta-api conecta no prod. Smoke do botão na UI = mantenedor. | — |
| BL-CF-TUNNEL-TOKEN-SCOPE | aberto (ação mantenedor) | `sessoes/26-06-20_3_infra_cf-token-scope.md`, `docs/agents/access-registry.md` | Cloudflare API token (`CLOUDFLARE_API_TOKEN`) | Token API (`cfat_…`) só cobre Zone/DNS/Cache; `GET /accounts/{id}/cfd_tunnel` → **403 Forbidden**. Faltam permissões `Cloudflare Tunnel:Read` + `Cloudflare Tunnel:Edit`. Sem isso agentes não podem listar/criar/editar túneis ou hostnames públicos. | **Mantenedor:** Cloudflare Dashboard → API Tokens → editar token → adicionar permissões `Account:Cloudflare Tunnel:Read/Edit`. Validar com `GET /accounts/{id}/cfd_tunnel` 200. |
| BL-CF-TOKEN-DOC-GAP | aberto | `sessoes/26-06-20_3_infra_cf-token-scope.md` | `docs/agents/access-registry.md`, `docs/agents/context-capsule.md` | Documentação registrava "tunnel/logs read pendente" como nota, sem item acionável no backlog. Gap de processo: enablement documentado como "check later" sem tarefa → débito invisível. | Adicionar regra em `AGENTS.md` ou checklist de sessão: nota de pendência de infra/credencial em doc operacional deve vir acompanhada de item no `specs/backlog.md` com critério de aceite e próximo passo. |
| BL-DEP-PATHFILTERS | fechado | `docs/agents/deploy-flow.md`, `specs/026` F1, commit `8713ee0` | GitHub Actions | FECHADO: manifests raiz nos `paths:` de mesas/glossario/site; pushado, `pr-checks`/actionlint + deploys verdes | — |
| BL-MESAS-AUTO-ARCHIVE-CF | aberto | `sessoes/26-06-16_4_mesas_auto-archive-ci.md`, run `27607245699` | GitHub Actions + Cloudflare + `apps/mesas` prod | workflow agendado `Mesas Auto-Archive` falha repetidamente; run observado em 2026-06-16 retornou HTTP 403 com Cloudflare challenge antes da API (`Just a moment...`) | diagnosticar regra Cloudflare/WAF/bypass para endpoint cron ou mover execucao para caminho interno seguro; validar com `workflow_dispatch`/schedule sem expor segredo |
| BL-SEC-AUDIT-DEPS | fechado | `pnpm audit` (2026-06-21), `specs/039-sec-audit-deps/`, `sessoes/26-06-21_2_sec_audit-deps.md`, PR #80 (spec 041, commit `3761da7`) | monorepo (`pnpm-workspace.yaml` overrides + `apps/mesas/frontend`) | FECHADO 2026-06-21: mergeado via PR #80. `pnpm audit` 16 → 2. `pnpm-workspace.yaml` overrides (6): `form-data`, `undici<8`, `@opentelemetry/core`, `nanoid`, `uuid`, `esbuild` + bump `dompurify`. Build 15/15 + testes verdes. Restam 2 HIGH `xlsx` → spec 034. | — |
| BL-TEST-MESAS-SSO-ENV | fechado | `sessoes/26-06-21_2_sec_audit-deps.md`, `specs/039-sec-audit-deps/spec.md` (Anexo D1), PR #80 (spec 041, commit `3761da7`) | mesas-frontend testes | FECHADO 2026-06-21: mergeado via PR #80. Fix: `test.env: { VITE_PUBLIC_SITE_URL:'', VITE_API_URL:'' }` em `vitest.config.ts`. ssoRedirect 6/6, mesas-frontend 19/19 verde. | — |
| BL-TEST-SITE-MEDIA-MOCK | fechado | `sessoes/26-06-21_2_sec_audit-deps.md`, `specs/039-sec-audit-deps/spec.md` (Anexo D2), PR #80 (spec 041, commit `3761da7`) | site importer testes ↔ `@artificio/media` (spec 036) | FECHADO 2026-06-21: mergeado via PR #80. Fix: helper `enableCloudinary()` seta 3 vars Cloudinary. media.test 22/22 verde. | — |
| BL-DOCS-BACKLOG-INDEX-DRIFT | fechado | `sessoes/26-06-16_7_governanca_bug-registry.md`, `sessoes/26-06-16_8_docs-backlog-harness-audit.md` | `specs/backlog.md`, `sessoes/index.md`, specs/tasks | auditoria leve executada: sessoes recentes e drift evidente em sessoes historicas corrigidos; bug de harness registrado/fechado; mapa Spec 025 conferido | reabrir item novo se surgir drift novo; manter regra de registrar bug no mesmo turno |
| BL-DEP-MESAS-DISPATCH-ENV | fechado | `sessoes/26-06-16_9`, `sessoes/26-06-16_10`, runs `27629745457`/`27630434690`, commit `485b363`, `specs/026` F3 | `.github/workflows/_deploy-module.yml` | fix pontual em prod (`485b363`); **F3 (local) absorve a classe**: env derivado central no job `resolve`, inline removido dos 3 deploys -> dispatch-em-dev nunca mais vira prod por copia driftada | F3 aguarda commit/push (aprovacao) |
| BL-DEP-MESAS-AUTO-PUSH | fechado (PR #76) | `sessoes/26-06-18_3_infra_toolchain-update-spec.md`, PRs #65/#66, PR #76 | `.github/deploy-manifest.json` | FECHADO: `auto_deploy_on_push: false` implementado na PR #76, mergeada em `dev`. Mesas agora dispatch-only como glossario/site/accounts/links. | — |
| BL-KYSELY-029-ESM | fechado | `sessoes/26-06-18_3_infra_toolchain-update-spec.md`, T28c, spec 033, D078, commit `85063da`, run `27805626853` | `apps/mesas/backend` test runner + deps | FECHADO 2026-06-19: kysely `^0.29.2` unificado; mesas migrado jest→vitest; vitest 16/16 (114), tsc 0, turbo build 13/13, accounts 8/8. Deploy mesas beta SUCCESS (run `27805626853`); smoke `/` 200, `/api/v1/me/options` 401, `/api/v1/auth/google` 302 — runtime CJS `require(esm)` provado na VM node:24-alpine | — |
| BL-MESAS-BETA-MIGRATION-DRIFT | fechado | `sessoes/26-06-16_9_quality-beta-deploy.md`, runs `27629743341`, `27630434690` | mesas beta DB + migrations | drift nao reproduziu; arquivo existia em `/opt/artificio-beta`, script retornou `schema em conformidade`, e deploy beta `27630434690` passou migrations/deploy/smokes | reabrir so se novo deploy beta acusar drift novamente |
| BL-DEP-CONTAINER-NAMES | futuro | `specs/009-infra-deploy-hardening/` T9, D050 | `_deploy-module`/composes | substituir `container_name` fixo por nomes por projeto compose, se o custo compensar | adiar ate nova dor real de colisao; toca health-check/deploy de varios apps |
| BL-SITE-PROD-PARITY-030 | FECHADO | `specs/030-site-prod-deploy-parity/`, D075/D076/D077 | `apps/site` compose/manifest + VM (DB prod + rota Tunnel) | Fase 0 (codigo) ✅: PR #58. Fases 1-4 → spec 031 executou seed, flip autoria, flip rota, critical_routes, noindex beta (T3e resolvido rebuild `--build`). Site em paridade total com mesas/glossario/accounts. T14: PR #60 mergeado em `dev` (`37d9b4d`), promovido a `main`. Zero pendências. | — |
| BL-SITE-DATA-FLUXO-031 | FECHADO | `specs/031-site-prod-data-fluxo/`, D077 | `apps/site` DB prod/beta + autoria/admin + deploy | Fases 1-4 operacionais concluidas: seed ✅, flip autoria ✅, flip rota ✅, noindex beta ✅ (rebuild `--build` + source `.env.beta` + limpeza cache tsx). `site-prod-app` healthy na raiz, `site-beta-app` healthy com noindex. D075 aposentado. T4b/T4c: PR #60 mergeado (`37d9b4d`), promovido a `main`. Zero pendências. | — |
| D-DEP1 | fechado | `sessoes/26-06-12_2_debitos_ux-marca.md`, `specs/033-infra-toolchain-update/` | Express em todos os backends | FECHADO 2026-06-20: monorepo 100% Express 5. accounts=`^5.1.0`, glossario/backend=`^5.2.1`, mesas/backend=`^5.2.1` (migrado 4→5.2.1 na spec 033), site=`^5.1.0`. | — |
| D-DEP2 | aberto | `sessoes/26-06-12_2_debitos_ux-marca.md` | VM/toolchain/deps | atualizar apt, Node, pnpm, imagens Docker e deps npm com plano seguro | spec propria em fatias; backup/janela para VM |
| BL-PNPM-11 | fechado | `specs/033-infra-toolchain-update/tasks.md` T33c (2026-06-19) | pnpm 10→11 (monorepo) | FECHADO: `packageManager: pnpm@11.8.0`, campo `pnpm` removido de `package.json`, `patchedDependencies` + `allowBuilds` migrados para `pnpm-workspace.yaml`, 6 Dockerfiles atualizados (9 ocorrências). **Validação CI (PR #72, 2026-06-19) expôs gap:** `allowBuilds` listava só `sharp`/`bcrypt`; `esbuild` (transitivo) barrado por `strictDepBuilds` → `[ERR_PNPM_IGNORED_BUILDS]` em 5 checks. Corrigido: `esbuild: true` enumerado (validado `--frozen-lockfile` local exit 0). Política durável = **D080** (enumerar, nunca `"*": true`) | validação CI fechada no PR #72 |

| D-DOCKERFILE-LONGLINES | fechado | review PR #74 (Sonar/CodeRabbit), `sessoes/26-06-20_5_infra_dockerfile-sonar-debts.md`, commits `bfa98be`/`647ffd4`/`ca7b6dd` | `apps/links/Dockerfile` (L18, L23), `apps/mesas/backend/Dockerfile` (L35+39 merge), `apps/site/Dockerfile` (L18) | FECHADO: 5 code smells corrigidos e commitados. links Dockerfile fix em `bfa98be` (L18 split RUN, L23 split RUN). mesas/backend L38-41 mergeado. site L18 split. | — |
| BL-BUILD-CACHE-PRUNE-ALL | fechado | `sessoes/26-06-20_6_infra_build-cache-audit.md`, `specs/045-debitos-pendentes-deploy/investigacao.md` §1, commit `bfa98be` | `.github/workflows/_deploy-module.yml:502` + `.github/workflows/docker-cleanup.yml:159` | FECHADO (investigação spec 045, 2026-06-22): `docker builder prune --all -f` JÁ presente nos dois (`_deploy-module.yml:502`, `docker-cleanup.yml:159`). Commit `bfa98be` (2026-06-20), ancestral de origin/dev E origin/main → **em prod**. Backlog estava stale (linhas 494/159 desatualizadas). | — |
| BL-DEPLOY-ENV-PGPASS-FAILFAST | aberto | `specs/037-lint-frontends-cleanup/deploy-table.md`, E009, runs `27890695988`/`27890759220`/`27891034346` | bootstrap de `.env` na VM + `_deploy-module.yml` (passo Deploy module on VM) | 3 falhas do links (1ª subida) por `.env` corrompido na VM e senha presa em volume Postgres já inicializado → `28P01 password authentication failed` em loop. Fix-na-raiz (camada código, > doc): (1) procedimento/script de criação de `.env` na VM à prova de `\n` (here-string + `ssh '... cat > .env'` + validação `wc -c`/hash), proibir `ssh "...$(...)..."` em PowerShell; (2) `_deploy-module` deve detectar `28P01`/app-unhealthy-por-auth e abortar com mensagem clara ("volume tem senha velha — recriar volume OU ALTER USER", igual ao fail-fast de cloudinary). | SDD Lite/infra própria, branch+PR; runtime do links já corrigido 2026-06-21 (volume dropado + redeploy) |
| BL-DEPLOY-SSH-KEEPALIVE | aberto (Fase 1 commitada, Fase 2 pendente) | `specs/040-infra-deploy-ssh-keepalive/`, run `27894032104`, `sessoes/26-06-21_1_links_deploy-038-media.md`, PR #80 (commit `3761da7`) | `.github/workflows/_deploy-module.yml` (passo Deploy module on VM) | Fase 1 (keepalive): commitada em `3761da7` — `ServerAliveInterval=30`/`CountMax=10` no ssh+scp. **Robusto (núcleo):** execução remota destacada (`setsid`+logfile+sentinela de exit code) + poll do runner + `trap` estendido `ERR INT TERM HUP EXIT` idempotente → queda de SSH não aborta/derruba deploy. | SDD Completo, validar em BETA antes de prod, branch+PR. Follow-ups: otimizar `chown` / `BL-INFRA-GHCR-F12`. |
| D-LINKS-ADMIN-UUID-VALIDATION | aberto | review PR #78 (CodeRabbit), `sessoes/26-06-21_1_links_deploy-038-media.md` | `apps/links/server/server.ts` rotas admin com `:id` | Aplicado em `PATCH /reports/:id`; falta padronizar nas demais: `POST /groups/:id/accept`, `PATCH /groups/:id`, `POST /groups/:id/archive`, `DELETE /groups/:id`, `PATCH /tags/:id`, `DELETE /tags/:id`. UUID inválido → 400, não 500. | aplicar validação UUID nas 6 rotas restantes (SDD Lite, fatia própria) |
| D-DOCKERFILE-CHOWN-ORDER | aberto | deploy links `27894032104` falhou (Broken pipe), `sessoes/26-06-21_1_links_deploy-038-media.md` | todos os Dockerfiles (links/mesas/site/glossario) | `chown -R node:node /repo` no final do Dockerfile percorre `node_modules` gigante via SSH → timeout/Broken pipe. Mover `chown` para antes do `pnpm install` (quando /repo tem poucos arquivos) ou usar `COPY --chown=node:node`. | otimizar ordem do chown nos 4 Dockerfiles (SDD Lite, fatia própria) |
| D-PROMOTE-033-UPGRADES-REGRESSION | fechado (investigacao concluida) | promote `dev→main` 2026-06-21 (run `27894586895`), `sessoes/26-06-21_3_theme-regression.md`, spec 041 | monorepo (build Astro, Vite, Tailwind, Node) | FECHADO: causa confirmada = cache Cloudflare (HTML cacheado pre-promote), não código. Beta.artificiorpg.com com mesmo código funcionava. Spec 041 implementou site Astro híbrido com toggle funcional em beta. Purge CF pendente (mantenedor). | — (purge CF = mantenedor) |

## P1 — Produto / Apps

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| BL-071-STORAGE-MULTIPROVIDER | aberto (local, F1-F5 implementados) | `specs/071-downloads-b-storage-multiprovider/`, branch `feat/070-downloads-schema-api` | `apps/downloads/backend/src/storage/` | F1 (interface `StorageAdapter`, agora com `download()` p/ reconciliação), F2 (R2/B2 via `@aws-sdk/client-s3`, Fastio via REST cru, Cloudinary via `@artificio/media`), F3 (failover automático + auditoria + **cota local anti-cobrança, regra pétrea 2026-07-12**: contador `download_storage_usage`/migration 010, nunca bate no provider pra medir, checa ANTES de cada operação, default R2 = 90% do free tier real 10GB/1M/10M), F4 (job de migração entre providers + reconciliação por checksum SHA-256, `src/storage/migration.ts`, lógica pura testada com mock — checksum divergente preserva origem, nunca apaga sem confirmação de integridade) e F5 (magic bytes) implementados e testados (21 testes verdes, lint/build verde, 2026-07-12). **Falta:** T0.1 (confirmar contrato REST real da Fastio contra doc oficial) e T6.2 (teste de upload real em R2 com credencial real) — ambos exigem decisão/infra fora do escopo desta rodada (sem credencial real disponível). | Confirmar contrato Fastio real (T0.1) antes de deploy; rodar T6.2 com credencial real quando decidido; aprovação nominal pendente para commit de 070+071 |
| BL-072-SUBMISSAO-MODERACAO-TAXONOMIA | aberto (local, F0-F7 implementados, 4/5 débitos fechados) | `specs/072-downloads-c-submissao-moderacao-taxonomia/`, branch `feat/070-downloads-schema-api` | `apps/downloads/backend/src/{routes/moderation.ts,routes/reports.ts,routes/comments.ts,routes/materialMetadata.ts,services/editorialStateMachine.ts,services/ratingGuard.ts,services/reportAbuseGuard.ts}`, migration 011 | Taxonomia com validação cruzada (F1: `PUT/GET /material-metadata/:id`, `vtt_platform` exige `system_id`), submissão em etapas via rotas de moderação (F2), prova/evidência exigida na aprovação (F3), **máquina de estados editoriais completa** (F4: transições draft→in_review→published\|rejected→(reenvio)in_review, published→withdrawn; fila; reprovação com motivo obrigatório; batch; `auto_publish_enabled` kill switch), denúncia **agora exige conta `accounts.` (revoga anonimato, decisão nominal 2026-07-12)** com contenção automática (1 denúncia P0 em material publicado → `withdrawn`), retirada voluntária e detecção de abuso por usuário (F5: `/reports`, `reportAbuseGuard.ts`), comentário com conta obrigatória (F6: `/comments`), guard de avaliação com checker injetável (F6: `ratingGuard.ts`). 44 testes verdes, lint/build verde, `verify:api` exit 0 (2026-07-12). **Débitos:** DEB-072-01 a 04 fechados; **DEB-072-05 aberto** (abandono/reivindicação de material, sem critério de elegibilidade definido). | Nenhum commit feito (aguardando autorização nominal); specs 073/074 destravadas para consumir estas rotas |
| BL-073-DESCOBERTA-PUBLICA | aberto (local, F0-F6 implementados + DEB-073-01/02 fechados) | `specs/073-downloads-d-descoberta-publica/`, branch `feat/070-downloads-schema-api` | `apps/downloads/backend/src/routes/{materials.ts,creators.ts,destinations.ts}`, `apps/downloads/database/migration_013/014`, `apps/downloads/frontend/` (novo app) | Specs filhas 073-076 recuperadas de `stash@{1}` (só esqueleto spec/plan/tasks/reviews/debitos.md, zero código, criadas em sessão anterior — spec 061 T7.3). Gap descoberto: backend (070) não tinha listagem/busca, só ficha por slug — implementado `GET /api/v1/materials` (filtro q/system_id/edition_id/material_type/access_kind, ordenação relevance≈recent/recent/popular/name, paginação) e `GET /api/v1/creators/:slug` (novo). Criado `apps/downloads/frontend` do zero (Vite/React19/TS/Tailwind, padrão mesas): rotas T4.1, `AppShell`, busca/filtro/ordenação/paginação como contrato único de URL, card/ficha com CTA disparando evento de funil antes do redirect e aviso de destino degradado, AA básica. **DEB-073-01 fechado:** `migration_013` torna `download_creator.user_id` nullable (índice único parcial); ownership de material confirmado que nunca lê essa coluna, sem impacto; `creators.ts` trata `user_id` null retornando materiais vazio. **DEB-073-02 fechado:** `migration_014` cria `download_destination` (id opaco 1:1 por material) + rota `GET /api/v1/destinations/:id`; ficha faz get-or-create do destino e expõe `destination_id`; frontend usa esse id em `/ir/:destinationId` em vez do slug. Após correções: 50 testes backend + 6 frontend verdes, lint/build verdes, `verify:api` exit 0 (downloads breaking=0 non-breaking=3, 2026-07-12). **Falta:** T1.3/T1.4 (sidebar contextual/drawer mobile), T6.3 (E2e leve). **Débito residual:** DEB-073-03 (`/obter/:fileId` placeholder, depende de 071 T6.2 com credencial real). | Nenhum commit feito (aguardando autorização nominal); spec 074 (painel) destravada para reaproveitar componentes de card/ficha |
| BL-074-PAINEL-EDICAO | aberto (local, F0-F6 implementados) | `specs/074-downloads-e-painel-usuario-edicao/`, branch `feat/070-downloads-schema-api` | `apps/downloads/backend/src/routes/{downloads,ratings,favorites,collections,organizations,notifications,materials}.ts`, `apps/downloads/database/migration_015-018`, `apps/downloads/frontend/src/pages/painel/*` (novo) | 2 decisões nominais confirmadas antes de codar: (1) criar schema novo para dedup de download e avaliação; (2) implementar Organizações/Notificações como escopo mínimo inventado (sem spec/decisão prévia detalhando o domínio). 4 migrations: `download_user_material_download` (dedup por conta, D111 item 7), `download_rating` (1 por conta+material, usa `ratingGuard.ts` já existente), `download_organization`/`_member`, `download_notification` (feed só-leitura). Backend: `GET /materials/mine` (todos estados do autor) + `GET /materials/:id/history` (histórico por campo — PATCH que grava já existia desde 070) + 6 rotas novas (downloads/ratings/favorites/collections/organizations/notifications). Frontend: 9 rotas `/painel/*` (`PainelShell` com sidebar desktop+drawer mobile, `RequireAuth` via `@artificio/auth/client`), edição de material com histórico visível, ficha pública ganhou avaliação/comentário/favorito, CTA registra download com dedup antes do redirect. 56 testes backend (+6) + 6 frontend verdes, lint/tsc limpos, `verify:api` exit 0 (downloads breaking=0 non-breaking=17, 2026-07-12). **1 débito aberto (DEB-074-01 — Organizações/Notificações podem divergir do domínio real quando definido, não implementável sem decisão de escopo); 3 fechados em rodada posterior (2026-07-12):** DEB-074-02 (`GET /reports/mine`), DEB-074-03 (`AddToCollectionButton.tsx` na ficha pública), DEB-074-04 (`services/notify.ts` emite notificação real em approve/reject/batch/decisão de denúncia). | Nenhum commit feito (aguardando autorização nominal); spec 075 (gestão/admin) destravada, consome os mesmos dados de edição/histórico |
| BL-075-GESTAO-ADMIN | aberto (local, F0-F7 implementados) | `specs/075-downloads-f-gestao-admin/`, branch `feat/070-downloads-schema-api` | `apps/downloads/backend/src/{routes/admin.ts,services/linkChecker.ts,services/sanitizeText.ts}`, `apps/downloads/database/migration_019`, `apps/downloads/frontend/src/pages/gestao/*` (novo) | Link checker SSRF-safe (rejeita loopback/rede privada/link-local/metadado de nuvem, resolve DNS e revalida IP contra rebinding, não segue redirect sem revalidar hop) só sob demanda (`POST /admin/materials/:id/check-link`), sem job agendado (sem infra de scheduler). Sanitização de texto sem nova dependência (decisão nominal do mantenedor — strip de tags/entidades, sem jsdom/isomorphic-dompurify). Reforço de magic bytes no upload admin mesmo sem storage real (decisão nominal — contrato de validação implementado, storage real segue bloqueado, mesma raiz DEB-073-03). 12 rotas `/gestao/*` com sidebar de recursos agrupada (Conteúdo/Operação/Comunidade/Sistema) e contagem por fila; Mídias/Publicadores são placeholders sem dado real. **Achado fora do plano, resolvido na mesma sessão (2026-07-12):** campo de editora (`publisher_name` em `download_material_metadata`, migration_019) e relacionados por sistema/edição (`GET /materials/:slug` expõe `system_id`/`edition_id`/nomes resolvidos via `catalogClient`, ficha linka pro catálogo filtrado). 73 testes backend (+17) + 6 frontend verdes, lint/tsc limpos, `verify:api` exit 0 (downloads breaking=0 non-breaking=25, 2026-07-12). **3 débitos abertos** (sem infra externa disponível ainda): DEB-075-01 (link checker sem job agendado), DEB-075-02 (Mídias/Publicadores sem dado real), DEB-075-03 (storage real de evidência ainda bloqueado). | Nenhum commit feito (aguardando autorização nominal); spec 076 (infra/deploy) destravada |
| BL-076-INFRA-DEPLOY | aberto (local, F0-F4 implementados; F5-F6 fora de escopo desta rodada) | `specs/076-downloads-g-infra-deploy/`, branch `feat/070-downloads-schema-api` | `.github/deploy-manifest.json`, `apps/downloads/{Dockerfile×2,docker-compose.beta.yml,docker-compose.prod.yml,frontend/nginx.conf}` (novos), `apps/downloads/backend/src/services/moderationAuditLog.ts`, `docs/agents/{deploy-runbook.md,infra-map.md}` | Escopo autorizado nominalmente: só código local, zero write em VM/Cloudflare real. F0: T1 relido (infra-map/deploy-manifest/`_deploy-module.yml`/AGENTS.md vigentes). F1: entrada `downloads` adicionada ao manifesto compartilhado (só adição, nenhuma entrada existente tocada; mesmo padrão `mesas`, dispatch-only). F2: Dockerfiles + composes beta/prod criados do zero (app não tinha nenhum), DB isolado por ambiente, sintaxe validada (`docker compose config`). F3: confirmado que as 19 migrations já existentes têm os 5 campos de header, todas `online-safe`, zero DDL destrutivo. F4: runbook de bootstrap documentado (`deploy-runbook.md`), health check `GET /api/v1/health` já existia (070). **Ambos débitos fechados (2026-07-12):** DEB-076-02 (Cloudflare Tunnel `downloadsbeta.`/`downloads.` criado pelo mantenedor); DEB-076-01 (`moderationAuditLog.ts` — log JSON `[moderation-audit]` grepável, integrado em `moderation.ts`/`reports.ts`). Lint/tsc/test/build/`verify:api` todos verdes. | Nenhum commit feito (aguardando autorização nominal); F5 (smoke beta)/F6 (promoção+deploy prod) exigem aprovação nominal separada para write real de VM/Cloudflare — fora do pedido desta rodada |
| BL-087-DOWNLOADS-HOME-CATALOGO | ✅ implementada e deployada em beta (2026-07-26, PRs #214/#215, run 30222244974 success); 4 débitos residuais abertos (smoke visual, canonical, smoke local, prod) | `specs/087-downloads-home-catalogo-unificado/` (spec/plan/tasks), pedido direto do mantenedor 2026-07-26, pesquisa visual real (DMs Guild/DriveThruRPG/Estante Virtual/Universo dos Livros/MercadoRPG via browser), pesquisa de algoritmo de ranking (Wilson score/Bayesian average/Hacker News decay) | `apps/downloads/frontend` (`pages/HomePage.tsx` removido, `CatalogoPage.tsx` expandido com modo vitrine/resultado, `App.tsx`, `components/AppShell.tsx`, `MaterialCard.tsx`, novo `MaterialShelf.tsx`); `apps/downloads/backend/src/routes/materials.ts` (view_count, Bayesian average de rating/popularidade, corte de elegibilidade); `packages/ui/src/Header.tsx` (busca embutida — decisão confirmada, pacote compartilhado É tocado) | Home hoje é landing vazia separada de `/catalogo`; busca do Header não busca nada. Spec final: home = catálogo direto com 3 prateleiras horizontais roláveis curadas (Recém adicionados/Mais visitados/Mais bem avaliados), busca embutida no Header compartilhado (decisão explícita de escalonamento/reaproveitamento entre módulos), filtros como pills+popover no modo vitrine e sidebar completa no modo resultado. **Achado de débito real:** `download_metric_daily.view_count` nunca incrementado desde spec 070 — painel admin sempre mostrou `total_views=0`. Sistema de avaliação 5 estrelas já existe, só faltava agregação. **Popularidade/rating usam Bayesian average** (não constante fixa — 1ª proposta com `PESO` fixo foi rejeitada pelo mantenedor por não escalar) **+ corte de elegibilidade obrigatório**: material só visualizado (zero download) nunca aparece/escalona em "Mais visitados", regra de produto fixa não ajustável por constante. **3ª revisão independente (2026-07-27, fase-a-fase exaustiva) achou mais 3 bloqueantes + 4 importantes:** consumidor real `accounts` (SSO) faltava na lista de smoke do Header; `esferas`/`srd` listados como consumidores mas não têm frontend implementado (removidos); faltava task pra estender `materialSchema` Zod/`SORT_OPTIONS` do frontend com os campos novos (violaria normalização pétrea); canonical entre `/` e `/catalogo` levantado na spec mas sem task; risco de estrelas quebrarem alvo de clique único do card; `routes/materials.ts` não tem nenhum teste hoje (regressão silenciosa possível); navegação por teclado da prateleira horizontal não coberta. Todos corrigidos em `tasks.md`/`plan.md`. Zero linha de código escrita ainda. | **FECHADA 2026-07-26 com débito residual explicito.** Entregue e verificado em beta: home = catálogo (`/` e `/catalogo` no mesmo componente), busca embutida no `Header` compartilhado (contrato aditivo, consumidor não-migrado intacto), 3 prateleiras, pills+popover, sorts `trending`/`rating`, `view_count` gravado, Bayesian average nas duas métricas. **Corte de elegibilidade PROVADO com dado real em beta:** `sort=recent` total=103, `sort=popular` total=103, **`sort=trending` total=2** — 101 materiais com view e zero download excluidos da ordenação, não mandados pro fim. `sort=rating` total=0 com `avg_rating: null` (acervo sem avaliação; nada aparece como "0 estrelas"). Migration 029 aplicada pela esteira (delta 1, guard `MAX_AUTO_PENDING` com folga, zero intervenção manual por SSH). Validação: testes 42/216/310, lint 23/23, build 23/23, `verify:api` breaking=0. **Débito residual (NÃO entregue):** (1) smoke visual/viewport real T3.4/T4.1/T4.3 — sem browser no ambiente, decisão do mantenedor de deixar aberto; (2) **T4.4 canonical route-aware não existe no código** — bloqueada pela decisão pendente do alvo (`/` ou `/catalogo`), é critério de aceite da spec; (3) T5.3 smoke manual local; (4) T6.4 promoção pra prod — **produção segue sem esta spec**, exige `workflow_dispatch` nominal em `--ref main`. |
| BL-083-REJEICAO-MOTIVO-LEGAL-EMAIL | 🟢 fechado (PR #192 mergeada em `dev`, 2026-07-24) | `specs/083-downloads-rejeicao-motivo-legal-email/`, PR #192 (`520c254`, `553a348`, `162d297`, `60db578`) | `apps/downloads/backend/src/{routes/rejectionCategories.ts,routes/emailLog.ts,routes/moderation.ts,services/accountsClient.ts,services/moderationEmail.ts}`, `apps/downloads/database/migration_021_download_rejection_reason_email.sql`, `apps/accounts/src/{app.ts,users.ts}`, `packages/email/` (novo), `apps/downloads/frontend/src/{components/EmailLogPanel.tsx,hooks/useAdminRejectionCategories.ts,hooks/useAdminEmailLog.ts,pages/gestao/GestaoModeracaoPage.tsx}` | — | — |
| BL-056-MODERACAO-BATCH | 🟢 fechado | `specs/056-mesas-moderacao-batch-acoes/`, `sessoes/26-06-29_2_mesas_moderacao-batch.md` | `apps/mesas` (FE+BE) | Ações em lote na Moderação: checklist em Rascunhos (rejeitar selecionados/limpar todos) e Mensagens (ignorar selecionadas) + 2 endpoints batch (`PATCH …/messages/batch`, `…/drafts/batch`) + Logs de integração real (reusa `discord_import_runs` via `/metrics`). Lint+build+test+verify:api verdes. Sem migration. PR #107 mergeado em `dev` (3 commits, `a77270a8`, 2026-06-29). Reviews Codex+CodeRabbit resolvidos. Deploy beta + smoke + promote prod ✅ (mantenedor). SDD Completo. | — |
| BL-SITE-GATED | aberto | `specs/008`, `specs/011`, `project-state.md` | `apps/site`, `apps/site-admin` | Gate D site: validacao Opus/mantenedor, paridade editorial, E2E admin autenticado, midia real, SEO/redirects finais conforme fase | quebrar em fatias menores; nao tocar Gate C/WP raiz |
| BL-LINKS-MEDIA-038 | 🟢 fechado | `specs/038-links-group-media-report/`, `sessoes/26-06-21_1_links_deploy-038-media.md`, `specs/045-debitos-pendentes-deploy/` §3/§6 | `apps/links` (+ packages/media/ui, CI/cron, schema) | FECHADO 2026-06-24: T1-T11 mergeados (PR #78). T12 nav ✅. Logos 12/13 ✅. Report público ✅. Cron VM ✅ — configurado domingo 06:00 (crontab ubuntu), testado bem-sucedido. | — |
| BL-LINKS-NAV-CROSSAPP | fechado | `specs/038` R7/T12, `specs/045-debitos-pendentes-deploy/investigacao.md` §2 | `packages/ui/modules.ts` + site/glossario/mesas (prod) | FECHADO 2026-06-22 (spec 045, prod read-only): nav "WhatsApps"/`links.artificiorpg.com` servido em site (raiz, Astro), glossário e mesas (bundle JS). accounts **não tem** nav cross-app por design (`apps/accounts/frontend/src/main.tsx:3` importa só logo/tema do `@artificio/ui`, não Header). Medição original do backlog usou host errado (`site.` em vez da raiz) + grep de HTML em SPA (nav vem no JS) → falso-negativo. | — |
| BL-LINKS-GROUP-LOGOS | 🟢 fechado | `specs/038` Fatia A, `sessoes/26-06-21_1_links_deploy-038-media.md`, `specs/045-debitos-pendentes-deploy/investigacao.md` §3 | `apps/links` (groups.logo_url) | FECHADO 2026-06-24: **12/13 reidratados em prod**. "Canal de Notícias" (canal WhatsApp, `kind: channel`) sem logo é legítimo — canais não expõem og:image de avatar. Aceito como null. | — |
| BL-SITE-CMS-PARITY | aberto | `specs/011-site-cms-authoring/` | CMS/autoria | operacoes editoriais basicas, lixeira/restaurar/apagar, agendamento/autosave/revisoes, roles editoriais, dashboard/build status, redirects UI, auditoria | priorizar MVP editorial real antes de features novas |
| BL-LINKS-013 | fechado (mergeado dev 2026-06-20, deploy prod 2026-06-21) | `specs/013-links-regras-restore/`, D085, PR #74 (`5d83a6e`) | `apps/links` (app completo: Astro+Express+Kysely/PG+Cloudinary+SSO), `packages/ui` nav, `.github/deploy-manifest.json`, `.github/workflows/deploy.yml` | FECHADO: PR #74 mergeada em `dev` (2026-06-20). Deploy prod concluído 2026-06-21 (smoke 200). | — |
| BL-NAV-LINKS-014 | fechado (mergeado dev 2026-06-20, deploy prod 2026-06-21) | `specs/014-ui-nav-whatsapp/` | nav compartilhada `packages/ui` | FECHADO: item "WhatsApps" em `modules.ts` mergeado via PR #74. Deploy links prod 2026-06-21 confirma nav consumidores em prod. | — |
| BL-CLOUDINARY-SHARED | fechado | `specs/036-media-shared/`, `sessoes/26-06-20_4_media-shared.md`, `sessoes/prompt-BL-CLOUDINARY-SHARED.md`, espec 037 (commits `8258e4a`/`77179b7`) | `packages/media` (novo), `apps/links/server/lib/cloudinary.ts`, `apps/site/server/lib/media-store.ts`, `apps/site/importer/media.ts`, `apps/mesas/backend/src/discord/uploadDiscordImage.ts` | FECHADO 2026-06-20: `@artificio/media` criado com 5 funções atômicas. 3 consumidores migrados. Commitado via spec 037. | — |
| BL-MESAS-EXPRESS5-016 | fechado | `specs/016-mesas-express5/`, E004, `specs/033-infra-toolchain-update/` | `apps/mesas/backend` | FECHADO 2026-06-20: mesas backend migrado para Express `^5.2.1` na spec 033 (T28, junto com jest→vitest). Monorepo 100% Express 5. | — |
| BL-MESAS-TEST-DB-SIDEEFFECT | fechado (raiz, Option 2) | `specs/033-infra-toolchain-update/` T28/T28d, `sessoes/26-06-18_3`, D078 | `apps/mesas/backend/src/db/index.ts` | FECHADO 2026-06-19 pela correção de raiz: T28d trocou `db/index.ts` por Proxy lazy, eliminando o side-effect `process.exit(1)` no módulo. Option 1 (`jest.setup.ts`+setupFiles) foi revertida (saiu com o jest na migração vitest T28c). `ingestMessages.test.ts` passa sem injeção de env | — |
| BL-MESAS-DB-LAZY-OPTION2 | fechado | `specs/033-infra-toolchain-update/` T28d, `sessoes/26-06-18_3`, D078, commit `85063da`, run `27805626853` | `apps/mesas/backend/src/db/index.ts` | FECHADO 2026-06-19: `db/index.ts` virou Proxy lazy (`getDb()` igual `prodDb`, `Reflect.get` simétrico); side-effect eliminado; `process.exit`→`throw` (review); `jest.setup.ts` removido. `ingestMessages.test.ts` passa com `DATABASE_URL=''`; DT-004 preservada (server.ts boot + throw no 1º uso). Deploy beta verde + smoke OK | — |
| **BL-MESAS-INBOX-047** | ✅ deployado em beta/prod (PRs #87/#88; spec fechada 2026-06-23) | `specs/047-mesas-inbox-importacao/` | `apps/mesas` (backend + frontend) | Backend (3 novas rotas + 16 testes) + frontend completo (types, inboxApi, TextPasteArea, InboxPanel, InboxDraftReviewTable, DiscordDraftPreview/ReviewTable generalizados, aba Inbox no GestaoPage). Lint 15/15, build 17/17, backend 21/178 ✅, frontend 4/19 ✅. | Com autorizações separadas: commit → push → PR → merge → deploy beta → smoke E2E T1.13-T1.16. |
| **BL-MESAS-DISCORD-EXPORTER-048** | ✅ MVP deployado em beta/prod (PRs #99/#101); Fase G local | `specs/048-mesas-discord-chat-exporter-json/` | `apps/mesas` | Fase G completa (T-G1..G8) **não commitada**. Pendências: DEB-048-25 (UX unificada), DEB-048-26 (contato=autor), DEB-048-27 (descartar sistema próprio). Smoke beta real pendente (DEB-048-23 fix em branch separada). | Commit/push/PR da Fase G → merge → deploy beta → smoke → promote prod (aprovações nominais). DEB-048-25/26/27 → próximos PRs. IA real (T-G5/T-G7 ativos) → spec 052 futura. |
| **BL-MESAS-AUTOMACAO-INTELIGENTE-052** | ✅ deployado em beta/prod (PRs #116/#117); DEB-052-01/02 abertos | `specs/052-mesas-automacao-inteligente/`, `sessoes/26-07-10_1_mesas_parser-importacao-4-bugs.md` §DEB-052-02 implementado | `apps/mesas` + infra VM | Spec 052 iniciada 2026-06-29. Implementado local: Bloco A TA1/TA2/TA4/TA5/TA6/TA7; Bloco B com IA apenas como sugestão revisável (`_ai_suggestions`), minimização de prompt, eval offline, shadow report e kill switch; Bloco C R15-R20 determinístico (mentions/evidências/preço/autoral/autoParse opcional). Auto-aprovação real segue bloqueada por gate nominal. Testes/backend/lint/verify:api verdes. Sem VM write/cron/segredo/deploy. **D087 (2026-06-30):** provider DeepSeek + learning-store determinístico (migration_133 `discord_field_learning`, lookup token-zero antes da IA) implementado local — cobre só `'value'` (correção de valor errado). **DEB-052-01 aberto:** `cleanLabelLine` perde labels decorados (`»`/emoji + bug de ordem `**`) — evidência em JSONs reais (100% drafts disparam IA; labels colapsam em template estável: slots ~138, dia/hora ~61). Fix = TC.7+TC.8 (parser, não-IA). **DEB-052-02 implementado (2026-07-10, autorização explícita "corrija os bugs, inclusive o DEB-052-02 é exatamente essa lacuna"):** `discord_learning_rules` já tinha `rule_type: 'label_alias'` no schema (migration 138) nunca usado — implementado `recordLabelAliasFromCorrection` (grava rótulo→campo quando correção humana resolve campo vazio e `raw_text` tem linha `rótulo: valor` batendo) + `loadActiveLabelAliases` (lookup por escopo, consumido em `parseDiscordMessage` antes de cada parse) + `parseDiscordAnnouncement` ganhou parâmetro `labelAliases` somado à allowlist fixa em título/sistema. Sem hardcode novo pra rótulo futuro — aprende da curadoria manual real. Validado: build tsc limpo, 434/434 testes backend (+7 novos cobrindo grava/não-grava/carrega/parser-com-e-sem-alias). Escopo desta rodada: só campos `title`/`system_name` recebem aliases dinâmicos (onde o bug apareceu); outros campos de label são extensão futura trivial (mesmo padrão). | TA3/TA8 exigem VM/deploy/aprovação nominal. Auto-aprovação real exige gate nominal futuro. **Débito residual:** extensão de `labelAliases` pros demais campos de label (`day_of_week`, `contact_url` etc.) não foi feita — só onde o bug reportado exigiu. Sem migration nova necessária (schema já suportava `label_alias`). |
| BL-MESAS-PARSER-DRAFT-PATCH-422 | aberto (investigação incompleta) | `sessoes/26-07-10_1_mesas_parser-importacao-4-bugs.md` §Bug 8 | `apps/mesas` backend (`routes/discord/drafts.ts:334`, `routes/discord/utils.ts:993` `handlePatchDraft`) | 422 real em produção no `PATCH /admin/discord/drafts/:id` (handleSaveFields, "Salvar campos"). Print de console só trouxe o stack genérico do fetch, sem o corpo `{error, details}` da resposta — suspeita de ser mesmo padrão dos bugs 6/7 (missing_fields bloqueando `ready`) em campo ainda não coberto, mas não confirmado. | Pedir print da aba Network (corpo da resposta 422) na próxima ocorrência, ou reproduzir localmente com o mesmo draft_id. |
| **BL-057-GESTAO-REDESIGN** | 🟢 fechado (Fase 9 smoke/promote validados pelo mantenedor, 2026-07-09) | `specs/057-mesas-gestao-redesign/`, `sessoes/26-06-30_1_mesas_gestao-redesign.md` | `apps/mesas` (frontend admin + backend + contrato API admin) | Redesign completo de `/gestao` (hoje cara de gambiarra; 049/054/056 melhoraram em partes mas não ficou usável). Decisões mantenedor 2026-06-30: sidebar do zero (IA estilo Cloudflare), Mesas central = fila unificada de drafts (`discord/drafts`+`import/drafts`), Dashboard enxuto, Bot de Discord (Config/Importação/Relatórios), a11y teclado fora de escopo. 7 débitos (DEB-057-01..07; 5 iniciais + 2 na investigação extra de notificações/ChatExporter) + 10 herdados da 052 (DEB-057-08..17; D1-D10 do CodeRabbit no PR #120). | Fases 0–7 ✅ (local). **Fase 6 fechada:** T2.5 `POST /admin/tables/batch`+bulk UI; T6.3 delta `GET /profiles/:id/delta`; T6.4 relatórios com filtro/ordenação; T6.7 wizard 3 passos; T6.8 "Ver novidades"; T6.9 runner `runDiscordChatExporterSchedule` + tick no `cronRunner` (código; `isProfileDue` testada). Validação: backend 332 tests, frontend 148, lint+build+verify:api (mesas +6 non-breaking, exit 0) ✅. **Gated:** runtime real (bot token/CLI/`mesas-cron` beta) = smoke T9.2; deploy Dockerfile DCE; sem commit. **Fase 8 ✅:** `import/metrics` removido (órfão+redundante); `import-json/reparse` mantido (não redundante — batch de mensagens cruas); `api:bundle`+`verify:api` exit 0 (breaking=1 intencional). Falta só **Fase 9** (T9.2 smoke beta auth-gated + T9.3 promote gated). |
| **BL-060-GESTAO-MESAS-IMPORTADAS** | 🟢 fechado (T1-T9 concluidas, validado pelo mantenedor 2026-07-09) | `specs/060-mesas-gestao-mesas-importadas/` | `apps/mesas` backend admin + `ConteudoSection.tsx` | `TableRepository.findById` sem gm scope + `GET/GET:id /api/v1/admin/tables` (lista/detalhe sem scope de GM) + gestão admin lista mesas em qualquer status com filtro + guard removido do publish a partir de draft. Lint/build verdes, smoke completo (sync draft → gestão → publicar → catálogo público) validados pelo mantenedor. | — |
| **BL-058-PARSER-LEARNING-DEEPSEEK** | 🟢 fechado (todos os débitos DEB-058-01..08 corrigidos, commit/deploy/promote validados pelo mantenedor, 2026-07-09) | `specs/058-mesas-parser-learning-deepseek/`, `sessoes/26-07-01_1_mesas_parser-learning-spec.md` | `apps/mesas` importador/parser/gestão | Fases 1-5 implementadas: parse/feedback, duplicatas (rota+UI Fase 5 T5.1-T5.3), learning rules e DeepSeek ContextPack com `discord_llm_decisions`, cache por hash/model/prompt, auditoria, prompt anti-injection e schema de resposta validado. Fix scanner `scripts/api/inventory.ts` (achado Codex PR #124). Fix auto-parse `apps/mesas/backend/src/routes/discord/import.ts` (achado próprio): `POST /import-json` e `/import-json/reparse` truncavam calado acima de 500 mensagens; agora em lotes com `truncated:true`. Fix CodeRabbit PR #124 (teto `messageIds` em `/reparse`, `MAX_REPARSE_MESSAGE_IDS=2000`). **DEB-058-BUILD-01 fechado:** fixture `chatExporterImportService.test.ts` corrigida (campos `reactions`/`mentions`/`inlineEmojis`) durante resolução de merge conflict com `origin/dev`. | **DEB-058-02 (achado 2026-07-02 pós-deploy beta, FRONTEND CORRIGIDO local — falta deploy):** já reportado em spec 057/T8.1 (`import-json/reparse` mapeado como órfão de frontend, mantido por não ser redundante, mas o wiring de UI nunca foi feito). 50 mensagens presas em `pending` no mesas-beta-db desde 2026-07-01 21:02 UTC (corrida provável entre import e restart de deploy) evidenciaram o buraco: reimport com hash igual zera `inserted`/`updated`, `autoParsePendingImportedMessages` nem roda, e não havia botão de recuperação. Corrigido: `discordSyncApi.reparsePending()` (+ `reparseResultSchema` zod) e botão "Reparse pendentes" em `DiscordJsonImportPanel.tsx`, chamando `POST /import-json/reparse` sem `messageIds`. `tsc --noEmit` limpo, lint limpo, `discordSyncApi.test.ts`+`DiscordJsonImportPanel.test.tsx` 43/43 verdes, `import.test.ts` (backend) 3/3 verde, `pnpm verify:api` exit 0. **RESOLVIDO 2026-07-02:** PR #125 (`fix/mesas-import-reparse-pending`) commitado/pushado/mergeado em `dev` (merge commit `498d5a5`), todos checks verdes. Incluiu nitpick CodeRabbit da PR anterior: `reparseResultSchema` (5 contadores) migrado de `z.number()` puro p/ `z.number().int().nonnegative()`, alinhado ao `batchResultSchema` (rejeita fracionário/negativo do backend antes da UI). Também regenerado `docs/api/generated/api-diff.generated.md`: `dev` local estava stale vs `origin/dev`, e `diff-api.ts:86` usa ref `dev` local antes de `origin/dev` → gerava tabela fantasma de 3 rotas mesas sem path/method; fast-forward do ref local (`git update-ref refs/heads/dev origin/dev`) + regen deu "nenhuma mudança". Deploy beta disparado (`deploy.yml` module=mesas env=beta, run 28625933373). **Falta:** rodar o botão "Reparse pendentes" manualmente em beta pós-deploy p/ zerar as 50 mensagens `pending` travadas (dado órfão sobrevive ao deploy — 1 clique manual). **DEB-058-03 (SDD Lite, 2026-07-02, achado do mantenedor via screenshot, CORRIGIDO local — falta deploy):** 3 bugs reportados em `/gestao/importacao` + 1 achado próprio durante o fix. (1) Capa sempre quebrada mesmo em JSON recém extraído — `cover_url_source` (URL Discord CDN assinada, expira em minutos) era usada como fallback de `<img src>` em `useDraftForm.ts` e `DiscordDraftReviewTable.tsx` quando o upload real pro Cloudinary (`persistCoverUpload`/`uploadCoverForDraft`) falha silenciosamente; agora só `cover_url` confirmado renderiza, senão mostra "sem capa". (2) Título com `#`/decoração markdown Discord (`**`, `-#`, `▬»━─`, emoji) sobrevivendo à extração — nova `stripDecorativeMarkup()` em `parseDiscordAnnouncement.ts`, plugada em `normalizeTitle()`/`splitThreadName()`, roda pré-parse com whitelist `letra+número+acento+espaço` + pontuação presa a palavra (apóstrofo/hífen/`&`/`:`, decisão nominal do mantenedor). (3) Filtro funcional de mesa paga — mecanismo de detecção (`price_type`) já existia mas nunca foi ligado a um filtro real; novo checkbox "Incluir mesas pagas na importação" (default `false`, decisão nominal) em `DiscordJsonImportPanel.tsx`, propagado via `acceptPaidTables` até `processDiscordMessageToDraft()` nas 3 rotas de import (`/`, `/file`, `/reparse`), sem migration nova. (4) Achado próprio: minha 1ª versão de `stripDecorativeMarkup` colou caractere zero-width **literal** no regex-source (corrompeu leitura do arquivo pro `grep`, tratado como binário) e o `\s+` collapse destruía a quebra de linha que `getAnnouncementSystemHint` depende pra isolar a 1ª linha do campo "Sistema:" — quebrou o teste `NÃO descarta sistema conhecido (D&D)`. Corrigido com `\u` escapes explícitos + collapse que preserva `\n`. Validação: backend 42 files/368 tests ✅, frontend 14 files/149 tests ✅, `pnpm run lint`/`build`/`test` repo-wide ✅ (24/24), `pnpm verify:api` exit 0 (sem breaking change). **Correção pós-feedback (2026-07-02):** mantenedor apontou que a limpeza inicial era rasa — separadores reais do Discord (`▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬`, `━━━━`, `═══`) aparecem em blocos de repetição (linha inteira do mesmo char), não como pontuação solta, e a limpeza não cobria description nem o texto de `discord_parse_cases`. Nova `stripSeparatorLines()` (4 regras: linha 100% decorativa, marcador solto no início de linha-de-campo, bloco de 4+ repetições em qualquer posição, glifos puramente gráficos em qualquer quantidade) aplicada na montagem do `body` ANTES de qualquer extração de campo; nova `cleanDescriptionText()` separada (remove mentions `<@id>` crus + markdown de ênfase) roda só no `description` final, DEPOIS que host/contato/evidências já extraíram os mentions do body original (1ª tentativa misturou tudo numa função só e quebrou 5 testes de extração de host/contato — corrigido separando responsabilidades). `normalizeParseLearningText()` (`parseLearning.ts`) também usa `stripSeparatorLines()` antes do collapse de espaço. Validado iterativamente contra os 50 casos reais de `D:\teste.json` até zero ocorrência residual. Sessão `26-07-01_1_mesas_parser-learning-spec.md` §"Retomada — SDD Lite" + §"Correção pós-feedback". Código local, sem commit/push/deploy. **DEB-058-04 (achado do mantenedor 2026-07-08, prod, CORRIGIDO parcial — dado + defesa de entrada; causa raiz do writer original NÃO identificada):** 3 drafts em prod (`0fed9e03`, `b80161f2`, `b93a455d`) com campos de `normalized_payload.table` gravados como `{before, after}` em vez de valor escalar (`description`, `type`, `price_type`, `slots_total`, `requires_pc`, `requires_camera`, `session_zero_free`, `frequency`, `requires_microphone`, `contact_url`, até `_ai_suggestions` aninhado) — sync trava com 422 "campo faltando" mesmo com valor visível e preenchido na tela do editor. Rastreei TODO caminho de escrita ativo hoje (`handleSaveFields`/`buildUpdatedPayload`, `handleConfirmSlots`, `registerDraftCorrection`, `enrichDraftWithLlm`/`attachAiSuggestions`, `upsertDraftWithShadow`, `normalizeDiscordTableDraft`, parser determinístico) e nenhum produz esse shape hoje — todos gravam escalar limpo ou fazem coerção segura (`table.requires_pc === true`). Raiz provável: bug já corrigido em commit anterior que deixou resíduo — `discord_field_learning`/`discord_learning_rules` (`output_value` JSONB) tinham 9+7 linhas com o mesmo shape poluído, incluindo `input_token='[object object]'` (prova de `String(objeto)` em `normalizeToken`), confirmando que a poluição entrou pelo learning-store em algum momento passado e se autoperpetuou via `registerDraftCorrection`→`recordFieldLearning` sempre que um draft já poluído era corrigido de novo. **Corrigido:** (1) dado — 3 drafts + 9 linhas `discord_field_learning` + 7 linhas `discord_learning_rules` normalizados/purgados em prod (backup salvo antes de cada UPDATE); (2) defesa de entrada — `registerDraftCorrection` (`apps/mesas/backend/src/routes/discord/utils.ts`) agora rejeita com 400 qualquer `corrections[campo]` no formato `{before,after}` antes de persistir; `applySuggestion` (`apps/mesas/frontend/src/features/discord-sync/useDraftForm.ts`) desembrulha sugestão poluída (`.after`) antes de aplicar ao form, blindando contra resíduo remanescente de sugestões antigas. **Não corrigido:** o writer histórico exato que gravou o shape errado originalmente não foi identificado — código atual não reproduz mais o bug, mas sem repro não há certeza de que não existe outro caminho ainda não mapeado (ex.: chat-exporter, rota de import alternativa). | Se o padrão reaparecer em novo draft com código pós-fix, é sinal de writer ainda ativo não mapeado — investigar `chatExporterImportService.ts`/rotas de import batch como próxima hipótese. Sweep periódico (query de `jsonb_each` procurando `{before,after}` em `table`) até confirmar ausência de recorrência por 2+ semanas. **DEB-058-05 (achado do mantenedor 2026-07-08, mesma sessão, CORRIGIDO local):** causa raiz real do 422 "type, price_type" do print mais recente — `validateForm`/`buildMissingFields` (`apps/mesas/frontend/src/features/discord-sync/draftFormUtils.ts`) checavam só `.trim()` truthy em `type`/`modality`/`price_type`, nunca pertencimento ao enum. `validateDraftForSync` no backend (`syncHelpers.ts:137-139`) exige `VALID_TABLE_TYPES.includes(t.type)`/`VALID_MODALITIES.includes(t.modality)`/`VALID_PRICE_TYPES.includes(t.price_type)`. Draft com valor fora da whitelist (correção manual, resíduo de parser antigo, etc) passava no frontend como "Pronto" e só travava no sync com 422 sem indicar o valor problemático. Fix: 3 whitelists (`VALID_TABLE_TYPES`/`VALID_MODALITIES`/`VALID_PRICE_TYPES`) espelhando o backend adicionadas em `draftFormUtils.ts`, `validateForm` e `buildMissingFields` agora usam `.includes()` em vez de `.trim()`. Auditoria completa pediu pelo mantenedor achou mais 2 furos da mesma classe no mesmo arquivo: `day_of_week` (front só truthy, backend `isDayOfWeek` exige `VALID_DAYS.includes()`) e `start_time` (input de texto livre sem `type="time"`, front só truthy, backend `isValidTime` exige regex `HH:MM` estrito) — ambos corrigidos com `VALID_DAYS`/`isValidTimeString()` espelhando o backend, aplicados em `validateForm` e `buildMissingFields`. Campos auditados e confirmados OK: `age_rating`/`experience_level`/`table_level` (select fechado, backend não valida enum, sem risco), `frequency` (backend tem fallback silencioso pra `'semanal'`, não trava sync), `system_id`/`contact_url`/`contact_discord`/`slots_total`/`slots_open`/`description`/`title` (sem enum, validação já compatível). `tsc --noEmit`, `pnpm --filter @artificio/mesas-frontend lint` e 133/133 testes de `discord-sync` (10 suites) verdes. Draft `b93a455d` checado em prod (read-only): `type`/`price_type` já válidos agora (`campanha`/`gratuita`) — não reproduz mais nesse draft específico, mas a divergência estrutural front/back existia e vai morder em qualquer draft futuro com valor fora do enum. Sem commit/push. **DEB-058-06 (achado 2026-07-08, RESOLVIDO — PR #135 mergeada, deploy beta + promote main ok):** causa raiz estrutural dos 422 recorrentes — 3 validadores independentes de "draft completo" nunca unificados: parse-time (`normalizeDiscordTableDraft.getMissingFields`, o mais fraco: truthy-only em type/modality/price_type, nunca checava day_of_week/start_time), frontend (`validateForm`, só roda com editor aberto) e sync-time (`validateDraftForSync`, o gate real). Draft nascia `ready` do parse e estourava 422 no botão "Sincronizar drafts prontos" (synca da lista sem abrir editor). Parse agora delega pra `validateDraftForSync` (assinatura estreitada `Pick<ImportTableDraft, 'table'>`). Bônus: `hasPositiveNumber` (v>0) rejeitava `slots_total:0` (mesa fechada, spec 017) — virou `hasNonNegativeNumber` (v>=0). Também: `SystemSuggestionModal` plugado no editor de draft (+ botão de recarregar catálogo, cache 5min bypassado; nome do sistema criado aplicado direto do retorno do modal), opção "Nenhuma" sempre visível em VTT/comunicação. **DEB-058-07 (achado do mantenedor 2026-07-08, 500 no sync em prod, CORRIGIDO local — falta commit/deploy):** `invalid input value for enum age_rating: "18+"` — enum Postgres real é `+18` (sinal ANTES, conferido em prod via `pg_enum` e `StepConfig.tsx` do form principal), mas o pipeline Discord inteiro usava `18+` invertido: `db/types.ts:238` (tipo Kysely errado — por isso compilava), `discord/types.ts` (`TableDraftAgeRating`), `parseDiscordAnnouncement.ts:681-685` (`extractAgeRating`), frontend `DraftAgeRating` + options do select do editor. Corrigidos todos + normalizador de legado dos dois lados (`normalizeAgeRating` no `buildTableDraftFields` do sync; `normalizeLegacyAgeRating` no `buildForm` do editor) pra drafts antigos já persistidos com `18+` não estourarem nem renderizarem select vazio. Doc legado `apps/mesas/docs/legacy/ERRORS_SOLUTIONS.md:242-251` documenta o formato errado — fonte provável da dessincronia ("sincronia manual" falhou). Na mesma sessão: candidatos da auditoria de completude eram só texto sem ação (achado do mantenedor) — botão "Aplicar" por candidato + "Aplicar todos" via `mapAuditCandidateToForm`, badge "manual" pra campo sem correspondente no form (`_notes`, `end_time`, `slots_filled`). Validação: tsc backend+frontend limpos, 267 testes backend + 133 frontend verdes, eslint limpo. **DEB-058-08 (auditoria completa pedida pelo mantenedor 2026-07-08, RESOLVIDO local):** varredura de TODO enum/CHECK real do Postgres (`pg_enum`+`\d tables`+`\d table_contacts`+`\d table_schedules` na VM) contra todos os tipos TS do pipeline Discord (backend+frontend). Achado 1 (corrigido): CHECK `tables_announcer_requires_name` exige `actual_gm_name` não-nulo com >=2 chars — `getGmName` (Discord: `author_name`; inbox: `adminDisplayName`) nunca era validado antes do insert, 500 direto do Postgres se vazio/curto. Check adicionado em `syncDraftToTable` (`syncHelpers.ts`) antes do insert, vira 422 `gm_name` (cobre Discord e inbox, função compartilhada). Achado 2 (corrigido): CHECK `check_slots_valid`/`slots_filled_valid` (`slots_open<=slots_total`, `slots_filled<=slots_total`, ambos >=0) — parser pode extrair vagas ambíguas onde open>total; `normalizeSlots()` clampa os 3 valores antes do insert em vez de confiar direto no dado extraído. Demais enums (`table_type`/`table_modality`/`price_type`/`price_frequency`/`experience_level`/`table_level`/`table_contacts.channel`/`day_of_week`/`publisher_role`/`table_audience`/`table_origin`/`table_status`) conferidos e batem exato — sem outro desvio de formato. Validação: tsc limpo, 325 testes backend verdes. |
| **BL-MESAS-GESTAO-049** | ✅ deployado em beta/prod (PR #93 mergeado) | `specs/049-mesas-revisao-gestao/`, branch `feat/049-revisao-gestao` | `apps/mesas` (backend + frontend), docs da spec | Spec fechada 2026-06-24. Fases A-D + E (TE1-TE22 + D09 + D10, 5 commits, 76 arquivos modificados, adminDiscordSync 1278→29, adminImportInbox 573→29, frontend 543→308 e 356→115, +2 hooks +5 componentes, +40 testes backend, +146 testes frontend) + Fase F parcial. REV-001 a REV-038 + D09 + D10 implementados (D01-D18 resolvidos). Débitos abertos: D06 (8 P1). **Código não mergeado em `dev`.** | Commit/push/PR para `dev` (autorização pendente). |
| BL-MESAS-PARSER-4BUGS-062 | 🟢 6/7 corrigidos + DEB-052-02 implementado; [PR #144](https://github.com/FarenRavirar/artificio/pull/144) mergeada e deployada em beta+prod (2026-07-10); bug 3 aberto (sem evidência); bug de correction-tracking achado pós-deploy corrigido, ainda não commitado | `sessoes/26-07-10_1_mesas_parser-importacao-4-bugs.md`, spec 058 (fechada) T11.10, spec 062 tasks.md I6, [PR #144](https://github.com/FarenRavirar/artificio/pull/144) | `apps/mesas` backend (`parseDiscordAnnouncement.ts`, `learningRules.ts`, `routes/discord/utils.ts`) + frontend (`useDraftForm.ts`, `ModeracaoSection.tsx`) | 7 bugs reportados pelo mantenedor com screenshots reais, 6 corrigidos: (1) `applySystemNameSuggestion` nova resolve `system_id` por nome no catálogo antes de aplicar sugestão learning; (2) `normalizeTitleCapitalization` nova normaliza CAPS LOCK por palavra, aplicada em `normalizeTitle` e `splitThreadName`; (3) **não corrigido** — diagnóstico original refutado, sem causa raiz confirmada, fica aberto; (4) `LINE_PREFIX_MARKDOWN_RE` nova remove heading/blockquote residual em `cleanDescriptionText`; (5) label composto de sistema resolvido de forma definitiva via DEB-052-02 (ver acima), não mais hardcode; (6) `FALLBACK_DESCRIPTION_KNOWN_LABEL_KEYS` (allowlist positiva) em `buildFallbackDescription`; (7) `isSuspiciousUrl` reescrita pra validar forma de URL. CodeRabbit no PR #144 apontou regressão real no fix do bug 7, corrigida no mesmo PR via `extractContactUrl` com flag `confident`. PR #144 passou por 4 levas de review (CodeRabbit x2, Sonar x2), mergeada em `dev` e promovida a `main` com deploy prod de `mesas` confirmado (`deploy=true`, run 29115788124) em 2026-07-10. **Bug pós-deploy achado pelo mantenedor (2026-07-10):** `ModeracaoSection.tsx` (`handleBeforeSync`, DEB-054-03) mandava `corrections` no shape `{before,after}` por campo pro backend, que rejeita esse shape (`isDiffShapedObject` em `routes/discord/utils.ts`) — todo sync de draft disparava erro no console e a correção manual nunca era registrada no learning-store (best-effort, não travava o sync, mas perdia o aprendizado silenciosamente). Bug pré-existente à sessão desta PR, só ficou visível quando o mantenedor testou sync em prod. Corrigido extraindo só `after` de cada campo do diff antes de montar `corrections`. Validado local: build+lint verdes; ainda **não commitado**. **Também reportado pelo mantenedor, não é bug:** fluxo de sugestão de sistema no editor de draft (`SystemSuggestionModal.tsx`) não permite cadastrar aliases/URL/logo/edição-variante encadeada direto do draft, forçando ida ao `site-admin` depois — é gap de UX, documentado como detalhamento da task I6 em `specs/062-sistemas-catalogo-canonico/tasks.md` (duas rotas possíveis: ampliar campos do modal reusando a API central já existente, ou extrair pacote de UI compartilhado — decisão adiada pra quando a task for priorizada). | Commitar/pushar fix do `ModeracaoSection.tsx` quando autorizado. Bug 3: se reaparecer, capturar print do estado ANTES de qualquer "Aplicar" no editor pra diferenciar sugestão pendente de bug real. I6: priorizar rota 1 (campos no modal do mesas) quando a task entrar em execução. |
| BL-FEEDBACK-MESAS-ANTIDRIFT | aberto | `specs/021-feedback-site-glossario/` notas FR-010 | `apps/mesas`, `packages/ui/feedback` | decidir se mesas deve consumir `@artificio/ui/feedback` para reduzir drift visual/comportamental | abrir fatia pequena quando mexer em feedback do mesas |
| BL-FEEDBACK-MERGE | futuro | `specs/021-feedback-site-glossario/` fora de escopo | feedback site/glossario | unificar/mesclar feedbacks entre projetos, se o produto pedir uma fila central | adiar ate haver dor operacional real |
| D-FEEDBACK1 | validacao | `specs/021-feedback-site-glossario/`, `project-state.md` | site + glossario | glossario foi para prod; site fica vivo em beta enquanto Gate C mantem WP na raiz; falta validacao admin/beta quando o site for retomado | validar admin/beta quando retomar site; nao acionar prod raiz antes do Gate C |
| D-SYNC1 | aberto | `sessoes/26-06-12_2_debitos_ux-marca.md` | mesas + glossario + bancos | sync inteligente sistemas/cenarios cross-app com merge, backup, dry-run | criar SDD Completo proprio antes de qualquer SQL |
| D-SITE-REQUIREADMIN | fechado | `sessoes/26-06-20_2_links_whatsapp-013-014.md` C2 | `apps/site/server/server.ts:39` | `session?.user.role !== "admin"` → corrigido: `if (!session || session.user.role !== "admin")`. Idêntico ao fix do links. `tsc` sem erros novos. Build Astro com erro pré-existente Vite/Tailwind (não relacionado). | — |
| BL-ROOTLESS-CONTAINERS | fechado | `sessoes/prompt-BL-ROOTLESS-CONTAINERS.md` C3, commits diversos (especs 037/038/041/042) | 4 apps: site, accounts, mesas, glossario (links ✅) | FECHADO: 5/5 Dockerfiles com `RUN chown -R node:node /repo && USER node`. Commitado em lotes via PRs #74, #76, #80, #83. Containers rodam como `node` em prod (links/mesas/glossario/accounts/site). | — |
| D-SITE-ADVISORY-LOCK | fechado | `sessoes/26-06-20_4_site_advisory-lock.md` | `apps/site/db/{connection,migrate}.ts` | Interface `Db` estendida com `getClient()` (conexão dedicada `pool.connect()` no pg, noop no pglite). `migrate.ts` usa `lockClient` p/ lock+unlock na mesma sessão PG. `tsc --noEmit` + build Astro verdes. | — |
| BL-MESAS-DRAFT-7ACHADOS | 🟢 fechado — PR [#156](https://github.com/FarenRavirar/artificio/pull/156) merged em `dev` (squash, 2026-07-13) + deploy beta de `mesas` verde (run [29280604577](https://github.com/FarenRavirar/artificio/actions/runs/29280604577)) | `sessoes/26-07-13_1_mesas_draft-importado-7-achados.md` | `apps/mesas` backend (`services/catalogClient.ts`, `discord/syncHelpers.ts`, `discord/parseDiscordAnnouncement.ts`, `routes/inbox/utils.ts`) + frontend (`DraftEditorTab.tsx`, `draftFormUtils.ts`, `useDraftForm.ts`) | 7 bugs reportados pelo mantenedor via prints no draft de mesa importada do Discord. Mantenedor rejeitou deixar 2 como débito ("tem que corrigir todos") e forneceu `D:/teste.json` (100 mensagens Discord reais) como evidência pro achado 6. **Todos corrigidos, lint+build+verify:api verdes, teste 449 backend (+11) + 172 frontend (+2):** (1) badge de sistema mostrava só nome do nó folha ("7e" em vez de "Call of Cthulhu 7ª Edição") — nova `composeSystemDisplayName` em `catalogClient.ts` sobe `parent_id` e compõe nome completo; (2) draft vinculado não permitia trocar sistema — `showEmptySearchResults={Boolean(form.system_id)}` no `SystemPicker`; (3) ressync de mesa PUBLICADA resetava pra `draft` — `syncHelpers.ts` lê `status` atual antes do UPDATE, só força `draft` se já estava nesse estado; (4) draft sem opção "a definir" pra dia/hora — implementado fim a fim em 3 camadas: UI+tipo (`to_define` no enum, checkbox de horário em `DraftEditorTab.tsx`), schema do PATCH (`inbox/utils.ts`), e sync (`syncHelpers.ts`: `validateDraftForSync` não bloqueia mais, `extractSchedules` não cria linha em `table_schedules` quando "a definir" — coluna é NOT NULL, mesmo design do form manual —, `buildTableDraftFields` agora popula `tables.schedule_day_status`/`schedule_time_status`/`schedule_day_hint`/`schedule_time_hint`, migration_124 que já existia mas nunca era escrita pelo draft); (5) PATCH 400 com frequência "outra" — decisão do mantenedor: form manual (`SessionRepeater.tsx`) nunca teve essa opção, então foi **removida** do draft em vez de mapeada pra null, draft agora espelha exatamente o enum real (`semanal/quinzenal/mensal/avulsa`); (6) parser não perguntava "Como interpretar N/M?" pra vários casos reais (só "8/10" funcionava) — testado com `extractSlots` contra as 100 mensagens reais, achadas 3 causas: `RE_SLOT_AMBIG_SLASH` não reconhecia label composto ("Vagas Disponíveis: N/M"), `RE_SLOT_OPEN`/`RE_SLOT_TOTAL` (rodam antes na cascata) resolviam silenciosamente ignorando o "/M", e `BULLETS` não reconhecia bullet emoji/símbolo (📌, »)  — 3 fixes de regex, 8/9 casos reais de ambiguidade agora disparam certo; **regra de interpretação também corrigida** por pedido do mantenedor (maior=total, menor=disponível, independente da ordem no texto — `useDraftForm.ts` usava `first` cru, agora usa `min`/`max`); (7) parser corrompia título com URL do CDN quando heading+link de anexo na mesma linha — `splitThreadName` detecta URL inline e busca separador `:` só antes dela. Bot review (2026-07-13) na PR #156 achou 4 problemas reais no diff, corrigidos no mesmo PR: URL só a 1ª removida em `splitThreadName` (agora remove todas via regex global); `existingTable.status` lido fora da transação e reaplicado dentro, condição de corrida com transição concorrente (agora `CASE WHEN status='draft'` direto no SQL do UPDATE); `composeSystemDisplayName` sem guarda de ciclo (agora com `visited` set); `<label>` aninhado em `<label>` no campo de horário (HTML inválido, agora `<div>` externo). PR mergeada e deploy beta de `mesas` concluído com sucesso. | Smoke manual em beta (importar mesa via Discord sync, testar draft com sistema vinculado, frequência, dia/horário "a definir", vagas ambíguas) — não bloqueante, pode ficar pro mantenedor validar quando quiser. |

## P1 — UI / Design System

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| BL-UI-B3-HEADERACTION | fechado | `specs/020` B3, `sessoes/26-06-15_5_ui-020-b3-b4-primitives.md` | `packages/ui` | `HeaderAction` visual-only exportado; dados/fetch/modais continuam por app | build `@artificio/ui` validado |
| BL-UI-B4-PRIMITIVES | fechado | `specs/020` B4, `sessoes/26-06-15_5_ui-020-b3-b4-primitives.md` | `packages/ui` | primitives shared implementadas e testadas (`Button`, `Badge`, `Field`, controles, `Panel`, `Toolbar`, states, `Modal`, `Drawer`) | rollout consumidor fica em fatia propria quando houver tela piloto |
| BL-UI-PRIMITIVES-CONSUMERS | aberto | `specs/020` B4/T14, `primitive-consumers-pilot.md`, `sessoes/26-06-15_6_ui-primitives-consumers-spec.md` | `apps/site-admin` primeiro; depois glossario/mesas conforme duplicacao real | primeiro consumidor usando primitives shared com build/smoke/contraste registrados, sem big-bang | executar fatia 1: `PostsList`/`PagesList` com `Button`/`Badge`/controles/states |
| BL-UI-B12-FONTS | fechado | `specs/020` B12, `sessoes/26-06-15_4_ui-020-b12-fonts.md`, `a9a4437` | `packages/ui`, site publico | `@import` Google Fonts removido do CSS compartilhado; preconnects Google Fonts removidos do site publico; tipografia local-first com fallbacks equivalentes; residual `apps/accounts` fora do shell publico/site | promovido `dev→main`; builds `ui`/`site`, zero-JS e grep validados |
| BL-UI-RADIUS-SPACING | aberto | `specs/019` FSU-021 | `packages/ui`, apps consumidores | revisar escala visual de radius/shadow/spacing para evitar drift entre apps | tratar junto de primitives/recipes |
| BL-UI-T5-THEME-DEDUP | baixo | `specs/020` execution-priority Tier 4 | `apps/accounts`, `apps/site`, `packages/ui` | deduplicar helpers de tema em API `@artificio/ui/theme` | fazer quando voltar a tocar theme runtime |
| BL-022-SEMANTIC-VARS | aberto | `specs/022` T2/T3/T4 | `packages/ui/styles.css`, consumidores | implementar/validar variaveis semanticas e checklist AA por tela | concluir camada vars + parity antes de novas migrações grandes |
| BL-022-MESAS-T8-BETA | validacao | `specs/022` T8 | `apps/mesas` perfil | T8 ficou local/branch; falta prova visual com dados vivos em beta/prod antes de fechar definitivamente | validar apos publicacao autorizada da fatia |
| BL-022-MESAS-CATALOGO | aberto | `specs/022` T9 | `apps/mesas` catalogo publico | aplicar paleta/tokens/fontes e validar AA/responsivo | executar proxima fatia UI do mesas |
| BL-022-MESAS-PAINEL | aberto | `specs/022` T10 | `apps/mesas` painel mestre/gestao | aplicar paleta/tokens/fontes em painel, listas e estados | executar apos catalogo/perfil |
| BL-022-MESAS-FORMS | aberto | `specs/022` T11 | `apps/mesas` forms/modais/toasts | migrar forms multi-step, drawers, modais, toasts e css UI residual | executar com smoke de fluxos autenticados |
| BL-022-MESAS-REMOVE-REMAP | aberto | `specs/022` T12 | `apps/mesas` tema light | remover remap local `[data-theme="light"]` quando todas as telas dependerem dos tokens compartilhados | fazer apos T9-T11 |
| BL-022-ACCOUNTS-R7 | aberto | `specs/022` T14 | `apps/accounts` | decidir/migrar accounts para styles/tokens globais ou documentar excecao R7 | se migrar, rodar smoke SSO login/me/logout |
| BL-022-FINAL | bloqueado | `specs/022` T15 | tema global | validacao final, changelog e fechamento dependem das T2-T14 | fechar somente depois das fatias anteriores |
| BL-SHELL-B13 | fechado | `specs/041-ui-shell-nav-unification/`, PR #80/#82, prod 2026-06-21 | shell/nav/footer | FECHADO pela spec 041: shell único cross-app em prod (glossário/mesas/accounts/links; site beta). Botões padrão centralizados + menu conta padronizado + site Astro híbrido. Residual: `SiteFooter.astro` fork (D-041-08). | — |
| BL-UI-COPYRIGHT-027 | fechado | `specs/027-ui-copyright-usage/`, `sessoes/26-06-16_11_ui-copyright-usage.md`, commit `d077185`, runs `27647997860`, `27650843958` | `packages/ui`, `apps/site`, `accounts` | FECHADO: página `/termos-de-uso-e-direitos-autorais/` e resumo no rodapé universal publicados; beta site 200 e footer com link; prod accounts redeployado por consumir `packages/ui`; site raiz/WP seguem intocados por Gate C | — |
| D-SHELL1 | fechado | `specs/041-ui-shell-nav-unification/`, prod 2026-06-21 | shell/nav/footer | FECHADO pela spec 041: site Astro híbrido eliminou markup forkado; shell único em prod. | — |
| BL-UI-THEME-TOGGLE-SITE-REGRESSION | fechado (em beta; raiz = Gate C) | `sessoes/26-06-21_3_theme-regression.md`, `specs/041-ui-shell-nav-unification/`, 2026-06-21 | `apps/site` (Astro SSG), `artificiorpg.com` | FECHADO pela spec 041: site Astro híbrido (ilha React + tema central `theme.tsx`/`applyHeaderVariant`) resolve o toggle; **provado em `beta.artificiorpg.com`**. Prod-raiz NÃO redeployado (raiz = Gate C, adiado); site fica em beta até cutover. Causa original incluía cache Cloudflare (purgado). **(histórico abaixo)** — Regressao critica original: toggle dark/light no site principal nao funciona — clicar no botao nao altera `data-theme` nem `data-variant`. Site permanece em light. **Evidencia:** (a) beta.artificiorpg.com (build de ~3h atras, commit `df4336e`) funciona — toggle alterna tema e nav corretamente; (b) artificiorpg.com (build pos-promote `dev→main`, commit `f29943c`) quebrado; (c) JS inline (`applyVariant`/`toggleTheme`) identico entre beta e prod (4444 bytes, mesmo hash); (d) CSS compilado identico entre beta e prod (MD5 `722bcdb34c2f4eb3b834468866dc9436`); (e) Cloudflare responde `Cf-Cache-Status: HIT` com `max-age=7200` — pode estar servindo HTML cacheado pre-promote. **Promote `dev→main` trouxe ~30 commits acumulados** incluindo Node 24 LTS, Vite 8, Tailwind 4, ESLint 10 (spec 033), bump deps (spec 039), e PRs #77/#78. HTML do container (`dist/index.html`) tem CSP, mas resposta live NAO tem CSP — evidencia de que Cloudflare serve versao diferente da origem. | (1) Purgar cache Cloudflare `artificiorpg.com` (precisa token API — mantenedor); (2) verificar se toggle funciona pos-purge; (3) se nao resolver, investigar se upgrades (Node 24/Vite 8/Tailwind 4) quebraram build Astro; (4) smoke cross-browser |
| BL-UI-THEME-REACT-HEADER-VARIANT | fechado | `packages/ui/src/theme.tsx`, `specs/041-ui-shell-nav-unification/`, PR #80/#82, prod 2026-06-21 | `packages/ui` + apps React (links/glossario/mesas/accounts) | FECHADO **em prod** via spec 041: `applyHeaderVariant()` (seta `data-variant` no header/footer, integrada em `setTheme`/`applyTheme`) entrou no `packages/ui` e foi deployado com os 4 apps React em prod. Toggle alterna nav+conteúdo+footer juntos. | — |
| BL-MESAS-CATALOGO-SELOS-DUP | fechado 2026-07-18 | PR #179 (feat StartPlaying feature parity, mergeada em `dev` 2026-07-18), achado do mantenedor; spec retroativa criada em `specs/081-mesas-startplaying-feature-parity/` | `apps/mesas/frontend/src/pages/CatalogoPage.tsx`, `apps/mesas/frontend/src/components/SealToggle.tsx` (novo) | FECHADO: componente `SealToggle` (variant `'pill'`\|`'toolbar'`\|`'drawer'`) extraído, substitui os 3 blocos JSX duplicados (hero/desktop/mobile). `tsc`/`eslint` limpos, `pnpm verify:api` verde. Header "Catálogo de Mesas" redundante NÃO removido (achado sem confirmação explícita do mantenedor sobre remoção). Detalhe em `specs/081-mesas-startplaying-feature-parity/tasks.md`. | — |
| BL-MESAS-081-POS-FECHAMENTO | fechado 2026-07-18 | PRs #182 (merged) e #183 (aberto), achados do mantenedor via screenshot pós-fechamento da spec 081; adendo AD1-AD7 em `specs/081-mesas-startplaying-feature-parity/tasks.md` | `apps/mesas/frontend/src/pages/CatalogoPage.tsx`, `TableCard.tsx`, `StyleFacetPicker.tsx` (novo), `hooks/useCatalogTables.ts`, `hooks/useInfiniteCatalogTables.ts` (bug real), `apps/mesas/database/changelogs.json`, `packages/changelog/src/index.ts`, ~20 arquivos com hex cru de tema light (onboarding/admin/forms) | FECHADO: (1) contraste tema light global (hex cru não remapeava, `833efda`); (2) badges sobre overlay + reset de página em filtro (`7571e62`); (3) badge de vagas/bookmark do card não remapeavam em tema light (`var(--surface-panel)`/`var(--fg)`); (4) redesign hero+toolbar do catálogo (mockup aprovado pelo mantenedor) — hero encolhido, faixa "◆ Catálogo" removida, estilos com `StyleFacetPicker` (top 8 + popover com busca, sem scroll horizontal); (5) bug real corrigido em `useCatalogTables.ts` (array `[]` recriado a cada render causava setState instável em cascata quando fetch falhava — confirmado finito via StrictMode+retry, não infinito real, mas causa raiz corrigida mesmo assim); (6) changelog do mesas atualizado (entrada `2026-07-18-favoritar-review-stats-mestre` cobrindo T1-T9 da spec 081 inteira + AD1-AD4, marker de selo de novidade avançado). `tsc`/`turbo lint`/`turbo build`/`pnpm verify:api` verdes. Validação visual do modal de changelog não foi possível localmente (exige backend real/Postgres). | — |

## P1 — Qualidade / Lighthouse

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| BL-QA-LH-HARNESS | fechado | `specs/025-quality-lighthouse-program/`, `sessoes/26-06-15_7_lighthouse-quality-specs.md`, `sessoes/26-06-16_1_quality_lighthouse-harness.md` | site/glossario/mesas betas | harness usavel: `lighthouse` devDependency instalado, `pnpm quality:lighthouse` roda com perfil temporario sem extensoes, mobile+desktop, 3 repeticoes, JSON/HTML e mediana; smoke real salvo em `artifacts/lighthouse/smoke-025-t1` | executar T2 baseline completo com `pnpm quality:lighthouse --out artifacts/lighthouse/baseline-025-<data>` e registrar resumo na spec |
| BL-QA-LH-MULTIURL | fechado | `sessoes/26-06-16_6_quality_perf-images-robots.md`, `sessoes/26-06-16_8_docs-backlog-harness-audit.md` | `scripts/quality/lighthouse-harness.mjs` | parser acumula `--url`/`--urls` repetido e tambem aceita lista com virgula; validado por dry-run local com dois alvos | uso recomendado em PowerShell: repetir `--url` ou colocar lista com virgula entre aspas |
| BL-QA-SITE-IMAGES | FECHADO | `sessoes/26-06-17_3_site_media-finalize.md`, PRs #49-#55, `docs/agents/infra-map.md` | `apps/site/importer/{media.ts,run.ts,inventory.ts}`, Cloudinary | FECHADO 2026-06-17: finalizacao D074 via PRs #49->#55 (captura nao-imagem, resource_type, prune remover-ao-falhar, pruneMode auto-detect, residual publish-only, tolera carga remota, resgate por bytes). Gate D re-run real: `migradas=39 falhas=12`, **RESIDUAL servido posts=0 pages=0 ZERO**; dist grep `wp-content/uploads`=0; live grep=0; webm serve Cloudinary video; HTTP 200. Imagens usadas vivas todas migradas. Residual removido = assets 404 mortos + 6 PDFs grandes (salvos, re-host na VM via BL-SITE-VM-MEDIA-LIBRARY). | — (re-host PDFs segue em BL-SITE-VM-MEDIA-LIBRARY) |
| BL-SITE-MEDIA-ERR-SERIAL | FECHADO (PR #50) | `sessoes/26-06-17_2_site_cloudinary-real-migration.md`, `apps/site/importer/media.ts` | `apps/site/importer/media.ts` | FECHADO: commit `4b0d073` (PR #50). Fix: `failureReason` extrai `.message`/`.error.message`, fallback `JSON.stringify`. Regressão cobre objeto comum fatal e falha tolerável. Pnpm test 9/9 + build verdes. | — |
| BL-SITE-AVIF-FAIL | FECHADO | `sessoes/26-06-17_3_site_media-finalize.md`, PR #50/#55 | 3 AVIF usados em posts do site | FECHADO 2026-06-17: raiz confirmada = origem serve `content-type: text/plain`. Os 3 AVIF (Daggerheart Core/Expanded, Matt-Mercer) migraram no Gate D re-run (path AVIF->WebP PR #50 + resgate por bytes PR #55); `media_map` aponta `res.cloudinary.com/.../image/upload/...`. | — |
| BL-SITE-NONIMG-MEDIA | FECHADO | `sessoes/26-06-17_3_site_media-finalize.md`, PRs #51-#55 | midias nao-imagem em posts (`.webm`, `.pdf`...) | FECHADO 2026-06-17: importador passou a capturar nao-imagem (`<a>/<audio>/<video>/<source>`) com `resource_type` correto (image/video/raw). Gate D re-run: webm (video 22.6MB) migrado; PDFs grandes (>10MB free) podados e salvos p/ re-host (BL-SITE-VM-MEDIA-LIBRARY). Residual servido = 0. | re-host dos 6 PDFs segue em BL-SITE-VM-MEDIA-LIBRARY |
| BL-SITE-VM-MEDIA-LIBRARY | spec-aberta (028) | `specs/028-site-vm-media-library/`, `sessoes/26-06-17_3_site_media-finalize.md`, decisao do mantenedor 2026-06-17 | `apps/site` (admin/server) + VM storage (volume Docker) + os 6 PDFs salvos em `artificiobackup/site-cloudinary/rescued-pdfs/` | **Spec 028 escrita** (spec/plan/tasks) com o contexto completo desta sessao: biblioteca de midia self-hosted na VM estilo FileBird dentro do beta (futuro principal) p/ PDFs/downloads grandes (>10MB raw, fora do Cloudinary free); storage persistente em volume, ampliar MIME/limite, modelo de pastas, UI admin, servico publico, e re-host dos 6 PDFs (fichas D&D 2024 + UA 2025/2026, 16-22MB) com reescrita de links. Estende `media-store.ts`/`admin-api.ts` existentes (T18). | executar SDD Completo da spec 028 (T1->T14) com aprovacao por gate; T1 (volume) e write na VM |
| BL-SITE-CUTOVER-029 | FECHADO | `specs/029-site-cutover-beta-principal/`, `sessoes/26-06-18_1_site_cutover-prod.md`, D075 | apps/site, site-admin, accounts, packages/ui | FECHADO 2026-06-18: cutover beta->raiz efetivado em PROD. Flip por redirect interno Cloudflare (D075, NAO Gate C cerimonial). T2-T7 done (PRs #56/#57, `950a7fc`+`2cc260a`); T8 done (import-on-start OFF default, PR #57). Promote `dev->main` ff (run 27741596778); redeploy PROD glossario/mesas/accounts (runs 27741699473/27741844218/27742012941) 3/3 verdes; smoke 100%; Footer/Portal aponta raiz, zero `beta.`. SEO origem cache-busted OK. Cache Cloudflare purgado pelo mantenedor. | follow-up aberto: T9 noindex/redirect beta; T10 sitemap GSC FECHADO spec 080 (propriedades GSC criadas + 3 sitemaps submetidos, 2026-07-17); BL-SITE-PRINCIPAL-GAPS |
| BL-SITE-PRINCIPAL-GAPS | aberto (item D fechado) | `sessoes/26-06-17_3_site_media-finalize.md`, investigacao 2026-06-17; item D fechado spec 080 (2026-07-17) | apps/site (analytics/newsletter/sitemap/contato) | Gaps de paridade com WP achados na investigacao (mantenedor decidiu virar principal mesmo assim, follow-up): (A) `PUBLIC_GA_ID` VAZIO no beta -> GA4 nao dispara; (B) pagina newsletter renderiza literal `[newsletter]` (shortcode WP nao resolvido) -> sem signup; (C) `src/data/{posts,pages}.json` git-tracked com `wp-content/uploads` stale (VM regenera no export, mas repo sujo); (D) ~~`/sitemap.xml` 404~~ **FECHADO spec 080/T0.1/T3.2: nao e bug — `/sitemap-index.xml` 200 e e o nome esperado do Astro, `robots.txt` ja aponta certo, GSC aceita normalmente**; (E) confirmar form de contato funcional. | pos-cutover: setar PUBLIC_GA_ID; implementar newsletter; regenerar/gitignore os json; verificar contato |
| BL-SITE-RESCUE-STRIPPED | baixo | `sessoes/26-06-17_3_site_media-finalize.md`, `apps/site/importer/media.ts` | `apps/site/importer/media.ts` `uploadWithFallback`/`uploadRemoteFileToCloudinary` | No Gate D, `Jason-Bulmahn-1024x577.jpg.webp` (derivado 404) foi podado mesmo com o original `Jason-Bulmahn.jpg.webp` vivo (200): o resgate por bytes busca a URL ORIGINAL de entrada (derivado 404), nao a stripped. Asset = avatar de autor, salvo no backup. Gap menor; importador descartavel. | se reabrir o importador: no resgate, tentar tambem `stripSizeSuffix(wpUrl)` no fetch local. Caso contrario re-host manual junto dos PDFs |
| BL-SITE-MEDIA-REMOTE-403 | FECHADO | `sessoes/26-06-17_3_site_media-finalize.md`, PR #55 | midias WP que Cloudinary nao busca server-side (MIME `text/plain`) | FECHADO 2026-06-17: raiz = origem serve `text/plain` -> fetch server-side Cloudinary falha (403/"Error in loading"). Resgate por bytes (PR #55, provado por teste real) migrou o webm de 22.6MB no Gate D re-run. Limite separado dos PDFs (>10MB raw free) = BL-SITE-VM-MEDIA-LIBRARY. | — |
| BL-QA-SHELL-CLS | fechado | `specs/025-quality-lighthouse-program/lighthouse-findings.md`, `specs/025-quality-lighthouse-program/baseline-2026-06-16.md`, `sessoes/26-06-16_3_quality_glossario-cls.md`, `sessoes/26-06-16_5_ui-footer-logo-cls.md`, `sessoes/26-06-16_9_quality-beta-deploy.md`, `specs/020` B13 | `packages/ui`, `apps/site` Astro, `apps/glossario`, consumidores React | FECHADO para footer/logo/glossario beta: fonte compartilhada publicada; Lighthouse beta final mediu CLS 0.000862 contra baseline 0.647 | reabrir item novo se `div.artificio-session` ou outro shell voltar a causar CLS relevante |
| BL-QA-GLOSSARIO-PERF | fechado | `specs/025-quality-lighthouse-program/lighthouse-findings.md`, `specs/025-quality-lighthouse-program/baseline-2026-06-16.md`, `sessoes/26-06-16_6_quality_perf-images-robots.md`, `sessoes/26-06-16_9_quality-beta-deploy.md` | `apps/glossario` frontend/backend | FECHADO para fatia inicial: endpoint `/api/terms` paginado/busca server-side; bundle inicial reduzido (~1.14 MiB -> ~340 kB); `ResultCard`/`AddTermModal` chunks separados; beta real perf 12 -> 61 e TBT 1065ms -> 413ms | residual LCP 6153ms: abrir nova fatia se a meta passar a ser perf boa (ex.: assets/fonts/cache/SSR ou virtualizacao/lista) |
| BL-QA-MESAS-PERF | aberto | `specs/025-quality-lighthouse-program/lighthouse-findings.md` | `apps/mesas` frontend | reduzir TBT/main-thread, JS/CSS unused e imagem pesada sem quebrar catalogo/login/painel | abrir spec perf mesas com baseline limpo e smokes por rota |
| BL-QA-ROBOTS-SEO | fechado | `specs/025-quality-lighthouse-program/lighthouse-findings.md`, `sessoes/26-06-16_6_quality_perf-images-robots.md`, `sessoes/26-06-16_9_quality-beta-deploy.md`, `docs/agents/infra-map.md` | glossario/mesas SPAs; site Astro; Cloudflare beta | FECHADO: app/origem coberto (`public/robots.txt`, nginx exato no mesas, origem beta 200 texto cru); mantenedor desativou Cloudflare Managed robots.txt (`Configuração Robots.txt: Desativado`); validação pública 2026-06-16: `beta`, `glossariobeta`, `mesasbeta` retornam `User-agent`, `Allow` e `Sitemap` da origem; Lighthouse beta SEO 100 | — |
| BL-QA-SECURITY-HEADERS | aberto | `specs/025-quality-lighthouse-program/lighthouse-findings.md` | infra + apps publicos/admin | desenhar CSP/HSTS/COOP/XFO/Permissions/Trusted Types sem quebrar OAuth, Cloudinary, Pagefind, analytics ou WP raiz | abrir spec infra headers; Gate C/WP raiz fora |
| BL-SITE-CSP-INLINE | ✅ em prod (PR #112); residual: Cloudflare (Email Obfuscation/Rocket Loader) + gtag inline | sessao 26-06-29_5, PR #112 (mergeada, deploy prod run 28397288730), chat 2026-06-29 (busca nav + erros CSP prod artificiorpg.com), `apps/site/astro.config.mjs`, `apps/site/src/layouts/Base.astro`, `apps/site/src/components/SearchModal.astro` | `apps/site` Astro 6.4.8 CSP | RAIZ: Astro 6 CSP **só hasheia scripts que ele bundla** (config.d.ts §securitycsp); `<script is:inline>` NUNCA e hasheado → theme/feedback/SearchModal/toc bloqueados em prod → **busca do nav nao abria** (modal `is:inline` morto; alem do Pagefind precisar `'wasm-unsafe-eval'` p/ WASM). FEITO+VERIFICADO no preview (build local, 0 CSP violation, busca 5 resultados): (1) `'wasm-unsafe-eval'` no script-src; (2) `img-src` + `*.googleusercontent.com` (avatar Google SSO); (3) SearchModal+toc viraram script bundlado (`self`, auto-hash); (4) theme+feedback ficam `is:inline` com hash manual em `scriptDirective.hashes`. FALTA: gtag inline (so quando `PUBLIC_GA_ID` setado em prod — hoje vazio, latente) sem hash → refatorar `@artificio/analytics` (shared, SDD+smoke) ou hash por env; **Cloudflare** injeta script (Email Obfuscation/Rocket Loader) e o beacon RUM (`static.cloudflareinsights.com`) que CSP por hash nao cobre → mantenedor decide desligar essas features e/ou allowlist; risco: hashes manuais quebram se o script mudar (sem guard CI). | build+branch+PR+deploy do site (autorizacao); mantenedor: Cloudflare dashboard (desligar Email Obfuscation+Rocket Loader; decidir beacon keep+allowlist vs drop CF Web Analytics, ja existe GA4); decidir gtag (refator analytics vs hash por env); avaliar guard CI p/ hashes inline |
| BL-ACCOUNTS-CONTA-STALE | aberto (aguarda aprovacao de deploy) | sessao 26-06-29_5, `apps/accounts/frontend/src/{main.tsx,styles.css}` | `apps/accounts` (SSO, prod-only) | "Lateral branca" na pagina de conta em dark = **build VELHO em prod** (pre-PR #110). Fix do rail dark (`--accounts-rail #070b1a`) ja em `origin/main` (commits c051971/b7a43f1) mas accounts nunca deployado depois do #110. Verificado local: build de `main` no escuro fica todo escuro, sem faixa branca. | deploy accounts prod (`gh workflow run deploy.yml --ref main -f module=accounts -f mode=deploy -f env=prod`) + smoke login/me/logout — APROVACAO NOMINAL (SSO sagrado) |
| BL-ACCOUNTS-DOCKERFILE-MEDIA | PARCIAL — cloudinary RESOLVIDO, mas SSO AINDA DOWN (jsonwebtoken) | sessao 26-06-29_5 §OUTAGE COMPLETO, PR #113 (merged be7fafd, em main), runs 28398923363/28400194166/28402041335/28403066320 | `apps/accounts/Dockerfile`, `apps/accounts/package.json` | RESOLVIDO o cloudinary: Dockerfile novo (WORKDIR /repo, double `--filter accounts`+`--filter media`, guard `test -f`, COPY dist por pkg) + `cloudinary` dep direta. Build da imagem passou (run 28403066320, guard OK, `naming to ...accounts-api:latest done`). PORÉM container nao fica healthy → ver BL-ACCOUNTS-DOCKERFILE-AUTH-ESM. **SSO segue 502.** | continuar em BL-ACCOUNTS-DOCKERFILE-AUTH-ESM |
| BL-ACCOUNTS-DOCKERFILE-AUTH-ESM | ABERTO — SSO DOWN (outage ativo) | sessao 26-06-29_5 §OUTAGE COMPLETO, run **28403066320** | `apps/accounts/Dockerfile`, `packages/auth` | Imagem nova builda OK mas `accounts-api` crash-loop em runtime: `Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'jsonwebtoken' imported from /repo/packages/auth/dist/jwt.js`. Mesmo padrao do cloudinary, agora com dep de `@artificio/auth`. `jsonwebtoken` JA e dep direta de `apps/accounts/package.json` — logo "dep direta" NAO basta; o `pnpm install --prod --filter accounts` nao popula as deps de `@artificio/auth` no node_modules que o ESM resolve a partir de `packages/auth/dist/jwt.js`. | espelhar EXATO `apps/mesas/backend/Dockerfile` (mesas usa @artificio/auth e esta UP): conferir `ENV NODE_PATH`, `--filter @artificio/auth` no install, copia de `dist-cjs`, e como mesas popula node_modules dos workspace pkgs. **VALIDAR `docker build` na VM (ssh faren) ANTES de tocar main** (host local sem docker). NAO redeployar as cegas |
| BL-INFRA-DEPLOY-BUILDS-MAIN-ONLY | aberto (documentar regra/footgun) | sessao 26-06-29_5 §OUTAGE COMPLETO, `.github/workflows/_deploy-module.yml` L285-293; sessao 26-06-29_7, run `28463625927` | deploy workflow | `_deploy-module.yml`: deploy prod faz `git reset --hard origin/main`; beta `origin/dev`. VM SEMPRE builda main/dev, NUNCA o branch dispatchado em `deploy.yml --ref <branch>`. Footgun: dispatch de branch de fix builda CI do branch mas a VM ignora e builda main → erro identico repetido sem feedback. Causou 3 deploys inuteis. **Novo caso 2026-06-30:** `gh workflow run deploy.yml --ref feat/052-mesas-automacao-recuperado -f module=mesas -f mode=deploy -f env=beta` rodou CI da feature (`ad2554e`) mas deployou `origin/dev` (`1a8739d`) em `/opt/artificio-beta`; migration_133 não aplicada (`discord_field_learning` ausente). | documentar em infra-map.md / deploy-runbook: "validar Dockerfile so apos merge em dev (beta) ou main (prod), ou com docker build local/VM"; avaliar warning/fail-fast no workflow quando `workflow_dispatch --ref` != branch efetivamente deployada; se precisar deployar branch isolada em beta antes do merge, usar procedimento manual excepcional aprovado ou criar suporte explícito no workflow |
| BL-ACCOUNTS-CONTA-NAV | aberto (decisao pendente, aprovado — sessao 26-06-29_6) | sessao 26-06-29_5, `apps/accounts/frontend/src/main.tsx` | `apps/accounts` + `packages/ui` | Pagina de conta nao usa o header/nav compartilhado da suite (`@artificio/ui`); so tem `AccountHeader` interno. Mantenedor quer o nav compartilhado tambem na conta. Feature nova, nao deploy. | decidir; se sim, SDD Completo + branch propria + smoke (accounts/ui sagrados) |
| BL-INFRA-AUTODEPLOY-FF | aberto | sessao 26-06-29_5 | `.github/workflows/deploy.yml` + fluxo de promocao | Merge/FF para `dev`/`main` NAO dispara deploy automatico (`deploy=false`; `deploy_paths` nao casa em fast-forward). Site precisou dispatch manual; accounts ficou stale desde o PR #110 sem ninguem notar. | investigar deteccao de `deploy_paths` em FF (BEFORE..SHA) ou documentar que promocao exige dispatch manual de deploy por modulo afetado |
| BL-QA-A11Y-SWEEP | aberto | `specs/025-quality-lighthouse-program/lighthouse-findings.md` | site/glossario/mesas + tokens UI | corrigir contraste, nomes acessiveis, links ambiguos e touch targets achados nos anexos | rodar axe/Lighthouse limpo; migrar padroes comuns para UI quando provado |
| BL-QA-THIRD-PARTY | aberto | `specs/025-quality-lighthouse-program/lighthouse-findings.md`, `BL-ANALYTICS`, **spec 032** | analytics/Cloudflare/scripts por host | separar ruido de extensoes de scripts reais; auditar Cloudflare beacon/RUM e convergir glossario/mesas para `@artificio/analytics` quando aplicavel. Convergencia analytics tratada na spec 032 | inventariar scripts live por host e decidir politica de terceiros |

## P2 — Shared / Conteudo / Observabilidade

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| BL-GLOSSARIO-CHANGELOG-JSON | fechado | `sessoes/26-06-21_4_shell-041-merge-deploy.md`, spec 041, 2026-06-21 | `apps/glossario/backend` | FECHADO: migradas 13 entradas do DB → `changelogs.json`; controller trocado para leitura de arquivo com cache (padrao mesas); `mergeUsers.ts:48` removido; `LAST_SEEN_UPDATE` atualizado; build verde. Dropar tabela `update_log` no proximo deploy. | dropar `public.update_log` apos deploy confirmado |
| DEB-088-01 | contornado | spec 088 Fase 3, 2026-07-27 (revisto 2026-07-27) | ferramenta `rtk` (local, todos os projetos) | `rtk lint` falha com `ESLint output (JSON parse failed: EOF while parsing a value at line 1 column 0)` — o subcomando dedicado tenta parsear JSON do ESLint e o turbo nao entrega esse formato. **Contorno confirmado: `rtk pnpm run lint` e `rtk pnpm run build` funcionam** (23/23 nos dois, medido 2026-07-27). A entrada anterior afirmava que `rtk pnpm run build` tambem falhava — medicao errada, corrigida aqui. Logo NAO ha necessidade de cair pro `pnpm` cru: o proxy generico cobre o caso. | so `rtk lint`/`rtk tsc` (subcomandos dedicados) falham no monorepo turbo; usar `rtk pnpm run <script>` nesses casos. Abrir issue no rtk sobre o parse do ESLint sob turbo continua pendente |
| DEB-088-02 | aberto | spec 088 Fase 2, 2026-07-27 | `apps/downloads/backend` (scrapers OneBookShelf) | DriveThruRPG e DMs Guild: parser existe e funciona (validado contra 4 DOMs reais), mas `blockedSourceScraper` nunca o invoca e o WAF devolve **403 em ambas** as listagens (verificado 2026-07-27). Extracao pronta, coleta impossivel. Nao e regressao: nunca coletou. | decidir se vale fiar `blockedSourceScraper` ao override (codigo pronto, sem efeito enquanto houver 403) ou aguardar mudanca de acesso; ver `spec.md` 42b/43a/43b |
| DEB-088-03 | 🛠 corrigido localmente na spec 089 F3 (2026-07-27) | spec 088 Fase 2, 2026-07-27 | `apps/downloads/backend` (`itchIoParser.ts`) | Hint de sistema e de tipo NAO extraidos de itch.io/Grimorios: o parser le so `og:image`, `og:description` e o link da conta. A pagina de jogo tem secao "More information" que pode conter esses campos, mas nao foi observada em DOM real — escrever regex contra estrutura nao vista viola o requisito 43. Adiado por decisao do mantenedor. **Escopo era MAIOR do que este debito supunha:** a medicao da Fase 2 em beta (2026-07-27) provou que NENHUMA das 3 fontes produz `systemHint`/`materialTypeHint` — `opera_rpg` tambem nao, e ele responde por 118 dos 141 materiais. `com_sistema`=0 e `hint_bruto`=0 no acervo recoletado; `hint_bruto`=0 e a prova decisiva (parser que tentasse e falhasse teria preenchido o hint bruto). | código local em `fix/089-fases-2-3`; mergear e validar serviço real na Fase 5 |
| DEB-089-01 | 🛠 corrigido localmente na spec 089 F2 (2026-07-27) | spec 089, medicao da Fase 2 da 088 em beta, 2026-07-27 | `apps/downloads/backend` (`scraperIngest.ts:323`, `languageDetector.ts`) | **`itch_io` nao barra material nao-portugues: 0 de 14 barrados**, contra 14/132 no `opera_rpg` e 5/14 no `grimorios_e_dados` (o filtro funciona nas outras fontes). Passaram titulos em ingles (`World's Doom: Mission Salvation`, `Grimm's Hollow`, `It's Not Me, It's My Basement`). Agravante independente: `language: 'pt'` e **hardcoded** ao criar o material, entao o idioma detectado nunca chega a coluna, mesmo quando a deteccao acerta. | código local em `fix/089-fases-2-3`; mergear e validar corpus real na Fase 5 |
| DEB-089-02 | ✅ mergeado via PR #221 (2026-07-27) | spec 089, medicao da Fase 2 da 088 em beta + inspecao do catalogo pelo mantenedor, 2026-07-27 | `apps/downloads/backend` (`operaRpgScraper.ts:49` e demais parsers) | **Entidade HTML crua chega a tela do usuario, em multiplos campos.** Texto vem de regex direta sobre o HTML (`TITLE_RE`) sem decodificar; `decodeHtmlEntities` existe (`sanitizeRichHtml.ts:100`) mas so a descricao rica passa por ela. Alcance medido em beta: **titulo** (3 materiais, `World&#039;s Doom`, `Machados &amp; Bruxarias`), **summary/description** (6 materiais, `D&amp;D`, `pink &amp; cutesy`), **publisher_name** (`Editora Grimorios &amp; Dados Editora`). **Dois agravantes:** (1) o titulo contamina o slug — `world-039-s-doom-mission-salvation` e URL publica permanente, corrigir depois exige redirect 301 (regra petrea de SEO); prod nasce vazia, entao basta corrigir o parser ANTES do cutover. (2) `D&amp;D` deveria ser `D&D`, que e exatamente o nome de sistema que `resolveSystemHint` precisa casar — a entidade crua **sabota o casamento** mesmo depois de DEB-088-03 ser corrigido. | validar acervo real na Fase 5 |
| DEB-089-04 | aberto | spec 089, inspecao do catalogo de beta pelo mantenedor, 2026-07-27 | `apps/downloads` (`MaterialCard.tsx:44-45`, `routes/materials.ts`, `CatalogoPage.tsx`) | **Editora, autor e sistema nao sao clicaveis no card** — renderizam como `<p>` puro, sem `<a>`. Esperado e o padrao de catalogo de e-commerce: clicar na faceta filtra o acervo por ela. **Editora e autor nem filtro tem**: o catalogo aceita so `q`, `material_type`, `system_id`, `edition_id`, `sort`, `page`. Tornar clicavel exige construir a busca antes, nao so ligar link. `system_id` e o caso facil (filtro pronto, falta o link). Complicador: `publisher_name` e texto solto — sem normalizar, "Grimorios & Dados Editora" e "Grimorios e Dados" viram facetas distintas da mesma editora. | spec 089 Fase 4 (T4.1-T4.6), na ordem: normalizar na gravacao, filtro na API, filtro no catalogo, links no card. Decisao do mantenedor: normalizar texto, **nao** promover editora a entidade/tabela |
| DEB-089-05 | aberto | spec 089, inspecao do catalogo de beta pelo mantenedor, 2026-07-27 | `apps/downloads/frontend` (`MaterialCard.tsx`) | **Card afirma "Em portugues" em 100% dos itens, inclusive nos que estao em ingles** (`Fight slimes. Argue with villagers.`, `Learn Japanese in immersion`, `decay together.`). E a manifestacao VISIVEL do `language: 'pt'` hardcoded (DEB-089-01) — o site nao so guarda dado errado, ele afirma algo falso ao usuario. Achado lateral no mesmo elemento: `Editora Grimorios &amp; Dados Editora` duplica a palavra "Editora" (prefixo do label colide com nome de fonte que ja a contem). | spec 089 T4.5. Decisao do mantenedor: **remover o rotulo do card** — acervo e em portugues por premissa, carimbar em todo card e ruido; o filtro de idioma garante, a interface nao anuncia |
| DEB-089-06 | aberto | spec 089, inspecao do catalogo de beta pelo mantenedor, 2026-07-27 | `apps/downloads` (`routes/comments.ts:49`, caixa de comentarios) | **Comentario nao mostra quem comentou** — `GET /comments/:materialId` devolve `user_id` cru, sem nome nem avatar. `download_creator` NAO serve de fonte: e tabela de criador de MATERIAL, nao de usuario; unico registro em beta e `Indexacao automatica` (creator sintetico do scraper, `user_id` nulo de proposito). Identidade do usuario vive no `accounts.` (SSO). **Pedido junto:** permitir responder comentario (thread) — feature nova, exige `parent_id` + migration + UI aninhada. | spec 089 Fase 6. Decisoes do mantenedor: identidade vem do **accounts. (SSO)**; thread entra **nesta spec**. Trava: indisponibilidade do accounts degrada identidade, NUNCA derruba a listagem (rota publica) |
| DEB-089-07 | aberto | spec 089, inspecao do painel pelo mantenedor, 2026-07-27 | `apps/downloads/frontend` (`NovoMaterialPage.tsx:17,73`) | Dois defeitos no formulario de material novo: (1) **slug e campo manual** (`useState('')`), pedindo que o usuario digite detalhe tecnico de URL — o backend ja sabe derivar (`generateUniqueSlug`, usado pelo ingest); (2) **`<select>` ilegivel no tema escuro** — `bg-transparent` + `text-[var(--fg)]` faz o popup nativo do navegador pintar fundo branco enquanto o texto do `<option>` herda cor clara: branco sobre branco. Mantenedor relatou que o contraste ja estaria registrado; busca no backlog achou itens de contraste de tema (`BL-QA-A11Y-SWEEP`, `BL-MESAS-081-POS-FECHAMENTO`, `BL-UI-B6-E2E`) mas **nenhum especifico deste `<select>` no downloads**. | spec 089 Fase 7. Decisao do mantenedor: **investigar `packages/ui` primeiro** — se o defeito atinge outros modulos, corrigir na origem compartilhada (exige aprovacao propria + verificacao de consumidores) |
| DEB-089-08 | aberto | spec 089, inspecao do painel pelo mantenedor, 2026-07-27 | `apps/downloads` (`summary`/`description`) + `packages/ui` | **`summary` e `description` sao texto puro, sem HTML formatado.** Assimetria real: `description_html` (metadata) JA e rica, sanitizada nos dois sentidos (`materialMetadata.ts:48,79`) e editavel em `GestaoEditarDescricaoPage`; mas `summary`/`description` do material seguem `string` plana, sem editor. `mesas` tem `RichTextArea.tsx`/`MarkdownEditor.tsx`, porem em `apps/mesas`, nao em `packages/ui`. | spec 089 Fase 8. Decisoes do mantenedor: **os dois campos ficam ricos, com strip de tags no card e no meta description** (HTML vazando em snippet de busca e regressao de SEO); editor **extraido para `packages/ui`** — o que torna `mesas` consumidor e **exige aprovacao propria + verificacao de impacto** |
| DEB-089-09 | aberto | spec 089, inspecao do painel pelo mantenedor, 2026-07-27 | `apps/downloads/frontend` (`EditarMaterialPage.tsx`) | **Edicao de material nao cobre sistema nem capa.** Verificacao refina o relato original: **titulo E editavel** (input em `:111`, link "Editar" em `MeusMateriaisPage.tsx:42`) — nao e defeito. Faltam: (1) campo de **sistema**, o que agrava DEB-088-03 (nem onde o parser falha ha caminho manual de correcao); (2) envio de **capa**, apesar de o backend ja ter `storage/cloudinaryAdapter.ts` e `cover_image_url` na metadata — infra pronta, sem UI; (3) **orientacao ao usuario** (formato, dimensao recomendada, limite de tamanho). | spec 089 Fase 9 (requisitos 28-30). Upload sempre pelo backend com signed preset do Cloudinary, nunca credencial no frontend (`AGENTS.md` §Regras de Produto) |
| DEB-089-10 | aberto | spec 089, inspecao do mantenedor, 2026-07-27 | `apps/downloads/frontend` (`AppShell.tsx:18-19`) + painel | Dois achados de navegacao/cobertura: (1) **nav expoe `Inicio` e `Catalogo`**, deveria ser `Catalogo` e `Perfil` — `PerfilPage.tsx` JA existe em `pages/painel/`, so nao esta exposta; (2) **painel nao cobre a API disponivel**: backend tem ~75 rotas em 21 arquivos, painel tem 11 paginas, e dominios inteiros nao tem tela (`ratings` 2 rotas, `comments` 3, `creators` 2, `destinations` 1, `systemSuggestions` 2, `downloads` 1). Mantenedor pediu onboarding robusto que de saida ou consuma todas as APIs aplicaveis. | (1) e correcao de nav, pequena. (2) **nao e bug, e desenho de produto** — exige mapear fluxo do autor (publicar/enriquecer/acompanhar) e decidir o que e onboarding vs painel. Escopo maior que todos os defeitos de parser somados |
| DEB-089-11 | aberto | spec 089, inspecao do mantenedor, 2026-07-27 | `apps/downloads` (comentarios, UI de admin) | **Permissoes existem no backend, falta UI que as exerca.** Verificado no codigo: `materials.ts:637-638` ja permite `isOwner \|\| moderator \|\| admin` editar qualquer material; `comments.ts:63` ja exige `requireRole(['moderator','admin'])` pra retirar comentario. **Nao implementado:** autor nao ve nem responde comentarios do proprio material (sem tela, sem `parent_id`); nenhuma distincao visual de papel no comentario (autor do material/moderador/admin aparecem iguais — e a API nem devolve identidade, ver DEB-089-06); admin sem UI pra editar material alheio ou moderar comentario. | Trabalho e de interface + contrato de leitura (devolver papel junto do comentario), **nao de autorizacao** — essa parte esta pronta e correta |
| DEB-089-12 | aberto | spec 089, debug de Open Graph pelo mantenedor, 2026-07-27 | `apps/downloads/frontend` (`index.html`, `MaterialPage.tsx`) | **Sem OG por material: todo compartilhamento mostra imagem e titulo genericos.** `index.html:13-15` tem `og:image` COM `width`/`height` (1200x630) — a recomendacao do aviso do Facebook ja esta atendida na tag estatica. O problema e maior: `MaterialPage.tsx` nao define OG nenhum e o downloads e **SPA pura, sem SSR nem prerender**, entao o crawler recebe sempre o `index.html` estatico. Evidencia com User-Agent `facebookexternalhit` em `/materiais/red-sand-warriors-dominus-rpg`: `og:title` = titulo do SITE, `og:image` = `og-default.png`. **Nao e artefato de beta** — a URL da imagem e absoluta e aponta pra `downloads.artificiorpg.com` (prod); o mesmo `index.html` vai pra prod e o comportamento sera identico. | Nenhuma correcao de tag resolve sem estrategia de renderizacao: SSR, prerender de rotas, ou rota de OG no backend servida por User-Agent de crawler. Colide com a regra petrea de SEO — **conta para o cutover de prod** |
| DEB-089-13 | ✅ mergeado via PR #221 (2026-07-27) | spec 089, 1a revisao do Codex (Fase 0), 2026-07-27 | `apps/downloads/backend` (`scrapers/itchIoScraper.ts:15`) | **BLOQUEADOR — corpus do itch_io e de VIDEOGAMES, nao de RPG de mesa.** `LISTING_URL = 'https://itch.io/games/genre-rpg/lang-pt-BR'`; `/games/` e o catalogo de videogames. Rota correta pra RPG de mesa e `/physical-games/genre-rpg/lang-pt-BR` — o itch.io distingue videogame, tabletop game e book na classificacao de projeto. Confirmado pelo acervo de beta: `Pigeon Ascent`, `Dungeon Raid`, `Wagotabi: Learn Japanese [Demo]` sao jogos digitais. **Consequencias:** os 14 itens nao formam corpus valido; baseline de 141 precisa ser refeita; `materialTypeHint` classificaria videogame como material de mesa; diagnostico do filtro de idioma estava contaminado. | validar corpus real na Fase 5 |
| DEB-089-14 | 🛠 corrigido localmente na spec 089 F2 (2026-07-27) | spec 089, 1a revisao do Codex (Fase 0), 2026-07-27 | `apps/downloads/backend` (`scraperIngest.ts:226`, `scrapers/itchIoScraper.ts:26`) | **Diagnostico do DEB-089-01 estava ERRADO — corrigido aqui.** A hipotese era "detector falha em texto curto"; a causa real e **bypass por desenho**: `if (item.sourceLanguageHint !== 'pt')` envolve a chamada de `detectPortuguese`, e o `ItchIoScraper` passa `'pt'` fixo confiando no filtro `lang-pt-BR` da URL. O detector **nunca roda** nessa fonte e `detectedLanguage` fica `'pt'` sem verificacao. Agravante: o pressuposto que justificava o bypass tambem e falso — `lang-pt-BR` significa "tem versao em portugues", nao "pagina escrita em portugues" (`World's Doom` e `Grimm's Hollow` tem traducao pt-BR real, apesar do titulo ingles). | código local em `fix/089-fases-2-3`; mergear e validar corpus real na Fase 5 |
| DEB-089-15 | ✅ mergeado via PR #221 (2026-07-27) | spec 089, 1a revisao do Codex (Fase 0), 2026-07-27 | `apps/downloads/backend` (`scrapers/types.ts:25`, fixtures de parser) | Tres achados de contrato e de rastreabilidade: (1) **`systemHint?`/`materialTypeHint?` sao opcionais** — adapter pode omitir os dois e compilar, que e exatamente como os 3 parsers chegaram ao estado do DEB-088-03 sem erro de tipo; requisitos 1-6 exigem `null` explicito mas o tipo nao obriga. (2) **Premissa de "uma pagina de produto por fonte" e errada**: `opera_rpg` consome 6 paginas de secao + links diretos de arquivo (`operaRpgScraper.ts:20`), `itch_io` consome listagem E pagina individual, `grimorios_e_dados` reutiliza `itchIoParser.ts:7` (mesmo parser) — e o `plan.md` omitia `itchIoParser.ts` dos arquivos afetados. (3) **Fixture inline na sessao nao e regressao executavel**: provam observacao hoje, nao protegem amanha; nao ha teste dedicado ao `GrimoriosEDadosScraper`. | contrato e fixtures mergeados; validar corpus real na Fase 5 |
| DEB-089-21 | mergeado em `dev` via PR #226 (`353d95f`, 2026-07-28) — **aguarda deploy Beta** | revisao da Fase 7 da spec 089 (2026-07-27), Fase 6B (2026-07-28) | `apps/mesas/backend`, `apps/mesas/frontend` | **Duas P0 corrigidas na árvore local.** (1) Detalhe, `/view`, `/click` e favoritos agora exigem mesa ativa, não arquivada e, se importada, não expirada; sete testes cobrem as negações e o caminho público. (2) Markdown de usuário passa por `sanitize-html` na escrita e defensivamente na leitura; preview desliga HTML cru; `RichTextArea.tsx` morto foi removido. Medição read-only: Beta 12 perfis/19 mesas e Prod 23 perfis/66 mesas, com zero HTML/entidade detectado; Markdown legítimo apareceu em 1 descrição Beta e 3 Prod. | Decisão do mantenedor: sem migration retroativa. Gates locais verdes: backend 626 testes, frontend 191, lint/build raiz e `verify:api`. Não fechar como entregue até registrar PR/SHA em `dev` e deploy Beta |
| DEB-089-20 | 🛠 mergeado em `dev` via PR #226 (`353d95f`, 2026-07-28) — **aguarda deploy Beta** | spec 089 T5.8, smoke de facetas em Beta, 2026-07-28 | `apps/downloads/backend` (`routes/materials.ts:203-205`) | **`GET /api/v1/materials?author=<qualquer>` devolve HTTP 500, nao lista vazia.** `.where('author_keys', '@>', [author])` faz o Kysely serializar o array JS como parametro escalar; o Postgres tenta ler o valor como literal de array e aborta: `malformed array literal: "leo andrade"`, SQLSTATE `22P02`, `detail: Array value must start with "{" or dimension information.`. Medido em Beta: `?author=intruder` 500, `?author=leo andrade` 500, sem o parametro 200. **Nao e dado ausente** — o banco tem 9 materiais publicados com `author_keys @> ARRAY['leo andrade']`, e `/facets` anuncia 30 entradas de autoria, todas quebrando ao clicar. `publisher` usa `=` em coluna escalar e funciona (conferido: 7 e 1). Erro interno vazando em rota publica. | **Corrigido 2026-07-28** sob autorizacao nominal, depois de o achado reaparecer na revisao da PR das Fases 6B-8. `.where('author_keys','@>',[author])` virou `.where(sql\`download_material_metadata.author_keys @> ARRAY[${author}]\`)` — o literal e montado no servidor e o valor segue como bind, sem interpolacao. Provado no banco de Beta com `PREPARE`/`EXECUTE`: `leo andrade` 9, `intruder` 2, inexistente 0 (lista vazia, nao 500), batendo com o que `/facets` anuncia. Teste de regressao compila o fragmento pelo dialeto Postgres real e asserta `author_keys @> ARRAY[$1]` com `parameters: ['agata']`, garantindo que a chave nunca entra no texto do SQL. Verificado: `materials.list` 27/27, backend downloads 401/401, `tsc` limpo. A spec 090 R29 segue valendo (autoria sai das facetas) — a diferenca e que ate la a rota nao devolve mais erro interno |
| DEB-089-19 | ✅ **fechado** — corrigido na PR #225, mergeado e aplicado em Beta; recoleta limpa provou `log_rows = items_found` (137/137, 11/11, 25/25) em 2026-07-28 | spec 089, conferencia do log apos T5.5, 2026-07-28 | `apps/downloads/backend` (`services/scraperIngest.ts:108-125`), `apps/downloads/database/migration_033` | **Log de item perdeu 83 de 173 registros em silencio; a Fase 5 mede em cima dele.** Nas tres runs de 2026-07-28 18:21-18:29 UTC, `log_rows` bate exatamente `items_created` (OPERA 80/80, Grimorios 7/7, itch 3/3) e nenhuma das 83 rejeicoes por idioma foi persistida. Causa: `'skipped_not_portuguese'` tem 22 caracteres e `download_scraper_item_log.outcome` e `VARCHAR(20)`. `'skipped_duplicate'` (18) e `'skipped_error'` (13) cabem — por isso so apareceu nesta rodada, a primeira sem nenhum duplicado. Consequencia na medicao: ground truth observado 4/11 e taxonomia 2/4 sairam de um log com 100% de `created`, o que subestima rejeicao em **todas** as fontes, nao so a de idioma. Agravante de observabilidade: `logItem` captura a excecao e termina em `console.error` — o `try/catch` e deliberado e correto (achado da PR #193, CodeRabbit: falha de log nao pode mudar classificacao nem abortar a run), mas a falha morre em stderr de container, sem contador, sem `error_detail` e sem sinal na tabela de run. | Decisao do mantenedor 2026-07-28: **entra na 089 agora**, nao vira debito. migration 033 (`VARCHAR(32)`) autorizada em Beta sem backup — ambiente descartavel, nao toca catalogo de prod. Correcao precisa cobrir os dois lados: coluna **e** rastro recuperavel da falha de gravacao, preservando a razao da PR #193. Exige recoleta: os 83 registros sao irrecuperaveis |
| DEB-090-01 | aberto — **P0**, escopo na spec 090 req. 32 (2026-07-28) | spec 089, conferencia pos-recoleta da Fase 5, 2026-07-28 | `apps/downloads/backend` (`services/languageDetector.ts:60`, gate em `scraperIngest.ts:278`) | **Detector aprova ingles como "portugues confiante" — furo do D119 em producao de beta.** `detectWithFranc` calcula `confident = runnerUpScore < 0.95` olhando **apenas a margem para o segundo colocado**, sem verificar se o primeiro e `por`. Quando franc erra o topo, a margem folgada faz o guarda-corpo nao disparar justamente onde deveria. Reproduzido com texto real: `francAll("Cat5Crew A party game ready to take you to space.")` devolve `[['por',1],['eng',0.82],['spa',0.81]]` — `0.82 < 0.95`, logo `confident:true` e `isPortuguese:true`, e o gate deixa passar. Afetou **2 dos 3 materiais do `itch_io`** na recoleta de 2026-07-28: `cat5crew` e `minihex` gravados com `detected_language='por'` e `language_confident=true` sobre texto ingles puro. Causa contribuinte: os dois textos tem 49 e 42 caracteres, passando `MIN_TEXT_LENGTH_FOR_FRANC=40` por pouco — faixa em que franc e instavel. Distinto do DEB-089-14 (aquele era bypass por desenho, ja corrigido; este e o calculo de confianca dentro do proprio detector). | Decisao do mantenedor 2026-07-28: corrigir na spec 090 (req. 32), nao na 089. Consequencia registrada em 089 T5.4: T5.5-T5.8 medem acervo contaminado, e a taxa de rejeicao por idioma sai como **piso**, nao valor real |
| DEB-090-02 | aberto — escopo na spec 090 req. 30 (2026-07-28) | spec 089, conferencia pos-recoleta da Fase 5, 2026-07-28 | `apps/downloads/backend` (`services/scrapers/itchIoParser.ts:133,171`) | **Parser le a tabela de informacoes inteira do itch e descarta quase tudo no mesmo escopo.** `parseInformationRows` percorre todas as linhas da `game_info_panel_widget`, mas `parseItchGameDetail` devolve so `tags`, `description`, `coverImageUrl` e `publisherName`. Medido no acervo recoletado (90 materiais): `file_format`, `license_kind`, `page_count`, `genre`, `scenario`, `target_audience`, `creation_method` e `description_html` em **0 de 90**. A ficha de material tem bloco Detalhes e faixa de tiles preparados pra esses campos (`MaterialPage.tsx`), exibindo "N/D" permanente. | Escopo na 090 req. 30/30a/30b/30c: extrair e persistir quando a fonte declarar, nulo explicito quando nao, `description_html` pela fronteira de `sanitizeRichHtml`, cobertura medida antes/depois por plataforma |
| DEB-090-03 | aberto — escopo na spec 090 req. 31 (2026-07-28) | spec 089, revisao da ficha de material pelo mantenedor, 2026-07-28 | `apps/downloads/frontend` (`pages/MaterialPage.tsx:315-319`) | **Editora/selo sem hierarquia nem afordancia de clique.** Renderizado como paragrafo solto no fim da ficha, depois do bloco Detalhes, em `text-sm` com `--fg-muted` sobre fundo neutro — cinza sobre cinza, sem link, no ponto de menor atencao da pagina. Mantenedor apontou (2026-07-28) que Amazon/Mercado Livre resolvem isso com nome em cor de link logo abaixo do titulo, e que o texto atual "parece texto minusculo". Nao e so estetica: a editora **e faceta real** do catalogo, entao o dado que mais agrupa esta no lugar que menos convida ao clique. | Escopo na 090 req. 31/31a/31b/31c: mover pra baixo do titulo, cor de link, destino no catalogo filtrado pela faceta, contraste e alvo de 44px, sem espaco morto quando ausente |
| DEB-090-04 | aberto — escopo na spec 090 req. 29 (2026-07-28) | spec 089, revisao das facetas pelo mantenedor, 2026-07-28 | `apps/downloads` (facetas de `/facets`, sidebar do catalogo) | **Autoria como faceta de selecao nao filtra e ocupa a sidebar inteira.** Medido no acervo recoletado: 80 materiais `opera_rpg`, 61 com autoria preenchida, majoritariamente unica por material — como faceta viraria lista de dezenas de entradas com contagem 1. Decisao do mantenedor (2026-07-28): autoria e **buscavel**, nao selecionavel; editora/selo continua faceta, por ser cardinalidade baixa e agrupar de fato. | Escopo na 090 req. 29/29a: remover da exposicao em `/facets` e da sidebar, manter `authors`/`author_keys` gravados pelo ingest (R30 e a busca dependem deles) |
| DEB-089-18 | 🛠 corrigido localmente na spec 089 T5.3d (2026-07-28) | 1a revisao do Codex na PR #224, 2026-07-28 | `apps/downloads/backend` (`routes/scraper.ts`), `apps/downloads/frontend` (`hooks/useScraperRuns.ts`) | **[P1] Polling do painel esgotava o rate limiter em ~3min.** `GET /runs` e `GET /run/:id` sao leitura mas usavam `writeRateLimiter` (60 req/15min); o polling de 3s gera 300 req/15min. Consequencia composta: os polls passam a receber 429, a tela congela com estado obsoleto, o botao fica travado e o proprio `POST /run` perde orcamento junto — a falha aparece exatamente quando a run esta rodando. **[P2] Exclusao de runs concorrentes era so visual.** `POST /run` inseria sem checar run ativa; duas abas, dois admins, ou o cron disparando entre dois polls observam `hasRunning=false` ao mesmo tempo e o pipeline roda em paralelo, contra o requisito sequencial do T5.4. O advisory lock de `scraperScheduler.ts:51` nao cobria: protege contra dois CRONS, nao contra cron + rota manual. | P1: rotas GET movidas para `readRateLimiter` (300 req/15min, mesmo padrao de creators/favorites/ratings) e polling condicionado a existir run ativa (`resolvePollInterval`). P2: `INSERT ... SELECT ... WHERE NOT EXISTS` resolve a exclusao na propria instrucao, sem janela entre checar e inserir; devolve 409 quando ja ha run ativa. Frontend ganhou flag derivada pra cobrir a janela entre o 202 e o refetch. Verificado: backend 33/33, frontend 21/21, lint 23/23, build 23/23, test 35/35, `verify:api` exit 0 |
| DEB-089-17 | 🛠 corrigido localmente na spec 089 T5.3d (2026-07-28) | spec 089, `verify:api` antes do commit do painel, 2026-07-28 | `scripts/api/diff-api.ts` (`getBaseVersion`) | **`api:diff` reportava breaking change FALSO em qualquer maquina com a branch `dev` local desatualizada.** `getBaseVersion` tentava os refs na ordem `['<base>', 'origin/<base>', 'FETCH_HEAD']` e parava no primeiro existente; local, `dev` existe mas envelhece — o diff saia contra um `dev` 19 commits atras. Medido: `downloads breaking=1` (`POST /api/v1/materials`, `request.body.scope.remove`) e `site breaking=1` (`path.remove`), ambos mudancas **ja mergeadas** em `origin/dev`. `git diff origin/dev -- docs/api` vinha vazio, provando que o disco batia com o remoto. Ordem herdada de antes de `fdd0b2c` (fix de CI do Codex, que tratou checkout raso e manteve a precedencia local). Risco real: falso positivo recorrente treina a ignorar o relatorio, e o breaking verdadeiro passa junto. | Corrigido invertendo para `['origin/<base>', '<base>', 'FETCH_HEAD']` — remoto e a base contra a qual a PR abre; em CI e inocuo, pois `origin/<base>` e a ref que o fetch cria. Comentario do Codex sobre FETCH_HEAD preservado. Verificado: os 6 apps passam a `breaking=0`, exit 0; lint 23/23 e build 23/23 |
| DEB-089-16 | 🛠 corrigido localmente na spec 089 T5.3d (2026-07-28) | spec 089, execucao da Fase 5 pelo mantenedor, 2026-07-28 | `apps/downloads/frontend` (`pages/gestao/GestaoPlataformasPage.tsx`, `hooks/useScraperRuns.ts`) | **Recoleta so podia ser disparada colando `fetch` no console do navegador — nao havia UI.** Custo real medido nesta rodada: a primeira tentativa usou `/api/v1/scraper/run` e devolveu **404 com HTML do frontend**, porque o router e montado em `/api/v1/admin/scraper` (`server.ts:109`); o path foi montado a partir do `router.post('/run')` sem conferir o prefixo. Falha aconteceu com o acervo de Beta **ja truncado**, dentro da janela destrutiva. Mantenedor relatou que a operacao manual ja se repetiu varias vezes e sempre reintroduz esse tipo de erro. | Decisao do mantenedor (2026-07-28): resolver na raiz com painel, aceitando o ciclo completo de deploy — escopo ampliado **dentro da 089** como T5.3d. Implementado local: seletor filtrado por `supports_auto_scrape`, polling de 3s (disparo e fire-and-forget, 202), coluna de aceite aplicando os seis criterios do T5.4. Aberto: `verify:api`, commit/PR/deploy sob autorizacao nominal |
| DEB-089-03 | 🛠 corrigido localmente na spec 089 F3 (2026-07-27) | spec 089, achado ao diagnosticar o `catalog_material_type_not_found`, 2026-07-27 | `apps/downloads/backend` (`scraperIngest.test.ts:85`) | **Mock esconde o defeito que quebrou o runtime.** O teste mocka o catalogo com `nao-classificado` presente, entao passa verde enquanto a run real aborta com `catalog_material_type_not_found`. Foi exatamente o que aconteceu em beta: teste 352/352 verde, scraper falhando em 10ms. A causa real era o site responder 404 em `/api/catalog/v1/material-types` (deploy defasado), fazendo o downloads cair no `MATERIAL_TYPES_ROLLOUT_FALLBACK`, que so tem `aventura`. | código local em `fix/089-fases-2-3`; T3.7 exige smoke com serviço real na Fase 5 |
| DEB-088-04 | ✅ resolvido 2026-07-27 | spec 088, 2ª rodada de review da PR #218, 2026-07-27 | `apps/site` (`db/repo/catalog.ts`, `server/catalog-api.ts`) + `apps/downloads/backend` (`catalogClient.addCatalogNodeAlias`) | Acrescimo de alias de SISTEMA nao era atomico: `addCatalogNodeAlias` lia os aliases, concatenava e reenviava a lista inteira, e `replaceAliases` faz `DELETE`+`INSERT`. Duas aprovacoes simultaneas de sugestao pro MESMO node perdiam um dos aliases (as duas sugestoes ficavam `approved`, so um alias sobrevivia, o raw_value perdido voltava pra fila). Mesmo defeito que o Codex achou no fluxo de TIPO (requisito 56d). | **Resolvido:** `addAliases` novo em `catalog.ts` (`INSERT ... ON CONFLICT (node_id, lower(alias), COALESCE(locale,'')) DO NOTHING`, sem migration — o indice unico existe desde a 006), campo `add_aliases` no PATCH de `/api/admin/v1/catalog/nodes`, e o Downloads deixou de ler antes de escrever. `replaceAliases` intacto para edicao admin deliberada (remover alias errado continua possivel). Escopo real era menor do que este debito supunha: **so o Downloads usa `addCatalogNodeAlias`** — `mesas` e `glossario` fazem create/update com lista completa, nunca append, e o campo novo e aditivo. Validado: site 64/64 (4 testes novos contra Postgres real via PGlite, incluindo o cenario concorrente), downloads 352/352, **mesas 607/607, glossario 22/22** |
| DEB-088-05 | ✅ resolvido 2026-07-27 (reaberto e refechado no mesmo dia) | spec 088 Fase 4, 2026-07-27 (achado ao verificar T5.0) | `apps/site` (`docker-entrypoint.sh`), infra/deploy | **Migration nova do site fica pendente sem erro visivel.** O guard de resiliencia de restart (spec 009 R6/D049) faz `exec pnpm run serve` quando `dist/index.html` existe, e `pnpm run migrate` mora DEPOIS desse `exec` — todo restart de container existente pula a migracao. So container novo (sem `dist`) ou `SITE_FORCE_REBUILD=true` aplica. Descoberto porque `015`/`016` (taxonomia central) estavam mergeadas mas ausentes em beta E prod, com os dois containers `Up (healthy)` de 2026-07-20 o tempo todo. Nao ha alarme: nem log de deploy, nem healthcheck, nem CI detectam a defasagem. O guard em si esta correto pro que foi desenhado (evita re-import WP + rebuild Astro a cada OOM/reboot); o efeito colateral e que nao ha caminho automatico de migracao para container ja no ar. Ver [[E018]] | decidir o desenho com o mantenedor: (a) mover `pnpm run migrate` para ANTES do guard de `dist` (migracao sempre roda, rebuild continua condicional) — corrige a raiz e mantem o restart instantaneo, ja que `migrate` e idempotente e barato; (b) checagem de drift no deploy que compare `db/migrations/` com `schema_migrations` e falhe o job; (c) manter manual e documentar a verificacao obrigatoria pos-merge. **Resolvido pela opcao (a), autorizada pelo mantenedor 2026-07-27:** `pnpm run migrate` movido para ANTES do guard de `dist` no `docker-entrypoint.sh`. Migrar sempre nao custa o restart instantaneo — `db/migrate.ts` e no-op sem pendencia; o guard segue protegendo o REBUILD (export + astro build + pagefind), que e o caro. Validado por smoke que discrimina: ordem antiga nao executa o migrate (defeito reproduzido), ordem nova executa e ainda serve direto. `015`/`016` aplicadas em beta e `015` em prod pelo procedimento pontual de [[E018]] (container defasado nao enxerga arquivo novo, entao `docker exec ... pnpm run migrate` retornava `0 new` sem aplicar nada). `016` fica fora de prod ate a spec 088 ser promovida a `main`. **REABERTO no mesmo dia** (achado do Codex na review da PR #219): a opcao (a) sozinha NAO fecha o E018. Site e `auto_deploy_on_push: false`, entao merge/promote nao deploya; e container existente roda a IMAGEM antiga, cujo `db/migrations/` nao tem o SQL novo — `migrate` sai com `0 new` sem aplicar. Container novo ja migrava antes da mudanca (o `dist` e excluido da imagem, entao o guard nao pegava). **Refechado pela opcao (b):** `scripts/deploy/check_migration_drift.sh` roda no `_deploy-module.yml` apos o health-check e falha o deploy com rollback se disco e `schema_migrations` divergirem, nas duas direcoes. Validado contra bancos reais (drift detectado em site-prod e site-beta; caso limpo em downloads-beta 29/29). Entrou junto a remocao do importador WP, na mesma branch |
| BL-CONFIG-AUTH | aberto | `specs/019` B2/B3 | `packages/config`, `packages/auth` | dominios/projetos canonicos + auth HTTP client compartilhado | SDD Completo; smoke todos consumidores SSO |
| BL-ANALYTICS | fechado | `specs/019` B5, spec 032 (historica, superada), **D117 2026-07-20** | `packages/analytics`, site/glossario/mesas/downloads | FECHADO 2026-07-20: GA4 mudou de property unica (D020) para 1 property por app (D117) apos investigacao de "GA4 sem dados". PR #185 merged: site `G-2H50PCM950`, mesas `G-43T3C6K6V6`, glossario `G-YVHMJEVTE4`, downloads `G-2H50PCM950` (provisorio). `.env` prod stale da VM corrigido (achado Codex). Ver `sessoes/26-07-20_1_analytics_ga4-property-por-app.md` | pendente: stream GA4 proprio pra downloads (mantenedor); confirmar Realtime GA4 recebendo hits pos-deploy prod |
| BL-DOWNLOADS-BETA-VOLUME-MISMATCH | fechado | `sessoes/26-07-20_1_analytics_ga4-property-por-app.md` (registro original), spec 087 Fase 1, 2026-07-26 | `apps/downloads`, infra VM (docker volumes) | FECHADO 2026-07-26: **premissa do debito nao valia mais** — beta se recuperou em deploy posterior sem ninguem fechar a linha. Estado verificado: 3 containers `Up (healthy)` (`downloads-beta-app`/`-api`/`-db`), API `GET /api/v1/materials` HTTP 200 com dado real, front HTTP 200. Volume ativo `downloads-beta_pgdata_downloads_beta` (65MB): banco `downloads` (nao `downloads_beta` — nome do debito estava errado), **103 materiais, 28 migrations**. Volume orfao `downloads_pgdata_downloads_beta` (47MB) inspecionado via container temporario read-only: **0 materiais, 19 migrations** (schema parcial), residuo do deploy que o guard `MAX_AUTO_PENDING` bloqueou em 2026-07-12. Sem dado a recuperar. Orfao **removido** com aprovacao nominal do mantenedor (`docker volume rm downloads_pgdata_downloads_beta`); smoke pos-remocao confirmou beta integro (3 healthy, 103 materiais, API/front 200). | nada pendente. Nota: `docker inspect <container> --format '{{range .Mounts}}...'` e o jeito certo de saber qual volume esta em uso antes de mexer — `docker volume ls` sozinho nao distingue ativo de orfao |
| BL-MIGRATION-TRACKING-DUPLICATE-INSERT | fechado | `sessoes/26-07-20_1_analytics_ga4-property-por-app.md`, achado durante deploy prod GA4 (nao relacionado) | `scripts/deploy/apply_required_migrations.sh` (compartilhado por todos os modulos) | FECHADO 2026-07-20: `migration_118_discord_drafts_invariant.sql` reapareceu na lista PENDING apesar de ja estar registrada em `schema_migrations` prod desde 2026-06-01 (confirmado read-only); `INSERT INTO schema_migrations` (linha 134, sem `ON CONFLICT`) violou unique constraint, derrubou deploy mesas prod (rollback recuperou o servico). Fix: `ON CONFLICT (migration_name) DO NOTHING` no INSERT, validado contra prod real (INSERT 0 0, registro original intacto). Causa exata da corrida (por que PENDING incluiu migration ja aplicada) nao isolada — mitigada, nao raiz | reexecutar deploy mesas prod para confirmar; se o falso-positivo em PENDING se repetir, investigar timing/concorrencia entre calculo de PENDING e commit anterior |
| BL-SEO-SHARED | aberto | `specs/019` B6 | `@artificio/content`, `@artificio/config` | helpers SEO por projeto/modulo alem do site | criar spec shared quando novo app publico precisar |
| BL-NORMALIZERS | aberto | `specs/019` B7 | site/site-admin/apps | issues por app para normalizacao de payload externo | iniciar por site/site-admin |
| BL-COPY-PUBLICA | aberto | `specs/019` B8/FSU-013 | copy publica compartilhada | decidir se textos publicos repetidos devem virar pacote/helper compartilhado ou permanecer por app | registrar decisao antes de criar abstracao |
| BL-GLOSSARIO-LEGACY-CLEAN | aberto | D061/project-state | `apps/glossario` | limpeza futura de `password_hash` legado apos migracao SSO estabilizada | spec/migration propria com backup e smoke |
| BL-GLOSSARIO-NONGOOGLE-E2E | baixo | `specs/015-glossario-sso-compat/` | `apps/glossario` login legado/migracao | E2E browser opcional para cenario seedado de usuario nao-Google/compat legado | fazer apenas se reabrir fluxo legado D061 ou houver regressao |
| BL-SITE-ADMIN-TS-VARIANCE | baixo | `sessoes/26-06-17_3_site_media-finalize.md`, commit `e7d737a` | `apps/site/server/admin-api.ts:220` | `tsc --noEmit` aponta `error TS2345` em `upload.single("file")(req,res,cb)` (multer): route handler infere `Request<{}>`, multer espera `RequestHandler<ParamsDictionary>`. Variancia de tipo conhecida multer x express. ZERO impacto operacional: nao esta no gate CI (`import`/`export` rodam via `tsx` sem typecheck; `build`=astro so frontend; `lint`=stub; sem script `typecheck`); runtime executa normal (upload de midia admin funciona). | dívida cosmetica: tipar `req` como `Request` generico no handler ou alinhar versao @types/multer; nao bloqueia nada, fazer junto de outra fatia do site server |
| BL-SITE-ADMIN-WP-PUBLISH-GUARD | aberto | `sessoes/26-06-17_3_site_media-finalize.md`, review PR #54, `apps/site/server/admin-api.ts` | `apps/site/server/admin-api.ts` (save `/posts` `/pages`, status `/posts/:id/status` `/pages/:id/status`), `db/export.ts` | Lacuna de ciclo de vida (D074): o importador so poda conteudo WP-publicado; o gate residual e publish-only (correto p/ disponibilidade — alargar p/ draft derrubaria o boot via `set -e`). Mas o admin pode CRIAR/EDITAR um draft com `wp-content/uploads` (save roda `cleanHtml`, NAO `pruneWpAssets`) e depois PUBLICAR via `/status` pos-EOL -> `export.ts` serve HTML/featured/OG com link WP morto. Fora do escopo do importador descartavel. | guard na camada admin: rodar `pruneWpAssets`/rejeitar `wp-content/uploads` no save e/ou na transicao para `publish`; ou validar no `export.ts`. SDD Lite; smoke do editor. Prioridade sobe se o admin do site voltar a ser usado para autoria com colagem de conteudo WP |
| **BL-033-SECRET-BLOCK** | fechado | 2026-06-19 push rejeitado + `specs/045-debitos-pendentes-deploy/` T1, PR #85 commit `289364d` | `.gitignore`, `artifacts/*` (16 arquivos) | FECHADO 2026-06-22 (spec 045 T1, PR #85): `.gitignore` `artifacts/lighthouse/` → `artifacts/` + `git rm --cached` nos 16 (15 `artifacts/033/*` + 1 `artifacts/cloudinary/*`); conteúdo no disco intacto. `git ls-files artifacts/` = 0. Incidente original (dump) já encerrado (E007). | — (merge PR #85) |
| BL-ASTRO6-CSP | fechado (2026-06-19) | T66b, `specs/033-.../breaking-changes.md` §12b, `apps/site` Astro 6.4.8 | `apps/site/astro.config.mjs` (`security.csp`) | Astro 6 CSP usa `<meta>` tag, nao HTTP header — funciona em SSG sem adapter. Config: 5 hashes SHA-256 auto (inline scripts) + diretivas `default-src 'self'`, `img-src data: cloudinary`, `connect-src accounts. google-analytics`, `script-src googletagmanager`. 46/46 paginas. turbo 13/13 verde. | — |
| **BL-AUDIT-033** | absorvido por `BL-SEC-AUDIT-DEPS` (spec 039) | T67 2026-06-19, `pnpm audit --prod`, `specs/039-sec-audit-deps/` | glossario-frontend (`xlsx`, `axios>form-data`), mesas-frontend (`dompurify`, `nanoid`), glossario-backend (`exceljs>uuid`) | ABSORVIDO 2026-06-21: a spec 039 (`BL-SEC-AUDIT-DEPS`) remediou form-data/dompurify/nanoid/uuid (nanoid 5 + uuid 11 funcionaram via override, **não** bloqueados upstream como se previa). xlsx segue na spec 034. | seguir `BL-SEC-AUDIT-DEPS` + spec 034. |

| BL-AUDIT-CHANGELOG-ABORT-NOOP | fechado | auditoria changelog 2026-06-21, `apps/mesas/frontend/src/components/ChangelogModal.tsx:74-77` | mesas-frontend | AbortController criado sem signal no fetch. Corrigido: fetch inline com `{ signal: controller.signal }` + retry separado com `useCallback`. | — |
| BL-GLOSSARIO-CHANGELOG-CLEANUP | aberto | auditoria changelog 2026-06-21 | `apps/glossario/database/` — scripts `insert_changelog*.sql/.sh` obsoletos, DDL `update_log` em `init.sql` | Limpar artefatos do `update_log` + dropar tabela no DB apos deploy confirmado. | SDD Lite; dropar tabela com aprovação |

## Debitos fechados ou absorvidos (nao acionar sem nova evidencia)

| ID | Estado | Origem | Observacao |
|---|---|---|---|
| D-UX1 | fechado | spec 017 | Icone lua/sol em `accounts` entregue em prod. |
| D-UX3 | fechado | spec 017 | Texto duplicado do glossario/rodape tratado. |
| D-MARCA1 | fechado | spec 018 / D063 | Linguagem publica "projetos" entregue. |
| D-INFRA1 | fechado | spec 017 | Favicon fonte unica em `packages/ui`. |
| D-INFRA2 | fechado como auditoria | spec 019 | Gerou backlog derivado; nao e item de implementacao sozinho. |
| BL-REALIP-023 | fechado | `specs/023-infra-real-ip-contract/`, sessao 26-06-15_1 | PR #39 mergeado; `dev -> main`; deploy prod accounts/mesas/glossario verde; `nginx -t`, config renderizada e smokes HTTP beta/prod OK. |
| D-NGINX1/D-NGINX2 | fechado | spec 023 / D069 | Virou contrato Real IP geral; fechado por BL-REALIP-023. |
| D-SHELL1b | fechado | release 2026-06-14 | Shell capado do glossario resolvido em prod. |
| D-MESAS1 | fechado | release 2026-06-14 / `project-state.md` | Arquivar mesas e `MESAS_CRON_SECRET` foram promovidos; reabrir so com nova evidencia. |
| D-CONT1 | fechado/absorvido | `sessoes/26-06-12_2_debitos_ux-marca.md`, release 2026-06-14 | Changelogs entraram no acumulado publicado; reabrir so se smoke visual mostrar ausencia. |
| D-MESAS-UI1 | fechado | `sessoes/26-06-12_2_debitos_ux-marca.md`, `apps/mesas/frontend/src/components/TableCardDashboard.tsx` | Botao/badge Arquivar usam `--artificio-bronze`; `bg-slate-600` removido do componente. |
| BL-UI-B2-STATIC | fechado | `specs/020` B2/T11, `sessoes/26-06-15_3_ui-020-b6-b2.md` | `@artificio/ui/static` criado; site Astro consome favicon/logos/nav por subpath data-only; `ui` e `site` builds verdes; deploy beta + validação manual logada OK. |
| D-UX2 | fechado | `sessoes/26-06-13_1_ui-theme-020-b6b7-e2e-prod.md`, `specs/020` B6/B7 | Glossario dark e mesas light foram promovidos; B6 aprovado pelo mantenedor em prod, B7 validado pelo mantenedor em 2026-06-15. |
| BL-UI-B6-E2E | fechado | `specs/020` B6, `sessoes/26-06-13_1_ui-theme-020-b6b7-e2e-prod.md` | Glossario dark readiness com dados/admin/forms/selects/validacao aprovado pelo mantenedor em prod; evidencias AA e fluxos registrados na sessao. |
| BL-UI-B7-E2E | fechado | `specs/020` B7, validação mantenedor 2026-06-15 | `/perfil` light com dados + mestre com `banner_url` custom validados em prod pelo mantenedor. |
| D-GLOS-CTA | fechado/stale | commit `652cf20` | CTA logado do glossario ja corrigido; resta so validacao visual se suspeita nova. |
| D-CSS1 | fechado local/site-only | sessao 26-06-12_2 | Higiene local; residual compartilhado de fontes e BL-UI-B12-FONTS. |
| D-MARCA2 | superado/absorvido | D064/spec 020 | Canonico atual e `#FF5722`, nao reabrir sem nova decisao. |
| BL-DUPLICATE-042 | fechado | `specs/042-duplicate-code-refactor/`, `sessoes/26-06-21_5` | `packages/feedback` (novo) + `apps/mesas/backend` + `apps/glossario/backend` + `apps/site/server` | FECHADO 2026-06-21: PR #83 mergeada em `dev` (154 arquivos, +7663/-3040). Deploy beta mesas/glossario/site disparado. cpd: 5.57% → 4.60% (-411 linhas, -18 clones). 13 revisões de PR resolvidas. `packages/feedback` CJS+ESM dual build, `actorNameResolver` fonte única, `suggestionHelpers` 3 factory functions. Build 17/17, lint 14/14, testes 219/219. | — |
| BL-DOCS-AUDIT-024 | fechado | `specs/024-docs-specs-backlog-audit/`, `sessoes/26-06-15_2_docs-specs-backlog-audit.md` | Auditoria documental criada e aplicada; backlog verificado e atualizado. |
| BL-LINKS-VISUAL-AUDIT-043 | fechado | `specs/043-links-visual-audit/`, `sessoes/26-06-21_6`, PR #84 | apps/links + packages/ui | FECHADO 2026-06-22: PR #84. F1-F4 completas. 14 DEBs resolvidos + 17 REVs resolvidos. 28 issues de auditoria. Build 17p verde. | — |
| DEB-001 a DEB-014 | fechados (14 débitos spec 043) | `specs/043-links-visual-audit/debitos.md`, PR #84 | apps/links + packages/ui | Todos resolvidos (2026-06-22). DEB-001 a DEB-005: (ReportButton→UI, Escape, --error token, inputs, spinner). DEB-006 a DEB-013: (drawer mobile, breakpoint 220px, chips categoria, 404, voltar-ao-topo, transição tema, accordion regras, onboarding). DEB-014: âncoras sidebar prefixo `/`. | — |
| D-LINKS-REPORT-UNDO-ERROR | fechado (já resolvido — spec 043 T10) | CodeRabbit PR #78/#84, `ReportButton.tsx:100-104` | `apps/links/src/components/ReportButton.tsx` | Major: `setError` no catch do `onUndo` não era renderizado no estado `done`. Corrigido pela refatoração do T10 (PR #84, commit `286c1ba`): modal `done` agora é `<Modal>` separado com renderização de `error` na linha 194. | Nenhuma ação. |
| D-LINKS-REPORT-TRIGGER-FOCUS | fechado (falso positivo) | CodeRabbit PR #78, `global.css:627-640` | `apps/links/src/styles/global.css` | `.report-trigger` é `<button>`; `button:focus-visible` em global.css:618 já cobre com `outline: 3px solid var(--brand-deep)`. Bot revisou só o diff, não o arquivo inteiro. | Nenhuma ação. |

## Documentacao / Governanca — spec 046

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| DEB-046-01 | fechado (2026-06-22) | `specs/046-arquiteture-docs-sync/debitos.md` DEB-001 | `.specify/arquiteture.md` | `arquiteture.md` nao tem metadata de ultima verificacao. | ✅ T11 implementado: `> **Última verificação:** 2026-06-22` adicionado no topo. |
| DEB-046-02 | fechado (2026-06-22) | `specs/046-arquiteture-docs-sync/debitos.md` DEB-002 | `.specify/arquiteture.md` + `.claude/skills/add-module/SKILL.md` | `module.manifest.ts` referenciado como contrato em docs desatualizados. | ✅ `arquiteture.md` §1/§2 corrigidos + `add-module` SKILL.md §3 substituído por "Registrar em packages/ui/src/modules.ts". Remover codigo morto do mesas = spec futura (fora do escopo). |
| DEB-046-03 | aberto | `specs/046-arquiteture-docs-sync/debitos.md` DEB-003 | `packages/ui/src/modules.ts` | Nav contem apps inexistentes (Downloads, Esferas, SRD) com URLs `artificiorpg.com`. | Decidir se remove ou mantem como placeholder. |
| DEB-046-04 | investigado — majoritariamente falso positivo | `specs/046-arquiteture-docs-sync/debitos.md` DEB-004 | `apps/site-admin/` | `site-admin` zero deps `@artificio/*`. Investigado (2026-06-22): NAO e app independente — SPA servida pelo Express do site em `/admin`. Isolamento arquitetonico correto (cookie same-origin, admin panel propria, sem GA4). Unico ponto real: URL hardcoded em `api.ts:7` (severidade baixa, env override presente). | Usar `@artificio/config` para `ACCOUNTS_ORIGIN` (prioridade baixa). |

## Smoke beta 048 (Discord Sync) — achados 2026-06-26

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| DEB-048-30 | corrigido local (aguarda PR) | smoke beta 2026-06-26, log `mesas-beta-api`, `specs/048-mesas-discord-chat-exporter-json/debitos.md` | `apps/mesas/backend/src/discord/syncDiscordDraftToTable.ts` | 🔴 BLOQUEADOR: sync Discord→mesa dava 500 (snowflake em `tables.source_id` uuid). Fix `getSourceId: message.id` aplicado local. | Teste de regressão de sync Discord + entrar no próximo PR + smoke real pós-deploy. |
| DEB-048-31 | aberto (design+impl) | smoke beta 2026-06-26, `status: expired_url` | `apps/mesas` import+cover+preview, Cloudinary | URL Discord CDN é assinada/efêmera → preview quebra + upload 404. Suba ao Cloudinary no IMPORT (URL fresca), promova no sync. | Decisão de custo Cloudinary p/ drafts órfãos + impl SDD Lite. |
| DEB-048-32 | ✅ fechado (WS4 2026-06-27) | log beta `EACCES /app/logs/routes.log` | `apps/mesas/backend` requestLogger/Dockerfile/compose | requestLogger falha em toda request (perm de volume). | FECHADO: `requestLogger` degrada p/ stdout (flag `loggerDisabled`, sem re-tentar) + Dockerfile `mkdir/chown /app/logs` + compose `./logs` no cron. REV-033 (volume nomeado) cancelado won't-fix: bind `./logs` é preferência operacional. |
| DEB-048-33 | implementado local (aguarda PR) | mantenedor 2026-06-26 | `apps/mesas/backend/src/discord/parseDiscordAnnouncement.ts` | Priorizar banner landscape em 2+ imagens. Implementado (maior razão w/h) + fix `filename`. | Teste retrato+paisagem → escolhe paisagem; entrar no próximo PR. |
| DEB-048-34 | aberto (UX/impl) | mantenedor 2026-06-26 | `apps/mesas/frontend` | Unificar UI inbox dentro de "Discord Sync" (backend já DRY). | Decisão UX + impl SDD Lite. |
| DEB-048-35 | aberto — precisa decisão | mantenedor 2026-06-26 | `apps/mesas` (opção 1) OU `accounts.`/`packages/*` (opção 2) | Local p/ API key DeepSeek (admin universal). Opção 1 por-módulo (SDD Lite) vs opção 2 central (SDD Completo+aprovação). | Mantenedor escolhe 1 vs 2 antes de codar. Key sempre cifrada. |
| DEB-048-36 | ✅ corrigido (hotfix `fix/048-config-cjs-exports`) | deploy mesas beta run 28280222833 falhou: `ERR_PACKAGE_PATH_NOT_EXPORTED` `@artificio/config/secret-crypto` | `packages/config/package.json` | `exports` de `./secret-crypto` (e `.`) só tinha condição `import` (ESM); mesas-backend é CJS → `require()` quebrou no boot → crash-loop beta. tsc+vitest passaram (não exercitam `require` CJS do dist) → CI green enganoso. | FECHADO: +`"require"` espelhando `@artificio/media`. **Gap de CI a cobrir (DEB-048-37).** |
| DEB-048-37 | **→ spec 053 (DEB-053-03)** | incidente DEB-048-36 | CI / `packages/*` consumidos em CJS | Nenhum check exercita `require()` CJS do pacote compilado (`dist`) respeitando `exports`. tsc e vitest não pegam condição faltante → bug só aparece no runtime do container. | Transferido p/ spec 053 Frente C. Adicionar smoke de resolução (`require('@artificio/config/secret-crypto')` num runner CJS) no CI. |
| DEB-048-38 | **→ spec 053 (DEB-053-02)** | mantenedor 2026-06-27 (deploy prod) | `apps/accounts/frontend` (tema) | Ao entrar no accounts, tema fica preso em **light** (persistente light); não respeita preferência/toggle de tema do design system. | Transferido p/ spec 053 Frente B. Investigar theme provider/persistência + default; alinhar com `packages/ui`. |
| DEB-049-D06 | **→ spec 053 (DEB-053-01)** | auditoria spec 049 | `apps/mesas/frontend` (revisão gestão) + `packages/ui` | 8 issues P1 de a11y/UX (modal sem Escape/trap/role, labels checkbox, focus visível, dirty-state, window.confirm, navegação automática, design system, erro p/ leitor de tela). | Transferido p/ spec 053 Frente A. SDD Completo (toca `packages/ui`). |
| BL-053-CONSOLIDADO | 🟢 fechado | consolidação 047–051 (2026-06-27) | transversal (mesas FE, accounts FE, `packages/ui`, CI) | FECHADO 2026-06-30: concentrou débitos das 047–051. Frentes A (a11y gestão), B (tema/conta accounts), B2 (busca site), C (smoke CJS CI), D (doc 051), E (ingestão→052 Bloco A). O que não foi feito foi descartado. **052 destravada.** | — |
| BL-054-GESTAO-IA | 🟢 fechado | mantenedor 2026-06-27 | `apps/mesas/frontend` (`/gestao`) | FECHADO 2026-06-30: reorganização da IA do admin: abas-topo → sidebar persistente; 6 grupos; rotas aninhadas; renomeações; padrão de botões; moderação unificada. Fases 1-4 + reviews 001-025. PR mergeada em `dev` → deploy beta + smoke + promote prod ✅. O que não foi feito foi descartado. **053 Frente A destravada.** | — |

## Spec 079 (mesas, parser texto colado + pré-preenchimento público) — 2026-07-16/17

| ID | Status | Origem | Escopo | Falta para fechar | Proximo passo |
|---|---|---|---|---|---|
| BL-079-PARSER-TEXTO | FECHADO | `specs/079-mesas-import-texto-polimento/`, PR #172 (Fases 1-5), PR #174 (Fase 6 + fixes review) | `apps/mesas/backend` (segmentation, normalizeLooseText, parseDiscordAnnouncement, parseTextForPreview, gmPanel, learningRules, learningFeedbackOutbox, fieldLearning, routes/discord/utils) | FECHADO 2026-07-17: Fases 1-6 implementadas, commitadas (`038f51e`/`2b139d3`), mergeadas em `dev` via PR #174, deploy beta+prod verdes. T5.12/T4.4 (smoke manual real) confirmados pelo mantenedor em produção. Fase 6: `rules_notes` propagado ponta-a-ponta, VTT "Roll 20"/"Roll20" corrigido, mecanismo de aprendizado generalizado (`recordEntityHintRule`, `ENTITY_HINT_FIELDS` + enums simples). 607/607 testes backend verdes, tsc/lint/verify:api limpos. | — |
