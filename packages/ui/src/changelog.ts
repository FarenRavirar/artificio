// Contrato de changelog re-exportado do pacote leaf @artificio/changelog.
// Fonte única; backends CJS consomem o leaf direto (build dual ESM+CJS), o
// frontend continua importando via @artificio/ui (este barril) sem mudanca.
export * from "@artificio/changelog";
