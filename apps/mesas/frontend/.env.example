VITE_API_URL=""

# Alvo da API para o SSR (`loader` rodando no servidor). Em produção/beta o
# `docker-compose` manda o nome do container (`mesas-api`/`mesas-beta-api`); no
# `react-router dev` local o padrão já é `localhost`, então só preencha para
# apontar para outro host.
# API_UPSTREAM=localhost
# API_UPSTREAM_PORT=3000
VITE_PUBLIC_SITE_URL="https://mesas.artificiorpg.com"
VITE_ACCOUNTS_URL="https://accounts.artificiorpg.com"
VITE_CLOUDINARY_CLOUD_NAME=""
VITE_CLOUDINARY_UPLOAD_PRESET=""
VITE_GA_ID=G-43T3C6K6V6
