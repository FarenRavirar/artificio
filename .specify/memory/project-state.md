# Estado do Projeto — Artifício RPG

> Atualizar a cada mudança de estado operacional. Fonte de verdade do "onde estamos".

## Fase atual
**Fase 2 — monorepo + SSO ✅ Gate B APROVADO (D037, 2026-06-04).** Fase 1 ✅. `accounts.artificiorpg.com` no ar (SSO Google/JWT), cross-subdomínio provado, CI/CD `deploy-accounts.yml`. CDX-307/D038 design system real em mudanças locais. **CDX-308A+B concluídos localmente; CDX-308C deploy técnico concluído:** `mesas.artificiorpg.com` agora roda build monorepo, usa UI G1, backend valida `artificio_session` via `@artificio/auth`, `JWT_SECRET` igual ao accounts, smoke `200/200/401` e login legado redireciona para `accounts.`. **Falta p/ fechar Gate D:** E2E browser real Google/logout + allowlist prod de `accounts` (D037 local ainda precisa redeploy).

## Gates (ativos: A, B, D · Gate C adiado — D016)
- ✅ **Gate A** — Backups completos/verificados/off-VM. **APROVADO pelo mantenedor 2026-06-04.** Libera Fase 1.
- ✅ **Gate B** — SSO (`accounts.`) no ar + cross-subdomínio provado. **APROVADO pelo mantenedor 2026-06-04 (D037).** Libera import de conteúdo / construir módulos. (Integração de módulo real ao SSO = Gate D.)
- [ ] **Gate D** — (por módulo) smoke → próximo módulo.
- ⏸️ **Gate C (adiado/futuro)** — Site validado em beta → cutover DNS raiz + desligar WP. **Fora do escopo destes ~3 meses.** WP intocável todo o projeto.

## Decisões fechadas
- Monorepo único `artificio` (pnpm + Turborepo).
- **Topologia: subdomínio-por-módulo (D017, supera D002/D015).** Cada módulo no próprio subdomínio (`glossariorpg.`, `mesas.`, `downloads.`, `esferas.`, `srd.`, `links.`), root próprio, sem basename. Blog em `beta.artificiorpg.com` (→ raiz futuro). WP na raiz `artificiorpg.com` (intocável). Cloudflare Tunnel hostname→container.
- **SSO central em `accounts.artificiorpg.com` (D018):** 1 OAuth Google, cookie JWT `Domain=.artificiorpg.com` (vale em todos subdomínios).
- Blog na raiz = aposta SEO (D019). Search Console Domain property + GA4 cross-subdomínio (D020).
- **Blog beta→raiz adiado (D016/Gate C)** — só o blog converge; módulos não. Fora do escopo agora.
- WordPress roda em paralelo; corte DNS só no Gate C.
- CMS: store nativo Postgres + importador WP one-shot (descartável pós-cutover).
- Blog: pré-render estático (SSG) com rebuild incremental disparado pelo admin (não SSR).
- Stack canônica = stack do mesas (React19/Vite/Tailwind + Express/Kysely/PG16/Cloudinary).
- Infra: Oracle 24GB/200GB, Docker, Cloudflare Tunnel, GHCR, Watchtower(beta).
- Backup destino: `C:\projetos\artificiobackup` (local, 300GB livres). uploads WP ≈ 6.34GB.
- Acesso DB da VM via RaiDrive (read-only por padrão).
- Site sobe primeiro como `beta.artificiorpg.com`; demais serviços integram em versão principal.

## Ativos existentes (origem)
- `C:\projetos\mesas_rpg_artificio` — referência de robustez (SDD, Actions, deploy). Repos beta+prod no GitHub.
- `C:\projetos\glossario_rpg_artificio` — glossário. Repos beta+prod no GitHub.
- `links.artificiorpg.com` + `servidorvirtual.artificiorpg.com` — página TS única, **sem GitHub** (backup pendente).
- Site `artificiorpg.com` — WordPress, **sem GitHub**, 300+ posts (backup + import pendente).
- Tradução Spheres of Power (Drop Dead Studios) → módulo `esferas` (`esferas.artificiorpg.com`), multi-sistema D&D 2014/2024 + Pathfinder futuro (D028).
- SRD DnD 5.2.1 (regras 2024) → futuro módulo `srd`.

## Construído neste monorepo
- Governança: `AGENTS.md`, `.specify/memory/{constitution,project-state,errors,decisions}.md`, `.specify/arquiteture.md`, `docs/agents/{operating-model,context-capsule,token-economy}.md`, `.claude/agents/*` (3), `.claude/skills/*` (2), `README.md`, `.gitignore`, `sessoes/index.md`. ✅ coração de governança + economia de contexto fechado.
- Código de aplicação: `packages/{config,auth,ui}`; `apps/accounts` SSO; `apps/mesas` importado do legado (`frontend` + `backend` + DB/scripts/docs), ainda pendente de integração SSO/UI.

## Próximo passo
**Fase 2 CDX-301..306 executados e Gate B aprovado:** monorepo + `packages/{config,auth,ui}` + `apps/accounts` SSO deployado. `accounts-api`/`accounts-db` rodam em `/opt/artificio/accounts` na `artificio_net`; Cloudflare `accounts.artificiorpg.com` smoke 200/200/401; OAuth criou user; `/me` com sessão válida 200; cross-subdomínio via `@artificio/auth verifyToken` OK. **CDX-308A+B+C técnico:** `apps/mesas` importado/buildado/integrado e deployado em `mesas.artificiorpg.com`; smoke técnico OK. **CDX-309E concluído:** 1º deploy real do `mesas` via `deploy-mesas.yml workflow_dispatch mode=deploy` passou no run `26994910262`; snapshot criado, migrations online-safe `125/126` aplicadas, build/recreate/health/smoke OK. Próximo: **E2E browser Google real + logout + allowlist prod** → Opus valida Gate D mesas. **CDX-311 DEPLOYADO + marca validada no ar** (PR #2 merged; deploy-accounts/mesas verdes; CSS/JS servem `#FF9457`/`#020740`/`#1B2A4A`; hex velho sumiu). **Spec 005 (esteira beta, D041/D042) em execução:** T1/T3/T4/T5/T6/T7/T7b/T8/T9/T10/T11 feitos; T2 branch protection bloqueado pelo GitHub em repo privado sem Pro/repo público (API 403), compensação = gate `main ⊆ dev` + alarme. T11 estrito passou no run `27030832884`: `mesasbeta.` 200/401/302 com `return=mesasbeta`, prod 200/401/302 com `return=mesas`, snapshot/migrations/health OK. Promoção prod deve preservar invariante: usar workflow `promote-prod-fast-forward.yml` ou "Rebase and merge", nunca squash/merge commit. **Pendente:** hydrate aprovado + T12 E2E beta. **CDX-310:** reconciliar `deploy-accounts.yml` ao `_deploy-module.yml` e substituir shim operacional de `apps/accounts/docker-compose.prod.yml` por arquivo versionado correto. **Spec futura — auditoria visual cross-módulo (dedicada/token-eficiente):** auditar consistência visual+marca de TODOS os módulos contra `@artificio/ui` (cores, logo, header/footer, contraste AA, layout) — rodar como gate de validação antes de promover páginas; reaproveitar `seo-usability-auditor` como base. Pendência segurança (mantenedor trata): rotacionar tunnel token, PAT, WP creds.

## Log
- 2026-06-05 — **Módulo `site` — `packages/analytics` (GA4 cross-subdomínio, D020).** Pacote `@artificio/analytics` (TS puro, zero deps): `gtagSrc`/`gtagInlineConfig` (cookie_domain raiz `.artificiorpg.com`), `trackEvent`/`trackPageview`; 3/3 testes. Integrado no site (`Analytics.astro`, gated `PUBLIC_GA_ID`); build com id dummy injeta loader+config (cookie_domain raiz) — verificado. **Tripé de pacotes compartilhados completo: `config`/`auth`/`ui`/`content`/`analytics`** (falta só `crosslink` p/ srd/esferas). G-ID real + exclusão de referral interno + 1 property = painel GA4 (mantenedor). **Pendente site:** pages institucionais, backend HTTP/admin + rebuild webhook, deploy beta.
- 2026-06-05 — **Módulo `site` — `packages/content` (SEO) + Cloudinary mídia (env-gated).** **F5 content:** pacote `@artificio/content` (TS puro, zero deps) — `buildMeta`(OG/Twitter), `articleLd`/`breadcrumbLd`/`organizationLd`/`websiteLd`, `sitemapXml`, `robotsTxt`, `SITE`/`canonicalUrl`; 6/6 testes; integrado no site (Base/[slug]/index + rota `robots.txt`); reusável por mesas/glossário. Build verifica OG/Twitter/JSON-LD(Article+BreadcrumbList+Organization)/canonical raiz/robots OK. Commit `9c65452`. **Cloudinary:** `importer/media.ts` + migration `002_media_map` — env-gated por `CLOUDINARY_URL`: com creds faz upload original WP + reescreve `src` (featured+inline) + cache idempotente `media_map`; sem creds = dry-run mantém URLs WP. Funções puras (extract/rewrite/publicId) verificadas; dry-run não popula `media_map`. Migração real de mídia = mantenedor setar `CLOUDINARY_URL`. **Pendente:** pages institucionais, backend HTTP/admin + rebuild webhook, `packages/analytics`, deploy beta.
- 2026-06-05 — **Módulo `site` — F2 store + F3 importador rodados (local, pglite).** Sem Docker/PG local → **pglite** (Postgres WASM in-process) p/ dev, **pg** p/ prod, atrás de adapter `query()` (`apps/site/db/connection.ts`); driver por env (DATABASE_URL). **F2:** schema `001_init.sql` (posts/taxonomies/post_taxonomies/media/pages/comments/redirects; slug UNIQUE, categoria pai/filho, N:N) + runner `db/migrate.ts` (schema_migrations + advisory lock pg + transacional, D039). **F3:** importador WP REST read-only (`importer/{wp,sanitize,run}.ts`), idempotente `ON CONFLICT id`, sanitize allowlist, Yoast→meta. **Rodado:** `migrate→import→export→build` em pglite → **paridade 125/125 posts ✓**, 82 taxonomias, 25 comentários; `db/export.ts` gera `src/data/posts.json` (desacopla Astro do DB); **build = 209 páginas** (125 artigos+arquivos+home) + Pagefind (125, 16460 palavras). Pipeline em `apps/site/README.md`. `posts.json` versionado = seed pequeno; `.pgdata` gitignored; full content via `pnpm sync`. Mídia dry-run (URL WP; Cloudinary depois). **Pendente:** Cloudinary, pages institucionais, backend HTTP/admin + rebuild webhook, `packages/content`+`analytics`, deploy beta. Kysely (canon) no backend HTTP futuro; importador descartável usa SQL cru (anotado).
- 2026-06-05 — **Módulo `site` — D048 + Etapa-1 frontend Astro no ar (local).** Mantenedor: Codex pausado (créditos), **Opus executa**; pediu opções de framework/template/funcionalidade antes (rubric: minimalismo+leitura+SEO+10 heurísticas Nielsen+ISO 9241-11+marca D040). Opções documentadas em `specs/008-site-foundation/template-options.md`; **D048** fixou **Astro** (SSG islands, zero-JS) + admin React SPA + backend Express/PG + Pagefind; visual **híbrido A+B**; **core completo**. Protótipo estático (conteúdo WP real, marca real) construído e **aprovado pelo mantenedor** (desktop). Portado p/ **`apps/site` Astro real**: build verde (10 páginas+sitemap), paridade total, reusa `@artificio/ui` (CSS+logos), home/artigo/404/dark-light/TOC/SEO(JSON-LD/canonical/OG). **Etapa-2 core ✅** (build **35 páginas**): arquivos `/blog/` + categoria + tag + pills; **busca Pagefind** (modal marca, `/` e Cmd/Ctrl+K, índice 8 artigos); **RSS** (8 itens) + sitemap + 404. **Commit `44ccae9` em `dev`** (spec 008 + D044–D048 + apps/site + docs; `main ⊆ dev` OK; **sem push**). Header/Footer = `.astro` zero-JS (island React adiado — `@artificio/auth/client` é browser-only → exigiria `client:only`; zero-JS é melhor p/ blog público; decisão do mantenedor). **Próximo:** F2 (store Postgres) + F3 (importador WP REST, agente `wp-importer`) → Content Layer lê do PG (hoje lê fixtures `src/data/posts.json`). Depois: deploy beta (T20/T21, secrets/VM/DNS = mantenedor). `.claude/launch.json` (untracked) p/ preview local (proto:4399 / build:4321). Web search/fetch caíram nesta sessão (re-verificar nomes de tema/versões Astro antes de produção).
- 2026-06-05 — **Módulo `site` — Fase 3 iniciada (spec 008, fundação).** Recon READ-ONLY do WP (REST): **risco re-dimensionado p/ BAIXO** — 125 posts (não 300+), HTML **Gutenberg limpo** (zero Elementor/shortcode nos posts), **Yoast meta** completo, 1 autor, 13 cat aninhadas, 69 tags, 25 comentários, 485 mídia. WP é multi-módulo (`definicao`163/`magia`328/`docs`17 = outros módulos, fora). Inventário durável: `docs/agents/wp-content-inventory.md`. 4 decisões resolvidas c/ mantenedor → **D044** (site só `beta.` por ora, fase = levantamento/decisão/doc, "não se perder"), **D045** (importador = WP REST, read-only), **D046** (escopo post+taxonomias+mídia+pages filtradas+comentários; CPTs fora), **D047** (preservar `/blog/<slug>/`, 301 ativa só no cutover). Spec `008-site-foundation` criada (spec/plan/tasks, 7 fases F1–F7) + sessão `26-06-05_2`. **F1 (fundação) ✅.** Próximo: F2 (store Postgres) — Codex, quando mantenedor liberar execução de código.
- 2026-06-05 — **CDX-312 + guard imagem + hydrate-fix → DEV e PROD.** PR #7 squash em `dev`; deploy beta verde; beta validado (dropdown OK por mantenedor; smoke 200/401/302; CSS `artificio-usermenu*`). FF `main←dev` (`86b26c4`, bootstrap manual do mantenedor pois `promote-prod-fast-forward.yml` ainda não estava em `main`). Deploy prod: `deploy-mesas mode=deploy` (`27034408480`) + `deploy-accounts mode=deploy` (`27034731785`) verdes. Prod validado: mesas 200/401/`return=mesas`, accounts 200, CSS CDX-312 servido. **Menu de conta restaurado em prod+beta.** Pendências: (1) **hydrate** `/sync/hydrate` dá 500 — falta `PROD_DB_URL` no `mesas-beta-api` env (`.env.beta` não tem); adicionar DSN read-only prod + restart (write VM+segredo = mantenedor/Codex). (2) **Imagem data-url** = task spawnada (guard já em prod; falta limpeza de dado/Cloudinary). (3) `.claude/settings.local.json` ganhou allowlist ampla (`git push/commit *`, `gh api *`, `irm|iex`) — revisar depois.
- 2026-06-03 — Plano G1 aprovado em decisões macro. Camada de governança criada (13 arq).
- 2026-06-03 — Coração de economia de contexto: `decisions.md` (append-only), `token-economy.md` (reload T0/T1/T2, caveman default), `arquiteture.md` (contratos por seção). Reload contract em tiers ativo.
- 2026-06-03 — Revisão de consistência vs requisitos. Achada contradição topologia (path único × WP fica × site beta). Resolvida 1ª via D015/D016 (interim híbrido). Gaps menores (páginas não-importáveis→rebuild manual; versão principal=branch main) corrigidos.
- 2026-06-03 — **Virada de topologia (mantenedor):** path único → **subdomínio-por-módulo (D017)**, SSO em `accounts.` (D018), blog na raiz (D019), analytics/SC cross-subdomínio (D020). Supera D002/D015. Dissolve a contradição WP-raiz e a dança de cutover dos módulos. Re-propagado em 11 arquivos. Grep limpo.
- 2026-06-03 — Spec `001-infra-backup-runbook` criada (spec+plan+tasks T1–T13) + sessão `26-06-03_1`. Primeira spec SDD real.
- 2026-06-03 — **T1 executada** via `ssh faren` (D023). Mapa em `docs/agents/infra-map.md`. 4 bancos G1, WP externo, telegram/foundry fora (D021).
- 2026-06-03 — Escopo backup final: **VM only** (WP fora=Hostinger cloud D024; uploads on-demand D025; secrets→`secrets.7z` D026). Criado `access-registry.md`. tasks T3 dropada, T4 diferida p/ Fase 3, T7 = `.env` dos serviços.
- 2026-06-03 — Módulo **`esferas`** (`esferas.artificiorpg.com`), multi-sistema sistema×edição (D&D 2014/2024 principal, PF futuro) — D028. Rename `spheres`/`wiki-sop`→`esferas` em todos os docs.
- 2026-06-04 — **CDX-308A concluído:** legado `C:\projetos\mesas_rpg_artificio` importado para `apps/mesas` sem segredos reais. `pnpm install` OK; `pnpm --filter @artificio/mesas build` OK; frontend tests 13/13; backend tests 104/104 com env dummy local. VM/deploy não tocados.
- 2026-06-04 — **CDX-308B concluído local:** mesas usa `@artificio/ui` + `@artificio/auth`; backend valida `artificio_session`; OAuth local aposentado. Turbo build OK; accounts tests 6/6; auth tests 3/3; mesas frontend 15/15; mesas backend 106/106. VM/deploy não tocados.
- 2026-06-04 — **CDX-308C deploy técnico concluído:** `mesas-api`, `mesas-app`, `mesas-cron` rebuildados na VM com Docker no-cache; `mesas-api` e `mesas-app` healthy; smoke `internal_api=200`, `public_home=200`, `private_no_cookie=401`, `/api/v1/auth/google` → `302 accounts.artificiorpg.com/login`. Falta E2E browser real para fechar Gate D.
- 2026-06-05 — **CDX-309B PR #1 verde:** CI/CD canonico de modulos via GitHub Actions (`_deploy-module`, `deploy-mesas`, lint/enforce migration, break-glass, scripts migration). Amazon Q blockers corrigidos. Deploy job skipped em PR. Sem VM.
- 2026-06-05 — **CDX-309C aplicado:** allowlist migration sem `sed`, self-test permitido/bloqueado; migration lock com `flock` interno autoritativo + `pg_advisory_xact_lock` por transaction; self-test concorrente no Actions. Sem VM.
- 2026-06-05 — **CDX-309D concluído:** PR #1 squash-mergeado (`3cf08d0`) e Parte C executada com aprovação. Backup off-VM criado em `C:\projetos\artificiobackup\cdx-309d-20260605-002955`; VM `/opt/artificio` agora é clone `main` com deploy key read-only; `.env` de `accounts`/`mesas` preservados; sem deploy/build/migration; smoke pós-conversão OK.
- 2026-06-05 — **CDX-309E concluído:** docs pendentes commitados (`cbee1a0`), bugs do deploy corrigidos (`b5c172d` CRLF em JWT, `986516b` script remoto por arquivo), e `deploy-mesas.yml mode=deploy` real passou (`26994910262`). DB antes `tables=48/users=15`; depois `tables=49/users=15`; `schema_migrations=55`; smoke `200/401/302`.
- 2026-06-05 — **CDX-311 (design system / marca) local:** cores reais amostradas pixel dos logos (laranja `#FF9457`, navy `#020740`); `@artificio/ui` (tokens/styles/Header/Footer/brand) + `apps/accounts` login + `apps/mesas` (vars + 5 arq com laranja velho `#E8521A`→`#FF9457`, contraste AA dos botões) atualizados; logo branco `brandLogoNeg` embutido p/ fundo escuro; footer virou hub. Nome do produto corrigido: "Artifício G1"→**"Artifício RPG"** em 23 arq + nota (G1 = referência conceitual). Build turbo 7/7. Verificado em preview local. Aguardando deploy via pipeline (codex).
- 2026-06-05 — **CDX-311 DEPLOYADO + marca validada no ar.** PR #2 squash-merge em `main` (`0c30fa7`); main avançou p/ `c5ff42d` (docs/roadmap). Deploys rodaram no HEAD `c5ff42d`. **Accounts:** 1ª tentativa falhou (`ACCOUNTS_ENV` do GitHub com senha DB divergente do volume); codex restaurou serviço com `.env` real da VM, sincronizou `ACCOUNTS_ENV` Secret (sem imprimir segredo), rerun verde `27022120573`; smoke health=200/login=200/me-sem-cookie=401; DB `accounts_tables=1 accounts_users=1`. **Mesas:** deploy verde `27022250217`, `jwt_secret_shared=true`, snapshot criado, migrations sem novidade, `mesas-api`+`mesas-app` healthy; smoke home=200/private_no_cookie=401/auth_redirect=302; DB `mesas_tables=49 mesas_users=15 schema_migrations=55`. **VM:** `/opt/artificio` em `c5ff42d`, git limpo, accounts-api/db + mesas-api/app/db/cron no ar. Marca servida (CSS/JS) contém `#FF9457`+`#020740` (+`#1B2A4A` mesas); hex velho `#E8521A`/azul-Google sumiram. **Validação Opus (Gate D, marca):** ✅. Próximo: **mesasbeta** (staging/canary essencial) rebuild do main.
