# Estado do Projeto — Artifício RPG

> Atualizar a cada mudança de estado operacional. Fonte de verdade do "onde estamos".

## Fase atual
**Fase 3 — módulos + conteúdo em andamento.** Fases 1/2 ✅; Gate B aprovado (D037, 2026-06-04). `accounts.artificiorpg.com` no ar (SSO Google/JWT), cross-subdomínio provado, CI/CD `deploy-accounts.yml`. **Gate D `mesas` FECHADO (2026-06-08):** `mesas.artificiorpg.com` roda build monorepo, usa UI G1, backend valida `artificio_session` via `@artificio/auth`, `JWT_SECRET` igual ao accounts, smoke técnico OK, login real confirmado pelo mantenedor e allowlist prod validada (`evil.com` sanitizado, `mesas.` preservado). **Spec 011 / site CMS:** T17/refino UX + mídia T18/T19 deployados no beta; WP raiz intocado. **Fix de persistência de sessão SSO (refresh-retry no cliente, `packages/auth`) deployado em beta + mesas prod** (commit `356b650`). **T16 FECHADO — mantenedor validou E2E autenticado no beta (2026-06-08).** **Glossário:** spec 012 fechada; **spec 015 FECHADA — Gate D glossário FECHADO (2026-06-12).** SSO Google via `accounts.` é o único login do glossário; compat legado por email-linking (`users.sso_user_id`) + fluxo de reivindicação (`/api/migration/{verify,claim}`) p/ contas não-Google (exceção controlada D061, BCrypt só no verify); login/register legados 410. Migration aditiva `sso_user_id` aplicada em prod (snapshot auto). Promote `dev→main` + deploy prod glossario/accounts/mesas; smoke prod verde; WP raiz intocado. Hotfix #22 (guards de rota esperam sessão SSO). **Próximo módulo / pendências:** ver "Próximo passo".

## Gates (ativos: A, B, D · Gate C adiado — D016)
- ✅ **Gate A** — Backups completos/verificados/off-VM. **APROVADO pelo mantenedor 2026-06-04.** Libera Fase 1.
- ✅ **Gate B** — SSO (`accounts.`) no ar + cross-subdomínio provado. **APROVADO pelo mantenedor 2026-06-04 (D037).** Libera import de conteúdo / construir módulos. (Integração de módulo real ao SSO = Gate D.)
- [ ] **Gate D** — (por módulo) smoke → próximo módulo. `mesas` ✅ fechado em 2026-06-08; `glossario` ✅ fechado em 2026-06-12 (spec 015, SSO+compat).
- ⏸️ **Gate C (adiado/futuro)** — Site validado em beta → cutover DNS raiz + desligar WP. **Fora do escopo destes ~3 meses.** WP intocável todo o projeto.

## Decisões fechadas
- Monorepo único `artificio` (pnpm + Turborepo).
- **Topologia: subdomínio-por-módulo (D017/D057, supera D002/D015).** Cada módulo no próprio subdomínio (`glossario.`, `mesas.`, `downloads.`, `esferas.`, `srd.`, `links.`), root próprio, sem basename. `glossariorpg.` foi alias histórico pré-monorepo e não é rota ativa a preservar. Blog em `beta.artificiorpg.com` (→ raiz futuro). WP na raiz `artificiorpg.com` (intocável). Cloudflare Tunnel hostname→container.
- **SSO central em `accounts.artificiorpg.com` (D018):** 1 OAuth Google, cookie JWT `Domain=.artificiorpg.com` (vale em todos subdomínios).
- Blog na raiz = aposta SEO (D019). Search Console Domain property + GA4 cross-subdomínio (D020).
- **Blog beta→raiz adiado (D016/Gate C)** — só o blog converge; módulos não. Fora do escopo agora.
- WordPress roda em paralelo; corte DNS só no Gate C.
- CMS: store nativo Postgres + importador WP one-shot (descartável pós-cutover).
- Blog: pré-render estático (SSG) com rebuild incremental disparado pelo admin (não SSR).
- Stack canônica = stack do mesas (React19/Vite/Tailwind + Express/Kysely/PG16/Cloudinary).
- Infra: Oracle 24GB/200GB, Docker, Cloudflare Tunnel, GHCR, Watchtower(beta).
- Backup destino: `C:\projetos\artificiobackup` (local, 300GB livres). uploads WP ≈ 6.34GB.
- Acesso DB da VM por linha de comando local/PowerShell via `ssh faren` (read-only por padrão).
- Site sobe primeiro como `beta.artificiorpg.com`; demais serviços integram em versão principal.
- Comunicação padrão dos agentes = **caveman ultra** (D068).

## Ativos existentes (origem)
- `C:\projetos\mesas_rpg_artificio` — referência de robustez (SDD, Actions, deploy). Repos beta+prod no GitHub.
- `C:\projetos\glossario_rpg_artificio` — glossário. Repos beta+prod no GitHub.
- `links.artificiorpg.com` + `servidorvirtual.artificiorpg.com` — página TS única, **sem GitHub** (backup pendente).
- Site `artificiorpg.com` — WordPress, **sem GitHub**, 300+ posts (backup + import pendente).
- Tradução Spheres of Power (Drop Dead Studios) → módulo `esferas` (`esferas.artificiorpg.com`), multi-sistema D&D 2014/2024 + Pathfinder futuro (D028).
- SRD DnD 5.2.1 (regras 2024) → futuro módulo `srd`.

## Construído neste monorepo
- Governança: `AGENTS.md`, `.specify/memory/{constitution,project-state,errors,decisions}.md`, `.specify/arquiteture.md`, `docs/agents/{operating-model,context-capsule,token-economy}.md`, `.claude/agents/*` (3), `.claude/skills/*` (2), `README.md`, `.gitignore`, `sessoes/index.md`. ✅ coração de governança + economia de contexto fechado.
- Código de aplicação: `packages/{config,auth,ui,content,analytics}`; `apps/accounts` SSO; `apps/mesas` integrado ao SSO/UI e Gate D fechado; `apps/site` + `apps/site-admin` no beta com CMS Fase 1.

## Próximo passo
**Fase 2 CDX-301..306 executados e Gate B aprovado:** monorepo + `packages/{config,auth,ui}` + `apps/accounts` SSO deployado. **Gate D `mesas` e `glossario` fechados.** **Spec 005 (esteira beta, D041/D042) entregue/em uso:** T2 branch protection bloqueado pelo GitHub em repo privado sem Pro/repo público (API 403), compensação = gate `main ⊆ dev` + alarme; pendência isolada: hydrate precisa `PROD_DB_URL` no beta + restart (segredo/write VM = mantenedor). **Spec 017/018 (UI/marca compartilhada + terminologia "projetos") FECHADAS e em produção para `accounts`/`glossario`/`mesas`; `site` atualizado no beta.** **Spec 019 (D-INFRA2 auditoria de fonte única) FECHADA como auditoria/roteador; backlog derivado em `specs/backlog.md`. Spec 020 (Theme Artificio padrao) parcial:** unifica somente a linguagem visual comum (tokens, dark/light, nav, header actions, primitives/recipes, brand/static parity), usando glossario como base clara/nav e mesas como referencia dark/actions; nao unifica regra de negocio, copy contextual, dados ou layouts especificos. **D064/D067 resolvidos:** laranja canonico `#FF5722` em `packages/ui`; tema lua/sol compartilhado via cookie `artificio_theme`; dark do glossario e light do mesas implementados/promovidos, com defaults preservados. **Spec 023 (D069 Real IP ingress) FECHADA EM PROD:** PR #39; `dev -> main` em `c785bb8`; deploys prod `accounts`/`mesas`/`glossario` verdes; `nginx -t`, config renderizada RealIP (`set_real_ip_from 172.18.0.0/16`, `CF-Connecting-IP`, `$remote_addr`) e smokes HTTP beta/prod OK. Debito residual: `accounts` ainda publica `ports: 3000:3000` (`BL-ACCOUNTS-PORT`), avaliar troca para `expose` em fatia separada. **B10b/B11 promovidos via PR #26:** tokens semanticos + superficies dark/light em `packages/ui`, e remaps de glossario/mesas consumindo esses tokens; PR #26 mergeado em `dev`, promovido para `main` e deploy prod de `accounts`/`glossario`/`mesas` verde. **B6 e B7 fechados por validação do mantenedor; B2/T11 fechado localmente em 2026-06-15** com `@artificio/ui/static` data-only consumido pelo site Astro. Debito novo **B13/D-SHELL1:** unificar nav/footer entre todos os projetos, incluindo glossario; fatia 1 de dados em prod, residual Astro/accounts registrado. Regra de promoção: doc-only também exige aprovação explícita para commit/push; se o mantenedor pedir subir/promover e o diff for só documentação, usar fast-forward `dev→main`; código segue fluxo normal branch/PR/checks/revisão/merge autorizado. **Próximo imediato recomendado:** B4 primitives compartilhadas e B3 `HeaderAction`; ou alternativas não-visuais da 019: config/auth shared, CDX-310, analytics/SEO/normalizers. B5 recipes ja esta fechado como documento. **CDX-310** segue pendente: reconciliar `deploy-accounts.yml` ao `_deploy-module.yml` e substituir shim operacional de `apps/accounts/docker-compose.prod.yml` por arquivo versionado correto. Pendência segurança (mantenedor trata): rotacionar tunnel token, PAT, WP creds.

**Mapa geral de pendências:** `specs/backlog.md` é o índice vivo de débitos/tarefas acionáveis por spec; `specs/README.md` explica o fluxo. Toda spec nova deve atualizar esse mapa.

## Log
- 2026-06-14 — **Release prod consolidado: Spec 021 (feedback) + D-MESAS1 + shell/nginx glossário PROMOVIDOS A PROD.** Repo tornado **PÚBLICO** (histórico reescrito p/ sanitizar recon; SHAs mudaram). Sequência autorizada nominalmente passo a passo: (1) PR #33 (`fix/glossario-nginx-xff-cf`) → dev: **D-NGINX1 v2** — nginx `/api/` usa `X-Forwarded-For $http_cf_connecting_ip` (não `$proxy_add_x_forwarded_for`, que anexava o hop do túnel e com `trust proxy 1` o Express pegava o túnel, não o visitante; topologia CF Tunnel→nginx→backend). Achado no review da PR #32 (fix v1 errado). (2) Merge `main→dev` restaurou invariante `main⊆dev` (2 commits de segurança feitos direto no main durante preparo público; zero delta de conteúdo). (3) PR #34 (`fix/mesas-cron-secret-prod-env`) → dev: compose prod mesas passa `MESAS_CRON_SECRET` ao `mesas-api` (sem isso `/admin/tables/auto-archive` dava 503). (4) Promote FF `dev→main` (`4c3fc49`). (5) Deploy prod **glossário** (migrations 16/17 aplicadas, `dev_feedback` criada, CLOUDINARY_* fiado, nginx XFF v2 renderizado no container) + **mesas** (D-MESAS1 arquivar manual+cron, MESAS_CRON_SECRET no container). **Segredos VM (gitignored, server-side):** prod glossário `.env` += CLOUDINARY_* (copiado do beta; backup `.env.bak-precloudinary`); prod mesas `.env` += MESAS_CRON_SECRET 64-hex (backup `.env.bak-precron`) + `gh secret set MESAS_CRON_SECRET` casado (mesmo valor via pipe, nunca exposto). **Smoke prod:** glossário home/terms 200; mesas home 200/me 401; auto-archive sem header = **401 "Segredo de cron inválido"** (não 503 → wiring ok); **WP raiz 200 intocado.** **Site NÃO tem container prod** (Gate C) — código feedback em `main` mas widget vivo só em beta. **D-NGINX1 e D-SHELL1b fechados em prod.** Cloudinary confirmado (URL real `res.cloudinary.com/dnln0btbo/...` numa smoke beta antes de excluir). Smokes de teste excluídas (beta glossário 5, site beta 2; prod nunca teve). **Pendências:** **D-NGINX2** (mesmo bug latente do `$proxy_add_x_forwarded_for` no `apps/mesas/frontend/nginx.conf` — auditar/corrigir igual); cron auto-archive tem **13 mesas elegíveis** em prod (rodará no agendado diário `mesas-auto-archive.yml`, arquiva de verdade — ou dispatch manual). Branch de trabalho: `fix/glossario-nginx-xff-cf`.
- 2026-06-13 — **D-FEEDBACK1 (feedback bug/sugestão no site + glossário) IMPLEMENTADO LOCAL — Spec 021. SEM commit/push/deploy.** Mantenedor escolheu (AskUserQuestion): **Híbrido B+** (port por app + contrato data-only compartilhado) + **island vanilla no site** (não quebra zero-JS D048) + **paridade total de dados**. **Fonte única:** `packages/ui/src/feedback.ts` (`@artificio/ui/feedback`, data-only: enum kind, `SubmitFeedbackPayload`, `FEEDBACK_LIMITS`, `FEEDBACK_COPY` PT exata do mesas) — molde B13 `./brand`/`./modules`. **Glossário:** migration_16 (tabela `public.dev_feedback`, fluxo D059); `feedbackValidator` (port `unknown`, 8 testes vitest); `optionalAuthMiddleware` novo; `POST /api/feedback` público (rate-limit, screenshot Cloudinary não-fatal gated por env) + admin `/api/admin/feedback` GET/PATCH/DELETE; dep `cloudinary`; front `features/dev-feedback/` (button+modal+diagnostics+axios interceptor rede≥400) + `AdminFeedbackPage` + rota/menu; deps `react-hot-toast`+`html2canvas-pro`. Notificação omitida (FR-009: enum `user_notifications` restrito; admin vê via triagem). **Site:** descoberto que `apps/site/server` TEM Postgres (pg/pglite, `apps/site/db`) — T0 supunha sem-DB; persistência = migration 005 (`dev_feedback`) pelo runner D039; rota pública `POST /api/feedback` (sessão opcional via `verifyToken`, rate-limit memória, screenshot via `storeUpload`) + admin em `admin-api.ts`; island vanilla `FeedbackWidget.astro` (markup+copy do contrato, 1 script hoisted, html2canvas lazy) + diagnóstico `is:inline` no `<head>` do `Base.astro`; triagem em `apps/site-admin` (`FeedbackPage`). **mesas inalterado** (FR-010 migrar p/ contrato = opcional futuro). **Validação:** `turbo build` 13/13 + parity OK + site static 45p (shell zero-JS, island/html2canvas code-split) + vitest glossário 22/22 + `git diff --check` limpo. Changelog glossário `migration_17`. **Pendências p/ publicar (autorização nominal):** PR→dev, migrations (glossário 16/17 fluxo próprio; site 005 runner), env Cloudinary por app (degrada sem), smokes. Spec/sessão: `specs/021-feedback-site-glossario/`, `sessoes/26-06-13_2`.
- 2026-06-14 — **D-MESAS1 (arquivar mesas) implementado e EM DEV/BETA (sem prod).** PR [#29](https://github.com/FarenRavirar/artificio/pull/29) (`feat/mesas-archive-tables-dmesas1`) → checks verdes (mesas CI, enforce-migration-dir, actionlint) → squash `dev` (`e8b3acc`); deploy beta mesas verde (migration_127 aplicou no DB beta); smoke beta `home=200`, `me=401`, `tables=200`. **main intacto (`25be29a`); sem promote/prod por decisão do mantenedor (só dev).** Arquivamento ortogonal ao status (`tables.archived_at`), reversível: `PATCH /gm/tables/:id/archive` (dono/admin), catálogo público filtra `archived_at IS NULL` (detalhe por link direto segue acessível), `published_at` gravado na 1ª publicação como âncora; `POST /admin/tables/auto-archive` PROD-only + segredo `MESAS_CRON_SECRET`, arquiva `active/full` publicadas há +30d (idempotente); workflow `mesas-auto-archive.yml` (diário). Frontend: botão Arquivar/Desarquivar + badge no painel do mestre e página da mesa. Changelog "Arquive suas mesas antigas". Isolado em `apps/mesas` (+ workflow), zero `packages/*`. **Pendências p/ prod (mantenedor):** aprovar deploy prod (migration+cron) + setar `MESAS_CRON_SECRET` (cron; manual independe). Decisão de produto: limite 30 dias.
- 2026-06-13 — **Spec 020 B13/D-SHELL1 fatia 1 (nav fonte única) + B7 badge hero PUBLICADOS em PROD.** PR [#28](https://github.com/FarenRavirar/artificio/pull/28) (`fix/020-nav-single-source-b13`, commit `a683f4a`) → checks verdes (CI mesas/glossario/site/accounts) → squash `dev` (`25be29a`); betas verdes (mesas/accounts auto + glossario/site dispatch); promote `promote-prod-fast-forward` (run 27481880383) verde (`main==dev==25be29a`); deploy prod `deploy-mesas` (run 27481953941) + `deploy-glossario` (run 27481954517) verdes (autorização nominal). **B13 estratégia A (fonte única de DADOS):** `packages/ui` ganhou subpath exports data-only `./brand`+`./modules` (sem React/auth, p/ Astro); `apps/site/content.ts` consome `defaultNavItems`+logos de `@artificio/ui` (espelhos `MODULES` e `src/data/brand.json` removidos — drift 3 fontes→1); `apps/glossario` brandHref corrigido `artificiorpg.com` (WP raiz!)→`glossario.artificiorpg.com`. **B7 badge:** `MestrePage.css` hero `:has(.hero-banner)` light restaura cor clara dos badges (`.badge-mestre/.badge-covil/.hero-promo-badge`) p/ AA sobre scrim navy (achado review PR #27). Smoke prod: `mesas_home/glossario_home/glossario_terms/wp_root=200`; bundle glossário com brandHref próprio (WP raiz=0); CSS mesas com badge fix. site beta-only (sem prod). **Atualizacao 2026-06-15:** B2 fechou o subpath `@artificio/ui/static`; residual B13 agora e so markup Astro espelhado/accounts quando fizer sentido.
- 2026-06-13 — **Spec 020 B7 fix `/perfil` light + hero banner custom PUBLICADO em PROD.** PR [#27](https://github.com/FarenRavirar/artificio/pull/27) (`fix/020-b7-profile-light`, commit `e6f0d4d`) -> checks verdes -> squash `dev` (`05c9117`); betas `deploy-accounts`/`deploy-mesas` verdes; promote `promote-prod-fast-forward` (run 27479772211) verde (`main==dev==05c9117`); `deploy-mesas` prod ref main mode=deploy (run 27479794040) verde. `ProfileEditPage.css` ganhou bloco `[data-theme="light"]` consumindo tokens compartilhados via aliases `--profile-*` (sem hex/rgb/rgba literal); `MestrePage.css` hero `.hero-section:has(.hero-banner)` light = scrim navy + `--artificio-dark-text`. Isolado em `apps/mesas` (zero `packages/*`). Smoke prod: `mesas_home=200`, `me_options=401`, `wp_root=200`; CSS servido confirma `profile-edit-page{--profile-ink` + hero banner dark-safe. **Atualizacao 2026-06-15:** B7 foi fechado por validacao do mantenedor em prod (`/perfil` light com dados + mestre com `banner_url` custom + smoke anti-regressao).
- 2026-06-13 — **Spec 020 B11+B10b fechados localmente — tokens semanticos + superficies dark/light consumidos.** B11 adicionou `success/warning/danger/info` (+`*Text`) em `packages/ui` com paridade 3-way. B10b adicionou `dark*`/`light*` estruturados em `tokens.ts`/`styles.css`/trava e migrou `apps/glossario/frontend/src/index.css`, `apps/mesas/frontend/src/index.css` e `apps/mesas/frontend/src/pages/MestrePage.css` para consumir `--artificio-dark-*`/`--artificio-light-*` nos remaps. Validacao: `check-token-parity` OK (31 papeis), builds `glossario`/`mesas` OK, `git diff --check` OK, `pnpm turbo run build` 13/13 OK. Sem commit/push/deploy naquele momento; pendencia de B7 foi superada em 2026-06-15 por validacao do mantenedor.
- 2026-06-13 — **Spec 020 B2-B7 revisados — backlog documental consolidado.** Criado `specs/020-ui-theme-artificio-padrao/backlog-b2-b7-review.md`: B3 parcial (slot/classes/actions existem; falta `HeaderAction` runtime), B4 parcial (contrato fechado; falta implementacao shared), B5 fechado (recipes documentadas). **Atualizacao 2026-06-15:** B2, B6 e B7 foram fechados; `tasks.md`/backlog alinhados.
- 2026-06-13 — **Spec 020 T11/B2 revisado — paridade brand/static shell documentada.** Criado `specs/020-ui-theme-artificio-padrao/brand-static-shell.md` com estado do site Astro e caminho recomendado. **Atualizacao 2026-06-15:** B2/T11 fechado localmente com `@artificio/ui/static` e builds verdes.
- 2026-06-13 — **Spec 020 T10 fechado — rollout piloto documentado.** Criado `specs/020-ui-theme-artificio-padrao/rollout-pilots.md` com ordem canonica **accounts → glossario → mesas → site → site-admin**, estado atual ja executado, regra de entrada, validacao, criterio de saida e rollback por consumidor. Plano separa o que ja foi promovido (D067, glossario dark, mesas light) dos residuais: B6, B2/T11 e T14; B7 foi fechado depois, em 2026-06-15. `tasks.md`: T10 fechado. Sem codigo runtime/build/deploy.
- 2026-06-13 — **Spec 020 T9 fechado / caminho Astro/zero-JS.** Revisado site publico (`Base.astro`, `SiteHeader.astro`, `SiteFooter.astro`, `SearchModal.astro`, `Analytics.astro`, `lib/content.ts`), server/admin e build output. Criado `specs/020-ui-theme-artificio-padrao/astro-zero-js.md`: publico = Astro SSG/static + shell `.astro` + CSS/tokens via `@artificio/ui/styles.css` + scripts vanilla permitidos (tema D067, login link/sessao, TOC, Pagefind lazy, GA4), sem React/auth client no publico; admin React fica isolado em `apps/site-admin`, auth em `apps/site/server`. Validação original: `pnpm --filter @artificio/site build` verde (`output: static`, 45 paginas, Pagefind 8 paginas). **Atualizacao 2026-06-15:** B2/T11 fechado com `@artificio/ui/static`; shell continua static.
- 2026-06-13 — **Spec 020 T8/B5 fechado — recipes de pagina documentadas.** Revisados exemplos vivos: `glossario` `HomePage`/`ResultCard`, `mesas` `CatalogoPage`/`GestaoPage`/`LoginPage`/`MesaPage`/`TableView`/`MasterProfilePage`, `accounts` login e `site` Astro editorial + `site-admin`. Criado `specs/020-ui-theme-artificio-padrao/page-recipes.md` com `PublicSearchPage`, `CatalogPage`, `AdminWorkspacePage`, `AuthPage`, `EditorialPage` e `DetailPage` como composicao (slots, estados, fronteiras, fora de escopo, rollout/validacao), nao layout forcado. `tasks.md`: T8 e B5 fechados. Sem codigo runtime/build; implementacao em `packages/ui` fica T14/fatia futura se necessaria.
- 2026-06-13 — **Spec 020 T7 fechado / B4 parcial — primitives de formulario e estado.** Revisado `packages/ui/src` (sem primitives hoje) e candidatos vivos em `mesas` (`Field`, `CatalogToolbar`, `LoadingState`, `ErrorState`, `FilterDrawer`, modais), `glossario` (`AddTermModal`, `FilterPanel`, admin) e `site-admin` (`.btn`, `.card`, inputs, badges, modal). Criado `specs/020-ui-theme-artificio-padrao/primitives-form-state.md` com contratos de `Button`, `Field`, `TextInput`, `Textarea`, `Select`, `Badge`, `Panel`, `Toolbar`, `FilterPanel`, `State`, `Modal`, `Drawer` e `HeaderAction`, fronteiras, fora de escopo, rollout e validacao. `tasks.md`: T7 fechado; B4 parcial porque implementacao em `packages/ui` fica para T14/fatia propria (shared code = SDD Completo + builds/smokes). Sem codigo runtime/build; validado por inventario local + `git diff --check`.
- 2026-06-13 — **Spec 020 T6 fechado — header/nav/actions.** Revisado o contrato real em `packages/ui/src/Header.tsx`/`Nav.tsx`/`modules.ts` e consumidores (`mesas` AppShell/HeaderActions/NotificationBell, `glossario` GlossarioHeader, `site` SiteHeader/Base/content). Criado `specs/020-ui-theme-artificio-padrao/header-nav-actions.md`: `HeaderProps` atual e fonte, fronteira do slot `actions`, inventario por app, proposta futura de `HeaderAction` visual, rollback e validacao. `tasks.md`: T6 fechado. Dados/fetch de changelog, notificacao e feedback ficam por app; `packages/ui` segue como shell visual. Comentario obsoleto de auth legado no `GlossarioHeader` corrigido. Sem build runtime; validado por grep + `git diff --check`.
- 2026-06-13 — **Spec 020 — lua/sol COMPARTILHADO + correções de review EM PRODUÇÃO (D067). Promote + deploy prod autorizados.** PR **#25** (`fix/020-theme-shared-reviews`) → checks verdes → squash `dev` (`2d1567d`); betas verdes (glossário/site/mesas/accounts; WP raiz 200). Promote FF `dev→main` (`promote-prod-fast-forward`, main=`2d1567d`). **Deploy PROD autorizado nominalmente (3 apps):** `deploy-mesas`/`deploy-accounts`/`deploy-glossario` ref `main` mode=deploy — **todos success**. **Smoke prod:** glossário/mesas/accounts 200; glossário CSS `[data-theme=dark]`×93; mesas CSS `[data-theme=light]`×56 + inline boot `m[1]:'dark'` (honra cookie + default-dark); mesas `/api/v1/me/options` 401; glossário `/api/terms` 200; **WP raiz 200 intocado**. **D067:** tema lua/sol **compartilhado/persistente cross-subdomínio** (cookie único `artificio_theme`); raiz corrigida — `accounts`/`site` não gravam mais o cookie no boot (só em toggle explícito) → cookie virou opt-in confiável; **mesas volta a honrar o cookie** (`cookie?cookie:'dark'`, removido marcador `mesas_theme` do D066). Defaults inalterados (glossário light, mesas dark) → variante faltante é **opt-in por clique**. Reviews PR #24 corrigidos: Amazon Q (flash → `GlossarioHeader` init `resolveTheme()`), "Entrar com Google"→"Entrar" (Header default), logo mobile não-deforma (`object-fit:contain`+`flex-shrink:0`). **Pendências:** E2E autenticado das telas com dados (glossário dark cards/admin; mesas light catálogo/painel/gestão/forms — opt-in, cosmético); **B8** dinamismo/animação da logo; 3 botões grandes "Entrar com Google" nas telas de login (accounts/glossário/mesas LoginPage) preservados (uso convencional — decidir se troca). Sessão `26-06-12_7`.
- 2026-06-13 — **Spec 020 T5 fechado — consolidacao `artificio_theme` planejada.** Revisado o que esta em `dev`: `packages/ui/src/theme.tsx` e fonte canonica; `glossario` usa runtime canonico com inline boot zero-flash; `mesas` usa `setTheme` e excecao deliberada `cookie ? cookie : dark`; `accounts` e `site` ja obedecem D067 (sem escrita no boot), mas ainda duplicam helpers locais. Criado `specs/020-ui-theme-artificio-padrao/theme-consolidation.md` com inventario, excecoes, migracao, rollback e validacao. `tasks.md`: T5 fechado como planejamento; remocao de duplicacao real fica em T14/fatia futura. Sem codigo runtime, sem build, sem VM/commit/push.
- 2026-06-13 — **Spec 020 T3 fechado — contrato de tokens.** Revisado o que esta em `dev`: `packages/ui/src/tokens.ts`, `styles.css`, `tailwind-preset.js`, `check-token-parity.mjs`, exports e consumidores principais. `node packages/ui/scripts/check-token-parity.mjs` OK (`ink, brand, brandDeep, focus, muted, line, surface, canvas, charcoal, bronze`). Criado `specs/020-ui-theme-artificio-padrao/token-contract.md` com fonte canonica, CSS vars, preset Tailwind, papeis, aliases temporarios por app e regra de paridade. `tasks.md`: T3 marcado fechado. Lacunas futuras (semanticos, spacing/shadow completos, dark tokens estruturados) seguem em T14/primitives/recipes. Sem codigo runtime; sem build; sem VM/commit/push.
- 2026-06-13 — **D-CONT1 resolvido localmente (changelogs mesas+glossário).** Pesquisa local em T0/sessões/decisões; sem VM/banco prod. `apps/mesas/database/changelogs.json`: nova entrada publicada "Login único e visual do Artifício chegaram ao Mesas" + entrada existente de feedback agora publicada. `apps/glossario/database/migration_15_changelog_ui_sso_updates.sql`: nova migration idempotente com entradas "Conta única..." e "Visual e navegação..." para `public.update_log`. Validação: JSON parse OK (17 itens, 17 publicados) + grep das entradas/migration idempotente. Produção depende de commit/push/deploy autorizados.
- 2026-06-13 — **Governança/backlog doc-only atualizado.** `AGENTS.md` agora fixa comunicação padrão em **caveman ultra**; D068 registrada e T0/T1 alinhados (`context-capsule`, `token-economy`). Backlog `26-06-12_2` recebeu débitos: **D-CONT1 urgente** atualizar changelogs de mesas+glossário; **D-MESAS1** arquivar mesas manualmente + autoarquivar após 1 mês publicadas **somente no prod**; **D-FEEDBACK1** levar reportar bug/sugerir melhoria do mesas para site+glossário. B8 da spec 020 corrigido: "dinamismo" da logo = responsividade/proporção mobile já feita (`object-fit:contain` + `flex-shrink:0`), sem débito de animação. Sem código, sem VM, sem commit/push.
- 2026-06-13 — **Spec 020 — lua/sol pilotos glossário(+dark)/mesas(+light) NO BETA (autorizado). PROD intocado.** PR **#24** (`feat/020-lua-sol-pilots`, commit `464b5e5`) → `pr-checks` verde → squash-merge `dev` (`69feb8e`). Deploys: `deploy-mesas` auto (mesasbeta) ✓, `deploy-glossario` dispatch `mode=deploy` (glossariobeta) ✓, `deploy-accounts` auto ✓, `promote-dev-to-main` = invariante `main ⊆ dev` (não é promote prod) ✓. **Smoke beta servido:** `glossariobeta` 200 + CSS `[data-theme=dark]`×93 + remap `.text-azul-escuro`/`eef1f8` + `/api/terms` 200; `mesasbeta` 200 + CSS `[data-theme=light]`×56 + `0b1220`×23 + inline boot **default-dark** (fallback `:'dark'`) + `/api/v1/me/options` 401; `accounts` 200; **WP raiz 200 intocado**. **PROD não tocado** (sem promote código `dev→main`, sem deploy prod). **Pendente:** E2E autenticado do mantenedor nos betas (alternar lua/sol, validar AA nas telas com dados — glossariobeta cards/admin/forms dark; mesasbeta catálogo/painel/gestão/forms/modais light, confirmando default-dark). Habilitar em PROD só após esse E2E + autorização nominal. Sessão `26-06-12_7`.
- 2026-06-12 — **Spec 020 — lua/sol 2º piloto MESAS (light mode) IMPLEMENTADO + VALIDADO LOCAL (sem commit/deploy). D066.** Mantenedor: "parte para o mesas light". mesas = Tailwind v4 dark-only operacional em PROD; variante faltante = **light**. Escopo: ~1873 ocorrências de utilities brancas (~28 classes `text-white`/`/NN`, `bg-white/NN`, `border-white/NN`) + ~21 hexes hardcoded (`bg-[#13213f]`/`[#0F1A2E]`/`[#1B2A4A]`…) + tokens `--surface/--fg/--border/--fill`. **Técnica:** folha `[data-theme="light"]` em `index.css` (não toca os 134 componentes): override de tokens `:root` (fg branco→ink `#0B1220`, borders/fills→navy-alpha, surfaces dark→`#FFFFFF`/`#EEF2F8`/`#E6EBF4`) + `@layer base` (body `#F4F6FB`, placeholder, `select`, `.glass`/`.app-select`) + remap das utilities brancas → ink-alpha + remap dos hexes/gradientes → light; `text-[#020740]` navy preservado. Regras unlayered vencem Tailwind v4 (sem `!important`). **DEFAULT-DARK (prod-safety):** boot (`main.tsx resolveMesasTheme` + inline `index.html`) = light SÓ com cookie/localStorage explícito; ausência/OS-prefers → **dark** (desvio deliberado do `resolveTheme` canônico p/ não regredir o app operacional). Toggle lua/sol no `AppShell` (`setTheme`/`ThemeIcon` canônicos) + `variant` reativo Header **e** Footer. **Validação:** `turbo build --filter mesas-frontend` verde; **default-dark confirmado** (sem cookie → `data-theme=dark`, body `#1B2A4A` — prod intocado); smoke landing pública `/` (única rota sem auth) dark(default) inalterado + light (page `#F4F6FB`, hero navy, search/chips/botões/header/footer light) + mobile(375) + troca **ao vivo** sem reload; console limpo (só `useFetchTables` = backend ausente); **AA light** hero/chip/botão **17.32**, muted (0.60α composto) **4.85** (≥4.5); `rg artificio_theme` = boot que LÊ o cookie canônico (não reimplementa; runtime único `packages/ui/theme.tsx`). **Isolamento:** zero `packages/*` tocado. **Residual (E2E mantenedor, auth+backend):** telas operacionais auth-gated (catálogo c/ dados, painel, gestão, forms multi-step, modais). Sessão `26-06-12_7`. **Sem commit/push/deploy.** **Estado Spec 020 lua/sol:** glossário+dark (D065) e mesas+light (D066) ambos LOCAL; nenhum em prod (habilitar = pós dark-readiness completo + autorização nominal).
- 2026-06-12 — **Spec 020 — lua/sol piloto GLOSSÁRIO (dark mode) IMPLEMENTADO + VALIDADO LOCAL (sem commit/deploy). D065.** Mantenedor escolheu (AskUserQuestion): piloto = **glossário ganha dark** (não mesas — app operacional em prod, blast radius alto) + **navy migra junto** `#1a2744`→`#020740`. **T4:** checklist objetivo de dark-readiness em `specs/020-.../dark-readiness-checklist.md` (contraste AA, estados, forms/select/validação, modais/drawers/toasts, header/footer/toggle, mobile, troca sem flash, evidência). **Diagnóstico de escopo:** glossário = Tailwind v3 sem `darkMode`, ~500 utilities cruas (`bg-white`/`text-gray-*`), zero infra de token semântico → técnica = `darkMode:['selector','[data-theme=dark]']` + **folha de remap** `[data-theme="dark"] .util→token` (não toca os 21 componentes; especificidade vence sem `!important`). Cobre grays + customs (`azul-escuro`/`azul-medio`/`cinza-fundo`) + tints semânticos (blue/green/red/amber/cyan `-50/-100`→translúcido, texto `-600..900`→clareado) + variantes de opacidade (`bg-gray-50/30`…). Paleta dark alinhada à suite (surface `#1B2A4A`, canvas `#0F1830`, texto `#EEF1F8`, brand `var(--artificio-brand)`). Toggle lua/sol no `GlossarioHeader` via `setTheme`/`ThemeIcon` canônicos + `variant` reativo (logo navy↔neg); `applyTheme()` no boot + inline boot script (zero-flash). **Validação:** `turbo build --filter glossario-frontend` verde (tsc+vite); smoke preview (porta 4324) claro+escuro+mobile(375)+modal+login+drawer; **AA medido** hero 13.94/muted 5.18/input 13.94/modal-btn 8.84/empty 5.18 (corrigido de 2.91 — `bg-gray-50/30` de opacidade escapou, remap de opacidade resolveu) — todos ≥4.5/≥3; `rg artificio_theme` no glossário = só inline boot que LÊ o cookie canônico (não reimplementa; runtime único em `packages/ui/theme.tsx`). **Isolamento:** zero `packages/*` tocado (só consome `@artificio/ui`). **Residual (E2E mantenedor):** telas com dados (cards de termo/admin/forms validação) não carregaram local (sem backend). **lua/sol habilitado SÓ no glossário** (mesas +light = fatia futura B7). Sessão `26-06-12_7`. **Sem commit/push/deploy.**
- 2026-06-12 — **Spec 020 (Theme Artifício padrão) — Fase B fatia 1+2 COMMITADA + NOS BETAS (laranja `#FF5722`).** Commits `a48a518` (tokens fonte única) + `d9c6d8a` (tints/sombras rgba que escaparam do grep de hex) em `dev`. **Betas verificados por CSS servido:** `beta.artificiorpg.com` (site), `mesasbeta`, `glossariobeta` todos com `#FF5722` e zero laranja velho. **Armadilha resolvida:** `deploy-site`/`deploy-glossario` têm job Deploy **gated a `workflow_dispatch mode=deploy`** (bootstrap/volume) → push-dev roda só CI; betas atualizados via dispatch manual (`mesas`/`accounts` auto-deployam no push). accounts em betas reusa accounts PROD (D042). **PROD intocado** (mesas./glossario./accounts.) — só com promote `dev→main` + deploy prod autorizado. **Pendente Spec 020:** lua/sol em glossário/mesas (dark readiness B6/B7), navy glossário `#1a2744`→`#020740`, primitives.** **D064:** laranja de marca canônico = **`#FF5722`** (Material Deep Orange 500), supera o `#FF9457` do D040 — mantenedor confirmou com certeza; amostra pixel provou que D040 pegou o **logo** (pêssego `#FF9457` em `Logo-PNG-Negativo-2.png`) e não o **acento de UI** (`#FF5722` em `telaprincipal.png`). Navy `#020740` mantido. **Fonte única real:** o hex concreto vive só em `packages/ui` (`--artificio-brand:#ff5722`/`--artificio-brand-deep:#e64a19`); consumidores **puxam a variável** (pedido do mantenedor "não repetir cor, puxar de uma variável"): site/mesas/glossário (que importam `@artificio/ui/styles.css`) aliasam seus tokens p/ `var(--artificio-brand)`; accounts/site-admin usam token concreto local (1 spot/app); literais Tailwind espalhados do mesas dev-feedback (`bg-[#FF9457]`→`bg-artificio-orange`), glossário export ARGB e site preview agora referenciam token/var. **Paridade interna corrigida** (`tailwind-preset.js` tinha drift `ink #10103A`/`brand #FC9054`) + trava nova `packages/ui/scripts/check-token-parity.mjs`. **Validação:** `turbo run build` 13/13; parity OK; `git grep` laranja velho em `apps/` vazio; CSS buildado do mesas confirma cadeia `--color-artificio-orange→var(--artificio-brand)→#ff5722`. **Incidente:** 1ª tentativa de migração via script PowerShell corrompeu 11 arquivos (`#`→`f`), restaurados via `git checkout` (autorizado) e refeitos com Edit. Sessão `26-06-12_6`. Pendente (autorização): lua/sol glossário/mesas (dark readiness), navy glossário `#1a2744`→`#020740`, primitives.
- 2026-06-12 — **Spec 017 (UI compartilhada: favicon fonte-única + rodapé + toggle de tema) + Spec 018 (terminologia pública "projetos") FECHADAS.** PR #23 (`feat(ui): unificar marca e terminologia`) mergeado em `dev` por squash (`7d90cb8`), beta verde (`site`, `glossario`, `mesas`), promoção fast-forward `dev→main` (`promote-prod-fast-forward` run `27434738197`) e deploy prod verde de `accounts` (`27434803027`), `glossario` (`27434806734`) e `mesas` (`27434810258`). **D062:** compartilhado = fonte única em `packages/ui` por **importação** (nunca cópia/sync). Favicon: `faviconV2` data-URI + `applyFavicon()` em `brand.ts`; site importa em build (`<link>`), SPAs (accounts/glossário/mesas) via `applyFavicon()` no boot; removidas 4 cópias `public/faviconV2.png` + `<link href=/faviconV2.png>` estáticos. Rodapé: frase "presente da Artifício…" movida p/ `Footer.tsx` (+ espelho `SiteFooter.astro`), removida do glossário `LandingSection` (badge do hero mantido). Tema: `packages/ui/theme.tsx` (cookie único `artificio_theme` + `applyTheme`/`setTheme`/`ThemeToggle`/`ThemeIcon`) + `Header` prop aditiva `showThemeToggle` (default off); **D-UX1** login accounts texto→ícone lua/sol. glossário/mesas **não** habilitaram toggle (sem CSS dark → backlog futuro). **D063/Spec 018:** UI pública e docs de retomada/produto usam **"projetos"**; "módulo" preservado como termo técnico interno. Smoke prod verde: accounts health/login/me sem cookie; bundle accounts com "projetos", `Alternar tema` e favicon data-URI; glossário 200/API terms 200; mesas 200/options 401/auth redirect para accounts; WP raiz 200 intocado. Site novo segue beta-only (Gate C adiado).
- 2026-06-12 — **Spec 015 FECHADA — Gate D glossário FECHADO; SSO+compat no ar em prod.** Glossário troca auth próprio por Google OAuth via `accounts.` (D018); `packages/auth` intocado. Backend: `resolveLocalUser` (account-linking sso_id→email→provisiona, preserva id legado/ownership), `authMiddleware` via `@artificio/auth verifyToken` (cookie/Bearer resiliente), `/api/migration/{verify,claim}` (BCrypt só no verify — anti-enum dummy cost 10 + rate-limit; migration_token 10min não cria sessão; claim transacional funde auto-provisionado sentinel + dedup voto, 409 se Google já é de outra legada real), login/register legados 410. Frontend: cookie+refresh-retry, AuthContext SSO, `/migrar`, hotfix #22 (guards esperam sessão antes de redirecionar). migration_13 (aditiva online-safe) em beta+prod. Entrega PRs #16–#22; promote `dev→main` (`bdbd89f`) + deploy prod glossario/accounts/mesas; smoke prod verde (glossário 200/terms 200/me 401; accounts 200; mesas 200/options 401/auth→accounts return; **WP raiz 200 intocado**). **Decisão D061.** Follow-up: limpeza coluna `password_hash` legada; E2E browser cenário (b) não-Google (opcional, coberto por unit). Também promovidos junto: redesign da login `accounts` (#20) + nav `packages/ui` glossário. Sessão `26-06-11_3` encerrada. Débitos UX/marca registrados em `sessoes/26-06-12_2_debitos_ux-marca.md`.
- 2026-06-12 — **Spec 015 (glossário SSO+compat) em T7 beta; docs atualizados antes do commit final.** PR #16 (`feat(glossario): integrar SSO com compat legado`) e PR #17 (`fix(glossario): incluir build auth no runtime docker`) mergeados em `dev`. Deploy beta falhou em `27390686273` por falta de `packages/auth/dist-cjs` na imagem runtime; PR #17 corrigiu cópia de `packages/auth/dist`/`dist-cjs`. Deploy beta falhou em `27391072535` por `Cannot find module 'jsonwebtoken'` vindo de `packages/auth/dist-cjs/jwt.js`; fix local pendente adiciona `NODE_PATH=/repo/apps/glossario/backend/node_modules`. Review PR #16 incorporado localmente: dummy BCrypt cost default 10 (legado real, env `GLOSSARIO_LEGACY_BCRYPT_COST`), claim só mergeia auto-provisionado sentinel `SSO_NO_PASSWORD` e retorna 409 para outra conta legada real, `/migrar` preserva estado `done` após claim. Validação local: backend tests **14/14**, backend build OK, frontend build OK. Próximo: publicar fix em PR→`dev`, redeploy beta, smoke T7.
- 2026-06-11 — **Spec 015 (glossário SSO + compat) — T2–T6 código-completo LOCAL (builds/testes verdes; SEM deploy).** Backend: migration aditiva `sso_user_id`+índice `lower(email)` (`migration_13_sso_link.sql`, online-safe); `resolveLocalUser` (account-linking sso_id→email→provisiona, preserva id legado/ownership); `authMiddleware` trocado p/ `@artificio/auth verifyToken` (cookie `artificio_session`/Bearer; resiliente bearer→cookie); admin global SSO não rebaixado; fluxo de reivindicação `/api/migration/{verify,claim}` (BCrypt só no verify, dummy-hash anti-enum, rate-limit IP+email, migration_token 10min, claim transacional com merge de FKs do auto-provisionado + dedup de voto); login/register legados → **410**. **13/13 testes** (resolveLocalUser 4 + migration 9) + tsc limpo. Frontend: api cookie+refresh-retry, AuthContext SSO, Login landing + aviso, `/migrar` (verify→Google→claim→herança), register→redirect, Bearer legado removido; **tsc+vite verdes**. `packages/*` intocados (isolamento). Sessão `26-06-11_3`. **Pendente:** T1 (count read-only prod — aprovação mantenedor); T7/T9 [APROVAÇÃO] snapshot+migration+deploy beta/prod + Gate D glossário.
- 2026-06-12 — **Spec 012 FECHADA — glossário no ar em PROD pelo monorepo.** `https://glossario.artificiorpg.com/` **200** servindo o glossário novo (`<title>Grande Glossário de RPG…</title>`) + `/api/terms` 200; BETA `glossariobeta.` 200. **"Não tá on" era infra Cloudflare, não os containers** (sempre healthy): faltava (a) CNAME `glossario`→tunnel e (b) remover regra de redirect legada `glossario.`→`glossariorpg.` 301 (corpo do 301 = `cloudflare`, provando borda). Registrado **E005**. **Nav (T8)** `packages/ui defaultNavItems` + `apps/site MODULES` → `glossario.` (commit `1d8d060`, deploys betas verdes ui/site/mesas/accounts/glossario). Login = legado (SSO = spec 015). Pendências isoladas: login real no browser (mantenedor); desativar workflows do repo legado `glossario_rpg_artificio`. Próximo (chat novo): spec 015 (SSO + compat). Sessão `26-06-11_2`.
- 2026-06-11 — **Glossário BETA deployado pelo monorepo; PROD bloqueado naquele momento.** Workflow `deploy-glossario.yml --ref dev -f mode=deploy` run `27382386493` verde: actionlint/ShellCheck/CI/deploy OK, `jwt_secret_shared=true`, snapshot criado, migrations no-op, containers `glossario-beta-{db,api,app}` recriados no project `glossario-beta` e rede `artificio_net`, health healthy. Smoke VM/public: `https://glossariobeta.artificiorpg.com/` 200, `/api/terms` 200 com 8785 termos, busca por termo real 200/1 resultado, `/login` 200, backend `/health` OK. `.env.beta` preserva senha do volume legado e JWT do accounts beta; volumes não removidos. Fixes no caminho: `b3e1fc3` corrigiu dispatch `dev` que escolhia prod; `d410787` adicionou opt-in `reconcile_same_project_orphans` para containers legados com mesmo project `glossario-beta` e service labels antigas (`app-beta`/`api-beta`/`db-beta`). `glossariorpg.` é alias histórico desativado, não rota ativa a preservar. Login real com usuário ainda precisa validação manual no browser/mantenedor.
- 2026-06-11 — **Promoção PROD glossário em andamento; containers prod no monorepo OK, DNS bloqueia smoke público.** `dev -> main` promovido por fast-forward (`27382873086`, depois `27383125796`) até `7229031`. VM PROD `/opt/artificio` resetada para `origin/main`; `/opt/artificio/apps/glossario/.env` criado `600` com `POSTGRES_PASSWORD` igual ao legado prod e `JWT_SECRET` igual ao accounts prod (valores não impressos). Deploy prod `27383164490`: CI verde, snapshot/build/recreate/health verdes; containers `glossario-{db,api,app}` agora são imagens monorepo `glossario-glossario-*`, project `glossario`, services novos, rede `artificio_net`, health healthy, backend `/health` OK. Workflow falhou somente no smoke público porque `glossario.artificiorpg.com` não resolve em 1.1.1.1/8.8.8.8. `glossariorpg.` também não resolve, mas isso é esperado: era alias histórico pré-monorepo e não rota ativa. Próximo: corrigir/criar DNS Cloudflare para `glossario.artificiorpg.com` apontando ao tunnel e re-rodar `deploy-glossario.yml --ref main -f mode=deploy` (ou smoke manual) para fechar.
- 2026-06-11 — **E004 destravado (ponte) + deploy-mesas verde.** PR #15/cherry-pick `d38307d` em `dev`: `pnpm.overrides "@types/multer>@types/express": "^4.17.21"` no root força os tipos do multer a express-4 → mesas-backend compila. `deploy-mesas mode=ci` dispatch em `dev` = **success** (mesas CI verde 3m38s). Runtime/rotas do multer intocados. **Ponte** — fix definitivo = **spec 016** (mesas→express 5, D060), que REMOVE o override. Path-filters dos deploys não cobrem root `package.json`/lock (anotado; por isso validei via dispatch).
- 2026-06-11 — **Spec 012 executada T1–T4 (LOCAL, build verde; sem deploy).** Glossário legado (`C:\projetos\glossario_rpg_artificio`, React18/Vite/Tailwind + Express/pg, DB `glossario_v2`) importado p/ `apps/glossario` (`@artificio/glossario-{frontend,backend}`): backend (34 arq) à mão, frontend via robocopy; React 18→**19** p/ consumir `@artificio/ui`; Dockerfiles multi-stage monorepo; `docker-compose.{beta,prod}.yml` na `artificio_net` (containers/volumes preservados); `deploy-glossario.yml` via `_deploy-module.yml`. **Reuso de chrome (D058):** `GlossarioHeader` compõe o `@artificio/ui` Header compartilhado (nav domínio + `userMenu`/`actions`/`sessionOverride` legado) — **não duplica shell**; `packages/ui` Header ganhou props aditivas `onLogout`/`onLoginClick`/`loginLabel` (default SSO) p/ reuso com auth não-SSO interino. `turbo build` verde: glossário (1580 módulos) + **mesas/accounts/site (pétrea de shared, 8/8 verde)**. **Decisões D057** (hostname canônico `glossario.artificiorpg.com`; `glossariorpg.` era alias histórico desativado) **e D058** (shell único reusável). **PR #14** aberto (feat/glossario-monorepo → dev). **Review (Amazon Q + Codex) corrigido:** 2 bloqueadores de deploy — `nginx.conf.template` ausente (E002, criado) + runner de migrations tentaria reaplicar legadas (E003 → `database/legacy/`, **D059**); 6 de segurança — JWT fallback removido, admin email→env, CORS restrito a `*.artificiorpg.com`, validação register/login, fail-fast POSTGRES_PASSWORD. **Achado pré-existente E004:** mesas-backend NÃO compila no `dev` (`@types/multer@2` traz tipos express-5 vs express-4 do mesas) → **spec 016** (padronizar monorepo em **express 5**, **D060**) + task. Falta: merge PR→dev, deploy (bootstrap VM = `.env` com senha do volume vivo + rota tunnel `glossario.` = mantenedor/segredo), nav/prod, SSO+compat (spec 015). Sessão `26-06-11_2`.
- 2026-06-11 — **Specs 012–015 criadas (planejamento glossário/links/nav/SSO; sem código).** Mantenedor definiu: glossário vai **direto pro monorepo** servindo em **`glossario.artificiorpg.com`**; `glossariorpg.` era alias histórico pré-monorepo, não rota ativa. Restaurar `links.artificiorpg.com` + `regras.artificiorpg.com` (fora do ar desde a migração; artefato de backup NÃO está em `artificiobackup` — `MANIFEST.md` sem `code/`, T6/spec 001 não executada, D027; T1 da 013 = localizar com mantenedor; confirmar `regras` = ex-`servidorvirtual`); item **"WhatsApp"** → `links.` no nav compartilhado (`packages/ui` + site `MODULES`, spec 014); SSO accounts no glossário com **compat por email** do login antigo (BCrypt/JWT legados aposentados; link `users.email`→`sso_user_id`; preserva termos/votos/comentários; email não-Google = **fluxo de reivindicação** autorizado: valida senha antiga (endpoint de migração, sem sessão) → conectar Google → herda tudo; spec 015, fecha Gate D glossário). links/regras: se artefato não aparecer, **refazer do zero**. Ordem: 012→013→014→015. Sessão `26-06-11_1`.
- 2026-06-08 — **Spec 011 Fase 2 — mídia (T18 backend + T19 UI) implementada + validada LOCAL.** **T18:** migration `004_media_library.sql` (online-safe: estende `media` com `source`/`url`/`cloudinary_public_id`/`size_bytes`/`caption`/`title`/`created_by`/timestamps; ids nativos do `site_content_id_seq`; `wp_url` opcional; backfill `url`; preserva `media`/`media_map`); API `/api/admin/v1/media` (GET busca+filtro tipo+paginação, POST multipart `multer` 15MB com validação de **MIME real `file-type`** + allowlist + **rejeita SVG/desconhecido 415**, PUT meta, DELETE); `server/lib/media-store.ts` (Cloudinary `upload_stream` gated por `CLOUDINARY_URL`; sem cred = modo local em `apps/site/uploads` servido em `/uploads`). **T19:** rota Mídia no `site-admin` (grid/upload/busca/filtro/editar meta/apagar — `MediaLibrary`/`MediaPage`), `MediaPicker` modal reusável (featured/OG/inserir no editor), `BlockEditor.insertImage`. Deps backend: `multer`+`file-type`. **Validação:** harness `t18-validate.ts` 10/10 ✓; smoke HTTP (401/201 PNG//uploads 200/SVG 415/TXT 415/PUT 200/filtro/DELETE) ✓; site tsc+astro build ✓; site-admin build ✓. **COMMITADO + DEPLOYADO no beta + promovido** (`main=dev=e5ee84e`). Commits: `292dd06` (mídia), `206c780` (compose passa 3 vars Cloudinary), `e5ee84e` (boot-fix). **Cloudinary no beta:** mantenedor copiou as 3 vars (`CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET`) do `.env.beta` do mesas → `apps/site/.env.beta` (reusa conta do mesas; `secrets.7z` do backup deu erro de senha/corrupção, não usado). **Incidente + fix:** 1º deploy caiu (502) — ativar Cloudinary fez o import-on-start subir ~485 mídias WP→Cloudinary no boot, estourando o healthcheck. Fix `e5ee84e`: **migração em massa WP→Cloudinary virou opt-in `SITE_MIGRATE_MEDIA=true`** (default off = import dry-run rápido); uploads NOVOS do admin (`media-store`) seguem indo pro Cloudinary. Redeploy verde, beta recuperado: healthz 200/posts:125, `/api/admin/v1/media` 401 (gated), WP raiz 200. Falta: E2E autenticado do mantenedor (upload no /admin → Cloudinary). Migração das mídias WP antigas = rodar com `SITE_MIGRATE_MEDIA=true` quando quiser (tarefa à parte).
- 2026-06-08 — **Fix de persistência de sessão SSO (refresh-retry no cliente) — `packages/auth` SDD Completo.** Sintoma (mantenedor): login não persistia, pedia login de novo ao fechar aba/navegador. **Diagnóstico (cookies do browser do mantenedor):** `artificio_refresh` presente e persistente (Domain `.artificiorpg.com`, Expires +7d); `artificio_session` ausente (access 15min expira e o browser remove). Backend já tinha `/api/auth/refresh`, mas **nenhum cliente o consumia** → ao expirar o access (15min) a sessão parecia morta apesar do refresh válido 7d. **Fix:** `@artificio/auth/client` ganhou `refreshSession()` (single-flight) + `authFetch()` (401→refresh→retry); `useSession` tenta refresh no 401. Consumidores: `apps/mesas/frontend/authenticatedFetch` (retry), `apps/site-admin/api.ts` (retry inline, toolchain isolado sem dep nova), `apps/site/Base.astro` (header tenta refresh no `/me`). beta→accounts é same-site (`artificiorpg.com`), cookie `SameSite=Lax` flui. **Validação:** auth build+test 3/3 ✓; accounts 8/8 ✓; mesas build ✓ + frontend 109/109 ✓ (backend: 1 suite `ingestMessages` falha por `DATABASE_URL` ausente local = pré-existente/ambiental); site-admin build ✓; site tsc+astro build ✓. Caveat: se o browser estiver setado p/ limpar cookies ao sair, nada no código reverte (cookies do servidor são persistentes, `Max-Age` confirmado). **COMMITADO + DEPLOYADO:** commit `356b650` (`12ce2ab..356b650 dev`); CI verde; deploys verdes em **mesasbeta** (auto push-dev), **site beta** (dispatch), **mesas prod** (`deploy-mesas mode=deploy` ref main); promote FF `main=dev=356b650`. Smoke: beta/mesasbeta/mesas prod 200, `/api/v1/me/options` 401, refresh 401 sem cookie, WP raiz 200. `accounts` não mudou (fix é no cliente). site prod = beta-only (Gate C).
- 2026-06-08 — **Spec 011 / Site CMS — T16 (núcleo) validado LOCAL + T17 implementado (LOCAL, sem deploy).** Claude executa, Codex valida/arquiteta. **T16:** E2E autenticado local (server tsx + pglite + token admin HS256 via `Authorization: Bearer`): gate 401/200, criar draft, preview stateless (`noindex`+XSS sanitizado), preview persistido, **rebuild/SSG completo verde** (astro 219 arquivos + pagefind 125 + swap atômico junction). Smoke beta público OK (home/blog/healthz=200, /admin/status=401, sitemap/robots=200, WP raiz=200). Falta E2E Google real no beta = mantenedor. **Bug corrigido:** publish via `/posts/:id/status` não carimbava `published_at` → `date:""` → `@astrojs/rss` abortava build; fix em `setPostStatus`/`setPageStatus` + RSS ignora data inválida. **T17 (operações editoriais + publicação honesta):** backend `deletePost`/`deletePage` (só de `trash`, 409 `must_trash_first`, limpa `post_taxonomies`, preserva `redirects`), `getPostStatus`/`getPageStatus`, `maybeRebuild(new, prev)` com prev real; frontend PostsList/PagesList com filtro de status + ações por item (publicar/despublicar/arquivar/lixeira/restaurar/apagar+confirm) + toast; PostEditor/PageEditor slug live-check + aviso 301, `scheduled`/`private` desabilitados, campos OG + avisos de `noindex`. **Validação:** tsc site ✓, vite build site-admin ✓, harness `scripts/t17-validate.ts` 15/15 ✓, smoke HTTP 401/403/409/200/404 ✓. **COMMITADO + PUSHADO + DEPLOYADO no beta (autorizado por ação).** Commit `11f1736` (`22fe461..11f1736 dev`, 15 arq). CI verde: pr-checks/deploy-site(CI)/deploy-accounts/promote-dev-to-main. `deploy-site mode=deploy` run `27139842102` **verde** (`Deploy site beta=success`). Smoke pós-deploy: healthz=200(posts:125), `/`,`/blog/`,`/admin/`,sitemap,rss,robots=200; `/admin/status`+`/api/admin/v1/posts`=401(gated); WP raiz=200 intocado. `.pgdata` local recriado via `pnpm sync` (125/125) após corrupção por kill durante E2E; seeds revertidos. Arquivos: `apps/site/db/repo/{posts,pages}.ts`, `apps/site/server/admin-api.ts`, `apps/site/src/pages/rss.xml.ts`, `apps/site/scripts/t17-validate.ts`(novo), `apps/site-admin/src/{api.ts,styles.css,pages/{PostsList,PagesList,PostEditor,PageEditor}.tsx}`. **Próximo:** revisão Codex → aprovação de commit/push/deploy beta → E2E autenticado do mantenedor (T16 fecha) → restante da Fase 2 (mídia T18/T19, taxonomias T20).
- 2026-06-08 — **Gate D `mesas` FECHADO.** Evidência read-only/HTTP: home 200; `/api/v1/me/options` sem cookie 401; `/api/v1/auth/google` 302 para `accounts.artificiorpg.com/login?return=https%3A%2F%2Fmesas.artificiorpg.com%2F`; allowlist prod sanitiza `https://evil.com/x` para `https://accounts.artificiorpg.com` e preserva `https://mesas.artificiorpg.com/perfil`. Browser chegou em `accounts/login?return=mesas/perfil`; mantenedor confirmou login real funcionando e autorizou fechar. Sessão `26-06-04_4` concluída; `specs/004` T8 marcado OK. Próximo: prompt para Claude executar Spec 011 T16/T17 sob orquestração/revisão Codex; commit/deploy só com autorização por ação.
- 2026-06-07/08 — **Docker cleanup semanal + lock RW VM-wide (D055/D056) CONCLUÍDO.** Workflow `docker-cleanup.yml` migrado do legado mesas → monorepo (cron domingo 03:00 UTC via DEPLOY_*, remove dangling/cache>7d/containers>7d + keep-3 imagens/repo; nunca toca volumes/redes). Review Amazon Q+Codex corrigido: Q1 guard `GITHUB_RUN_ID`, Q3 keep-3 por ordem-default do docker, Q2 rejeitado; **C1/C2 → lock RW `/tmp/artificio-vm-mutate.lock`** (deploys=shared `flock -s`; manutenção=exclusive `flock -x` segurado durante prune) tocando `_deploy-module`+`deploy-accounts` (SDD Completo). `.gitattributes` pina workflows como LF. PRs #11+#12 merged; `origin/main=origin/dev=22fe461`; `promote-prod-fast-forward`, `deploy-site`, `deploy-mesas`, `deploy-accounts`, `pr-checks` e `Docker Cleanup` verdes; stub do cleanup legado no `mesas_rpg_artificio` está na `dev` sem `schedule` e com job `if:false`. Sessão `26-06-07_1` fechada.
- 2026-06-06 — **Fix SSO cross-subdomínio COMMITADO + DEPLOYADO (beta + accountsbeta).** Bug: login no `beta` ia pro `accounts/login` sem `return` → OAuth voltava pro `accounts.`; tema não compartilhado; header não refletia sessão. Correção (Codex local → Opus commit/deploy): `SiteHeader` gera `/login?return=<URL atual>`; `Base.astro` cookie compartilhado `artificio_theme` (`Domain=.artificiorpg.com`) + atualiza link com `href`/theme + consulta `/api/auth/me` e troca botão p/ **Admin**(`/admin/`, role admin) ou **Conta**; `accounts/login` reusa sessão SSO (redireciona sem novo Google), lê/grava tema; allowlist de `return` (front+back) aceita ápice `https://artificiorpg.com` + `https://*.artificiorpg.com` (ápice = destino FUTURO pós-cutover, não fallback; WP/raiz intocáveis, Gate C adiado); CORS accounts `^https://(?:[^.]+\.)?artificiorpg\.com$`. Commits `612189c` (return+theme) + `<este>` (session link + CORS). Deploys verdes; smoke: link Entrar c/ return, `/api/auth/me` 401 sem cookie, `state.returnUrl` preservado por domínio (apex/beta/mesasbeta), externo rejeitado, WP raiz 200. `@artificio/auth` intocado. `.claude/launch.json` agora gitignored. Falta E2E browser (Google real, sync de tema, botão pós-login) = mantenedor.
- 2026-06-06 — **Spec 011 fase 1 COMMITADA + DEPLOYADA no beta (autorizado).** Commit `a24f187` (`7f2fbd1..a24f187 dev`, 50 arq). Deploys verdes: `deploy-site` dispatch `27075005165` (beta) + `deploy-mesas` push `27075001796` (mesasbeta, packages/content tocado) + pr-checks/gate verdes. **AR verificado:** `beta.artificiorpg.com/admin/` serve a SPA admin; `/admin/status`+`/api/admin/v1` = 401 (gated SSO); público 200 (home/blog/healthz, posts=125); mesasbeta 200; WP raiz 200 intocável. **Área administrativa do site NO AR** (autoria de posts/páginas, editor de blocos). Falta validação E2E autenticada do mantenedor (logar como admin SSO e criar/editar/publicar). Próximas fases: upload de mídia (Cloudinary), dashboard, curadoria portal/hub, roles granulares. `.claude/launch.json` + `midias/Intel-*.exe` ficaram untracked (não commitados).
- 2026-06-06 — **Spec 011 — revisão Codex 2ª rodada (5 achados) toda corrigida (LOCAL).** (1) preview publicava→**preview stateless** (`POST /api/admin/v1/preview` renderiza buffer sanitizado, não persiste; SPA abre via blob); (2) rebuild concorrente perdido→`jobs.ts` coalescing (trailing rebuild ao fim do job em curso); (3) redirect novo só após 30s→`server/redirect-cache.ts` + `reloadRedirects()` imediato pós-gravação; (4) code de redirect→só {301,302,307,308}; (5) GET preview/:id→valida inteiro>0. Verificado (preview sanitiza+noindex, redirect imediato, builds/tsc verdes). Detalhe `handoff-review.md` §2ª rodada. **Nada commitado/deployado.**
- 2026-06-06 — **Spec 011 — revisão Codex (8 achados) toda corrigida + T12 público (LOCAL).** Codex revisou o código da área admin; 8 achados (4 altos/3 médios/1 baixo), **todos corrigidos e verificados**: (1) XSS→sanitizador robusto `sanitize-html` no caminho de escrita; (2) 301 não servidos→middleware lê tabela `redirects` antes do estático (verificado 301); (3) swap não-atômico→`rebuild.mjs` com symlink A/B (POSIX) + fallback rename-restore; (4) publish 2-requests→servidor dispara rebuild no publish (`maybeRebuild`), SPA 1 request; (5) id sem validação→`parseId`+404; (6) taxonomias FK→pré-filtra ids válidos; (7) SPA salvava só draft/publish→"Salvar" respeita status; (8) pages sem canonical→coluna+wiring. **T12 público**: `@artificio/content` `buildMeta` aditivo (ogTitle/ogDescription/twitterCard/noindex; 6/6 testes; só site consome) + export + `Base.astro`/`[slug].astro` emitem OG/canonical/noindex (verificado no HTML buildado). Detalhe em `specs/011-site-cms-authoring/handoff-review.md`. Sem achados (Codex): authz das rotas, SQL injection, migration 003. Follow-up: transação plena nas taxonomias. Deps: +`sanitize-html`. **Nada commitado/deployado.**
- 2026-06-06 — **Spec 011 (CMS/autoria nativa do site) — Fase 0 + parte da Fase 1 (LOCAL, sem deploy).** Super spec criada/revisada p/ **portal/hub completo** (área K) + Apêndice A (paridade WordPress). Mantenedor reordenou: **funcional primeiro (add/editar posts+pages)**; dashboard+roles granulares adiados. Editor = **estilo Gutenberg de blocos** (não o `wp-editor`). **Decisões D051** (editor=BlockNote, `block_doc`+`content_html`), **D052** (roles/capabilities no `apps/site`/`site_users`, `packages/auth` intocado; gate atual=`requireAdmin`), **D053** (rebuild ATÔMICO build→`dist.next`→swap sem downtime + preview render no Express, sem adapter Astro), **D054** (admin = **pacote próprio `apps/site-admin`** no toolchain latest: vite 8 + plugin-react 6 + react 19.2 + router 7 + blocknote 0.51, servido pelo Express em `/admin`; regra do mantenedor: **sempre versões latest, sem gambiarra**; majors do monorepo Astro/TS = decisão à parte). **Construído+testado local:** migration `003_authoring.sql`; `db/repo/{posts,pages,taxonomies,redirects}`; `server/lib/content.ts` (slug PT/excerpt/sanitize/toc); API `/api/admin/v1/*`; preview `/admin/preview/:type/:id`; rebuild atômico; SPA `apps/site-admin` (editor BlockNote + CRUD posts/pages + slug+sugestão + tax inline + SEO/OG + publish→rebuild + preview). Smoke: tsc limpo, vite build verde, `/admin/` 200, API/status/preview **401 sem SSO**. **Falta:** OG público (export+templates), mídia/upload (Fase 2), E2E autenticado (no deploy, requer SSO real + autorização). **Nada commitado/deployado.** `pnpm-lock` mudou (deps admin). Sessão `26-06-06_1_site_cms-authoring`.
- 2026-06-06 — **Spec 010 CONCLUÍDA + no ar nos betas (nav cross-módulo + fix logo).** R1: `apps/site/SiteHeader.astro` agora mostra o nav cross-módulo do portal (`MODULES` = Portal/Glossário/Mesas/Downloads/Esferas/SRD, zero-JS, R4=A) como primário; categorias do blog → 2ª linha (`.artificio-subnav`/`SECTIONS`); `MODULES` em `content.ts` (espelha `defaultNavItems`, sem puxar barrel React no Astro); `SiteFooter` dedup. R2: logo "quebrada" era **bug real, não extensão** — `brandLogoNeg.src` em `packages/ui/src/brand.ts` (+ cópia `apps/site/src/data/brand.json`) tinha **PNG corrompido** (IHDR válido 300×100 mas body lixo pós-IHDR → browser `naturalWidth=0` → alt `⬚Artifício`); reproduzido no preview local. Fix: re-encodado de `packages/ui/src/_logo_neg.png` (válido) → `brand.ts`/`brand.json`. T5: `modules.ts` `Glossario`→`Glossário`. **Push+deploy autorizados** `c488e77..7f2fbd1 dev` (inclui R6 da spec 009 `2681612`). Deploys verdes: site(beta)/mesas(mesasbeta)/accounts(accountsbeta); smoke 200 + WP raiz 200; HTML servido confirma nav + logos 300×100 decode OK. **Afeta site+mesas+accounts** (asset compartilhado). Prod só após promote `main`. Commits locais pendentes zerados (dev = origin/dev = `7f2fbd1`).
- 2026-06-06 — **Spec 009 finalizada + spec 010 aberta (nav cross-módulo + logo).** 009: R1–R5 live+validados; **R6 (entrypoint restart-resilience) commit local `2681612` NÃO deployado** (mantenedor dispensou push+redeploy — não forçar). **Bloqueio ativo:** push e deploy do site exigem **autorização explícita** do mantenedor (classifier negou; "finalizar" não re-autoriza). Commits locais pendentes de push: `861128d`, `2681612`. **Spec 010** (`specs/010-ui-nav-logo/`): (1) site sem nav cross-módulo (usa categorias do blog em vez de `defaultNavItems` Portal/Glossário/Mesas/...); (2) logo "quebrada" no browser do mantenedor — **diagnóstico: asset PNG 300×100 VÁLIDO, data-URI no HTML, SEM CSP** → hipótese extensão do browser OU mesas com `@artificio/ui` stale; **desambiguar com janela anônima** (T2). Próximo chat retoma pela spec 010.
- 2026-06-06 — **Esteira de deploy BLINDADA (spec 009 / D050) — validada nos betas.** Após 3 incidentes no 1º deploy do site, hardening do `_deploy-module` (compartilhado): **R1** reconcile de leftover (remove container de nome esperado de outro projeto compose antes do 1º `up`; não toca volume/outros nomes), **R2** guard de exec-bit no `pr-checks` (`scripts/ci/check_entrypoint_exec_bit.sh`), **R3** erro `.env` acionável + `docs/agents/deploy-runbook.md` (bootstrap dispatch-first), **R4** smoke/health no `GITHUB_STEP_SUMMARY`. Commit `c488e77`. **R5 validado:** push disparou `deploy-mesas`(mesasbeta)+`deploy-accounts`(accountsbeta) **verdes** com a esteira nova (healthy, smoke 200/401/302), `guard-entrypoint-exec`✓, actionlint/ShellCheck✓, reconcile **no-op** em VM limpa = zero regressão. `container_name` fixo→nomes por projeto adiado (T9). Aprendizado: blindar classes, não tapar buraco.
- 2026-06-06 — **🎉 Módulo `site` NO AR em `beta.artificiorpg.com` (deploy beta OK).** `deploy-site mode=deploy` run `27065214548` verde (2m8s): `jwt_secret_shared=true`, snapshot criado, migração no-op (site migra no entrypoint), `healthy_site-beta-app=true`, smoke `home=200/healthz=200/blog_index=200/admin_protected=401`. Externo confirmado: `/`,`/blog/`,`/blog/<slug>/`,`/robots.txt`,`/sitemap-index.xml`=200, `/admin/status`=401, `/healthz`={ok,**posts:125**}, **WP raiz `artificiorpg.com`=200 intocável**. 125 posts reais importados WP→store→Astro SSG, atrás do Cloudflare Tunnel. **2 fixes no caminho:** (1) gate dispatch-only (bootstrap); (2) leftover `site-beta-db` da validação manual (mantenedor removeu container+volume); (3) `chmod +x docker-entrypoint.sh` (estava 100644 → permission denied) commit `13c2a84`. Containers `site-beta-app`+`site-beta-db` na `artificio_net`. **Falta p/ Gate D site:** habilitar auto-deploy push-dev (D041) agora que `.env.beta` existe; validação Opus final; opc. Cloudinary/GA reais; Header island. WP/raiz seguem p/ cutover futuro (Gate C).
- 2026-06-06 — **Módulo `site` — `dev` pushado; CI verde; deploy aguarda bootstrap do mantenedor.** Push `2daf035..9338f3d dev` (8 commits site: F2/F3+content+cloudinary+analytics+backend+pages+deploy-artifacts+gate-fix). **deploy-site CI verde** (`CI site` 1m27s: install/build `@artificio/site*` astro+pagefind/test ok); job Deploy skipado (gate `workflow_dispatch mode=deploy` only — bootstrap-safe). `_deploy-module` **verificado compatível** c/ site: passo `apply_required_migrations.sh ... database` é no-op gracioso (`apps/site/database` ausente → exit 0; site migra no entrypoint). Push tocou `packages/**` → **deploy-mesas + deploy-accounts (betas) redeployaram** (efeito esperado D041; mantenedor conferir mesasbeta/accountsbeta healthy). **Falta (mantenedor, guiado por ChatGPT):** na VM `/opt/artificio-beta` → `git fetch+reset --hard origin/dev` (materializa apps/site) → criar `apps/site/.env.beta` (POSTGRES_PASSWORD, DATABASE_URL=postgres://admin:…@site-beta-db:5432/site, JWT_SECRET=igual accounts/.env.beta, SITE_IMPORT_ON_START=true) → rota Cloudflare `beta.→site-beta-app:4322` → `Run workflow deploy-site mode=deploy` → smoke. Depois do 1º deploy OK, habilitar auto-deploy push-dev no `deploy-site.yml`.
- 2026-06-05 — **Módulo `site` — artefatos de deploy beta (D049, prontos p/ mantenedor flipar).** Modelo: 2 containers (`site-beta-app` Express serve Astro `dist/`+`/healthz`+`/admin/*` · `site-beta-db` postgres:16) na `artificio_net`; **build roda IN-CONTAINER no entrypoint** (`migrate→import WP→export→astro build→pagefind→serve`) porque o build do site depende de DB+WP. Autorados: `apps/site/{Dockerfile, docker-entrypoint.sh, docker-compose.beta.yml}` + `.github/workflows/deploy-site.yml` (DRAFT, espelha deploy-mesas via `_deploy-module`, env=beta, push `dev`). `server.ts` ganhou serve estático do `dist/`. **Validado local:** server único smoke `/ /blog/ /sobre-nos/ /healthz /robots.txt`=200, `/admin/*`=401, inexistente=404. Docker não testado (sem Docker local). **Ação do mantenedor:** GitHub Environment + secrets (`POSTGRES_PASSWORD`,`DATABASE_URL`,`JWT_SECRET`=prod,opc `CLOUDINARY_URL`/`PUBLIC_GA_ID`), rota Cloudflare `beta.→site-beta-app:4322`, validar passo de migração do `_deploy-module` (site migra no entrypoint), push `dev`. **Site feature-completo local; falta só infra/flip + Header island (decisão).**
- 2026-06-05 — **Módulo `site` — pages institucionais (D046).** Importador agora puxa `page` por allow-list (10/10: sobre-nos, contato, política-de-privacidade, termos-de-serviço, media-kit, newsletter, grupos-whatsapp, material-online, curso-de-traducao, coyote-e-crow), sanitize + media-map; export → `src/data/pages.json` (10 pages, 118KB, commitado real); rota raiz `[slug].astro` (getStaticPaths = slugs de pages, não colide c/ /blog,/rss,etc.). Build = **219 páginas**. Verificado: `sobre-nos` renderiza limpo (Gutenberg, 0 inline-style/script). Pages de mesas/woo/outros módulos excluídas. ⬜ dups (contact/politica-2 → 301 no cutover). **Pendente site:** Header island, deploy beta (secrets/VM/DNS = mantenedor).
- 2026-06-05 — **Módulo `site` — backend HTTP + admin + rebuild webhook (D006).** `apps/site/server/` (Express + `@artificio/auth`, cookie `artificio_session`): `GET /healthz` (`{ok,posts}`), `GET /admin/status` (stats+job), `POST /admin/rebuild` (export+astro build+pagefind = gatilho SSG incremental), `POST /admin/import` (re-import WP) — admin gated `role==='admin'`; jobs single-flight (`server/jobs.ts`, lock memória). Scripts `serve`/`rebuild`. Smoke local: health 200 `{posts:125}`, `/admin/*` 401 sem cookie (SSO protege). Estático (Astro dist) servido à parte. Commit pendente. ⬜ UI admin, webhook→GH Actions prod, deploy beta. **Pendente site:** pages institucionais, Header island, deploy beta (secrets/VM/DNS = mantenedor).
- 2026-06-05 — **Módulo `site` — `packages/analytics` (GA4 cross-subdomínio, D020).** Pacote `@artificio/analytics` (TS puro, zero deps): `gtagSrc`/`gtagInlineConfig` (cookie_domain raiz `.artificiorpg.com`), `trackEvent`/`trackPageview`; 3/3 testes. Integrado no site (`Analytics.astro`, gated `PUBLIC_GA_ID`); build com id dummy injeta loader+config (cookie_domain raiz) — verificado. **Tripé de pacotes compartilhados completo: `config`/`auth`/`ui`/`content`/`analytics`** (falta só `crosslink` p/ srd/esferas). G-ID real + exclusão de referral interno + 1 property = painel GA4 (mantenedor). **Pendente site:** pages institucionais, backend HTTP/admin + rebuild webhook, deploy beta.
- 2026-06-05 — **Módulo `site` — `packages/content` (SEO) + Cloudinary mídia (env-gated).** **F5 content:** pacote `@artificio/content` (TS puro, zero deps) — `buildMeta`(OG/Twitter), `articleLd`/`breadcrumbLd`/`organizationLd`/`websiteLd`, `sitemapXml`, `robotsTxt`, `SITE`/`canonicalUrl`; 6/6 testes; integrado no site (Base/[slug]/index + rota `robots.txt`); reusável por mesas/glossário. Build verifica OG/Twitter/JSON-LD(Article+BreadcrumbList+Organization)/canonical raiz/robots OK. Commit `9c65452`. **Cloudinary:** `importer/media.ts` + migration `002_media_map` — env-gated por `CLOUDINARY_URL`: com creds faz upload original WP + reescreve `src` (featured+inline) + cache idempotente `media_map`; sem creds = dry-run mantém URLs WP. Funções puras (extract/rewrite/publicId) verificadas; dry-run não popula `media_map`. Migração real de mídia = mantenedor setar `CLOUDINARY_URL`. **Pendente:** pages institucionais, backend HTTP/admin + rebuild webhook, `packages/analytics`, deploy beta.
- 2026-06-05 — **Módulo `site` — F2 store + F3 importador rodados (local, pglite).** Sem Docker/PG local → **pglite** (Postgres WASM in-process) p/ dev, **pg** p/ prod, atrás de adapter `query()` (`apps/site/db/connection.ts`); driver por env (DATABASE_URL). **F2:** schema `001_init.sql` (posts/taxonomies/post_taxonomies/media/pages/comments/redirects; slug UNIQUE, categoria pai/filho, N:N) + runner `db/migrate.ts` (schema_migrations + advisory lock pg + transacional, D039). **F3:** importador WP REST read-only (`importer/{wp,sanitize,run}.ts`), idempotente `ON CONFLICT id`, sanitize allowlist, Yoast→meta. **Rodado:** `migrate→import→export→build` em pglite → **paridade 125/125 posts ✓**, 82 taxonomias, 25 comentários; `db/export.ts` gera `src/data/posts.json` (desacopla Astro do DB); **build = 209 páginas** (125 artigos+arquivos+home) + Pagefind (125, 16460 palavras). Pipeline em `apps/site/README.md`. `posts.json` versionado = seed pequeno; `.pgdata` gitignored; full content via `pnpm sync`. Mídia dry-run (URL WP; Cloudinary depois). **Pendente:** Cloudinary, pages institucionais, backend HTTP/admin + rebuild webhook, `packages/content`+`analytics`, deploy beta. Kysely (canon) no backend HTTP futuro; importador descartável usa SQL cru (anotado).
- 2026-06-05 — **Módulo `site` — D048 + Etapa-1 frontend Astro no ar (local).** Mantenedor: Codex pausado (créditos), **Opus executa**; pediu opções de framework/template/funcionalidade antes (rubric: minimalismo+leitura+SEO+10 heurísticas Nielsen+ISO 9241-11+marca D040). Opções documentadas em `specs/008-site-foundation/template-options.md`; **D048** fixou **Astro** (SSG islands, zero-JS) + admin React SPA + backend Express/PG + Pagefind; visual **híbrido A+B**; **core completo**. Protótipo estático (conteúdo WP real, marca real) construído e **aprovado pelo mantenedor** (desktop). Portado p/ **`apps/site` Astro real**: build verde (10 páginas+sitemap), paridade total, reusa `@artificio/ui` (CSS+logos), home/artigo/404/dark-light/TOC/SEO(JSON-LD/canonical/OG). **Etapa-2 core ✅** (build **35 páginas**): arquivos `/blog/` + categoria + tag + pills; **busca Pagefind** (modal marca, `/` e Cmd/Ctrl+K, índice 8 artigos); **RSS** (8 itens) + sitemap + 404. **Commit `44ccae9` em `dev`** (spec 008 + D044–D048 + apps/site + docs; `main ⊆ dev` OK; **sem push**). Header/Footer = `.astro` zero-JS (island React adiado — `@artificio/auth/client` é browser-only → exigiria `client:only`; zero-JS é melhor p/ blog público; decisão do mantenedor). **Próximo:** F2 (store Postgres) + F3 (importador WP REST, agente `wp-importer`) → Content Layer lê do PG (hoje lê fixtures `src/data/posts.json`). Depois: deploy beta (T20/T21, secrets/VM/DNS = mantenedor). `.claude/launch.json` (untracked) p/ preview local (proto:4399 / build:4321). Web search/fetch caíram nesta sessão (re-verificar nomes de tema/versões Astro antes de produção).
- 2026-06-05 — **Módulo `site` — Fase 3 iniciada (spec 008, fundação).** Recon READ-ONLY do WP (REST): **risco re-dimensionado p/ BAIXO** — 125 posts (não 300+), HTML **Gutenberg limpo** (zero Elementor/shortcode nos posts), **Yoast meta** completo, 1 autor, 13 cat aninhadas, 69 tags, 25 comentários, 485 mídia. WP é multi-módulo (`definicao`163/`magia`328/`docs`17 = outros módulos, fora). Inventário durável: `docs/agents/wp-content-inventory.md`. 4 decisões resolvidas c/ mantenedor → **D044** (site só `beta.` por ora, fase = levantamento/decisão/doc, "não se perder"), **D045** (importador = WP REST, read-only), **D046** (escopo post+taxonomias+mídia+pages filtradas+comentários; CPTs fora), **D047** (preservar `/blog/<slug>/`, 301 ativa só no cutover). Spec `008-site-foundation` criada (spec/plan/tasks, 7 fases F1–F7) + sessão `26-06-05_2`. **F1 (fundação) ✅.** Próximo: F2 (store Postgres) — Codex, quando mantenedor liberar execução de código.
- 2026-06-05 — **CDX-312 + guard imagem + hydrate-fix → DEV e PROD.** PR #7 squash em `dev`; deploy beta verde; beta validado (dropdown OK por mantenedor; smoke 200/401/302; CSS `artificio-usermenu*`). FF `main←dev` (`86b26c4`, bootstrap manual do mantenedor pois `promote-prod-fast-forward.yml` ainda não estava em `main`). Deploy prod: `deploy-mesas mode=deploy` (`27034408480`) + `deploy-accounts mode=deploy` (`27034731785`) verdes. Prod validado: mesas 200/401/`return=mesas`, accounts 200, CSS CDX-312 servido. **Menu de conta restaurado em prod+beta.** Pendências: (1) **hydrate** `/sync/hydrate` dá 500 — falta `PROD_DB_URL` no `mesas-beta-api` env (`.env.beta` não tem); adicionar DSN read-only prod + restart (write VM+segredo = mantenedor/Codex). (2) **Imagem data-url** = task spawnada (guard já em prod; falta limpeza de dado/Cloudinary). (3) `.claude/settings.local.json` ganhou allowlist ampla (`git push/commit *`, `gh api *`, `irm|iex`) — revisar depois.
- 2026-06-03 — Plano G1 aprovado em decisões macro. Camada de governança criada (13 arq).
- 2026-06-03 — Coração de economia de contexto: `decisions.md` (append-only), `token-economy.md` (reload T0/T1/T2, caveman default — nível superado por **D068/caveman ultra**), `arquiteture.md` (contratos por seção). Reload contract em tiers ativo.
- 2026-06-03 — Revisão de consistência vs requisitos. Achada contradição topologia (path único × WP fica × site beta). Resolvida 1ª via D015/D016 (interim híbrido). Gaps menores (páginas não-importáveis→rebuild manual; versão principal=branch main) corrigidos.
- 2026-06-03 — **Virada de topologia (mantenedor):** path único → **subdomínio-por-módulo (D017)**, SSO em `accounts.` (D018), blog na raiz (D019), analytics/SC cross-subdomínio (D020). Supera D002/D015. Dissolve a contradição WP-raiz e a dança de cutover dos módulos. Re-propagado em 11 arquivos. Grep limpo.
- 2026-06-03 — Spec `001-infra-backup-runbook` criada (spec+plan+tasks T1–T13) + sessão `26-06-03_1`. Primeira spec SDD real.
- 2026-06-03 — **T1 executada** via `ssh faren` (D023). Mapa em `docs/agents/infra-map.md`. 4 bancos G1, WP externo, telegram/foundry fora (D021).
- 2026-06-03 — Escopo backup final: **VM only** (WP fora=Hostinger cloud D024; uploads on-demand D025; secrets→`secrets.7z` D026). Criado `access-registry.md`. tasks T3 dropada, T4 diferida p/ Fase 3, T7 = `.env` dos serviços.
- 2026-06-03 — Módulo **`esferas`** (`esferas.artificiorpg.com`), multi-sistema sistema×edição (D&D 2014/2024 principal, PF futuro) — D028. Rename `spheres`/`wiki-sop`→`esferas` em todos os docs.
- 2026-06-04 — **CDX-308A concluído:** legado `C:\projetos\mesas_rpg_artificio` importado para `apps/mesas` sem segredos reais. `pnpm install` OK; `pnpm --filter @artificio/mesas build` OK; frontend tests 13/13; backend tests 104/104 com env dummy local. VM/deploy não tocados.
- 2026-06-04 — **CDX-308B concluído local:** mesas usa `@artificio/ui` + `@artificio/auth`; backend valida `artificio_session`; OAuth local aposentado. Turbo build OK; accounts tests 6/6; auth tests 3/3; mesas frontend 15/15; mesas backend 106/106. VM/deploy não tocados.
- 2026-06-04 — **CDX-308C deploy técnico concluído:** `mesas-api`, `mesas-app`, `mesas-cron` rebuildados na VM com Docker no-cache; `mesas-api` e `mesas-app` healthy; smoke `internal_api=200`, `public_home=200`, `private_no_cookie=401`, `/api/v1/auth/google` → `302 accounts.artificiorpg.com/login`. Falta E2E browser real para fechar Gate D.
- 2026-06-05 — **CDX-309B PR #1 verde:** CI/CD canonico de modulos via GitHub Actions (`_deploy-module`, `deploy-mesas`, lint/enforce migration, break-glass, scripts migration). Amazon Q blockers corrigidos. Deploy job skipped em PR. Sem VM.
- 2026-06-05 — **CDX-309C aplicado:** allowlist migration sem `sed`, self-test permitido/bloqueado; migration lock com `flock` interno autoritativo + `pg_advisory_xact_lock` por transaction; self-test concorrente no Actions. Sem VM.
- 2026-06-05 — **CDX-309D concluído:** PR #1 squash-mergeado (`3cf08d0`) e Parte C executada com aprovação. Backup off-VM criado em `C:\projetos\artificiobackup\cdx-309d-20260605-002955`; VM `/opt/artificio` agora é clone `main` com deploy key read-only; `.env` de `accounts`/`mesas` preservados; sem deploy/build/migration; smoke pós-conversão OK.
- 2026-06-05 — **CDX-309E concluído:** docs pendentes commitados (`cbee1a0`), bugs do deploy corrigidos (`b5c172d` CRLF em JWT, `986516b` script remoto por arquivo), e `deploy-mesas.yml mode=deploy` real passou (`26994910262`). DB antes `tables=48/users=15`; depois `tables=49/users=15`; `schema_migrations=55`; smoke `200/401/302`.
- 2026-06-05 — **CDX-311 (design system / marca) local:** cores reais amostradas pixel dos logos (laranja `#FF9457`, navy `#020740`); `@artificio/ui` (tokens/styles/Header/Footer/brand) + `apps/accounts` login + `apps/mesas` (vars + 5 arq com laranja velho `#E8521A`→`#FF9457`, contraste AA dos botões) atualizados; logo branco `brandLogoNeg` embutido p/ fundo escuro; footer virou hub. Nome do produto corrigido: "Artifício G1"→**"Artifício RPG"** em 23 arq + nota (G1 = referência conceitual). Build turbo 7/7. Verificado em preview local. Aguardando deploy via pipeline (codex).
- 2026-06-05 — **CDX-311 DEPLOYADO + marca validada no ar.** PR #2 squash-merge em `main` (`0c30fa7`); main avançou p/ `c5ff42d` (docs/roadmap). Deploys rodaram no HEAD `c5ff42d`. **Accounts:** 1ª tentativa falhou (`ACCOUNTS_ENV` do GitHub com senha DB divergente do volume); codex restaurou serviço com `.env` real da VM, sincronizou `ACCOUNTS_ENV` Secret (sem imprimir segredo), rerun verde `27022120573`; smoke health=200/login=200/me-sem-cookie=401; DB `accounts_tables=1 accounts_users=1`. **Mesas:** deploy verde `27022250217`, `jwt_secret_shared=true`, snapshot criado, migrations sem novidade, `mesas-api`+`mesas-app` healthy; smoke home=200/private_no_cookie=401/auth_redirect=302; DB `mesas_tables=49 mesas_users=15 schema_migrations=55`. **VM:** `/opt/artificio` em `c5ff42d`, git limpo, accounts-api/db + mesas-api/app/db/cron no ar. Marca servida (CSS/JS) contém `#FF9457`+`#020740` (+`#1B2A4A` mesas); hex velho `#E8521A`/azul-Google sumiram. **Validação Opus (Gate D, marca):** ✅. Próximo: **mesasbeta** (staging/canary essencial) rebuild do main.
