# Estado do Projeto — Artifício RPG

> Atualizar a cada mudança de estado operacional. Fonte de verdade do "onde estamos".
> Histórico detalhado em `sessoes/` e `specs/`. Este arquivo é **sumário operacional**, não diário.

## Fase atual

**Fase 3 — projetos + conteúdo.** Gates A/B/C ✅; mesas, glossario e site com Gate D fechado. Site em `artificiorpg.com` (Astro SSG, spec 029/030/031), cutover de DNS raiz concluído (Gate C encerrado, D074). `beta.artificiorpg.com` = staging. WordPress desligado. Migração WP→site concluída, residual zero.

**Todos os 5 apps em prod** (2026-06-21). **Último promote `dev→main`: 2026-06-24 (`2756346`, ff via `promote-prod-fast-forward.yml`)** — promoveu specs 044-049 (PRs #93/#94 + acumulado). `main == dev`. **⚠️ Deploy prod mesas falhou e fez rollback** (run `28125222995`): guard de migration barrou `migration_128` (falso-positivo `online-safe`). **Spec 050 corrigiu o guard** (regex estreito + teste shell 28/28 no CI + cópia órfã removida). Mesas em prod segue no código pré-049. **Pronto para re-deploy prod** (gated por aprovação nominal do mantenedor).

**PRs:** PR #73 (dependabot) **merged** em `dev` (`09773fc`, 2026-06-22). PR #80 **(spec 041 shell)** mergeada. Corrigido via **PR #82**. **PR #83 (spec 042)** mergeada. **PR #84 (spec 043)** mergeada em `dev` (`39d2c7c`). **PR #85 (spec 045)** mergeada + promovida a `main` (`c269a46`). **PR #86 (chore 044/045/ecosystem)** mergeada (`560131f`). **PR #87/#88/#89/#90 (spec 047)** mergeadas em `dev`; última #90 (`f0e2e56`, 2026-06-23) removeu DeepSeek acidental da 047 e deixou CI verde. **Último promote a `main`: PR #85**. Spec 047 fechada em `dev`, ainda não promovida a `main`.

## Destaque: Spec 041 — Shell unificado (merge 2026-06-21)
- `packages/ui`: Header com busca/changelog/tema/menu cross-app + `useTheme()`/`useChangelogBadge()` hooks + `<ChangelogModal>` centralizado
- 5 apps consumindo shell único (site Astro híbrido, mesas/glossario/links React, accounts tema)
- `/busca` uniformizada em 4 apps; `/conta` no accounts; `/perfil` renomeado no glossario
- Changelogs padronizados em JSON nos 4 apps (glossario migrado de DB)
- Débitos fechados: `BL-SHELL-B13`, `D-SHELL1`, `BL-UI-THEME-REACT-HEADER-VARIANT`, `BL-UI-THEME-TOGGLE-SITE-REGRESSION`, `BL-GLOSSARIO-CHANGELOG-JSON`
- 19 revisões de bots + 15 Sonar aplicados (17 itens, 2 agrupados); auditoria changelog 53 ✅
- **Pós-merge (PR #82):** deploy beta quebrou — contrato de changelog vivia em `@artificio/ui` (ESM-only), backends CJS (glossário/mesas) não conseguiam `require`. Extraído p/ pacote leaf **`@artificio/changelog`** (build dual ESM+CJS, `exports {import,require}`); `@artificio/ui` re-exporta; backends deixam de depender de UI/React. + fix `turbo.json` (`dist-cjs/**` nos outputs). Detalhe em `tasks-2.md` D-041-22/23, `task-revisões.md` §Pós-#80.
- **PROD (2026-06-21):** glossário, mesas, accounts, links com 041 em prod ✅. Site já em produção na raiz `artificiorpg.com` (Gate C encerrado desde, ver seção Gates).
- **Spec 041 ENCERRADA.**

## Gates

| Gate | Status | Notas |
|------|--------|-------|
| **A** | ✅ | Backups completos/verificados/off-VM |
| **B** | ✅ | SSO `accounts.` no ar, cross-subdomínio provado |
| **C** | ✅ | Cutover DNS raiz concluído (D074, 2026-07-12 confirmado). `artificiorpg.com` = CNAME direto pro Cloudflare Tunnel (`<tunnel-id>.cfargotunnel.com`) → `site-prod-app:4322`. WordPress desligado |
| **D** | ✅ | `mesas` (2026-06-08), `glossario` (2026-06-12), `site` (2026-06-18 via spec 029/030/031) |
| **D-link** | ✅ | `links.artificiorpg.com` **no ar** (2026-06-21). Spec 038 completa: **Logos: 12/13 reidratados** (Canal de Notícias null = legítimo, kind=channel). **Report público funcional** ✅. **Cron VM configurado** ✅ (domingo 06:00, crontab ubuntu). `BL-UI-THEME-REACT-HEADER-VARIANT` **fechado** (em prod via spec 041, PR #80/#82). |

## Decisões fechadas (resumo)

- Monorepo único `artificio` (pnpm + Turborepo)
- **Topologia subdomínio-por-projeto** (D017/D057): `glossario.`, `mesas.`, `links.`, `accounts.` (SSO). `artificiorpg.com` = site/blog raiz; `beta.artificiorpg.com` = staging. Futuros: `downloads.`, `esferas.`, `srd.`.
- **SSO central `accounts.artificiorpg.com`** (D018): OAuth Google, 2 cookies JWT `Domain=.artificiorpg.com` (`artificio_session` 15min + `artificio_refresh` 7d), HS256 simétrico
- **Blog na raiz** = aposta SEO (D019). Search Console Domain property (D020, inalterado). GA4 = 1 property por app (D117, supera D020): site `G-2H50PCM950`, mesas `G-43T3C6K6V6`, glossario `G-YVHMJEVTE4`, downloads `G-2H50PCM950` (provisório)
- **Stack canônica:** React19/Vite/Tailwind + Express5/Kysely/PG16/Cloudinary + Astro 6 (site)
- **Infra:** Oracle 24GB/200GB, Docker, Cloudflare Tunnel, Watchtower(beta). Imagem buildada na VM (não GHCR).
- **Backup:** `C:\projetos\artificiobackup` (local, 300GB)
- **Acesso VM:** `ssh faren`, read-only sempre permitido, write exige aprovação nominal
- **Tema lua/sol** compartilhado cross-subdomínio (D067): cookie `artificio_theme`, opt-in explícito
- **Laranja canônico** `#FF5722` (D064), navy `#020740` (D040)
- **Linguagem pública:** "projetos" (D063); "módulo" = termo técnico interno
- **Comunicação agentes:** caveman ultra obrigatório (D068/D088)
- **Ferramentas agentes:** usar quando disponíveis `rtk`, `artificio-api-governance`, LSP e `codebase-memory-mcp`; fallback só registrado (D112)
- **Branch protection** `dev` (D073): tudo via PR, check `lint + build + test` obrigatório
- **WP EOL resolvido** (D074): migração concluída, residual zero de conteúdo servido com URL WP

## Construído neste monorepo

**Pacotes compartilhados:** `config`, `auth`, `ui`, `content`, `analytics`, `media`, `changelog`, `feedback`

**Apps:** `accounts` (SSO), `mesas` (anúncios de mesa), `glossario` (termos RPG), `site` + `site-admin` (blog/portal), `links` (WhatsApp, em prod)

### Estrutura de dados do Mesas e sistemas de RPG

- Catálogo/dados Mesas (mesas, usuários, perfis e preferências) não é o mesmo domínio que sistemas de RPG.
- Central = somente sistemas de RPG no Site Prod. Mesas Prod consome esse Central.
- Mesas Beta mantém projeção local de sistemas, hidratada do Central por upsert: preserva UUID, atualiza existentes, insere ausentes e nunca apaga extras Beta.
- Mesas Prod→Beta hidrata mesas/usuários e dependências do produto, excluindo domínio de sistemas. Runtime Beta usa projeção local.
- Spec 078 corrige implementação parcial da 062: adapter único por ambiente e onboarding/draft JSON unificados.

**Infra/CI:** `deploy.yml` (manifesto declarativo, 5 módulos), `_deploy-module.yml`, `ci.yml` (lint+build+test gate), `deploy-manifest.json`, `docker-cleanup.yml`, `mesas-auto-archive.yml`, CodeQL, Scorecard, lock RW VM-wide

**Governança:** `AGENTS.md`, `.specify/memory/`, `.specify/arquiteture.md`, `docs/agents/`, `sessoes/`, `specs/`

## Próximo passo (ordem)

**Spec 078 aberta — sistemas centrais e projeção Mesas Beta (2026-07-15):** Fase 0 material concluída; zero implementação runtime. Central=Site Prod; Mesas Beta tem projeção local aditiva. Inventário provou: parser/JSON, onboarding/preferências, grupos e DDAL ainda leem legado local; `/systems`/CRUD/hidratação visual leem Central; hidratação Mesas ainda mistura `systems/system_aliases/system_suggestions` e resolve `tables.system_id` por slug local. Referências vivem em colunas UUID, arrays UUID, JSONB de drafts/candidatos e regras de learning; `user_systems` e resolução de sugestões ainda têm FK local. Site usa ID `TEXT`, Mesas usa UUID: ID central não-UUID deve falhar antes de write. Snapshot Central existente (`catalog_version`, checksum, ETag, árvore+aliases) será reutilizado. Matriz escolheu `SystemPicker/useSystemsCatalog` como UI base e parser/candidate scorer como motor compartilhado; onboarding continua escolha humana, não vira parser. Próximo gate: Fase 1, adapter único Central em Prod/local em Beta.

**Spec 078 — Fase 1 parcial (2026-07-15):** reader único criado com seleção canônica por `APP_ENV`: Mesas Prod usa Central, Beta/local usa projeção local; processo production sem ambiente válido falha fechado. Testes focados 5/5, lint/build Mesas backend e diff-check verdes. Writer ainda não integrado: tabela local não possui lifecycle/redirect e `user_systems` tem cascade; hard delete local perderia preferências. Snapshot Central só lista ativos, então ausência não distingue extra Beta de nó Central arquivado/mesclado. Decisão do mantenedor necessária antes de writer/hidratador.

**Decisão lifecycle da Spec 078 (2026-07-15):** mantenedor aprovou solução recomendada. Projeção Beta terá origem, status, destino de merge e versão Central observada. Archive/merge será soft, preservando IDs, mesas, preferências e learning; hard delete proibido. Central exporá inativos/redirects explicitamente. Writer/hidratador liberados para implementação.

**Spec 078 — Fase 1 concluída (2026-07-15):** provider único read/write seleciona Central em Mesas Prod e projeção local em Beta. `/systems` e resolução/admin de sugestões usam fachada. Writer Beta cria/edita local e arquiva soft; migration 149 adiciona `catalog_source`, `catalog_status`, `merged_into_id`, versão/sync Central e constraints. Snapshot Site inclui `inactive_nodes` e `redirects`, ambos no checksum. Validação: 40 testes focados verdes, lint/build Mesas backend verdes, build Site verde; lint Site permanece script histórico TODO, não foi contado como validação real. Fase 2 liberada: hidratador Central→Beta, sem apply real.

**Spec 078 — Fase 2 implementada (2026-07-15):** endpoint admin Beta-only `systems/projection-sync` usa origem fixa Site Prod, token interno, dry-run padrão e apply explícito. Snapshot Zod exige UUID, versão/checksum, árvore `system>edition>variant`, inativos e redirects. Plano classifica create/update/unchanged/lifecycle/beta_extra/conflict; colisão de UUID/path/slug, órfão, contagem e hierarquia falham fechado. Apply transacional preserva UUID, substitui aliases centrais, atualiza lifecycle soft e nunca apaga extra Beta. Migration 149 usa unicidade parcial entre ativos para histórico não bloquear substituto. Testes do plano 7/7, lint/build backend e verify:api verdes. Apply real adiado à Fase 6. Fase 3 iniciada.

**Spec 078 — Fase 3 concluída (2026-07-15):** hidratação Mesas Prod→Beta removeu `systems`, `system_aliases` e `system_suggestions` da ordem e allowlist. `tables.system_id` agora atravessa intacto; join/resolução por slug local removidos. Guard executa dry-run da projeção Central antes da transação e bloqueia se houver create/update/lifecycle/conflito; também valida UUIDs usados em `tables`, `user_systems`, `user_preferences.systems` e `gm_profiles.closed_group_systems`. Mascaramento PII/dry-run original preservado. Testes guard+rota 6/6, lint/build verdes. Fase 4 iniciada.

**Spec 078 — Fase 4 concluída (2026-07-15):** parser Discord/JSON/texto/batch/reparse, `/systems`, onboarding/opções/preferências, perfil, grupos fechados, DDAL, filtros e hidratação visual de mesas, CRUD e sugestões usam o provider por ambiente. Helpers paralelos Central-only removidos. Busca estática limita query direta `systems/system_aliases` a provider, hidratador/guard e scripts permitidos. Primeira suíte completa revelou um mock antigo de 2 queries; harness atualizado ao provider de 3 leituras. Reexecução: backend Mesas 529/529, lint/build verdes. Fase 5 iniciada para UX compartilhada e equivalência.

**Spec 078 — Fase 5 concluída localmente (2026-07-15):** onboarding usa `SystemPicker` + `SystemSuggestionModal` compartilhados com draft; provider/árvore/aliases/hierarquia são únicos, enquanto extração textual, learning e proveniência permanecem no parser. Corrigidos `dnd5`, alias exato de edição filha direta sem liberar variante que pule edição, e dados centrais `3D&T > Victory`, `3DeT`, `OSE`, `V5`, `Lobisomem: O Apocalipse`. Testes focados 41/41. Auditoria read-only sobre os 200 anúncios reais, com migrations simuladas em memória contra snapshot Site Prod v1283: matches 130→137 (63→66 e 67→71); ausências de catálogo continuam sem chute/criação silenciosa. Fase 6 iniciou apenas preflight read-only; backups + restore-test precedem todo write real.

**Spec 078 — Fase 6/T6.1 concluída (2026-07-15):** backups custom-format read-only de `site-prod-db/site` e `mesas-beta-db/mesas_rpg` copiados off-VM para `D:\artificiobackup\spec-078\20260715-142510`, hashes SHA-256 registrados na sessão e arquivos validados por `pg_restore -l`. Docker local indisponível; T6.2 restore-test exige criação temporária na VM e aguarda autorização nominal. Nenhuma migration/apply executada.

**Spec 078 — Fase 6/T6.2 concluída (2026-07-15):** restore-test autorizado executado em PostgreSQL 16 temporário isolado. Ambos os dumps restauraram com exit-on-error; contagens críticas origem=cópia em Site (v1283, 1.273 nós, 412 aliases) e Mesas Beta (1.289 sistemas, 442 aliases, 18 mesas, 18 usuários, 20 drafts); zero constraint não validada. Container e dados temporários removidos. Bancos reais intactos. Próximo: dry-run real T6.3; nova autorização será exigida antes de migration/apply em cópia.

**Spec 078 — Fase 6/T6.3 em correção (2026-07-15):** ensaio nas cópias fez migrations 008/147/148 passarem e revelou ambiguidades PL/pgSQL reais nas Site 009/010; ambas corrigidas e reexecutadas com 011/149 verdes. Primeiro dry-run bloqueou 1.374 colisões porque Central e Beta usam UUIDs distintos. Auditoria provou que 1.264/1.289 UUIDs Beta têm `catalog_legacy_mappings` determinístico para nó Central, zero destino duplicado; 25 permanecem sem mapping. Snapshot/hidratador ainda ignoravam RF5. Próximo: transportar/aplicar mappings preservando IDs antigos como merged/redirect local; depois rerodar e levar somente os 25 residuais à decisão humana.

**Spec 078 — Fase 6/T6.3 refinada (2026-07-15):** snapshot admin/planejador agora transportam mappings Mesas. Teste 8/8; dry-run caiu de 1.374 para 1 colisão, classificando 1.264 remaps, 9 creates, 1.264 updates e 25 extras Beta. Conciliações inequívocas encontradas para 3D&T/Victory, CoC/7e, D&D/4e/5e/2014/2024 e Tormenta/20; filhos Beta sem equivalente Central serão preservados/reparentados. Decisão humana ainda necessária para `CAIN`→`CAIN RPG` e edição duplicada `Mutants And Masterminds`→raiz. Apply do remap ainda não implementado/executado. Ambiente temporário removido; bancos reais intactos.

**Spec 078 — D115/D116 e ensaio set-based pendente (2026-07-15):** mantenedor decidiu CAIN/Mutants como aliases do sistema canônico e explicitou que nome original + versão portuguesa são o mesmo nó (`name`/`name_pt`), nunca taxonomia duplicada. Dry-run sem conflito chegou a 1.273 remaps e 16 extras. Primeiro apply real em cópia excedeu 180 s por loop; conexão caiu e transação reverteu inteira, confirmado 1.289 ativos/zero merged/zero central. Próximo: auditar extras por tradução e converter remap para operações set-based antes de repetir. Bancos reais intactos.

**Spec 078 — D116 auditada + apply set-based validado (2026-07-15):** `name` é sempre o nome original, em qualquer idioma, e permanece principal/exibido; `name_pt` é tradução auxiliar para busca/contexto, sem preferência visual. Snapshot restaurado mostrou só 1/1.273 `name_pt`; parser/scorer já consomem o campo e teste cobre `Mutantes e Malfeitores`, mas Central viola D110/D116 com raízes paralelas `Vampire`/`Vampiro`. T6.3a não encontrou os 16 extras nos nomes localizados/aliases existentes. Apply set-based em cópia convergiu 1.273 remaps; rerun idempotente ficou create/update/remap/conflicts=0, unchanged=1.273 em 14,9 s. Ambiente temporário removido; bancos reais intactos. Próximo: migration de reconciliação `Vampire`/`Vampiro` e `Anniversary`/`Aniversário`, depois novo dry-run.

**Spec 078 — migrations 013/014 implementadas, restore-test pendente (2026-07-15):** 013 mantém `Vampire` como original, move `Vampiro` para `name_pt`, soft-merge/redirect das raízes e edições 5e duplicadas, reparenta filhos e localiza `Anniversary`. 014 promove os 16 extras Beta ao Central preservando UUIDs (11 sistemas, 5 edições), pais/hierarquia e aliases úteis; remove metadados automáticos ruins. Site 38/38, build e diff-check verdes. Nenhum banco real alterado. Próximo: nova autorização nominal para recriar restore-test isolado, aplicar 013/014, rerodar idempotência e recalcular plano Central→Beta.

**Spec 078 — T6.3/T6.4 concluídas em restore-test (2026-07-15):** Site 008–014 e Mesas 147–149 aplicadas em cópias; 013/014 idempotentes. Central final tem 1.287 ativos, 16/16 extras promovidos e `Vampire` original + `Vampiro` auxiliar; zero hierarquia inválida. Apply Central→Beta convergiu de create 5/update 1.282/remap 1.273 para rerun create/update/remap/beta_extra/conflicts=0 e unchanged=1.287. Projeção ficou 1.287 `active|central` + 1.273 históricos `merged|beta`. Ambiente temporário integralmente removido; bancos reais intactos. Próximo T6.5: ensaio ordenado projeção de sistemas → hidratação mesas/usuários.

**Spec 078 — gates locais verdes (2026-07-15):** `verify:api` breaking=0, lint/build verdes, testes monorepo 31/31 tasks e Mesas backend 540/540, diff-check verde. Restore dos dois dumps também prova rollback restaurável. T6.5 ainda pendente antes do rollout: ensaiar sequência projeção sistemas → hidratação mesas/usuários. Nenhum commit/push/PR ou write real executado.

**Spec 078 — T6.5 preflight (2026-07-15):** conjunto anterior não tinha Mesas Prod, então não provava a segunda hidratação. Backup read-only `mesas-prod.dump` criado off-VM (2.826.308 bytes, SHA-256 `5981d4ac0ed70e11979fcb74503b9b4c6c1ae55a6c7bde5ce2351b01ddd977b5`). Próximo: autorização nova para restaurar/validar terceiro dump e ensaiar ordem sistemas→dados Mesas em cópias. Bancos reais intactos.

**Spec 078 — T6.5 bloqueio validado (2026-07-15):** restore triplo e migrations passaram; projeção Central→Beta ficou idempotente (1.287 unchanged; zero create/update/remap/conflict). O handler real da hidratação Mesas Prod→Beta parou antes do write porque 8 referências Prod usam UUIDs legados já remapeados. Guard e payload ainda resolvem/copiariam ID cru em `tables`, `user_systems`, `user_preferences` e `gm_profiles`. Próximo: corrigir resolução canônica comum e repetir T6.5 desde cópias limpas. Bancos reais intactos; sem commit/push/PR.

**Spec 078 — T6.5 correção local (2026-07-15):** resolver comum legacy/redirect implementado no guard e reutilizado nos quatro payloads hidratados, com dedupe de arrays e ciclo bloqueado. Testes focados guard+rota 8/8 e lint verdes. Próximo gate: repetir restore triplo e T6.5 completo desde bases vazias.

**Spec 078 — T6.5 concluído em restore-test (2026-07-15):** quatro UUIDs sem identidade eram órfãos reais em 2 mesas + 1 preferência; migration 150 manual-risk limpa somente associações e preserva registros. Colisão nominal segue decisão Prod-wins. Hidratação Mesas Prod→Beta passou duas vezes; rerun teve zero inserts. `systems`/aliases ficaram bit a bit iguais antes/depois (2.560/851; hashes `dccb965e...`/`77aa2eb7...`). Próximo: gates finais locais. Nenhum banco real, commit, push ou PR tocado.

**Spec 078 — implementação pronta para commit (2026-07-15):** restore-test triplo, migrations/reruns e hidratação dupla verdes; ambiente temporário limpo. Mesas backend 542/542, `verify:api` breaking 0, lint/build monorepo 21/21 e diff-check verdes. Bancos reais intactos. Commit/push/PR exigem autorização nominal nova; migration 150 é manual-risk e exige backup antes de apply real.

**Bloqueio atual — Spec 077, rollout de hierarquia (2026-07-15):** Migration 146 aplicada em Mesas Beta; 147 falhou e fez rollback, sem deploy. Correções SQL locais existem, mas nova tentativa fica bloqueada pela Spec 078: provar identidade/projeção e ensaiar migrations em cópias restauradas.

0. **Spec 077 — dedupe de mesas ativas: implementada localmente em 2026-07-14; smoke runtime pendente.** Produção read-only: 31 mesas ativas; teste `exploratory-active-pairs.sql` varreu 465 pares e achou 3 pares fortes (`A Voz nas Cartas`, `Ecos Bastardos`, `Máscaras de Nyarlathotep`). Mantenedor aprovou tabela nova + gatilho admin sob demanda + score com sistema corroborativo. Implementados migration 145 `table_duplicate_candidates`, scanner mesa×mesa/draft×mesa, `GET /admin/tables/duplicates`, `POST /admin/tables/duplicates/scan`, `PATCH /admin/table-duplicate-candidates/:id`, badge na fila e aba Duplicatas com links/decisão manual. Testes backend 456/456, frontend 173/173; repo lint 21/21, build 21/21, `verify:api` exit 0 (3 non-breaking). Docker local indisponível: falta aplicar migration em beta/local e executar smoke UI real; não declarar concluída. Working tree contém mudanças concorrentes do mantenedor na branch `feat/mesas-og-scrape-catalog-backup`; preservar e não misturar sem reconciliação.
0. **Specs 070-076 (Downloads A-G) — pacote em implementação local, branch `feat/070-downloads-schema-api`, PR #151 aberta (código spec 070).** Diretriz do mantenedor (2026-07-11): todo o pacote 070-076 entra numa PR só, até nova ordem explícita. **070 (schema/API/ownership): implementada e validada localmente** (migrations, rotas, ownership, achados de segurança github-advanced-security/chatgpt-codex-connector corrigidos). **071 (storage multiprovider): F1-F5 implementados** (`apps/downloads/backend/src/storage/`: `StorageAdapter` + R2/B2 via `@aws-sdk/client-s3`, Fastio via REST cru, Cloudinary via `@artificio/media`; failover automático com log de auditoria; validação de tipo por magic bytes; **F4 job de migração entre providers + reconciliação por checksum SHA-256** em `src/storage/migration.ts`, lógica pura testada com mock — checksum divergente preserva origem, nunca apaga sem confirmar integridade). **Só falta T0.1 (confirmar contrato REST real da Fastio) e T6.2 (upload real em R2 com credencial real)** — ambos exigem decisão/infra fora do escopo desta rodada, sem credencial disponível. 21 testes verdes, lint/build verde (2026-07-12). **072 (submissão/moderação/taxonomia): F0-F7 implementados, 4/5 débitos fechados** — máquina de estados editoriais (`editorialStateMachine.ts`, 15 testes), rotas de moderação/submissão/reenvio/batch (`routes/moderation.ts`), rota de metadata com validação cruzada (`routes/materialMetadata.ts`), guard de avaliação com checker injetável (`ratingGuard.ts`, checagem real fica pra spec 074), migration 011 (`download_comment` + `rejection_reason`/`auto_publish_enabled`). **Decisão nominal do mantenedor (2026-07-12): denúncia deixou de ser anônima — exige conta `accounts.`** (revoga critério de aceite 5 original do `072/spec.md`), habilitando contenção automática (1 denúncia P0 em material publicado → `withdrawn`), retirada voluntária e detecção de abuso por usuário (`reportAbuseGuard.ts`: 3 denúncias seguidas "dismissed" = sinal de abuso). 44 testes verdes, lint/build verde, `verify:api` exit 0 (2026-07-12). DEB-072-01 a 04 fechados; **DEB-072-05 aberto** (abandono/reivindicação de material, sem critério de elegibilidade definido — spec futura). **073 (descoberta pública): F0-F6 implementados (2026-07-12).** Specs filhas 073-076 (só esqueleto spec/plan/tasks/reviews/debitos.md) estavam presas em `stash@{1}` da sessão anterior — recuperadas para o working tree via `git show stash@{1}^3:<path>` (sem aplicar o stash inteiro, sem perder outro conteúdo dele). Gap real descoberto: backend (070) só tinha `GET /materials/:slug` (ficha), sem listagem/busca — implementado nesta rodada `GET /api/v1/materials` com filtro (q/system_id/edition_id/material_type/access_kind), ordenação (relevance≈recent/recent/popular via `download_metric_daily`/name) e paginação. Criado `apps/downloads/frontend` do zero (Vite/React19/TS/Tailwind, padrão `apps/mesas/frontend`): rotas públicas (`/`, `/catalogo`, `/materiais/:slug`, `/criadores/:slug`, `/ir/:destinationId`, `/obter/:fileId`), `AppShell` com Header/Footer compartilhado de `@artificio/ui`, busca/filtro/ordenação/paginação como contrato único de URL, `MaterialCard`/`MaterialPage` (CTA dispara `trackEvent` antes do redirect, aviso de destino degradado), nova rota backend `GET /api/v1/creators/:slug` + `CreatorPage`. AA básica aplicada (alvo 44×44, foco visível, sem informação só por cor). 49 testes backend + 6 testes frontend verdes, lint/build verdes nos dois, `verify:api` exit 0 (downloads breaking=0 non-breaking=2). **DEB-073-01 e DEB-073-02 corrigidos na mesma sessão (2026-07-12), após pergunta explícita ao mantenedor sobre os 3 débitos (nenhum decidido sozinho):** DEB-073-01 — `migration_013_download_creator_nullable_user.sql` torna `download_creator.user_id` nullable (índice único parcial `WHERE user_id IS NOT NULL`); confirmado por leitura de código que ownership de material nunca lê essa coluna (sempre compara `download_material.creator_id === req.user!.userId`), logo a mudança não quebra ownership; `creators.ts` ajustado pra retornar `materials: []` sem query quando `user_id` é null (crédito-only). DEB-073-02 — nova tabela `download_destination` (`migration_014`, 1:1 com material, índice único em `material_id`) + rota `GET /api/v1/destinations/:id` (fail-closed, só resolve material `published`); `GET /materials/:slug` faz get-or-create do destino e devolve `destination_id`; frontend (`MaterialPage`, novo hook `useDestination`, `RedirectDestinationPage`) passou a usar esse id opaco em vez do slug do material. Após as correções: 50 testes backend + 6 frontend verdes, lint limpo nos dois, `verify:api` exit 0 (downloads breaking=0 non-breaking=3, 2026-07-12). **DEB-073-03 mantido aberto** (placeholder de download — depende de 071 T6.2 com credencial real de provider, fora do controle desta sessão). Sidebar contextual/drawer mobile (T1.3/T1.4) e E2e leve (T6.3) não implementados nesta rodada. Nenhum commit feito ainda (aguardando autorização nominal). ⚠️ Achado operacional (herdado, resolvido): código/specs de sessão anterior sumiram da working tree por stash sem `-u` (untracked ficou órfão); recuperado via `git fsck --dangling`/`git show stash@{N}^3:<path>`. Cuidado ao usar `git stash` neste app sem `-u` explícito. **074 (painel do usuário e edição): F0-F6 implementados (2026-07-12).** Escopo grande — 2 decisões nominais do mantenedor confirmadas antes de codar: (1) criar schema novo para dedup de download e avaliação (não existia); (2) implementar as 9 rotas de painel com "conteúdo real", incluindo Organizações/Notificações que não têm spec/decisão prévia detalhando o domínio — autorizado como escopo mínimo inventado. **4 migrations novas:** `migration_015` (`download_user_material_download`, PK composta user_id+material_id, dedup de download por conta — D111 item 7), `migration_016` (`download_rating`, 1 avaliação por conta+material, gate de escrita no `ratingGuard.ts` já existente da 072), `migration_017` (`download_organization`/`download_organization_member`, escopo mínimo), `migration_018` (`download_notification`, feed interno só-leitura, escopo mínimo). **Backend:** novas rotas `downloads.ts` (registra download com dedup real, incrementa `download_metric_daily` só na primeira vez por conta), `ratings.ts` (usa o `HasDownloadedChecker` que a 072 já previa), `favorites.ts`/`collections.ts` (CRUD sobre tabelas que existiam desde 070 sem rota), `organizations.ts`/`notifications.ts` (escopo mínimo); `materials.ts` ganhou `GET /mine` (todos os estados editoriais do próprio autor) e `GET /:id/history` (histórico por campo, guard de dono/moderador/admin) — o PATCH que grava histórico por campo já existia desde 070, só faltava rota de leitura. **Frontend:** `apps/downloads/frontend/src/pages/painel/*` com as 9 rotas (`VisaoGeralPage`, `MeusMateriaisPage`, `EditarMaterialPage` com histórico visível, `FavoritosPage`, `ColecoesPage`, `PerfilPage`, `OrganizacoesPage`, `NotificacoesPage`, `DenunciasPage` placeholder, `ConfiguracoesPage`), `PainelShell` (sidebar desktop + drawer mobile, T1.10), `RequireAuth` (guard de sessão via `@artificio/auth/client` `useSession`/`redirectToLogin`); ficha pública (`MaterialPage.tsx`) ganhou `RatingSection`/`CommentSection`/botão de favorito, CTA agora registra download (dedup) antes do redirect quando logado. Após validação: 56 testes backend (+6: `downloads.test.ts` dedup, `materials.history.test.ts` histórico sem entrada espúria) + 6 testes frontend verdes, lint/tsc limpos nos dois, `vite build` ok, `verify:api` exit 0 (downloads breaking=0 non-breaking=17, 2026-07-12). **4 débitos abertos:** DEB-074-01 (Organizações/Notificações são domínio inventado sem spec, pode divergir se o real vier a ser definido), DEB-074-02 ("minhas denúncias" sem rota de backend — só moderação lista hoje), DEB-074-03 (sem botão "adicionar à coleção" na ficha pública, só via painel), DEB-074-04 (notificação não é emitida por nenhum evento real ainda — tabela e leitura prontas, emissão pendente). **075 (gestão/moderação admin): F0-F7 implementados (2026-07-12).** Backend: `linkChecker.ts` (checagem SSRF-safe — rejeita loopback/rede privada/link-local/metadado de nuvem, resolve DNS e revalida IP contra rebinding, não segue redirect sem revalidar cada hop), `sanitizeText.ts` (strip de HTML sem nova dependência — decisão nominal do mantenedor: descartado jsdom/isomorphic-dompurify por não haver HTML rico em nenhum campo admin), `routes/admin.ts` (`/summary` dashboard com contagem por fila, `/materials/:id/link-history`, `/materials/:id/check-link` sob demanda, `/links` status mais recente, `/metrics` agregado, `/materials/:id/evidence/upload` reforço de magic bytes reusando `fileTypeGuard` da 071, `/sanitize-preview`); `GET /creators/me` novo (expõe role real do domínio downloads pro frontend decidir UI, já que SSO só tem user|admin). Frontend: `GestaoShell.tsx` (sidebar de recursos agrupada Conteúdo/Operação/Comunidade/Sistema, contagem por fila, P0 com ícone além de cor, link "Sistemas e edições" via env pro site-admin), `RequireGestaoAuth.tsx` (guard sessão+role), 12 páginas `/gestao/*` (Visão geral, Moderação com ações batch, Materiais, Auditoria com histórico completo de links já usados — não só o atual, Denúncias, Links, Arquivos com upload+magic-bytes, Mídias/Publicadores placeholders sem dado real, Taxonomias apontando ao Site, Métricas, Configurações). **Achado do mantenedor durante revisão (2026-07-12), fora do plano original da 075 mas resolvido na mesma sessão:** nav/dark-light/footer já eram compartilhados (`@artificio/ui`, confirmado); faltava campo de editora — `migration_019` adiciona `publisher_name` em `download_material_metadata`, editável no painel, exibido na ficha; faltava relacionado por sistema/edição — `GET /materials/:slug` passou a expor `system_id`/`edition_id`/`system_name`/`edition_name` (via `catalogClient` já existente), ficha linka pro catálogo filtrado, `CatalogoPage` passou a ler esses filtros da URL. Após tudo: 73 testes backend (+17: `admin.test.ts` 6, `linkChecker.test.ts` 7, `sanitizeText.test.ts` 4) + 6 frontend verdes, lint/tsc limpos nos dois, `vite build` ok, `verify:api` exit 0 (downloads breaking=0 non-breaking=25, 2026-07-12). **3 débitos abertos:** DEB-075-01 (link checker só sob demanda, sem job agendado — falta scheduler), DEB-075-02 (Mídias/Publicadores sem dado real), DEB-075-03 (storage real de evidência ainda bloqueado, mesma raiz DEB-073-03). **076 (infra/deploy) F0-F4 implementados (2026-07-12), F5-F6 fora de escopo desta rodada (exigem SSH/Cloudflare real, aprovação nominal separada):** T0 relido infra-map/deploy-manifest/`_deploy-module.yml`/AGENTS.md vigentes antes de agir (regra pétrea da própria spec). F1: entrada `downloads` adicionada a `.github/deploy-manifest.json` (só ADICIONADA, nenhuma existente tocada; mesmo padrão `mesas` — front+back separados, dispatch-only). F2: `apps/downloads/Dockerfile` (backend+frontend) + `docker-compose.beta.yml`/`docker-compose.prod.yml` criados do zero (app não tinha nenhum), DB isolado por ambiente (`pgdata_downloads_beta`/`_prod`), sintaxe validada via `docker compose config`. F3: confirmado que as 19 migrations já existentes (spec 070-075) têm os 5 campos de header obrigatórios, todas `online-safe`, nenhuma com DDL destrutivo — zero correção necessária. F4: `docs/agents/deploy-runbook.md` ganhou seção "Bootstrap do `downloads`" (mesmo padrão mesas/glossario/site/links); `docs/agents/infra-map.md` ganhou as 2 rotas Cloudflare pendentes; health check `GET /api/v1/health` já existia (spec 070), usado nos healthchecks Docker e `critical_routes` do manifesto. **Ambos débitos fechados após o mantenedor criar a rota Cloudflare (2026-07-12):** DEB-076-02 (Tunnel `downloadsbeta.`/`downloads.` criado pelo mantenedor no painel Zero Trust, `infra-map.md` atualizado). **4 débitos adicionais resolvidos na mesma sessão (2026-07-12), a pedido do mantenedor ("veja o que dá pra implementar agora, sem pensar em escopo"):** DEB-074-02 (`GET /reports/mine`, nova rota fixa antes de `/:id`; `DenunciasPage`/`useMyReports.ts` consomem), DEB-074-03 (`AddToCollectionButton.tsx` novo, dropdown de coleções na ficha pública reusando `POST /collections/:id/items` já existente), DEB-074-04 (`services/notify.ts` — `emitNotification()` chamado em `moderation.ts` approve/reject/batch e `reports.ts` decisão de mérito; `NotificacoesPage` passa a receber eventos reais), DEB-076-01 (`services/moderationAuditLog.ts` — log JSON `[moderation-audit]` grepável, integrado nas mesmas rotas de moderação/denúncia). Após os 4 fechamentos: 75 testes backend (+2: `moderation.notify.test.ts`, `moderationAuditLog.test.ts`) + 6 frontend verdes, lint/tsc limpos, `vite build` ok, `verify:api` exit 0. **Changelog implementado no mesmo padrão mesas/glossario (D041), autorizado nominalmente (2026-07-12):** `CHANGELOG_UPDATE_MARKERS.downloads` adicionado a `packages/changelog/src/index.ts` (só aditivo, pacote compartilhado, rebuild verde + 8/8 testes do pacote); `apps/downloads/backend/src/routes/changelog.ts` (novo, reusa `@artificio/changelog`, cache em memória) registrado em `server.ts`; `apps/downloads/database/changelogs.json` (novo, 3 entradas reais cobrindo specs 073-076); `apps/downloads/frontend/src/components/ChangelogModal.tsx` (novo) + `AppShell.tsx` ganhou `useChangelogBadge`/badge de novidade/botão no Header (mesmo contrato genérico já usado por mesas/glossario). `CatalogoPage.test.tsx` ajustado (mock de `@artificio/ui` ampliado com `useChangelogBadge`/`CHANGELOG_UPDATE_MARKERS`/`DynamicChangelogModal`). Nenhum commit feito ainda (aguardando autorização nominal).
1. **Spec 054 — ✅ ENCERRADA (2026-06-30).** Branch `feat/054-gestao-ia-sidebar`. 11 arquivos de código + 3 artefatos. Fases 1-4 + reviews 001-025 corrigidos. PR mergeada em `dev` → deploy beta + smoke + promote prod ✅. O que não foi feito foi descartado. **053 Frente A destravada** (roda sobre esta estrutura).
2. **Spec 053 — ✅ ENCERRADA (2026-06-30).** Concentrou débitos das 047–051: Frente A (a11y/UI gestão, 8 P1 WCAG), B (tema accounts + /conta + avatar/exclusão), B2 (busca site), C (smoke CJS CI), D (doc 051), E (ingestão→052 Bloco A). O que não foi feito foi descartado. `BL-053-CONSOLIDADO` fechado. **052 destravada.**
3. **Spec 052** (automação inteligente de importação) — iniciada localmente em 2026-06-29. Blocos A/B/C locais: A TA1/TA2/TA4/TA5/TA6/TA7 (TA3/TA8 exigem VM/deploy); B IA só sugere (`_ai_suggestions`), eval offline, shadow report e kill switch; C parser determinístico R15-R20. Auto-aprovação real segue bloqueada por gate nominal. Sem VM write/cron/segredo/deploy.
4. **Spec 041 — Shell único cross-projeto ✅ EM PROD.** Residual: `SiteFooter.astro` fork (D-041-08).
5. **Spec 038** — ✅ **ENCERRADA (2026-06-24).** T13 completo: logos 12/13, report público funcional, cron VM configurado (domingo 06:00, crontab ubuntu). `BL-LINKS-MEDIA-038`, `BL-LINKS-NAV-CROSSAPP` e `BL-LINKS-GROUP-LOGOS` fechados no backlog.
6. **PR #73 merged ✅** (dependabot, `09773fc`).
7. **`BL-SEC-AUDIT-DEPS` (spec 039)** — branch `fix/039-sec-audit-deps` **pushada** ao origin, **PR não aberto** (aprovação nominal pendente). `xlsx` 2×HIGH residual → spec 034.
8. **Spec 032** (analytics shared adoption)
9. **Spec 025** (Lighthouse residual)
10. **Spec 028** (biblioteca de mídia VM)
11. **Spec 042** — **✅ EM PROD.** cpd: 5.57% → 4.60%.
12. **Spec 043** (auditoria visual links + shared `packages/ui`) — **PR #84 mergeada em `dev` (`39d2c7c`, 2026-06-22).** Fase 1 T5 (logo base64 → PNG estático) completa. `brand.ts` com `import .png?url`, `ambient.d.ts`, build script. `assetsInlineLimit: 0` nos 5 consumidores. 46 arquivos, +6644/-256. CI verde. Próximo: T6 (auditoria Header shared).
13. **Spec 044** (otimizacao ecossistema OpenCode + Claude Code) — ✅ **FECHADA (2026-06-22).**
14. **Spec 045** (débitos pendentes em limbo) — **Investigação concluída + T1 ✅ (2026-06-22, PR #85).**
15. **Spec 047** (mesas inbox importação) — **fechada em `dev` via PR #90 (`f0e2e56`, 2026-06-23).** PR #87/#88/#89/#90 mergeadas. Hardening final: enums Zod no PATCH Inbox/Discord (DEB-047-21), `DiscordDraftPreview.api` obrigatório sem fallback cruzado (DEB-047-23), remoção do DeepSeek acidental da 047. Validação local: lint 15/15 ✅, build 17/17 ✅, test 24/24 ✅, backend 21 arquivos/180 testes ✅, frontend 4 arquivos/19 testes ✅; PR #90 checks verdes. Beta read-only anterior: `/opt/artificio-beta` em `b70367c`, containers mesas beta healthy, migrations 128/129 aplicadas, constraint `single_origin` presente, `/gestao` 200, rotas admin Inbox sem sessão 401. Smoke autenticado T1.13-T1.16 ✅ em 2026-06-23: import único, import múltiplo, edição+sync para `tables.status='draft'`, rejeição de draft. **Não promovido a `main`.**
16. **Spec 048** (mesas DiscordChatExporter JSON) — **MVP em `dev` + Fases C+F+Handoffs #5/#6 locais (2026-06-26).** PR #91 + spec 049 mergiram backend+UI em `dev`. **Fase C (PR-1):** T-F4 + T-C1/C2/C3/C6. **Fase F (PR-2):** T-F2/F3/F5/F6/F10. **Handoff #5 (PR-3):** DEB-048-13 (cover por extensão + anexos não-imagem em _notes) + DEB-048-14 (reference/reply, replyContext no parser, contentIndex no serviço). **Handoff #6 (reviews):** 6 débitos de review fechados — DEB-048-16 (extractSlots reordenado, guard "mesa em andamento" → fallback posição 8), DEB-048-17 (filtro /reparse inclui 'parsed' quando messageIds + contador `ignored`), DEB-048-18 (extractDiscordTimestamp via `Intl.DateTimeFormat` pt-BR/America/Sao_Paulo), DEB-048-19 (Array.isArray + remove `as any` em messageIds), DEB-048-20 (catch interno próprio para db.updateTable, parse_error preserva causa real), DEB-048-21 (helper `respondImportError` extraído). Testes 253/253 ✅, lint 15/15 ✅, build ✅. Código local, **não commitado**. Pendentes: T-C4/C5/C7/C8/C9, Fase G, smoke beta real.
17. **Spec 049** (revisão da aba /gestao do mesas) — **FECHADA (2026-06-24).** Fases A-D (investigação+auditorias+duplicação+proposta) + Fase E (refatoração completa: TE1-TE22 + D09 + D10, 5 commits locais, 76 arquivos modificados, backend adminDiscordSync 1278→29 linhas, adminImportInbox 573→29 linhas, DiscordSyncPanel 543→308, DiscordJsonImportPanel 356→115, +2 hooks +5 componentes, +40 testes backend, +146 testes frontend) + Fase F parcial (TF1-TF6 verdes, TF7/TF11 skip). Reviews REV-001 a REV-038 + D09 + D10 implementados. Débitos resolvidos: D01-D05, D08-D18. Débitos abertos: D06 (8 P1), D07 (nome de teste). Branch `feat/049-revisao-gestao` com 5 commits locais, código **não mergeado em `dev`**.

- 2026-07-10 — **Spec 062 PR #143 (I0a/I0b unificação frontend mesas).** `useSystemsCatalog()` hook único, `SystemPicker` árvore navegável, migração de 7 telas (StepSystem, CatalogoPage, UserSystemsSelector×2, OnboardingPage, SystemSuggestionModal, DraftEditorTab), remoção de `SystemTreeSelector` (grid legado) e `SystemSearchSelect` (editor draft). I0a.0 removeu rotas órfãs `/profile/me/systems*`. I0b migration 142 corrigiu 573 nomes sujos em prod. Reviews de bot (CodeRabbit/Codex/Amazon Q/Sonar) aplicados: coerce de contadores string, forceRefresh sem rejection, aria-label, Readonly props, ternário extraído, bloco duplicado CatalogoPage extraído, prefill de busca no editor draft. Lint+build+test+verify:api verdes.
- 2026-07-10 — **Spec 062 PR #144 (I6 admin contextual).** `SystemSuggestionModal.tsx` ampliado: admin no fluxo Discord cria nó central com aliases, Logo e Website Oficial via fachada Mesas → API central. Mantenedor identificou gap: criação encadeada admin direta (sistema→edição→variante) e campos ausentes no modal antigo; rota 1 adotada (ampliar modal existente, sem pacote compartilhado).

## Log

- 2026-07-12 — **Incidente: `artificiorpg.com` fora do ar (404 edge Cloudflare) + Gate C confirmado encerrado.** Diagnóstico read-only na VM mostrou container `site-prod-app` saudável e respondendo 200 internamente (`/`, `/healthz`), afastando hipótese de app quebrado. Causa real: domínio customizado de bucket R2 (`artificiodownloads`) registrado na raiz `artificiorpg.com` no painel Cloudflare, ocupando o hostname e impedindo o Tunnel de criar/atualizar o `CNAME` — sintoma era 404 cacheado no edge (`Cf-Cache-Status: HIT`, `Age` alto) mesmo com origem saudável. Mantenedor confirmou nesta sessão que o cutover Astro→raiz já tinha acontecido há tempo e WordPress já estava desligado — ou seja, Gate C já estava de fato encerrado, só não registrado como tal em `AGENTS.md`/`project-state.md` (que ainda descreviam DNS raiz como "intocável, WP na raiz, Gate C adiado"). Mantenedor removeu o registro R2 conflitante e recriou o `CNAME` correto (`artificiorpg.com` → `6417d3a0-b98b-42ed-97da-3fb9f6ecfac2.cfargotunnel.com`) via painel Tunnel. Gate C marcado como ✅ encerrado nesta sessão; `AGENTS.md` (trava "DNS raiz intocável pré-Gate C", topologia, regra de aprovação, seção SEO) e `project-state.md` (Fase atual + tabela Gates) atualizados para refletir o estado real. **Lição de diagnóstico:** ao investigar hostname raiz fora do ar, checar sempre o registro DNS real no painel antes de assumir causa (A/AAAA antigo, WordPress) — pode ser qualquer tipo de registro (neste caso R2) conflitando com o nome.
- 2026-07-09 — **Spec 062 I0b.4 concluída; D110.** Fila manual prod decidida: `Advanced Dungeons & Dragons` é edição de `Dungeons & Dragons`; `Vampire` é nome inglês e `Vampiro` nome português do mesmo sistema; `Anniversary` tem PT `Aniversário`; `Dungeons & Dragons 3.0` e `Dungeons & Dragons 3.5e` são edições irmãs de `Dungeons & Dragons`, não variante uma da outra. `migration_142` corrige só contaminados pelo padrão seguro; esses casos ficam fora de correção automática.
- 2026-07-09 — **Spec 062 I0b.3 aplicada em beta/dev.** `migration_142_fix_system_child_names.sql` executada em `mesas-beta-db` pelo runner oficial em `/opt/artificio-beta` com `COMPOSE_PROJECT=mesas-beta`, `ALLOW_MANUAL_MIGRATIONS=true` e backup VM-local visível ao guard (`/tmp/artificio-mesas-beta-i0b3-pre-20260709-143715.dump`, 1.5M, SHA256 `3eb7ffc055ca27616033c2449d7f6f9f5d1341b4efd454692233475e5e13b85a`). Resultado: 577 nomes corrigidos, `schema_migrations=1`, clone beta sincronizado com o arquivo da migration, health beta OK. Detector residual `1|0|591` é falso positivo legítimo `Time` → `Time Again`.
- 2026-07-09 — **Spec 062 I0b.3 aplicada em prod.** `migration_142_fix_system_child_names.sql` executada em `mesas-db` pelo runner oficial em `/opt/artificio` com `COMPOSE_PROJECT=mesas`, `ALLOW_MANUAL_MIGRATIONS=true` e backup VM-local visível ao guard (`/tmp/artificio-mesas-prod-i0b3-pre-20260709-162521.dump`, 1.5M, SHA256 `44d947913ec88505a5db7bdff9fccab27ad7126c153510425ec2114ac16dee75`). Guard pré-prod `573|0|6`; resultado: 573 nomes corrigidos, `schema_migrations=1`, clone prod sincronizado com o arquivo da migration, health prod OK. Detector residual `1|0|578` é falso positivo legítimo `Time` → `Time Again`. Próximo: I0b.6 smoke visual/build/lint em beta antes de I0a.
- 2026-07-09 — **Spec 062 I0b.6 fechada.** Validação pós-migration beta: `pnpm --filter @artificio/mesas lint` ✅; `pnpm --filter @artificio/mesas build` ✅; `mesasbeta /catalogo` HTTP 200; `/api/v1/systems?view=tree` HTTP 200 (469.612 bytes, `roots=697`, D&D→5e→2024 OK, sem string contaminada `Dungeons & Dragons 5e` no subgrafo); DB beta `schema_142=1`, residual esperado `Time -> Time Again`; containers beta healthy. Playwright visual real bloqueado por Chromium local ausente; smoke ficou por HTTP/DOM/assets. **I0a liberada.**
- 2026-07-09 — **Spec 062 I0a.0 fechada.** Removidas rotas órfãs `POST /api/v1/profile/me/systems` e `DELETE /api/v1/profile/me/systems/:id`; rota ativa `POST/DELETE /api/v1/profile/systems*` mantida; `useProfile.ts` removido e tipos extraídos para `apps/mesas/frontend/src/types/profileTypes.ts`. Validação: grep sem uso legado, smoke beta sem sessão da rota mantida = 401 (auth guard), `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅, `pnpm verify:api` ✅. `api:diff` mostra 2 breaking intencionais, mas com path/method vazios; débito `BL-API-DIFF-PATH-REMOVE-BLANK` aberto.
- 2026-07-09 — **Spec 062 I0a.1 fechada.** Criado `apps/mesas/frontend/src/hooks/useSystemsCatalog.ts`: fetch único de `GET /api/v1/systems?view=tree`, cache TTL 5 min, `forceRefresh`, `invalidateSystemsCatalogCache`, normalização Zod, árvore + flat + loading/error, sem `pathLabel`/label composto. `SystemTreeNode` ampliado para preservar `slug`, `node_type`, `aliases`, `children`, `logo_filename`, `website_url`, `children_count`, `tables_count`, `aliases_count`; teste cobre preservação desses campos e loading→dados→erro simulado. Validação: `pnpm --filter @artificio/mesas-frontend test -- src/hooks/useSystemsCatalog.test.ts` ✅ (suite frontend verde), `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅. Próximo: I0a.2 `SystemPicker` v1.
- 2026-07-09 — **Spec 062 I0a.2 fechada.** Criado `apps/mesas/frontend/src/components/SystemPicker.tsx` v1: componente único `single`/`multi`, árvore navegável com chevron/indentação, nome + nome PT sempre visível, aliases, destaque apenas no nó selecionado, bloco de confirmação com caminho montado em render, busca por nome/name_pt/slug/path_slug/alias filtrando a árvore com ancestrais visíveis, ações por `role` (`Sugerir`, `Criar agora`, editar inline admin). Sem lista plana e sem grid 3 colunas. Validação: `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx` ✅ (suite frontend verde, 17 arquivos/170 testes), `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅. Próximo: I0a.3 migrar `StepSystem.tsx`.
- 2026-07-09 — **Spec 062 I0a.3 fechada.** `apps/mesas/frontend/src/components/form-steps/steps/StepSystem.tsx` migrado do `SystemTreeSelector` para `SystemPicker` `single`/`user`; fluxo de criar mesa mantém `SystemSuggestionModal`, seleciona sistema criado no sucesso e remove estado local de busca antigo. Validação: `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅. Próximo: I0a.4 migrar `CatalogoPage.tsx`.
- 2026-07-09 — **Spec 062 I0a.4 fechada.** `apps/mesas/frontend/src/pages/CatalogoPage.tsx` migrada de fetch local + `SystemAutocomplete` para `useSystemsCatalog()` + `SystemPicker` single nos filtros desktop e mobile; mapas id→slug/id→nome agora vêm da view `flat`; retry usa `forceRefresh`; filtro continua salvando slug e enviando `trackFilterSistema`. Grep sem `SystemAutocomplete`, fetch cru ou reload local de sistemas na página. Validação: `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx src/hooks/useSystemsCatalog.test.ts` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅. Próximo: I0a.5 migrar `UserSystemsSelector.tsx`.
- 2026-07-09 — **Spec 062 I0a.5 fechada.** `apps/mesas/frontend/src/components/UserSystemsSelector.tsx` migrado de fetch cru + `SystemTreeSelector` para `useSystemsCatalog()` + `SystemPicker` multi; removidos `VITE_API_URL`, estado local de árvore/loading/busca, logs e toast de erro local. Contrato preservado por reconciliação de `selectedSystemIds` chamando `onAdd`/`onRemove`. Validação: grep sem legado no componente, `pnpm --filter @artificio/mesas-frontend test -- src/components/SystemPicker.test.tsx src/hooks/useSystemsCatalog.test.ts` ✅, `pnpm --filter @artificio/mesas lint` ✅, `pnpm --filter @artificio/mesas build` ✅. Próximo: I0a.6 migrar `OnboardingPage.tsx`.
- 2026-07-09 — **Spec 062 I0b.1-I0b.2 concluídas; D109.** `mesas_rpg` prod/beta dumpados read-only em `C:\projetos\artificiobackup\spec-062` (prod 5.060.121 bytes SHA256 `227D1F9B3EF4A0848950BAD155AA392CDDBF95EB5C747BAD9A9856D3A43AD803`; beta 5.200.707 bytes SHA256 `E5048F0767050CBCB32AF78B3AE271B0847AFB62CAF996AA573082E775E8223D`). Auditoria read-only: prod 688 sistemas/392 edições/187 variantes, 573 nomes contaminados e 6 irregulares; beta 697 sistemas/402 edições/189 variantes/1 subsystem, 577 contaminados e 15 irregulares. **Decisão:** prod é fonte real/principal do catálogo; beta/dev serve para implementação, ensaio e deploy inicial até centralizar. Próximo: I0b.3-I0b.4 (migration manual-risk + fila manual prod canônica), sem SQL write autorizado ainda.
- 2026-07-09 — **Spec 061 F4/T4.2 concluída; D108.** Hierarquia fechada: Header global compartilhado → submenu Downloads → sidebar contextual → conteúdo. Sidebar pública desktop incorpora exploração/filtros; painel e gestão usam rails próprias; <1024 px vira drawer acessível. Itens, breakpoints, sticky, CTA, persistência, foco, teclado, AA, estados e 25 critérios definidos. Gestão 062 continua no Site. Próximo: T4.3.
- 2026-07-09 — **Spec 061 F4/T4.1 concluída; D107.** Mapa de páginas/rotas fechado: `/catalogo`, `/materiais/:slug`, landings, `/criadores/:slug` separado de `/usuarios/:username`, coleções, painel e gestão deep-linkáveis, `/ir/:destinationId` e `/obter/:fileId` fail-closed. Redirects, canonical, sitemap, estados HTTP, beta noindex e ownership definidos. Sistemas/edições continuam geridos no Site/062. Próximo: T4.2.
- 2026-07-09 — **Spec 061 F3/T3.5 concluída; F3 encerrada; D106.** Capa pode ser reprodução editorial acessória e reduzida, nunca original/substitutiva: derivado canônico máximo 800×1200, sem upscale/zoom/original público, com fonte/crédito e retirada por asset. Preview/galeria/marca exigem base própria. Classes, integridade, conteúdo sensível, IA D103, Cloudinary shared, segurança de importação, moderação, CDN, limpeza e 25 critérios definidos. Próximo: F4/T4.1.
- 2026-07-09 — **Spec 061 F3/T3.4 concluída; D105.** Canais/categorias de denúncia, prioridade P0–P3, estados do caso, contenção proporcional, decisões, contraditório, recurso independente, abuso, retirada voluntária, abandono/reivindicação, checker e recuperação de links definidos. Denúncia comum não remove automaticamente; risco inequívoco permite contenção cautelar. Abandono nunca transfere autoria/direitos. Marco Civil/STF 2025, LDA e LGPD verificados. Próximo: T3.5.
- 2026-07-09 — **Spec 061 F3/T3.3 concluída; D104.** Estado editorial versionado, visibilidade pública e saúde técnica separados. Cadastro/edição exigem revisão; versão pública anterior permanece durante revisão comum. Transições, fila, papéis, conflito de interesse, decisões, auditoria, notificações, concorrência e fail-closed definidos. `downloads.content.auto_publish` nasce sem atribuição e bloqueada por gate global/kill switch em beta e prod; futura ativação exige spec e aprovação nominal. Próximo: T3.4.
- 2026-07-09 — **Spec 061 F3/T3.2 concluída; D103.** Separados direitos de catalogar, usar mídia, traduzir e hospedar; classes A1–A10, CC/OGL/ORC/programas fechados, fan content, traduções, IA, níveis de prova E0–E3, revalidação e aceite definidos. Produção de fã sem política/licença é somente link para página do criador, nunca upload. IA visual só integra obra humana substancial, declarada e moderada; texto final gerado e pacotes autônomos são proibidos. Próximo: T3.3.
- 2026-07-09 — **Spec 061 F3/T3.1 concluída.** Taxonomias de sistema/edição, compatibilidade, cenário, tipo, gênero, idioma, formato, plataforma, acesso, barreiras, licença, créditos, público, idade, avisos, criação e tags definidas. Campos obrigatórios/condicionais/opcionais/privados, facetas, validações, governança e aceite fechados. Sistemas/edições vêm só da 062; cenário fica próprio do Downloads. Próximo: T3.2.
- 2026-07-09 — **Spec 061 F2/T2.5 concluída; F2 encerrada.** Métrica principal = acessos qualificados ao destino. Funis, eventos conceituais, dimensões permitidas, dados proibidos, deduplicação/anti-fraude, métricas públicas/privadas/admin, baseline beta/30/90 dias e critérios de aceite definidos. Clique/redirect nunca é download concluído. Próximo: F3/T3.1.
- 2026-07-09 — **Spec 061 F2/T2.2 concluída.** MVP dividido em onze blocos obrigatórios; J1–J13 entram, auto-publicação só estrutura desativada e organização entra básica. Oito frentes pós-MVP, vinte não objetivos, lista não cortável e gate de lançamento definidos. Comentários, avaliações, favoritos, coleções, perfis, checker, moderação, prova, R2/B2/Fastio, scan e isolamento não podem ser adiados por conveniência. Próximo: T2.5.
- 2026-07-09 — **Spec 061 F2/T2.1 concluída.** Dez personas funcionais, matriz de capacidades, quinze jornadas ponta a ponta, ownership, regras de continuidade e perguntas encaminhadas definidos. Cobertura inclui descoberta, interações, autor, tradutor, editora, curador, link/upload, PWYW externo, moderação, denúncia, reivindicação, checker e retirada. Próximo: T2.2.
- 2026-07-09 — **Spec 061 F1/T1.4 concluída.** Matriz normativa final consolidou Dungeonist, DriveThruRPG, itch.io, RPGGeek, Keeper e D089–D102 em `adotar|adaptar|rejeitar`, cobrindo posicionamento, acesso, ficha, taxonomia, descoberta, submissão, direito, interações, UX, storage e operação. Próximo: F2/T2.1, personas e jornadas.
- 2026-07-09 — **D102 supera D101: PWYW externo aceito no Downloads.** Somente link para página externa onde valor zero libera o material; nunca `managed_upload`, providers, espelhamento ou armazenamento Artifício. Artifício não captura/exibe valores; sinaliza PWYW e manda consultar o link. Checker confirma opção zero. Preço mínimo positivo continua proibido.
- 2026-07-09 — **Spec 061 T1.3 concluído.** itch.io, RPGGeek e Keeper comparados como descoberta em escala, banco moderado e coleção gratuita/open. Rótulo `free` não prova condição de acesso; material/destino/arquivo ficam separados; link oficial pode ser juridicamente preferível; publicação e descoberta exigem estados distintos. D102 depois aceitou PWYW externo com opção zero. Próximo: T1.4.
- 2026-07-09 — **Spec 061 T1.2a concluído.** Modelo conceitual do Downloads consolidado sem comércio: material, versão, destino, créditos/perfis, classificação, evidência D100, revisão, verificação e interações; `external_link|managed_upload`; campos públicos/privados; barreiras controladas; estados editoriais/verificação; validações e exclusões financeiras. Próximo: T1.3.
- 2026-07-09 — **D101 histórica, superada por D102.** Formulação inicial excluía PWYW; decisão posterior passou a aceitar somente PWYW por link externo com aquisição possível por zero.
- 2026-07-09 — **Spec 061 T1.2 concluído.** Snapshot atual DriveThruRPG registrou seis famílias públicas (`Publisher`, `Genre`, `Product Type`, `Rule System`, `Languages`, `Format`), valores de primeiro nível, metadados de submissão/ficha e políticas oficiais. Filtro, taxonomia, navegação e comércio foram separados. Sistemas/editoras dinâmicos não viraram enums falsos. Próximo: T1.2a.
- 2026-07-09 — **D100 exige prova em toda aprovação do Downloads.** Submissão própria ou curadoria de terceiro precisa de URL, imagem/captura ou licença/autorização/base jurídica verificável que demonstre gratuidade ou possibilidade jurídica de divulgação/publicação. Declaração isolada não basta; regra permanece para futura auto-publicação.
- 2026-07-08 — **D099 fecha arquitetura da Spec 062 e muda seu escopo.** Serviço independente gerido pelo `artificiorpg.com`; todos os projetos leem/escrevem diretamente nele, sem projeções locais; cenários fora; fluxo único de sugestões/moderação/auditoria; modelo completo do Mesas; nunca apagar/arquivar, somente merge com destino; Mesas é catálogo principal correto; Glossário será mapeado manualmente. A 062 terá Etapa I investigação e Etapa II código.
- 2026-07-08 — **Spec 062: Etapa I concluída e aprovada, zero código.** Inventário, arquitetura, modelo, migração, compatibilidade, disponibilidade, rollout, rollback e provas fechados. Próximo passo: I0 detalhar execução da Etapa II.
- 2026-07-08 — **D098 libera reescrita do subsistema de sistemas/edições do glossário.** Catálogo local é pequeno (12+17); preservar 8.795 termos é gate duro via mapa de IDs, backfill e órfãos zero. Não autoriza reescrever o app inteiro.
- 2026-07-08 — **D097 define gestão do catálogo canônico.** Site admin/sidebar será superfície principal. Mesas, glossário e downloads também poderão administrar, mas todos como clientes do mesmo serviço/API central, sem banco/CRUD concorrente.
- 2026-07-08 — **Spec 062 criada e investigada.** Auditoria de código + API bundle + DBs beta/prod read-only. Mesas prod tem 1.265 nós/407 aliases; glossário prod 12 sistemas/17 edições/8.795 termos ligados. Modelos/UUIDs divergem. Registro histórico anterior a D099; decisão do mantenedor já obtida, zero implementação.
- 2026-07-08 — **D096 adiciona gate anterior a downloads.** Primeira spec filha planejará migração para um banco/catálogo canônico único e gerenciamento único de sistemas/edições para mesas, glossário e downloads. Unificação é firme; investigação decide ownership/localização/contrato/migração. Specs executáveis ficam bloqueadas.
- 2026-07-08 — **Spec 061 T1.1b concluído.** DriveThruRPG atual auditado por fontes oficiais; matriz `adotar|adaptar|excluir` registrada na spec. Artifício adota profundidade de ficha/taxonomia/formato/moderação/perfis/versões, exclui comércio inteiro e adiciona link/upload/provider/verificação/permissão. Browse público retornou 403; T1.2 permanece para enumeração literal dos valores atuais de filtros.
- 2026-07-08 — **D095 fixa essência de downloads.** Hub gratuito para catalogar/encontrar, semelhante ao Baixaki antigo na função de diretório/redirecionador. Usuário informa link ou envia arquivo para providers externos; saída resolve/redireciona. Nenhum pagamento/comércio. DriveThruRPG é referência máxima de configuração; Dungeonist, visual. T1.1b aberto para matriz completa do DriveThruRPG.
- 2026-07-08 — **D094 corrige D093:** downloads terá hosts beta/prod, mas nenhuma esteira própria. Implementação deve copiar o padrão canônico vigente dos outros módulos; detalhes de branch/promoção/isolation não são reinventados pela 061.
- 2026-07-08 — **Spec 061: beta/prod fixados por D093.** `downloadsbeta.artificiorpg.com` recebe `dev`; `downloads.artificiorpg.com` recebe `main` após promoção. App, DB, containers, secrets e storage ficam isolados por ambiente; beta não toca dados/jobs/métricas de prod.
- 2026-07-09 — **Specs 057, 058, 059 e 060 fechadas (validadas pelo mantenedor).** PR #140 (spec 059: copiar anuncio WhatsApp + OG de mesa + publicar mesa importada) mergeada em `dev`, promovida a `main` e deployada em prod e beta (confirmado via `docker ps` timestamps). Mantenedor confirmou que 057 (redesign `/gestao`), 058 (parser learning DeepSeek, DEB-058-01..08) e 060 (gestao de mesas importadas sem gm scope) tambem ja foram investigadas/codadas/aplicadas/validadas — smoke e deploy reais executados fora do fluxo de commit desta sessao. Backlog atualizado: `BL-057-GESTAO-REDESIGN`, `BL-058-PARSER-LEARNING-DEEPSEEK`, `BL-059-MESAS-COPIAR-ANUNCIO-OG` → 🟢 fechado; `BL-060-GESTAO-MESAS-IMPORTADAS` criado e fechado. `specs/060-mesas-gestao-mesas-importadas/tasks.md` T7-T9 marcadas concluidas.
- 2026-07-08 — **Spec 061: baseline corrigido por D092.** `apps/mesas` é a referência técnica principal de downloads por ser o app mais avançado; links fica referência complementar de sugestão/denúncia. R2, B2 e Fastio serão implementados obrigatoriamente na spec filha de storage; Cloudinary PDF é fallback final.
- 2026-07-08 — **Spec 061 ganhou storage multi-provider (D091).** Downloads poderá hospedar arquivos permitidos: Cloudflare R2 primário, fallback por cota para Backblaze B2, depois Fastio e por último Cloudinary para PDF. Capas continuam no Cloudinary shared. Antes de código: confirmar “Fastio”, quotas/APIs/egress; desenhar adapter, URL estável, failover, migração e reconciliação. Mapa passa a 13 specs filhas.
- 2026-07-08 — **Spec 061 refinada por D090.** Downloads confirmado como catálogo/hub, não armazenamento: links diretos autorizados ou páginas externas aceitos; barreiras como login/newsletter ganham flags. Material próprio e curadoria de terceiro permitidos com papel declarado e garantia de gratuidade/permissão. Moderação prévia em cadastro/edição; auto-publicação nasce modelada, desligada por padrão. MVP inclui comentários, avaliações, favoritos, coleções, perfis públicos e métricas. Taxonomia terá snapshot dos filtros atuais DriveThruRPG antes do schema. Não há legado WP a importar. Aberto: PWYW, evidência de permissão, políticas anti-abuso e storage futuro de arquivos.
- 2026-07-08 — **Spec 061 criada: investigação e definição do produto Downloads, sem código.** Norte D089: catálogo comunitário de materiais gratuitos em `downloads.artificiorpg.com`; usuários SSO cadastram metadados e links externos; Artifício não hospeda PDFs/arquivos; capas podem usar Cloudinary compartilhado por upload backend signed; design padrão com submenu sob nav e sidebar. A 061 investiga produto, referências (Dungeonist/DriveThruRPG), legado WP, gratuidade, autoria/licença, moderação, taxonomia, UX, arquitetura, segurança, infra e operação. Planejadas 12 specs filhas em ordem de dependência, de produto/políticas até deploy prod/Gate D. Backlog `BL-061-DOWNLOADS-DEFINICAO`; sessão `26-07-08_4_downloads_061-definicao-produto.md`.
- 2026-07-08 — **Spec 059 criada: copiar anuncio WhatsApp + OG de mesa.** Escopo `apps/mesas`, **SDD Completo** por decisao do mantenedor: botao para copiar texto pronto de divulgacao em formato WhatsApp na pagina publica `/mesas/:slug`, no painel do mestre e na gestao; corrigir Open Graph da URL da mesa para usar banner/imagem da propria mesa em `og:image`. Investigacao confirmou que `og.ts` hoje so tem caso dinamico para `/mestre/:slug` e `/mesas/:slug` cai em fallback generico. Criados `specs/059-mesas-copiar-anuncio-whatsapp-og/{spec,plan,tasks,reviews,debitos}.md`; backlog `BL-059-MESAS-COPIAR-ANUNCIO-OG`; sessao `26-07-08_1_mesas_copiar-anuncio-whatsapp-og.md`. Implementacao bloqueada ate decisao/autorizar proxima fase.
- 2026-07-02 — **Spec 058 Fase 7 local concluida: eval + shadow por camada.** Adicionada migration 140 `discord_parse_shadow_decisions`, serviço `discord/parseEval.ts`, endpoint admin `GET /admin/discord/automation/parse-eval` e integração best-effort em `recordParseCase()`/`recordParseFeedback()`. O sistema agora monta dataset de `discord_parse_cases` + feedback humano confirmado, mede `action`, `price_type`, `system_name`, vagas, dia/hora e contato, compara `parser` vs `learning` vs `deepseek`, e registra o que cada camada faria sem autoagir. Ajuste extra: `DuplicatesTab.tsx` sem `setState` sincronico em effect. Validacao: backend tests 41 files/360 tests ✅; backend build ✅; `pnpm verify:api` ✅; `git diff --check` ✅; `pnpm run lint` ✅; `pnpm run build` ✅; `pnpm run test` ✅. Sem commit/push/deploy.
- 2026-07-02 — **Spec 058 Fase 6 local concluida: rastreabilidade no frontend.** Editor de rascunho de mesas agora mostra, por campo, origem/evidencia curta da decisao (`Parser`, `Learning`, `DeepSeek`, `Humano`) a partir de `parsed_payload`, payload editado, evidencias brutas e `_ai_suggestions`, sem formulario extra e sem auto-aplicar sugestao. `useDraftForm` expõe `fieldInsights`; `DraftEditorTab` renderiza chips compactos. Validacao: frontend pontual 14 files/148 tests ✅; `mesas-frontend build` ✅; `mesas-frontend test` ✅; `pnpm verify:api` ✅; `pnpm run lint` ✅; `pnpm run build` ✅; `pnpm run test` ✅. Sem commit/push/deploy.
- 2026-07-01 — **Spec 058 Fase 4 local concluida: DeepSeek ContextPack.** Em cima das Fases 1-3, adicionada migration 139 `discord_llm_decisions`, `discord/llmContextPack.ts` com schema Zod/hash/politicas anti-injection, e `assistDiscordParseWithContextPack()` com cache por provider/model/prompt/context hash, auditoria de sucesso/falha/timeout/cache hit e resposta JSON validada. Fluxo continua sugestao revisavel, sem auto-publicacao. Validacao parcial: backend build ✅; backend tests 39 files/350 tests ✅. Sem commit/push/deploy.
- 2026-07-01 — **Spec 058 Fase 3 local concluida: learning rules ampliadas.** Em cima das Fases 1-2, adicionada migration 138 `discord_learning_rules` + `discord_learning_rule_applications`, backfill de `discord_field_learning` como `migration_seed`, `discord/learningRules.ts` com geracao por correcao humana, escopo/hash, confianca, rejeicoes, supressao e conflito, e integracao no enriquecimento antes da IA. Sem DeepSeek novo e sem auto-publicacao. Validacao parcial: backend build ✅; backend tests 38 files/348 tests ✅. Sem commit/push/deploy.
- 2026-07-01 — **Spec 058 Fase 2 local concluida: retrieval local + duplicatas em shadow.** Em cima do MVP-058-A, adicionada migration 137 `discord_duplicate_candidates`, indice `pg_trgm` em `discord_parse_cases.normalized_text`, features estruturais (`form_urls`, `attachment_urls`, `embed_urls`) e `discord/parseRetrieval.ts` com score por hash, similaridade, forms, anexos, autor/canal/guild e sistema. Retrieval separa candidatos, similares, positivos, negativos e corrigidos para futuro ContextPack. Sem DeepSeek novo e sem acao automatica sobre import/draft. Validacao parcial: backend build ✅; backend tests 37 files/343 tests ✅. Sem commit/push/deploy.
- 2026-07-01 — **Spec 058 Fase 1 local concluida: parser learning instrumentation-only.** Implementado localmente MVP-058-A sem mudar comportamento: migration 136 cria `discord_parse_cases` e `discord_parse_feedback` com FKs `ON DELETE SET NULL`; tipos Kysely; `discord/parseLearning.ts` com contratos Zod, hashes/features e escrita best-effort; instrumentacao em parse Discord/ChatExporter, inbox texto colado, correcao, rejeicao e sync/publicacao. Baseline `D:\teste.json` registrado em `specs/058-mesas-parser-learning-deepseek/baseline.md`: 50 total, 43 drafts, 7 descartes, 25 pagas, 3 gratuitas, 15 unknown, 43 `needs_review`. Validacao: backend build ✅; backend tests 36 files/339 tests ✅. Sem commit/push/deploy.
- 2026-07-01 — **Spec 058 Fase 0 concluida: parser learning + DeepSeek contextual.** Revisao de alto rigor registrada em `specs/058-mesas-parser-learning-deepseek/reviews.md` com evidencia do codigo real (`import_corrections`, `discord_shadow_decisions`, `discord_field_learning`, `llmAssist`). Decisoes: fonte duravel = `discord_parse_cases` + `discord_parse_feedback`; `discord_field_learning` fica cache/projecao; feedback nao pode depender de `draft_id ON DELETE CASCADE`; duplicata vem antes da IA; DeepSeek com ContextPack fica fora do MVP. MVP-058-A = instrumentation-only (parse cases + feedback imutavel + baseline), sem mudanca de comportamento e sem nova chamada DeepSeek. Proximo passo bloqueado por autorizacao nominal da Fase 1.
- 2026-07-01 — **Spec 058 criada (pre-implementação): parser learning + DeepSeek contextual.** Motivação: investigação de JSON real do Discord mostrou que micro-regex não escala; solução precisa de feedback humano como dado, regras aprendidas com escopo/confiança/rejeição, busca de casos próximos, duplicatas como primeira classe e DeepSeek recebendo ContextPack com exemplos/regras/rejeições. Criados `specs/058-mesas-parser-learning-deepseek/{spec,plan,tasks}.md`; backlog `BL-058-PARSER-LEARNING-DEEPSEEK`; sessão `26-07-01_1_mesas_parser-learning-spec.md`. Implementação bloqueada até revisão 5.5 altíssimo e escolha de MVP mínimo.
- 2026-06-30 — **Spec 057 — reviews CodeRabbit PR #120 triados e corrigidos (Fases 5-8).** 19 achados aplicados: token leak `publicProfile` (Critical), `admin/users` paginação+COUNT real, confirm no delete de mesa, guard toggle `full`/`ended`, hooks propagam erro + sem `placeholderData` (loading volta), UI `media`/`Tópicos`/`clearToken`, `deleteProfile` try/catch+busy, OpenAPI 201/204, `z.uuid()`, INSERT único do perfil, dedup `formatDate`/`tabButtonClass`, i18n (Tópicos/Última execução/status pt-BR). 2 não aplicados c/ justificativa (run síncrono por UX; schema+tipo = normalização mandatória). Validação: backend 330 tests, frontend 148, lint+build+verify:api exit 0. Veredictos em `reviews.md`. Sem commit.
- 2026-06-30 — **Spec 057 — Fase 8 (limpeza de contrato) ✅.** `import/metrics` (`GET /admin/import/metrics`) removido: órfão (0 caller) + redundante com `GET /admin/discord/metrics` (mount + `inbox/metrics.ts` + teste + path no `mesas.openapi.yaml`). `import-json/reparse` MANTIDO (não redundante — reprocessa `discord_import_messages` em lote, não draft único). `api:bundle` (281 ops) + `verify:api` exit 0 (mesas breaking=1 = remoção intencional aprovada, non-breaking=6). backend build+330 tests, lint 15/15 ✅. **Fases 2–8 completas; falta só Fase 9 (smoke beta T9.2 + promote gated). Sem commit.**
- 2026-06-30 — **Spec 057 — tudo antes da Fase 8 implementado (local, sem SDD separado — mantenedor: "tudo nesta spec").** T2.5 `POST /admin/tables/batch` (archive/unarchive/delete) + bulk UI no Catálogo; T6.3 dry-read delta (`GET /profiles/:id/delta` + `discoverChannelDelta`); T6.4 filtro/ordenação nos Relatórios; T6.7 wizard 3 passos (Conectar→Canais→Agenda); T6.8 "Ver novidades" (delta na Saúde); T6.9 runner agendado (`runDiscordChatExporterSchedule` + `isProfileDue` puro/testado + tick no `cronRunner`; `runProfileExport` extraído p/ service reusado pela rota). T6.6 Dockerfile DCE já bundlado. Validação: backend build+332 tests, frontend lint+build+148 tests, `verify:api` exit 0 (mesas +6 non-breaking). **Gated:** runtime (bot token/CLI/`mesas-cron` beta) = smoke T9.2; deploy DCE; **sem commit**. Falta Fase 8 (limpeza de contrato) + Fase 9. `BL-057-GESTAO-REDESIGN` atualizado.
- 2026-06-30 — **Spec 057 (redesign completo da gestão `/gestao` do mesas) CRIADA — investigação+plano primeiro.** Pedido do mantenedor: `/gestao` está com cara de gambiarra (desatualizada/feia/pouco informativa); 049/054/056 melhoraram em partes mas não ficou usável. Decisões nominais: (1) sidebar reestruturada do zero, IA estilo painel Cloudflare; (2) **Mesas central** = fila unificada de drafts (`discord/drafts`+`import/drafts`) por origem/status/lote, CRUD publicado fica em Conteúdo; (3) **Dashboard** enxuto (cards de estado real + feed único; remove Alertas/Atalhos stub e redundância Visão geral×Últimas atividades); (4) **Bot de Discord** sob grupo Importação (Configuração persistível / Importação com dry-read / Relatórios manipuláveis); (5) a11y de teclado fora de escopo (tela de uso pessoal). Escopo desta abertura = só investigação+plano; **implementação bloqueada por gate nominal G1**. Mapeamento inicial via MCPs `artificio-api-governance` + `codebase-memory-mcp` + leitura de `features/admin/*`. Achados (7 débitos DEB-057-01..07; 5 iniciais + 2 na investigação extra de notificações/ChatExporter): 2 árvores admin coexistem (`features/admin` novo + `modules/admin` velho), bundle API com `consumers:[]` e `api-orphans` stale (1970), ruído dedup de request no Dashboard, 2 sistemas de draft paralelos, Dashboard redundante/oco. Spec `057-mesas-gestao-redesign` (spec/plan/tasks/reviews/debitos), sessão `26-06-30_1_mesas_gestao-redesign.md`, `BL-057-GESTAO-REDESIGN` no backlog. **Atualização 2026-06-30:** G1 aprovado + G2 autorizado; Fases 2–5 implementadas/commitadas; **Fase 6 local parcial**: grupo Importação reorganizado, perfis multi-canal DCE com migration 134 + CRUD/test/run + `<AdminTable>`, Dockerfile bundla CLI DCE pinada; **Fase 7 local ✅**: Catálogo/Sistema reais, `admin/users` real + UI Usuários/Covil, UI CRUD de estilos por cenário, stubs removidos; faltam dry-read delta, relatórios filtráveis, wizard completo, runner agendado, Fase 8 e smoke beta.
- 2026-06-29 — **Spec 056 (mesas moderação batch + logs) ✅ ENCERRADA e em PROD.** Pedido do mantenedor em `/gestao/moderacao` e `/gestao/integracoes`: (1) checklist em Rascunhos → rejeitar selecionados / limpar todos; (2) checklist em Mensagens capturadas → ignorar selecionadas; (3) endpoints batch p/ otimizar (`PATCH /api/v1/admin/discord/messages/batch` e `…/drafts/batch`, status em lote, registrados antes de `/:id`); (4) Logs de integração real (substitui stub "em breve") reusando `discord_import_runs` via `GET …/metrics` — zero migration. Lint+build+test (122 FE/4 BE)+verify:api (0 órfãs, breaking=0, non-breaking=2) verdes. Branch `feat/mesas-moderacao-batch`, 3 commits (`664a739` feat, `3834f7d` fix batch reject + shadow transação, `89251fa` chore regenera docs/api). PR #107 aprovado e mergeado em `dev` (commit `a77270a863c654a05d37ad57daa55363d86ce0d2`, 2026-06-29 12:06:43 UTC). Reviews: Codex (revisão geral 22 arquivos) + CodeRabbit 2 rodadas (7+2 actionable comments) — resolvidos. Deploy beta + smoke + promote prod ✅ (mantenedor). SDD Completo (contrato API novo). Spec `056-mesas-moderacao-batch-acoes`, sessão `26-06-29_2_mesas_moderacao-batch.md`, `BL-056-MODERACAO-BATCH` fechado.
- 2026-06-29 — **Bug fix beta: enrich prod→dev 500 (BL-BETA-HYDRATE fechado).** Botão "Sincronização Prod → Dev" (`POST /api/v1/admin/sync/enrich`) em `mesasbeta` dava 500: `.env.beta` sem `PROD_DB_URL` → `getProdDb()` lançava. Fix (via mais segura): role read-only `mesas_ro` criada no DB prod (`mesas-db`/`mesas_rpg`; CONNECT+USAGE+SELECT all tables + default privileges), `PROD_DB_URL` apontando p/ `mesas_ro` no `.env.beta`, `mesas-beta-api` recriado (`docker compose -p mesas-beta --env-file .env.beta up -d`). Validação: health 200, SELECT OK, WRITE negado (read-only de verdade), smoke UI verde confirmado pelo mantenedor. Sem spec (só sessão `26-06-29_1_mesas_enrich-prod-db-url.md`). Senha do `mesas_ro` só no `.env.beta` (gitignored, não versionada).
- 2026-06-28/30 — **Spec 055 ✅ ENCERRADA.** Fases 1-11 implementadas, modo estrito ativo. Validação completa: `pnpm api:inventory` (~331 rotas), `pnpm api:consumers` (299 chamadas), `pnpm api:lint` (0 erros), `pnpm api:check --strict` (allowlist vazia, exit 0), `pnpm api:diff` (breaking bloqueia), `pnpm api:docs` (4 HTMLs), `pnpm api:bundle`, `pnpm verify:api` exit 0. CI: `api-governance` required check no `dev` + artifact commit check + `api:diff` sem `continue-on-error`. `apps/mesas/MAPA_DE_API.md` deprecado; `docs/api/README.md` proíbe agente de usar mapa manual como fonte primária. Débitos descartados: o que não foi feito, foi descartado (mantenedor). `BL-055-API-GOVERNANCE` fechado.
- 2026-06-27 — **Spec 055 — Investigação Fases 1 a 6 concluída.** Fases 0-5 registradas. **Fase 6 (órfãs e duplicadas) investigada:** ORPHAN_SUSPECT refinado por scope (admin/cron/webhook/internal = UNUSED vs public/sem scope = ORPHAN). DUPLICATE_SUSPECT com scoring 4 fatores (method 40, token 40, owner 10, scope 10). Normalização forte remove :params, /v1/. Thresholds ≥80 HIGH/70-79 MEDIUM. Relatório `api-orphans.generated.md` integrado ao `api:check.ts` sem alterar exit code. DEB-055-11 (falso positivo conhecido) registrado. Tasks T6.1–T6.9 completas.
- **2026-06-27 — Spec 055 — Fase 1 implementada.** `docs/api/` criado: README, `openapi/`, `generated/`. 4 YAMLs com 209 rotas documentadas e metadados x-artificio-* em 100% dos paths. T1.1–T1.8 concluídas.
- **2026-06-27 — Spec 055 — Fase 2 implementada.** `scripts/api/inventory.ts` — scanner AST TypeScript Compiler API. 291 rotas encontradas (accounts 10, glossario 61, links 23, mesas 197). 290 HIGH + 1 LOW. Scripts adicionados. DEB-055-12 registrado. T2.1–T2.11 concluídas.
- **2026-06-27 — Spec 055 — Fase 3 implementada.** `scripts/api/consumers.ts` — scanner AST multi-passo. 299 chamadas, 175 endpoints únicos em 8 apps/packages. 104 HIGH + 135 MEDIUM + 60 LOW. Scripts adicionados. T3.1–T3.12 concluídas.
- **2026-06-27 — Spec 055 — Fase 1+4 implementadas.** Redocly CLI instalado. 4 OpenAPI YAMLs gerados (226 operations). `redocly.yaml` configurado. `pnpm api:lint` passa com 0 erros. T4.1–T4.8 concluídas.
- **2026-06-27 — Spec 055 — Fase 5 implementada.** `scripts/api/check-api.ts` — comparador 3-way. 524 chaves únicas, exit code 0. Allowlist gerada (478 entries). T5.1–T5.12 concluídas.
- **2026-06-27 — Spec 055 — Fase 6 implementada.** detectOrphans() e detectDuplicates() adicionados ao check-api.ts. Relatório api-orphans.generated.md com 143 órfãs e 200 duplicatas. Threshold calibrado (minScore=75, tokenSimilarity≥0.5). T6.1–T6.9 concluídas. Fases 1-6 completas.
- 2026-06-27/30 — **Spec 054 ✅ ENCERRADA (sidebar + roteamento aninhado + 6 Sections + DiscordSyncPanel desmontado + renomeações + dashboard com subnav + stubs honestos + mortos deletados + 16 reviews de bot corrigidos).** Sidebar persistente 6 grupos, AdminMain com header/breadcrumb/outlet condicional, roteamento aninhado `/gestao/<grupo>`, `moderacao/:sub?` com deep-link, GestaoLayout, 6 Sections (Dashboard/Conteúdo/Comunidade/Moderação/Integrações/Sistema). Reviews 001-025 resolvidos. 11 arquivos de código + 3 artefatos. Validação: lint 17/17 ✅, build 17/17 ✅, test backend 285/285 ✅, test frontend 141/141 ✅, verify:api exit 0 ✅. PR mergeada em `dev` → deploy beta + smoke + promote prod ✅ (mantenedor). O que não foi feito foi descartado. **053 Frente A destravada.** `BL-054-GESTAO-IA` fechado.
- 2026-06-27 — **Spec 048 DEPLOYADA EM PROD + Spec 053 criada (consolidação 047–051).** Sequência completa de deploy da 048: PR #100 (overhaul import) + PR #101 (hotfix `@artificio/config` CJS `exports` — DEB-048-36, crash-loop beta) mergeadas em `dev`; mesas beta redeploy ✅; promote `dev→main` ff (`9e81f1b`); accounts prod ✅ (healthy, /health 200); **mesas prod ✅** (run 28293836960, `mesas-api` Up healthy, `/api/v1/health` 200, migration_132 `cover_public_id` aplicada). Mantenedor já inseriu `deepseek_api_key` no accounts prod → enriquecimento LLM ativo (best-effort). **Achado novo:** accounts preso em tema light (DEB-048-38). **Spec 053 criada** (`specs/053-mesas-ui-ci-debitos-consolidados/`) concentrando TODOS os débitos remanescentes das specs 047–051 antes de abrir a 052: **Frente A** a11y/UI da revisão de gestão (ex-049/D06, 8 P1 WCAG), **Frente B** tema accounts (ex-DEB-048-38), **Frente C** smoke CJS no CI (ex-DEB-048-37, o gap que causou o crash de hoje), **Frente D** doc REV-051-RABBIT-06, **Frente E** (FINAL) ingestão diária na VM (ex-048 Fase E, coordenar c/ 052 Bloco A — não duplicar). **Reclassificação (decisão mantenedor):** melhorias opcionais de parser da 048 (T-C4/C5/C7/C8/C9 + T-B5) → **Bloco C ao final da spec 052** (R15–R20). **052 BLOQUEADA até a 053 fechar.** Débitos de origem (047–051) marcados "→ 053"; backlog + project-state sincronizados. SDD Completo (toca `packages/ui` + accounts + CI). Sem código ainda — só planejamento.

- 2026-06-25 — **Spec 051 Onda 0 (F0 discovery) executada.** Varredura de duplicação real (`rg`+leitura) → `specs/051-.../f0-discovery.md` + sessão `26-06-25_1_ui-051_f0-discovery`. **Vereditos que fecham o escopo:** **F6 = NULO** (todo schema Zod vive só em `apps/mesas`; `packages/content` não tocado; Onda B cai → PRs viram A→C→D). **F4 = pequeno** (pesado já saiu na spec 041; só colapsar site+links idênticos). **F5 redimensionado:** ConfirmDialog=extrair p/ `packages/ui` (3 apps usam `window.confirm` cru: site-admin×4, glossario×3, mesas×3) com migração p/ tokens de tema; GestaoStateWrapper=**consolidar** com `primitives.tsx` já existente (não extrair novo); FileDropzone=extrair (2 apps); ImportResultGrid/StatCard=manter local (domínio mesas). Constraint transversal: primitivas locais usam paleta dark hardcoded → migrar p/ tokens antes de promover. Achado p/ backlog: `site-admin` não depende de `@artificio/ui` (rollout ConfirmDialog lá = débito futuro). Ondas A/C/D aguardam autorização de implementação (DeepSeek).
- 2026-06-25 — **Spec 051 criada (só planejamento; sem código; `debitos.md` zerado por definição).** 4 frentes (todas tasks, não débitos) em `specs/051-ui-changelog-nav-active/`: **F1** bug visual do changelog (mesas renderiza sobreposto "um na frente do outro"; glossário esconde o topo atrás do header); **F2** indicador de projeto ativo na nav — mecanismo **já existe** (`Nav.tsx` `aria-current` + `styles.css:254`/`:1197` traço laranja `#FF5722` light/dark) mas inerte por `currentHref` inconsistente (glossário não passa; mesas passa `publicOrigin` sem casar); **F3** = **DEB-050-02** absorvido da spec 050 (convergência `[`→`[[` + `>&2` nos scripts deploy); **F4** consolidar os 4 wrappers de changelog duplicados (mesas/glossario/site/links) em `packages/ui` — extração adiada da spec 020 (`header-nav-actions.md`); **F5** extrair primitivas de admin que a 049 criou locais no mesas → `packages/ui` (**G01/049**: `GestaoStateWrapper`/`ResultGrid`/`ConfirmDialog`/`FileDropzone`); **F6** centralizar schemas Zod **cross-app** → `packages/content` (**G02/049**, auditar quais são compartilhados de fato vs domínio mesas); **F0** varredura de duplicação ANTES de extrair. **Estratégia por ondas (evita retrabalho pré-lançamento):** Onda 0 (F0) → Onda A (`packages/ui`: F4+F5, depois F1 na fonte já consolidada — um review/smoke só) → Onda B (`packages/content`: F6) → Onda C (nav: F2) → Onda D (shell: F3). Regra anti-duplicação reforçada: extrair o compartilhado antes de consumir; cópia some no mesmo PR. G01/G02 marcados absorvidos em `specs/049-.../debitos.md`. Backlog: `BL-051-UI-CHANGELOG-NAV`. `debitos.md` começa vazio (débito = só o que surgir na impl).
- 2026-06-26 — **DEB-051-01 implementado (ConfirmDialog no site-admin).** 6 tasks T-DEB01.1–T-DEB01.6 concluídas: dep `@artificio/ui` adicionada ao `site-admin`, CSS ui/styles.css importado antes do styles.css local, `<ConfirmProvider>` no root, 4 `window.confirm` → `await confirm({...})`. 8 arquivos (+50/-5). Lint 15/15 ✅, build ✅. `BL-051-CONFIRMDIALOG-SITEADMIN-ROLLOUT` fechado. F5.2a agora **total** (3/3 apps migrados). Código local, não commitado.
- 2026-06-26 — **Spec 051 + 049/050 promovidas a PROD (rollout completo).** PR #97 (fix DEB-051-02 changelog Tailwind `@source`) mergeada em dev (`e9ccd25`). Redeploy beta glossario/mesas/site ✅ + smoke do mantenedor OK (changelog corrigido: topo, sobreposição e visibilidade resolvidos). **Promote dev→main ff** (`e9ccd25`, run 28236171046; main ⊆ dev). **Deploy prod ✅:** glossario (28236212406), site (28236213939), links (28236215542), **mesas (28236817132)**. Smoke prod 200/401 nos 4. **mesas-prod destravou:** estava preso em código pré-049 desde o rollback de 2026-06-25; o salto 049+050+051 com migrations 128/129 passou — o guard `validate_sql_against_class` corrigido na spec 050 funcionou (sem falso-positivo `DROP NOT NULL`). Pendência menor: REV-051-RABBIT-06 (falso-positivo CodeRabbit sobre Stylelint inexistente) registrado em reviews.md, não-commitado → próximo PR doc-only. Validação visual final do changelog em prod = mantenedor. **✅ Confirmada (2026-06-26): changelog + marcador OK em prod. Spec 051 ENCERRADA e entregue em prod (BL-051-UI-CHANGELOG-NAV fechado).**
- 2026-06-26 — **Spec 051 deploy beta + bug de changelog (DEB-051-02, BLOQUEIA prod).** PR #96 mergeada em dev; deploy beta de glossario/mesas/site ✅ (runs 28233047548/48779/50217, critical_routes_beta + HTTP 200). **Smoke do mantenedor achou changelog quebrado:** glossariobeta topo cortado, mesasbeta mistura com home, beta.artificiorpg não aparece (marcador nav F2 OK). Causa-raiz: `ChangelogModal` (em `packages/ui`) usa utilitários Tailwind (`z-[9999]`, `max-h-[calc(100dvh-2rem)]`, `fixed`/`bg-black`), mas os apps (Tailwind v4, sem config) não fazem `@source` de `packages/ui` → classes não geradas. Fix proposto: `@source 'packages/ui/src'` no entry CSS dos 4 consumidores (mesas/glossario/site/links). **Promoção a prod parada até corrigir + re-smoke beta.** DEB-051-02 registrado.
- 2026-06-26 — **Spec 052 criada (automação inteligente de importação de mesas — roadmap futuro/opcional).** Destaca da 048 o "futuro do futuro": Bloco A (ingestão agendada na VM, DiscordChatExporter pinado/seguro, pasta monitorada idempotente, `discord_import_runs`, métricas) + Bloco B (IA auxiliar revisável → eval offline → shadow mode → auto-aprovação conservadora gated → fine-tuning condicional, com kill switch). Travas: sem auto-publicação até shadow+gate, IA é camada auxiliar (parser determinístico da 048 nunca removido), sem credencial pessoal permanente, privacidade primeiro. **Bloqueada até a 048 fechar (Fase G com métricas reais) + aprovação nominal por bloco/degrau.** Sem código nesta abertura. Backlog `BL-MESAS-AUTOMACAO-INTELIGENTE-052`.
- 2026-06-26 — **Spec 048 (importador DiscordChatExporter JSON) retomada/reconciliada.** Auditoria read-only contra `origin/dev` (`ff24632`): **MVP já em dev** (PR #91 `ed3f4e0` Fase B backend + spec 049 `a4d2fb5` Fase D UI; DEB-048-09/10/11/12 todos mergeados — a doc dizia "pendente commit/PR", era falso). Experimentos locais (`deepseek.ts`, `chrono-node`/`fuzzball`, Playwright e2e) **não foram pra dev** → DEB-048-06/07/08 marcados moot. Parser em dev = regex puro (não extrai `<t:UNIX>`, `forms.gle`, contato implícito, vagas informais). **Decisão mantenedor:** terminar a feature com **Fases C (parser hardening) + F (robustez) + G (human-in-loop) completas na mesma spec**; Fase E (VM diária) fica futuro. Gate MVP aberto: smoke beta real (fixes em dev, falta confirmar deploy beta + re-smoke). Docs 048 (spec/tasks/debitos) reconciliados. Implementação por DeepSeek; arquitetura/revisão por Claude.
- 2026-06-26 — **Spec 048 Handoff #6: 6 débitos de review fechados.** DEB-048-16 (extractSlots reordenado: guard "mesa em andamento" → fallback posição 8, após 7 padrões explícitos), DEB-048-17 (filtro /reparse inclui 'parsed' quando messageIds + contador `ignored` na resposta), DEB-048-18 (extractDiscordTimestamp via `Intl.DateTimeFormat('pt-BR', { timeZone: 'America/Sao_Paulo' })`), DEB-048-19 (`Array.isArray(messageIds)` + remove `as any`; payload inválido retorna 400), DEB-048-20 (catch interno próprio para db.updateTable; parse_error preserva causa real), DEB-048-21 (helper `respondImportError(res, error)` extraído, aplicado nos dois handlers). Testes 253/253 ✅, lint 15/15 ✅, build ✅. Código local, não commitado.
- 2026-06-26 — **Spec 051 PR #96: revisão CI verde + 2 fixes.** Push da branch `feat/051-ui-onda-a` (`971bc3f`) disparou checks: **`lint + build + test` ✅** (pétreo), Sonar/CodeRabbit/Semgrep/Snyk/OSV/TruffleHog ✅; único **fail = CodeQL** `js/polynomial-redos` (bloqueante). Corrigidos 2 achados (autorização nominal do mantenedor p/ fix+test+doc, **sem commit/push**): **DEB-051-CODEQL-01** — `Nav.tsx` `normalizeHref` fallback troca regex `/\/+$/` por strip `charCodeAt` (sem ReDoS), build+lint ✅, comportamento idêntico 6/6; **DEB-051-RABBIT-04** — `apply_required_migrations.sh` `load_header_vars` ganha allowlist fail-closed `online-safe|manual-risk`, shellcheck ✅, guard 39/39 ✅. Ambos fechados (reviews.md §10/§11, debitos.md, backlog). **Pendente:** commit/push dos fixes + docs (aguarda autorização nominal própria) → re-roda CodeQL.
- 2026-06-25 — **Spec 051 Onda A concluída (DeepSeek).** F4 (StaticChangelogModal ganhou `rawChangelogs`, wrappers site+links deletados), F5 (ConfirmDialog+FileDropzone extraídos p/ `packages/ui` com tokens de tema; `GestaoStateWrapper`+`LoadingState` locais deletados como código morto; 7 `window.confirm` migrados), F1 (scroll lock adicionado ao `ChangelogModal.tsx`). Lint 15/15 ✅, build 17/17 ✅. 8 arquivos deletados, 2 novos em `packages/ui`. BL-051-CONFIRMDIALOG-SITEADMIN-ROLLOUT registrado no backlog (site-admin não depende de `@artificio/ui`). Ondas C (nav F2) e D (shell F3) pendentes. Onda B (F6) cancelada (F0 vereditou schemas Zod = domínio mesas, sem cross-app real). Sessão `sessoes/26-06-25_2_ui-051_onda-a-impl.md`.
- 2026-06-24 — **Spec 050 implementada (guard migration corrigido + reconcile tool).** Fases A-E concluídas: regex estreito (lista branca de DROP de objeto) em `scripts/deploy/lib_migrations.sh`; teste shell `test_migration_guard.sh` (28/28) plugado no CI `_lint-shell.yml`; cópia órfã `apps/mesas/scripts/deploy/` removida (6 arquivos, escopo A); débitos `BL-MESAS-MIGRATION-GUARD-FALSE-POSITIVE` e `BL-DEP-MESAS-LEGACY-SCRIPTS` fechados; erro `E010` registrado; R0a-e/T6a-d marcados na spec 035; 128+129 verificadas. **Fase E:** ferramenta de reconcile (`scripts/deploy/reconcile_migrations.sh`, `--list`+`--mark-applied` com R9) portada para canônico + `test_migration_reconcile.sh` (7/7) no CI. `BL-MIGRATION-RECONCILE-TOOL` fechado. **Pronto para re-deploy prod mesas** (gated por aprovação nominal). **Follow-up:** `gh workflow run deploy.yml --ref main -f module=mesas -f mode=deploy -f env=prod` (mantenedor, aprovação nominal).
- 2026-06-24 — **Promote `dev→main` ✅ + deploy prod mesas FALHOU (rollback).** Spec 049 fechada em `dev` (PRs #93/#94). Promovido ff `dev→main` (`2756346`, run `28125170428`) com specs 044-049 acumuladas; `main == dev`. Deploy beta mesas ✅ (run `28124340101`). **Deploy prod mesas (run `28125222995`) abortou** ao aplicar `migration_128`: guard `validate_sql_against_class` (`scripts/deploy/lib_migrations.sh:59`) tem `\bDROP\b` largo demais e barrou `DROP NOT NULL`/`DROP CONSTRAINT` (não destrutivos) de uma migration `online-safe` legítima → rollback automático restaurou prod (mesas em 127, código pré-049). Beta passou porque 128/129 já estavam em `schema_migrations`. **Spec 050 criada (planejamento Claude; impl. DeepSeek)** com correção (regex estreito + teste shell) + Fase C tratando a duplicação do guard (`BL-DEP-MESAS-LEGACY-SCRIPTS` / spec 035 R0/T6). Débitos: `BL-MESAS-MIGRATION-GUARD-FALSE-POSITIVE` (novo) + `BL-DEP-MESAS-LEGACY-SCRIPTS` (referência). Sessão `26-06-24_1_infra_migration-guard-online-safe`. Follow-up gated: re-deploy prod mesas após guard verde.
- 2026-06-23 — **Spec 048: smoke real beta achou 3 itens.** Mantenedor testou `D:\extracao_json.json` em `mesasbeta.../gestao` → Discord Sync → Importar JSON. (1) **P0 corrigido local:** embed com campos `null` (`messages.N.embeds.M.timestamp`) derrubava import com 400 — `discordChatExporterEmbedSchema`/author/attachment usavam `.optional()` (só `undefined`); migrado p/ `.nullish()`; +3 testes (`chatExporterAdapter.test.ts`); backend 183/183, lint 15/15 ✅ (DEB-048-10, T-FIX1). (2) `GET /settings` 500 no beta — pré-existente Spec 047, provável mismatch `JWT_SECRET`/credencial (DEB-048-11, aberto). (3) Falta upload de arquivo na UI (só cola texto) — DEB-048-12/T-D6. Fix DEB-048-10 pendente commit/PR (carona na PR #91) + redeploy beta + re-smoke.
- 2026-06-23 — **REV-025 + REV-026 implementados (spec 047).** Helper `toNumberOrNull` + `z.coerce.number()` corrigem confidence string→number. Textarea/banners migrados para tokens semânticos de tema (`--surface`/`--fg`/`--state-*`). Lint 15/15, build 17/17, backend 178 tests ✅.
- 2026-06-23 — **PR #89 (chore/047-debitos-finais).** 9 CodeRabbit reviews (REV-027 a REV-035) investigados e implementados: refatoração useReducer, dirty tracking, normalizador SystemTreeNode, API_BASE compartilhada, acessibilidade radios, guarda synced, reviewNotes fix. Documentação da spec atualizada com contagens e status corretos. Lint 15/15, build 17/17, backend 178 tests ✅, PR checks 33/33 ✅. Pendente merge + redeploy beta + smoke.
- 2026-06-23 — **Spec 047 retomada antes da 048.** DEB-047-21/23 fechados localmente: PATCH Inbox/Discord valida enums de `normalized_payload.table`; `DiscordDraftPreview` exige API injetada e não tem fallback `discordSyncApi`. Gates finais: lint 15/15, build 17/17, test 24/24, backend 21/180, frontend 4/19 ✅. Beta `b70367c` healthy; migrations 128/129 e constraint `single_origin` confirmadas. Smoke autenticado T1.13-T1.16 ✅ via Chrome autorizado: draft único criado; múltiplos anúncios segmentados; draft T1.13 editado e sincronizado para mesa `f4c56c7e-0283-4ed5-a9ae-2ce43255392d` com `status='draft'`; draft T1.14-A rejeitado. Sem auto-publicação.
- 2026-06-23 — **Spec 048 criada.** Auditoria dos JSONs reais do DiscordChatExporter: `extracao_json.json` válido (100 mensagens, 85 anexos, 28 embeds, 11 Google Forms, 4 timestamps Discord, 60 role mentions); `extracao_json2.json` truncado/inválido. Veredito: fonte Discord real usa `discord_import_messages`; MVP upload manual admin; futuro ChatExporter diário na VM; opcionais robustos viraram tasks explícitas.
- 2026-06-23 — **PR #90 mergeada + 048 realinhada.** PR #90 (`f0e2e56`) fechou Spec 047 em `dev` e removeu DeepSeek acidental da 047; checks GitHub verdes. Branch local da Spec 048 avançada por fast-forward para `origin/dev`; T0.7 concluído.
- 2026-06-23 — **Spec 048 Fase B/C/D implementada.** Handoff de execução executado: diff limpo (deps órfãos revertidos), backend MVP completo (Zod schemas, adapter, serviço idempotente, endpoint admin), UI (aba "Importar JSON" no Discord Sync com textarea + resultado). Lint 15/15, build 17/17, backend 180/180, frontend 19/19. Pendente commit/push/PR.
- 2026-06-23 — **Spec 049 Fases A-D investigação concluída** (sem código). codebase-memory-mcp + Serena MCP + LSP + skills (4 auditorias) + jscpd. Relatórios: inventario.md, auditorias-consolidadas.md (8 P1, 17 P2, 27 P3), duplicacao.md (17.3% backend), proposta-reorganizacao.md (13 tasks, ~55h).
- 2026-06-23 — **Spec 049 Fase E (refatoração) e Fase F (verificação) executadas.** 3 commits locais (`774c462`, `cfe6a96`, `a4d2fb5`) no branch `feat/049-revisao-gestao`. 76 arquivos modificados. TE1-TE22: backend extraído em 6 módulos, frontend extraído em 2 hooks + 5 componentes. Testes: backend 28 files 223/223, frontend 15 files 163/163. Build 17/17, Lint 15/15. TE23 skip. Fase F: TF1-TF6 verdes, TF7/TF11 skip. Spec **fechada**, código não mergeado. Pendência na época: D15 (CC 56 adminImportInbox), REV-036-038.
- 2026-06-24 — **Spec 049 encerrada (REV-036-038 + D15 + D09 + D10).** D15 resolvido via REV-032 (calcMissingFields + createImportMessage extraídos). REV-036 (`parseDiscordMessage` em utils.ts, 20 linhas eliminadas), REV-037 (`validateDraftStatusTransition` em utils.ts, formato erro 422 padronizado), REV-038 (`patchDraftSchema` unificado de inbox/utils.ts). D09 (adminDiscordSync 690→29 linhas, 5 submódulos em routes/discord/). D10 (adminImportInbox 573→29 linhas, 4 submódulos em routes/inbox/). 5 commits locais no branch `feat/049-revisao-gestao`. Build 17/17, lint 15/15, testes backend 223/223, frontend 163/163. Débitos resolvidos: D01-D05, D08-D18. Débitos abertos: D06 (8 P1), D07. Código **não mergeado em `dev`**.
- 2026-06-21 — **Promote `dev→main` ✅** (run `27894586895`). ~30 commits acumulados promovidos: Node 24 LTS, Vite 8, Tailwind 4, ESLint 10 (spec 033); bump deps (spec 039); PRs #77/#78. Redeploy 5 apps (`27894598616`..`27894601319`) 5/5 ✅. Deploy links `27894598616` — rebuild forçado na VM: código da spec 038 (rehydrate-logos.ts etc.) confirmado no container.
- 2026-06-21 — **Regressão de tema pós-promote.** Site `artificiorpg.com`: toggle dark/light quebrado (só light), nav sem "WhatsApps". Causa: Cloudflare cache servia HTML pré-promote com `Cf-Cache-Status: HIT` + `max-age=7200`. CSP ausente na resposta live vs presente no container confirmou cache antigo. Cache purgado via API (`purge_everything`). **2° bug encontrado:** React `ThemeToggle` (`packages/ui/theme.tsx`) não atualiza `data-variant` no header/footer → nav links/glossario/mesas nunca escurece ao trocar tema. Fix local aplicado (`applyHeaderVariant` integrada em `setTheme`/`applyTheme`). 3 débitos registrados: `BL-UI-THEME-TOGGLE-SITE-REGRESSION`, `BL-UI-THEME-REACT-HEADER-VARIANT`, `D-PROMOTE-033-UPGRADES-REGRESSION`. Sessão: `26-06-21_3_theme-regression.md`.
- 2026-06-21 — **Spec 039 sec-audit-deps executada LOCAL.** `pnpm audit` 16→2. Branch `fix/039-sec-audit-deps` pushada, PR não aberto (aprovação pendente).
- 2026-06-21 — **PR #78 mergeada** (spec 038: mídia Cloudinary + reidratação + reportar + cron). T1-T11 implementados, 20 fixes de review bots, `html-entities` lib. `lint+build+test` ✅. Rebuild manual na VM necessário (deploy `27894335659` usou cache Docker antigo; `27894598616` rebuild forçado resolveu).
- 2026-06-21 — **links.artificiorpg.com NO AR ✅** (smoke VM 200/200/200/401). Resolvido após 3 falhas: (1/2) `.env` corrompido (`\n` literal do PowerShell em `ssh "...$(grep)..."`); (3) senha presa em volume Postgres (`pg_authid` só grava na 1ª init → `28P01`), diagnóstico enganado por `pg_hba` localhost=`trust` → **E009**. Fix: drop volume `links_pgdata_links_prod` (DB vazio) + redeploy (`27891323485`). Roteamento: mantenedor criou public hostname Tunnel (`links-app:4324`) + CNAME proxied. Prevenção E009 em 4 camadas (errors/runbook/capsule/backlog).
- 2026-06-21 — **Deploy prod ✅** (mesas/glossario/site/accounts).
- 2026-06-21 — **Spec 042 mergeada em `dev`** (PR #83, `fa9b787`). 154 arquivos, +7663/-3040. CI verde. 13 revisões de PR resolvidas. Deploy beta mesas/glossario/site disparado. cpd: 5.57% → 4.60% (-411 linhas, -18 clones).
- 2026-06-22 — **Spec 047 (mesas inbox importação) criada.** Auditoria Fase 0 concluída: 17 arquivos lidos, pipeline Discord mapeado (parse→normalize→sync), adaptador mínimo viável (~15 linhas), 3 opções de arquitetura comparadas (recomendada: Opção B+C — rota separada + tabela paralela). `spec.md`, `plan.md`, `tasks.md`, `reviews.md`, `debitos.md` escritos. 7 fases planejadas (MVP: Fase 1). 3 débitos registrados (DEB-047-01/02/03). Backlog README e project-state atualizados. 3 falsos positivos nas auditorias originais (ReportButton, LinksSearch estados erro, CTA target=\_blank) — auditorias atualizadas com seção "Correções pós-investigação". T5: base64 é arquitetural (tsc puro sem bundler), 5 consumidores de brand.ts. T6/T7: 3 consumidores do `<Header>` (links/glossario/mesas). T10 downgrade Major→Minor (M→L esforço). Branch `feat/043-links-visual-audit` criada, todos arquivos staged sem commit. Sessão: `26-06-21_6_links_visual-audit`.
- 2026-06-26 — **Spec 048 C+F+T-F7/T-F8 em PROD-pipeline (beta).** PR #98 (`feat/048-fase-cf-hardening`) mergeada em `dev` (`440aa2a`): Fase C (parser hardening), Fase F (T-F2 guarda upload/T-F3 log/T-F5 perf/T-F6 reparse/T-F10), T-F7 (anexos: cover por extensão + notas), T-F8 (reply note, **migration_130** `discord_import_messages.reference` JSONB online-safe). Inclui fixes de review (REV-007..012, DEB-048-13..22), Zod 4 `flatten()`→`z.flattenError()`, e remediação SonarCloud (regex atômicas `(\d+)(?!\d)` + fragmentos DRY). Deploy beta mesas ✅ (run `28255494906`), migration_130 aplicada (`column reference added`). **Smoke beta real falhou** com `extracao_json.json`: schema rejeitava `reference.guildId: null` + `forwardedMessage` sem author → 400 (DEB-048-23). **Corrigido** em branch `fix/048-chatexporter-nullish-reference` (`.nullish()` + coerção adapter + teste regressão), **validado LOCAL** contra JSON real (tsx): 100 msgs → 99 drafts, sem 400; `json2` truncado → 400 amigável. build/test 262/262/lint ✅.
- 2026-06-26 — **DEB-048-24 implementado (DeepSeek).** Import por arquivo não trava mais o navegador. **Decisão do mantenedor:** arquivos <50KB mantêm fluxo original (`file.text()` → textarea); ≥50KB vão direto ao backend via FormData/multer (`memoryStorage`, sem disco), sem `file.text()`, sem estado React. Backend: `+buildPreviewFromExport()` + 2 rotas (`/preview/file`, `/file`), zero duplicação (`jsonFileUpload` exportado de preview.ts). Frontend: threshold 50KB, `selectedFile` state, chip de arquivo (📄 nome · tamanho · contagem), textarea oculta. Build/lint ✅, backend 263/263, frontend 163/163. **Não commitado.** Pendência: DEB-048-25 (UX unificada).
- 2026-06-26 — **Fase G completa (DeepSeek). Todas as tasks implementadas.** T-G1 (tiers), T-G2 (ambiguidade), T-G3 (correção Discord), T-G4 (infra), T-G5 (export few-shot/eval), T-G6 (migration 131 + métricas), T-G7 (shadow mode, migration 132), T-G8 (trava). Build 2/2, lint 15/15, backend 263/263, frontend 163/163. **Não commitado.**
- 2026-07-02 — **DEB-058-02 resolvido (PR #125 mergeado em `dev`, merge `498d5a5`).** UI "Reparse pendentes" (spec 058) fechada no fluxo Git: fix do nitpick CodeRabbit (`reparseResultSchema` → `z.number().int().nonnegative()` nos 5 contadores, alinhado ao `batchResultSchema`) + regen de `docs/api/generated/api-diff.generated.md` (tabela fantasma de 3 rotas mesas vinha de `dev` local stale vs `origin/dev`; `diff-api.ts:86` usa ref local antes de `origin/dev`; corrigido com `git update-ref refs/heads/dev origin/dev`). Checks verdes, deploy beta disparado (run 28625933373). **Próximo:** clicar "Reparse pendentes" no mesas-beta pós-deploy p/ zerar 50 mensagens `pending` travadas (dado órfão sobrevive ao deploy).
- 2026-07-07 — **Spec 058 Fase 11 (vazamento de descrição + gaps de campo) mergeada e deployada até PROD.** PR #129 (`fix/058-parser-fase11-vazamento-descricao`) fechou T11.1–T11.9 (T11.10 aberta): fix estrutural do fallback de descrição (remove qualquer `label: valor` reconhecido por `splitLabelLine`, não só allowlist restrita — 2ª correção pós-implementação Codex, 38 vazamentos residuais → 0), sanitização de mentions/timestamps Discord crus, fixture real sanitizada (`parserPhase11Samples.ts`), fix de URL de contato com wrapper de markdown-link quebrado, `experience_level`/`table_level`/`scenario_id` deixam de ser `null` hardcoded. 2º commit na mesma PR corrigiu achados de review (ReDoS em regex de contagem/captura de URL e nível → parsing manual sem backtracking). 253+14 testes ✅, `tsc --noEmit` limpo. Merge em `dev` (`94f9e3e`) → deploy beta mesas ✅ → promote `dev→main` ff → **deploy prod mesas FALHOU na 1ª tentativa** (guard `apply_required_migrations.sh`: 9 migrations pendentes > `MAX_AUTO_PENDING=5`; rollback automático teve sucesso, prod ficou saudável no estado anterior). Causa: migrations 133–141 (specs 057/058, todas `online-safe`/`requires-backup:false`, Discord parser learning) nunca tinham sido deployadas em prod — acúmulo de várias specs seguidas sem promote a prod. **Resolvido:** as 9 migrations aplicadas manualmente via SSH usando o script oficial (`apply_required_migrations.sh` com `MAX_AUTO_PENDING=9` só para essa rodada, mesmo pipeline de validação/lock que o CI usa, não SQL solto) — todas idempotentes/sem risco (`CREATE TABLE IF NOT EXISTS`/`ADD COLUMN`/`CREATE INDEX`, sem DROP/DELETE). 2ª tentativa de deploy prod ✅ (containers healthy, smoke 200/401/302). **Aprendizado registrado em `errors.md` (novo) + `specs/backlog.md`:** guard de 5 migrations por deploy é saudável, mas junta com o hábito real do fluxo (`dev→main` só promovido esporadicamente) sempre vai acumular >5 quando specs seguidas mexem em `apps/mesas/database/` sem promote a prod no meio — considerar promover `main` com mais frequência OU ajustar processo para aplicar migrations em prod incrementalmente a cada PR, não só no fast-forward.
- 2026-07-07 — **E013 corrigido: contato Discord de fallback usava nome de exibição, não contactável.** Achado do mantenedor testando draft real pós-deploy da spec 058 Fase 11: campo "Contato Discord" mostrava nome do servidor (ex. "João Pedro"), que não é pesquisável/contactável fora dele. Fix em `parseDiscordAnnouncement.ts` (fallback usa só `discord_author_id`, nunca nome), `syncHelpers.ts` (label "Perfil Discord" quando valor é snowflake/mention crua) e `TableContacts.tsx` (link `discord.com/users/{id}`, formato oficial confirmado via discord-api-docs#5183, em vez de tratar ID como invite code quebrado). Teste ponta a ponta real (mensagem simulada, autor 18 dígitos) validou pipeline completo. Também corrigido bug de ordem de cascata em `extractSlots` (`RE_SLOT_OPEN` interceptava "1 vaga / grupo de 5 pessoas" antes do padrão novo `slotsGroupSize` rodar). **Auditoria de completude por IA investigada** (usuário reportou que não achava lacunas mesmo com contato faltando): tinha 2 furos por design — `contact_discord`/`host_discord_id` nunca vão pro DeepSeek (privacidade) então IA nunca podia reportar contato faltando; prompt só detectava campo VAZIO, nunca campo preenchido com valor errado. Corrigido: prompt v2 (`issue_type: missing|incorrect`) + checagem local de contato sem LLM. **Feature nova:** botão pequeno "IA" por campo (ao lado do badge "Parser" em cada input do editor), reaudita só aquele campo sob demanda (`POST /:id/audit-field/:field`). 567 testes (415 backend + 152 frontend) ✅, build limpo, `verify:api` ✅ (0 breaking, 1 non-breaking = rota nova). Detalhe completo em `errors.md` E013. **PR #131 mergeada em `dev` (`b84927f`)** após 2 rodadas de fix de review (Codex: `contact_url` faltando na checagem local de contato; CodeRabbit Major: race condition em `auditingField` corrigida com `Set<DraftFieldKey>`; Sonar: nested ternary extraído). **Fix de infra no meio do caminho:** hook `pre-commit` novo (`scripts/api/pre-commit-verify.mjs`) — CI falhava repetido com "artefatos de API desatualizados" porque só existia `pre-push` (commit já carregava linha stale antes do hook rodar); agora `verify:api` roda e auto-stage o resultado ANTES de cada commit relevante. Deploy beta mesas ✅ (healthy, smoke 200/401/302) → promote `dev→main` ff ✅ → deploy prod mesas ✅ na 1ª tentativa (sem migration pendente desta vez, containers healthy, smoke 200/401/302).
- 2026-07-16/17 — **Spec 079 (mesas, parser texto colado + pré-preenchimento público) implementada, revisada e quase fechada.** Engine única de parser/aprendizado compartilhada entre import JSON e texto colado — sem duplicação (`raw_gm_name`, `normalizeLooseText`, `buildTableDraftFields` reaproveitados nos dois fluxos, incluindo o novo `parseTextForPreview` do fluxo público). Fases 1 (normalizador de labels grudados), 2 (WhatsApp), 3 (nome do mestre extraído do texto), 5 (pré-preenchimento público em `create-table` com 2 cards + `POST /gm/parse-preview` + loop de aprendizado via `parse_case_id`) implementadas. PR #172 aberto contra `dev`. **2 rodadas de correção de review:** Codex (3 achados: header único não removido, labels compostos "Data e Hora" ausentes de `BARE_LABEL_STOP_KEYS`, `parseCaseId` sem setter no restore de draft) + CodeRabbit (6 achados inline: regex catastrophic backtracking em data/WhatsApp, `parseTextForPreview` explodindo sem título, `.slice(1)` cego, falta de checagem `gm_profiles` na rota preview, correlação de `parse_case_id` sem proteção contra sobrescrita, cast cego de resposta de API no frontend — todos corrigidos com causa raiz completa, não patch pontual). **Auditoria "tudo implementado?" (2026-07-17) achou gaps reais e fechou:** testando a fixture completa real (`D:\texto_colado.txt`, 1030 linhas/33 anúncios, não os 22 do recorte anterior) apareceram 3 bugs novos — cargo Discord hardcoded `"Narradores"` (real: qualquer texto, servidor tinha também "Aventureiros"), timestamp só `HH:MM` nu (real: Discord usa "Ontem às HH:MM"), placeholder `"Imagem"` do export vazando em 30+ segmentos (nunca tratado antes) — todos corrigidos generalizando em vez de hardcoding pontual, por princípio explícito do mantenedor ("o produto lida com texto de comunidades infinitas, lista fechada é bug latente"). T5.9 (sugestão de nome do mestre no fluxo público, banner não-intrusivo comparando extraído vs conta logada) e T5.11 (teste do loop de aprendizado, `gmPanel.parsePreview.test.ts`, 8 casos novos) implementados — não existiam antes da auditoria. Achado extra no build repo-wide: import estático anulava code-split (`INEFFECTIVE_DYNAMIC_IMPORT`), corrigido pra import dinâmico. **Validação completa:** 498 testes backend + 185 frontend ✅, `tsc --noEmit` limpo (backend+frontend), `pnpm run build` 21/21 via `turbo run lint`/`turbo run build` direto (não `pnpm run lint`/`pnpm run build` na raiz — esses dois scripts têm bug de infra pré-existente não relacionado a esta spec, ver `BL-079-PARSER-TEXTO` no backlog), `verify:api` 0 breaking. **Regra nova registrada em `AGENTS.md`:** "solução mínima" proibido como critério de correção de bug/achado de review — causa raiz completa é o padrão, escopo mínimo vale só pra abrangência (não abrir código não relacionado), nunca pra profundidade da correção.

- 2026-07-17 — **Spec 079 ✅ FECHADA.** Fase 6 (achados pós-deploy: `rules_notes` propagado, VTT "Roll 20"/"Roll20" corrigido, mecanismo de aprendizado generalizado `recordEntityHintRule`) commitada e mergeada via PR #174. T5.12/T4.4 (smoke manual real) confirmados pelo mantenedor em produção com casos reais suficientes. `BL-079-PARSER-TEXTO` fechado.

- 2026-07-17 — **Spec 080 (Search Console + Open Graph + indexação site/mesas/glossario) ✅ FECHADA.** Investigação inicial (Fase 0) revelou escopo real menor que o assumido: mesas e site já tinham OG/sitemap funcionando, trabalho de código ficou concentrado em glossário (sitemap.ts + og.ts do zero, rota SSR por verbete com bot-detection via nginx) + fallback `og:image` no `Base.astro` do site (`/blog/` índice não tinha imagem própria) + correções de review de PR #174 (ESM/NodeNext completo em mesas, guard CLI `import.meta.url`, nginx proxy exato). GSC: propriedades obsoletas (`glossariorpg.`, `accounts.`) substituídas pelas corretas (`mesas.`, `glossario.`); domain-verified property de `artificiorpg.com` já cobre subdomínios. Smokes reais (Discord/WhatsApp/Facebook Debugger) confirmados pelo mantenedor nos 3 apps. `fb:app_id` decidido como não-implementar (produto não usa Facebook Login/Insights). `BL-SITE-PRINCIPAL-GAPS` item D fechado como não-bug (nomenclatura Astro); `BL-SITE-CUTOVER-029` T10 fechado.

  **3 PRs adicionais desta sessão, fora do escopo original mas achados durante deploy/investigação da 080:**
  - **PR #174** (spec 080 completa) — merge em `dev`, 4 achados SonarCloud (imports duplicados `discord/index.js`) corrigidos antes do merge.
  - **PR #175** (`fix(docker): instala deps de pacotes workspace usados em runtime`) — deploy beta de #174 falhou com `ERR_MODULE_NOT_FOUND` (jsonwebtoken/zod) porque `pnpm install --prod --filter <app>` no Dockerfile de produção não instala deps de OUTROS workspace packages cujo `dist` é copiado (`@artificio/config`/`content`/`auth`/`changelog`/`feedback`/`catalog-client`). Padrão já parcialmente corrigido antes (`@artificio/media`/`catalog-client`, spec 062/PR #145) generalizado pra todos os pacotes consumidos. Registrado como **E016** em `errors.md`.
  - **PR #176** (`fix(auth): remove reexport client de packages/auth barrel`) — MESMO sintoma de superfície (`ERR_MODULE_NOT_FOUND`) reapareceu após #175, causa raiz DIFERENTE: barrel `packages/auth/src/index.ts` reexportava `client.js` (React/`useSession`) junto com server-only (`jwt`/`middleware`/`csrf`) — ESM carrega o módulo inteiro em qualquer import nomeado, backend puxava `react` (peer dep nunca instalada em prod). Fix: barrel principal 100% server-only, client via subpath `@artificio/auth/client` (já existia, consumidores já usavam certo). Registrado como **E017**.
  - **PR #177** (`fix(seo): substitui og-default.png de glossario e site`) — achado via GSC/prévia de link: `og-default.png` de glossário e site eram byte-a-byte idênticos ao do mesas (asset copiado errado na spec 080). Imagens novas geradas no mesmo padrão visual (navy `#1b2a4a` + laranja `#ff5722`, ícone+nome centralizado, bbox calculado via `sharp.trim()`) — glossário usa ícone de livro/diário, site usa hexágono facetado da marca.
  - **PR #178** (`fix(site): migration 008 nao filtra source_environment`) — deploy beta do site (dentro do #177) travou `site-beta-app` em crash loop: `008_catalog_hierarchy_guard.sql` filtrava `source_environment='prod'` hard-coded numa busca de `legacy_id`, nunca rodava em beta (mesmo dado existe lá com `source_environment='beta'`). Aplicado manualmente com backup real verificado off-VM + `ALLOW_MANUAL_MIGRATIONS=true` (dry-run em transação antes do apply real); migrations 010-014 (spec-078) registradas em `schema_migrations` sem executar porque auditoria confirmou que o resultado delas já existe em beta por outro caminho (IDs diferentes dos hard-coded — rodar o SQL original criaria conflito). Registrado como **E018** (comando `confirm` errado do `promote-prod-fast-forward.yml`, precisa string exata `PROMOTE_DEV_TO_MAIN`) e causa raiz da migration documentada inline no commit.

  Todos os 5 PRs mergeados em `dev`, promovidos a `main`, deploy beta+prod verdes nos módulos afetados (mesas/glossario/site). `errors.md` ganhou E016/E017/E018 com sintoma+causa+solução+prevenção detalhados, especificamente para reduzir tempo de diagnóstico em recorrências futuras desse padrão (`ERR_MODULE_NOT_FOUND` em runtime Docker tem 2 causas distintas possíveis — checar as duas).

**Mesas — PR #184: FK obsoleta pós-spec 078 + editar mesa criando mesa nova (2026-07-18/19):** dois bugs reais fechados. **Bug 1:** `POST /system-suggestions/:id/resolve` (todas ações de aprovação em `/gestao/comunidade`) quebrava com 500 genérico em Prod — causa raiz confirmada via log real (`docker logs mesas-api`): migration_06/123 (spec 018, pré-078) deixaram 4 FKs em `system_suggestions` (`resolved_system_id`, `created_system_id`, `created_alias_id`, `parent_id`) apontando pra `systems`/`system_aliases` **locais**; spec 078 mudou Mesas Prod pra consumir sistemas de RPG do Central (site-prod) via `SystemCatalogProvider`, então todo UUID/slug gravado nessas colunas em Prod nunca existe na tabela local. Migration 155 remove as 4 FKs obsoletas (integridade passa a ser responsabilidade do provider) e corrige `created_alias_id` (UUID→TEXT, recebe slug do catálogo central, não UUID de linha). Achado adicional do review Codex (`parent_id`) confirmado e incluído antes do merge. **Bug 2:** reporte real do GM Douglas dos Santos — editar mesa (banner/horário/vagas) sempre criava mesa nova em vez de atualizar; causa raiz: `mapTableApiToInitialData` nunca incluía `id` no objeto retornado, `isEditing` calculava sempre `false` no `useCreateTableForm`, todo submit em modo edição fazia POST. Fix adiciona `id` normalizado (trim + rejeita vazio/whitespace, achado de review). Migration auditada contra `pg_constraint`/`information_schema` real em beta E prod (nomes de FK, tipos de coluna, contagem de dados afetados) antes do merge — zero divergência entre migration e estado real do banco. PR #184 merged → deploy beta (run 29670168905) → promote `dev==main` → deploy prod (run 29670372087), todos verdes; migration 155 e healthz 200 confirmados reais em prod pós-deploy. Sessão: `sessoes/26-07-18_1_mesas_bugs-catalogo-e-edicao-mesa.md`.
