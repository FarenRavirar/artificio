# Decisões — Artifício RPG (append-only)

> ADR-lite. Uma decisão = uma linha. **Append-only:** nunca reescrever histórico; para reverter, adicionar nova linha que supera (`supera D0NN`). Ler este arquivo evita re-decidir = evita retrabalho. Manter MINÚSCULO: sem prosa.
>
> Formato: `D0NN | AAAA-MM-DD | <decisão> | <motivo curto> | status`. Status: firme | revisar | superada.

| ID | Data | Decisão | Motivo | Status |
|---|---|---|---|---|
| D001 | 2026-06-03 | Monorepo único `artificio`, pnpm + Turborepo | leve, modular, build só do que mudou | firme |
| D002 | 2026-06-03 | Path único `artificiorpg.com/<módulo>` + gateway nginx | unidade G1, 1 host p/ SEO/analytics, cookie único | **superada (D017)** |
| D003 | 2026-06-03 | SSO: 1 OAuth Google, JWT em cookie `Domain=.artificiorpg.com` | logado no site = logado em tudo | firme |
| D004 | 2026-06-03 | WordPress roda em paralelo; cutover DNS só no Gate C | zero downtime | firme (Gate C adiado, D016) |
| D005 | 2026-06-03 | CMS = store nativo Postgres + importador WP one-shot descartável | leve, sem dep pesada; migração some pós-cutover | firme |
| D006 | 2026-06-03 | Blog = pré-render estático (SSG) c/ rebuild incremental, não SSR | mais leve, SEO-safe, menos runtime quebrando | firme |
| D007 | 2026-06-03 | Stack canônica = stack do mesas (React19/Vite/Tailwind + Express/Kysely/PG16/Cloudinary) | consistência, reuso, 1 stack só | firme |
| D008 | 2026-06-03 | Infra: Oracle 24GB/200GB, Docker, Cloudflare Tunnel, GHCR, Watchtower(beta) | já é o padrão do ecossistema | firme |
| D009 | 2026-06-03 | Backup off-VM em `C:\projetos\artificiobackup` (local, 300GB livre) | fora da VM antes de destruir | firme |
| D010 | 2026-06-03 | Acesso DB da VM por linha de comando local/PowerShell via `ssh faren` = read-only por padrão; escrita exige aprovação | segurança operacional | firme |
| D011 | 2026-06-03 | Site sobe como `beta.artificiorpg.com`; demais serviços integram em versão principal | site é o maior risco (300+ posts) | refinada por D015 |
| D012 | 2026-06-03 | Governança antes de código; avanço por gates A→B→C→D | controlar risco por processo | firme |
| D013 | 2026-06-03 | Caveman default na saída de todos os agentes; docs de reload mantidos minúsculos | economia de tokens em projeto de ~3 meses | firme |
| D014 | 2026-06-03 | Reload obrigatório em tiers (T0 minúsculo todo chat; T1/T2 sob demanda) | cortar custo de contexto por sessão | firme |
| D015 | 2026-06-03 | **Interim híbrido.** Novos (site, downloads, wiki-sop, srd) sob `beta.artificiorpg.com/<base>` (path único no host beta). Glossário e mesas ficam nos subdomínios prod atuais (`glossariorpg.`, `mesas.`), unidos por SSO + nav + ui; código (branch main/prod) entra no monorepo, deploy mantém o subdomínio. Path único na raiz = alvo só no cutover futuro | WP segura `artificiorpg.com`; site é só beta; mínima disrupção do prod existente | **superada (D017)** |
| D016 | 2026-06-03 | **Cutover adiado.** Apontar raiz `artificiorpg.com` ao novo site + desligar WP = fora do escopo destes ~3 meses. Gates ativos = **A, B, D**. Gate C = futuro documentado, não executado | "por enquanto não vai ter prod, só beta do site" | firme (refinada D017: só o blog vai à raiz; módulos não convergem) |
| D017 | 2026-06-03 | **Topologia = subdomínio-por-módulo** (supera D002/D015). Cada módulo no próprio subdomínio (`glossario.` por D057, `mesas.`, `downloads.`, `spheres.`, `srd.`, `links.`), root `/` próprio, **sem basename**. Blog novo em `beta.artificiorpg.com` (→ raiz `artificiorpg.com` no futuro, D016). WP fica na raiz agora. Unido por SSO (cookie `.artificiorpg.com`) + nav + design system. Cloudflare Tunnel mapeia hostname→container | dissolve a contradição WP-na-raiz, modelo Google-suite (docs./mail.), menos código por app | firme (`spheres.`→`esferas.` por D028; `glossariorpg.` era alias histórico pré-monorepo) |
| D018 | 2026-06-03 | **SSO central em `accounts.artificiorpg.com`** (OAuth callback, sessão, refresh). 1 OAuth client Google; cada módulo redireciona pra cá no login e valida o JWT do cookie raiz | host dedicado isola o auth, padrão de suite | firme |
| D019 | 2026-06-03 | Blog (aposta de SEO) fica na **raiz** `artificiorpg.com`; módulos-ferramenta em subdomínio | mitiga não-consolidação de SEO do modelo subdomínio (autoridade concentra no blog/raiz) | firme |
| D020 | 2026-06-03 | Search Console = 1 **Domain property** (`artificiorpg.com`, cobre todos subdomínios); GA4 = 1 property com `cookie_domain` raiz + exclusão de referral interno | "analytics pega tudo" no modelo subdomínio | firme |
| D021 | 2026-06-03 | **Escopo do backup = só G1** (mesas beta/prod, glossário beta/prod, WP, links/servidorvirtual, segredos, DNS). `gerenciador_telegram` e `foundry` **não** entram | mantenedor refaz ambos do zero; telegram está no GitHub | firme |
| D022 | 2026-06-03 | Tunnel Cloudflare **novo** na Fase 1 (o velho mora no telegram, descartado). Backup só do DNS export p/ saber hostnames a re-apontar; token velho descartável | tunnel atual acoplado ao telegram | firme |
| D023 | 2026-06-03 | Acesso à VM por alias **`ssh faren`** (host/chave em `~/.ssh/config` local, não versionado). Read-only sem aprovação; write na VM = aprovação pétrea | mantenedor liberou acesso direto | firme |
| D024 | 2026-06-03 | **WP fora do backup.** Hostinger tem backup em nuvem do `artificiorpg.com`. O backup que importa = **VM** (G1) | mantenedor confirmou; reduz escopo | firme |
| D025 | 2026-06-03 | **Mídia do WP = on-demand na migração** (Fase 3), não backup agora. Importer puxa só o original via REST/FTP; Cloudinary regenera | 6.34GB são variantes/lixo; não vale backupear | firme |
| D026 | 2026-06-03 | Segredos do G1 (`.env` de mesas/glossário + WP creds + Cloudinary/GHCR/CF) → `secrets.7z` AES-256 **off-VM**; registro em `docs/agents/access-registry.md` (sem valores). Rotacionar o que vazou em chat pós-migração | segredo durável e auditável; VM atual será destruída | firme |
| D027 | 2026-06-03 | `links` e `servidorvirtual` **não estão na VM** (não são containers). Localizar host depois (provável Hostinger/CF Pages); baixa prioridade | inventário T1 não os achou | revisar |
| D029 | 2026-06-04 | Bundle de segredos = **openssl AES-256-CBC + pbkdf2** (`secrets.tar.gz.enc`), não 7z | `7z` indisponível na VM e no Windows; openssl+tar são nativos | superada (D030) |
| D030 | 2026-06-04 | Bundle de segredos = `secrets.tar.gz` plaintext (planejado) → **Codex entregou `secrets.7z` encriptado** (p7zip via container Alpine); senha em `artificiobackup\secrets-7z-password.txt` (off-git) | sem fricção; bundle acabou encriptado (melhor); rotacionar pós-migração | firme (rotacionar!) |
| D032 | 2026-06-04 | Fase 1 = **recriar instância** (escolha do mantenedor). Pré-flight: site `artificiorpg.com` independe da VM (Hostinger) ✅. Gap: `/opt/glossario*` **não é git** → CDX-006 backupeia os dirs de deploy antes de destruir. DNS vive no Cloudflare (re-aponte na Fase 1, não é gap). **PAT GitHub vazado** no remote de `/opt/mesas-beta` → rotacionar | conferência pré-recriação | firme |
| D033 | 2026-06-04 | ~~Resize sem recriar~~ | escolhido e depois revertido | **superada (D034)** |
| D034 | 2026-06-04 | **Fase 1 = recriar VM do zero, limpa e centrada no Artifício, SEM dependência do telegram.** Terminar a VM atual, criar nova 200GB, restaurar **só o reconstruível** (código, `.env`, DBs do backup). Telegram/foundry não voltam (rebuild futuro do GitHub) | telegram foi o 1º projeto; agora tudo é estruturado pro projeto Artifício; nomes/estrutura refeitos | firme |
| D035 | 2026-06-04 | Rede Docker `gerenciador_telegram_default` → renomear **`artificio_net`**; **tunnel Cloudflare novo, standalone** (próprio do Artifício, fora de qualquer stack de app). Composes de glossário/mesas editados p/ a rede nova no redeploy | remover acoplamento ao telegram (refina D022) | firme |
| D031 | 2026-06-04 | **Gate A APROVADO.** Backup G1 validado (restore-test 14 tabelas, checksums OK, 66MB off-VM). Libera Fase 1 (instância 200GB) | mantenedor aprovou explicitamente | firme |
| D036 | 2026-06-04 | Fase 1 concluída: VM nova limpa (`artificio_net`, tunnel próprio), glossário+mesas restaurados e no ar, sem telegram | smoke 200 | firme |
| D028 | 2026-06-03 | Módulo wiki Spheres of Power = **`esferas`**, subdomínio **`esferas.artificiorpg.com`** (PT). **Multi-sistema**: eixo `sistema × edição`. D&D = principal (edições **2014** e **2024**); Pathfinder + edições = futuro. Conteúdo (spheres/talents) marcado por sistema+edição; interreferência só dentro do mesmo sistema+edição. UI com seletor de sistema/edição | nome PT + SoP tem variantes por sistema/edição | firme |
| D037 | 2026-06-04 | **Gate B APROVADO.** SSO `accounts.artificiorpg.com` no ar (OAuth Google/JWT), cookie `Domain=.artificiorpg.com` validado **cross-subdomínio** (probe noutro host leu sessão via `verifyToken`, mesmo `JWT_SECRET`). Topologia subdomínio provada (glossário/mesas no ar). Integrar módulo real ao SSO = Gate D. Libera import de conteúdo + construir módulos. Dívidas anotadas: open-redirect `return` (allowlist `.artificiorpg.com`); smoke E2E browser no 1º módulo. CI/CD `deploy-accounts.yml` (validate→deploy gated) durabiliza o deploy | infra SSO + topologia provadas no metal | firme |
| D038 | 2026-06-04 | **Design system = paleta real da marca**, extraída de `midias/` (não Google genérico). Assinatura = **laranja/coral `#EF8B4E`** (hexágono+RPG); navy `#11183A` (wordmark fundo claro); charcoal `#0F1014` (base escura); bronze `#A9805A` (secundário); branco (wordmark fundo escuro). Tipografia: **display bold condensada** (headings/wordmark) + sans limpa (corpo). Usar assets de `midias/` no layout (logo negativo p/ fundo escuro, logo azul p/ fundo claro). Corrige `packages/ui` que chutou azul-Google (`#2563eb`/`#1a73e8`) | tokens atuais erram a identidade; marca é laranja-sobre-escuro vibe RPG | firme |
| D040 | 2026-06-05 | **Paleta real corrigida (amostra pixel) + nome do produto.** Laranja `#FF9457` (de `midias/cropped-Logo-PNG-Negativo-2.png`, padrão máximo/acento) + navy `#020740` (de `midias/cropped-logo-header-site-azul.png`, contraste/texto/ink); superfície dark do app = `#1B2A4A` (não é cor de marca). Logo branco (`brandLogoNeg`) p/ fundo escuro. Nome = **"Artifício RPG"**; **"G1" = referência conceitual** (hub interconectado estilo portal de notícias), nunca nome do produto (codinome técnico interno pode usar). Supera os hexes de D038 (`#EF8B4E`/`#11183A` eram aproximação) | mantenedor fixou os arquivos-padrão de cor e corrigiu o nome | firme |
| D043 | 2026-06-05 | **Contrato do menu de conta no `@artificio/ui` Header (CDX-312, corrige regressão CDX-311).** Avatar logado abre **dropdown** (não link puro p/ accounts). Header genérico aceita prop `userMenu: UserMenuItem[]` ({label,href,external?,variant?}); sempre inclui **Sair** (via `logout()` novo em `@artificio/auth/client` → `POST accounts /api/auth/logout` credentials include → home do módulo). Módulo sem `userMenu` mostra Conta+Sair. mesas passa: Meu Perfil(/perfil), Painel(/painel), Gestão(/gestao, só `role==='admin'`), Conta(accounts, external). Role SSO só é `user|admin` (status `gm` é interno do mesas, fora do menu). Acessível (Esc/clique-fora/aria). Padrão reusado por glossário/site | header genérico perdeu o menu rico do legado; restaurar paridade sem quebrar SSO; contrato único p/ todos módulos | firme |
| D042 | 2026-06-05 | **Realm SSO do beta = reusar accounts PROD (Opção 1, refina D041).** Módulos beta (mesasbeta etc.) NÃO têm accounts próprio; login redireciona pro `accounts.artificiorpg.com` prod; cookie assinado com JWT_SECRET prod (`Domain=.artificiorpg.com`) vale no beta. Anchor `apps/accounts/.env.beta` (no clone `/opt/artificio-beta`) contém **só `JWT_SECRET` = mesmo valor do prod** (não é OAuth); zero credencial Google em qualquer env beta (módulo não tem OAuth). Sem fallback de leitura do `.env` prod (segurança). Consequência: sessão beta = sessão prod (logar em prod loga no beta); não dá p/ testar mudança de auth isolada no beta → upgrade futuro = **Opção 2** (accountsbeta + JWT novo + cookie distinto `artificio_session_beta` em `packages/auth`), quando precisar. Allowlist `return` `.artificiorpg.com` já cobre `mesasbeta.`; Google console sem mudança. **Atenção:** beta valida cookie (id/email) contra DB beta → hydrate prod→beta deve popular `users` senão rotas de usuário 401/404. Compensação à falta de branch protection (GitHub 403 repo privado sem Pro): gate `main ⊆ dev` fail-closed em todo deploy + workflow alarme `on push:main` que falha se `main` não-ancestral de `dev` | mínimo toque; cobre manutenção app-level do mesas (99%); isolar auth no beta é raro e vira Opção 2 quando necessário | firme |
| D041 | 2026-06-05 | **Modelo beta/staging G1 = branch `dev` (integração/beta) + `main` (prod), genérico por módulo.** Trabalho normal entra via `feat→dev` (não mais `feat→main`). **Invariante divergência-proof: `main` sempre ancestral de `dev` (`main ⊆ dev`)**; push direto em `main` proibido (branch protection); `main` só avança por fast-forward/rebase de `dev` (nunca squash/merge commit). Beta: clone `/opt/artificio-beta` trackeia `dev`; push em `dev` (path-filtered por módulo) → auto-deploy do beta (`<modulo>beta.artificiorpg.com`). DB beta próprio + endpoint admin hydrate on-demand de prod (legado mesas, gate NODE_ENV). Prod: promoção canônica = workflow `promote-prod-fast-forward.yml` (dispatch + confirmação) que move `main` para `dev` após validar invariante; PR standing `dev→main` serve para revisão/visibilidade e só pode ser usado com "Rebase and merge". Gate recusa deploy se `main` não-ancestral de `dev`. Padrão genérico no `_deploy-module.yml` (param `env=beta|prod`: dir/compose/container/domínio/smoke). Aplica mesas, glossário, site. Cicatrizes legado: nunca `down` por prefixo global (E144); flock host; snapshot pré-deploy; rollback banco+containers; health `healthy`; smoke rotas críticas. `deleted_client` do mesasbeta morre no rebuild (usa accounts SSO, sem OAuth próprio) | mantenedor quer esteira inteligente/mínimo toque; beta à frente de prod p/ conferir antes; vários módulos precisam de beta (mesas/glossário/site) | firme |
| D039 | 2026-06-04 | **Deploy canônico G1 = git-pull do monorepo na VM** (escolha do mantenedor), não tarball/scp. `/opt/artificio` = clone git; deploy = `git fetch + reset --hard origin/main` (sem `git clean` — `.env` por módulo gitignored sobrevive) + `turbo build --no-cache --pull` do módulo. Portar padrões do legado mesas: build/test antes do SSH, snapshot DB antes de migrar, migrations frameworkadas (schema_migrations/classe/lock/transacional), health por `docker inspect ...healthy`, smoke de rotas críticas, rollback restaura snapshot, lock de concorrência, break-glass separado. Workflow reusável `_deploy-module.yml` (workflow_call) param por módulo. `accounts` retrofita ao modelo (CDX-310). Maximizar autonomia: Codex publica GitHub + cria Environment + seta secrets + converte VM; mantenedor só aprova writes na VM | gold-standard legado dá rollback/ref rastreável; minimizar ação do mantenedor | firme |
| D044 | 2026-06-05 | **Site sobe SÓ em `beta.artificiorpg.com` por ora; sem staging dedicado (`sitebeta.`) nesta fase.** O par esteira `dev→<modulo>beta`/`main→prod` (D041) NÃO se aplica ao site agora: `beta.` é o destino vivo único. Domínio raiz `artificiorpg.com` + WP ficam intocáveis (cutover = Gate C, adiado, D016). Prioridade desta fase do site = **levantamento + decisão + documentação estratégica** ("para não se perder"), não pressa de código. Staging dedicado do site = decisão futura quando a operação for migrar | mantenedor: "vai demorar muito p/ migrar a operação; muitas coisas a fazer; agora é levantamento/decisão/doc" | firme |
| D045 | 2026-06-05 | **Importador WP→site = WP REST API** (não dump SQL nem FTP). Recon provou REST entrega: conteúdo **Gutenberg limpo** (zero Elementor/shortcode/inline-style nos posts), **Yoast meta** completo (`yoast_head_json`: title/desc/canonical/og_image), taxonomias e mídia. Zero credencial DB; WP intocável (D004). One-shot **idempotente por slug**; mídia on-demand (importer puxa original via URL REST, Cloudinary regenera, D025) | REST entrega tudo limpo; dump SQL exigiria credencial e tocaria infra WP sem ganho | firme |
| D046 | 2026-06-05 | **Escopo do importador `site` = `post`(125) + taxonomias(13 categorias aninhadas + 69 tags) + mídia + `page`s filtradas (institucionais) + comentários(25).** Pages institucionais do site (~12: home, sobre-nos, contato, política-de-privacidade, termos-de-serviço, media-kit, newsletter, blog-index, grupos-whatsapp, material-online, curso-traducao, coyote-e-crow). **Fora:** pages de outros módulos (mesas: encontrar-mesas/cadastro-mestre/cadastrar-mesa/sobre-anuncios; WooCommerce: loja/carrinho/finalizar-compra/minha-conta/confirmacao/falha; glossário/esferas/srd/docs: magias/glossario/dd/dnd-srd-52/sage-advice/documentation-login) e **CPTs** `definicao`(163)/`magia`(328)/`docs`(17)/`product`(3) | site = só blog+institucional; CPTs pertencem a glossário/esferas/downloads; arquitetura do importador fica reusável p/ eles depois | firme |
| D050 | 2026-06-06 | **Hardening da esteira `_deploy-module` (spec 009)** — blindar as 3 classes de falha do 1º deploy do site, não tapar buraco. **R1 reconcile de leftover:** antes do 1º `up`, remove container de nome esperado (`db_service`+`health_containers`) que pertença a OUTRO projeto compose (ex.: subida manual); não toca volume nem containers de outro nome (mesas/accounts intactos). **R2 guard exec-bit:** `pr-checks` falha se `ENTRYPOINT/CMD ["./*.sh"]` em Dockerfile referenciar `.sh` não-`100755` no git (`scripts/ci/check_entrypoint_exec_bit.sh`). **R3:** erro de `.env.<env>` ausente vira acionável + `docs/agents/deploy-runbook.md` (bootstrap dispatch-first: push→pull VM→criar `.env`→deploy; não subir container manual p/ "validar"). **R4:** smoke/health/snapshot/jwt no `GITHUB_STEP_SUMMARY`. **Backward-compat** (aditivo; R1 no-op em VM limpa). **Validar com `mesasbeta` verde antes de confiar p/ prod.** **Adiado (T9):** largar `container_name` fixo → nomes por projeto compose (elimina colisão na raiz, mas toca health-check de mesas/accounts) | 3 incidentes reais (name conflict, permission denied, bootstrap) eram classes preveníveis; esteira compartilhada deve se auto-curar/barrar em vez de exigir intervenção manual | firme |
| D049 | 2026-06-05 | **Modelo de deploy do `site` = 2 containers (`site-beta-app` + `site-beta-db`) na `artificio_net`; build roda IN-CONTAINER no entrypoint** (refina D039/D041 p/ o site). `site-beta-app` = Node/Express (`server/`) que serve o **Astro `dist/` estático** + `/healthz` + `/admin/*` (rebuild/import, role=admin). `site-beta-db` = `postgres:16-alpine` (o store, vol `pgdata_site_beta`). Entrypoint (a cada deploy): `migrate → import(se `SITE_IMPORT_ON_START`) → export → astro build → pagefind → exec server`. É o "build na VM" do D039, dentro do container (o build do site **depende do DB+WP**, então não dá p/ buildar na imagem). Domínio **`beta.artificiorpg.com`** (D044; site é beta-only, sem path prod até cutover Gate C) → `site-beta-app` via Cloudflare Tunnel. DB beta próprio populado do WP REST (sem hydrate-de-prod; site não tem prod). Rebuild incremental (D006) = `POST /admin/rebuild` re-roda export+astro build no dir servido. `deploy-site.yml` espelha `deploy-mesas` via `_deploy-module.yml` (env=beta, push `dev`). **Ação do mantenedor:** criar GitHub Environment + secrets (`POSTGRES_PASSWORD`, `DATABASE_URL`, `JWT_SECRET`=prod p/ SSO D042, opc. `CLOUDINARY_URL`/`PUBLIC_GA_ID`), rota Cloudflare `beta.→site-beta-app`, validar passo de migração do `_deploy-module` (site migra no entrypoint → passo reusável neutro p/ site) | build do site é DB-dependente (atípico vs mesas que builda na imagem); manter canon git-pull/snapshot/health/smoke mas mover build p/ runtime do container | firme |
| D048 | 2026-06-05 | **Stack do blog `site` = Astro (SSG islands) p/ o público + React/Vite SPA (canon) p/ admin + Express/Kysely/PG (canon) no backend.** Astro escolhido: zero-JS por padrão (melhor leitura/CWV/SEO = brief), island architecture (React só onde interage), usa **Vite+Tailwind** (reusa preset) e renderiza **React** (`@astrojs/react`) → `@artificio/ui` Header/Footer entram como **island** hidratado. Conteúdo vem do **Postgres via Content Layer loader** no build (D005); rebuild incremental por webhook do admin (D006). **Busca = Pagefind** (índice estático, zero backend). **Visual = híbrido A+B** (artigo editorial coluna-única distração-zero ~68ch + home/arquivos em cards limpos p/ consulta), restilizado à marca D040. **Funcionalidade = core completo** (TOC auto, breadcrumbs, relacionados, arquivos categoria-árvore/tag, Pagefind, dark/light, SEO meta/JSON-LD/sitemap/robots/RSS, 404-com-busca, A11y AA). **Anti-distração:** zero ads/popup/autoplay/sidebar (remove o que o WP tinha). Nuancia D007: layout do *blog* ganha `.astro`; backend e admin permanecem stack canon. Fallback se rejeitar Astro = Vite+React SSG (Vike) | Astro é o padrão-ouro p/ blog leitura-first/SEO e fica no ecossistema Vite/Tailwind/React; mantenedor aprovou as 3 escolhas | firme |
| D047 | 2026-06-05 | **Rotas do blog preservam o permalink WP `/blog/<slug>/`** (canonical Yoast confirma `https://artificiorpg.com/blog/<slug>/`); pages institucionais na raiz `/<slug>/`. Categorias/tags = arquivos aninhados sob `/blog/`. **Mapa 301** (slug WP → nova rota) é gerado/preparado no import mas **só ativa no cutover** (Gate C); enquanto site vive em `beta.`, não há colisão com o WP da raiz. `sitemap.xml`/`robots.txt`/JSON-LD gerados pelo SSG via `packages/content` | SEO inegociável: preservar slugs e 301, zero regressão (regra pétrea); cutover adiado isola o 301 até Gate C | firme |
| D051 | 2026-06-06 | **Editor de blocos do site (spec 011) = BlockNote** (React, sobre ProseMirror/TipTap, MIT). UX de blocos estilo Gutenberg pronta (slash/drag/toolbar), **não** o `wp-editor`. **Fonte da verdade dupla:** persistir `posts.block_doc JSONB` (documento de blocos, re-edição lossless) + `content_html` sanitizado derivado (consumido pelo SSG/`@artificio/content`). Posts importados do WP (só `content_html`) → `tryParseHTMLToBlocks` best-effort ao abrir; ganham `block_doc` na 1ª gravação. Validar fidelidade HTML/sanitização/bundle/custom-block no T11; fallback = TipTap+camada de blocos | requisito firme do mantenedor é UX de blocos; BlockNote entrega sem reconstruir a UX e gera HTML que cai no store; rota admin isola JS do público | firme |
| D052 | 2026-06-06 | **Roles/capabilities editoriais do site (spec 011) ficam no `apps/site`, NÃO em `packages/auth`.** SSO só tem `UserRole = "user"|"admin"`; papéis editoriais (Editor/Autor/Contribuidor) são conceito do módulo site → tabela `site_users` (`user_id` SSO → `editorial_role`); `packages/auth` **intocado** (auth sagrado, zero risco a mesas/accounts, sem SDD em shared). `admin` global do SSO = superusuário do site. Mapa role→capabilities (constante no site) + middleware `requireCapability(cap)` no backend; capabilities: edit_posts/publish_posts/edit_others_posts/delete_posts/manage_categories/upload_files/manage_users/manage_options/manage_curation/manage_redirects. Ownership via `posts.author_id` | não poluir o contrato SSO compartilhado com conceito editorial específico do site | firme |
| D053 | 2026-06-06 | **Pipeline SSG↔autoria do site (spec 011) = rebuild ATÔMICO + preview no Express.** Descoberta: `astro build` limpa `dist/` no início → servir `dist/` durante rebuild = downtime (R6 do entrypoint só cobre restart, não rebuild). Fix R37: buildar p/ dir temporário (`dist.next`) e **swap atômico** p/ `dist/` (`mv`/troca de symlink), Express serve o dist antigo até o swap (janela ≈ ms). Preview de rascunho (R8/R38): **Express renderiza** rota autenticada `/admin/preview/:id` (HTML sanitizado no shell/CSS do artigo, `@artificio/ui/styles.css`), **sem** adicionar adapter Node ao Astro (público segue SSG puro/zero-JS); fallback = preview client-side no editor | público não pode cair durante publicação; manter SSG puro evita peso/adapter no público | firme |
| D056 | 2026-06-07 | **Lock RW VM-wide `/tmp/artificio-vm-mutate.lock`** (resolve C1/C2 do review de docker-cleanup; eleva a tarefa a SDD Completo por tocar pipeline compartilhado). Deploys (`_deploy-module` mesas/site + `deploy-accounts`) pegam **shared** (`flock -s -w 600`, FD 8) → concorrem entre si, só esperam se manutenção segura exclusive. Manutenção (`docker-cleanup.yml` e futuros) pega **exclusive** (`flock -n -x`, FD 8, polling 5/5 min) e **segura durante todo o prune** → nunca prune concorrente a deploy. Sem deadlock (cleanup só FD8; deploy FD8 shared→FD9 por-módulo). Smoke: merge em `dev` redeploya mesasbeta+sitebeta com o lock. Também aplicados Q1 (guard numérico `GITHUB_RUN_ID`) e Q3 (keep-3 por ordem-default do docker, sem parsear `{{.CreatedAt}}`); Q2 rejeitado (sem lock = ocioso, não erro). Fix pleno do accounts no lock = ponte até CDX-310 (accounts→_deploy-module) | reviewers (Amazon Q + Codex) apontaram race real check→prune e accounts sem cobertura; lock RW exclui manutenção×deploy sem serializar deploys entre si | firme |
| D055 | 2026-06-07 | **Docker cleanup semanal = workflow próprio no monorepo (`docker-cleanup.yml`), cron domingo 03:00 UTC, via DEPLOY_\* (mecanismo SSH canônico).** Migrado do legado `mesas_rpg_artificio` (desativado lá: stub sem `schedule` + job `if:false`; precisa chegar na default `dev` do mesas p/ parar). Remove dangling + container parado >7d + cache de build >7d + mantém 3 imagens mais novas por repo. **NÃO toca volumes nem redes.** Cron = write autônomo na VM toda semana **NÃO viola** governança (manutenção via Actions é canônica + precedente legado). Nível **SDD Lite**. **Quiescência (R1, reusável):** antes de prunar, checa locks por-módulo do deploy (`/tmp/artificio-*-deploy.lock`) via `flock -n` e re-checa a cada 5 min (não trava o deploy). `timeout-minutes:45`, `permissions:{}`, `docker system df` antes/depois. Cron só vale em `main` (default branch) → ordem: artificio→main ANTES de desativar mesas | dockers cresceram, precisa limpeza semanal; fonte única no monorepo com secrets canônicos; padrão de espera reusável p/ futuros workflows de manutenção | firme |
| D054 | 2026-06-06 | **Admin do site (spec 011) = pacote PRÓPRIO `apps/site-admin`, toolchain no latest, sem pinar p/ baixo.** Astro 6 público usa vite 7; `@vitejs/plugin-react` 6 (latest) exige vite 8 → não cabem no mesmo pacote. Em vez de downgrade (gambiarra), o admin é SPA isolada em pacote próprio com stack latest mutuamente compatível: **vite 8 + @vitejs/plugin-react 6 + react 19.2 + react-router-dom 7 + @blocknote/{core,react,mantine} 0.51** (TS = padrão do repo 5.9, via consistência, não downgrade). Build vite → `apps/site-admin/dist`, **servida pelo Express do `apps/site` em `/admin`** (static + fallback SPA); buildada NA IMAGEM Docker (não depende de DB, ao contrário do Astro público). `deploy-site.yml` paths incluem `apps/site-admin/**`. Regra geral: **usar sempre versões mais recentes e boas práticas**; bump major do monorepo (Astro 5→6, TS 5→6) é decisão própria à parte (toca todos os módulos), não se faz inline numa feature. Acesso a dados do admin = SQL parametrizado sobre o adapter dual (pglite/pg) em `db/repo/*`; **Kysely adiado** (atrito de dialect pglite) | conflito real de major do vite entre Astro e plugin-react; separar respeita "latest + sem gambiarra" e isola o JS do admin do público zero-JS | firme |
| D057 | 2026-06-11 | **Glossário no monorepo serve em `glossario.artificiorpg.com`** (supera o hostname `glossariorpg.` do D017 só para o glossário). `glossariorpg.` foi alias histórico pré-monorepo e não é hostname ativo a preservar; não há requisito de 301 imediato a partir dele. Stack legada (React18→**19**/Vite/Tailwind + Express/pg) importada p/ `apps/glossario` (`@artificio/glossario-{frontend,backend}`), composes na `artificio_net`, deploy via `_deploy-module.yml` (`deploy-glossario.yml`). DB `glossario_v2` intacto. Spec 012 | mantenedor: glossário direto pro monorepo + hostname canônico curto; alias antigo inexistente | firme |
| D058 | 2026-06-11 | **`@artificio/ui` Header/Footer = shell ÚNICO reusável por TODOS os módulos** (site/mesas/accounts/glossário/futuros). Estrutura igual em todos; especialidades de cada site entram por **props/hooks**: `navItems` (nav do domínio Artifício, cross-módulo) + `moduleNav` (nav próprio do módulo) + `userMenu` + `actions` + `sessionOverride`. Para reusar com auth **não-SSO** (glossário pré-015), Header ganhou props **aditivas/retrocompatíveis** `onLogout`/`onLoginClick`/`loginLabel` (default = SSO; mesas/accounts/site intocados — build dos 3 verde). Glossário NÃO redefine chrome: `GlossarioHeader` compõe o Header compartilhado + injeta admin/changelog/notificações/sessão legada. Proibido duplicar shell/nav por módulo | mantenedor: "o que é compartilhado e já existe tem que ser reutilizado; nav do domínio + nav próprio, igual site/mesas" | firme |
| D059 | 2026-06-11 | **Glossário NÃO usa o runner de migrations do monorepo** (`apply_required_migrations.sh`/`schema_migrations`). DB `glossario_v2` já migrado pelo fluxo legado próprio; migrations históricas movidas p/ `apps/glossario/database/legacy/` (fora do glob `migration_*.sql` maxdepth 1) → runner no-op (igual site). `init.sql` (topo) só roda em volume novo. Adotar o framework no futuro exige baseline de `schema_migrations` no DB vivo + headers `@class/@requires-backup/...` | E003: runner tentaria reaplicar 12 migrations num DB pré-migrado e estouraria header/MAX_AUTO_PENDING | firme |
| D060 | 2026-06-11 | **Monorepo padroniza em Express 5** (uma única major). Glossário entra em express 5 (legado nativo); **mesas-backend converte de express 4 → 5** (spec 016) p/ eliminar o skew de tipos que já quebra o build no `dev` (E004: `@types/multer@2` traz tipos express-5 incompatíveis com express-4). Refina D007 (stack canônica) fixando a major do express | reviewers + build vermelho pré-existente; mistura de express 4/5 no workspace gera conflito de `@types/express-serve-static-core` | firme |
| D062 | 2026-06-12 | **Assets/UX compartilhados = fonte única em `packages/ui` via IMPORTAÇÃO, nunca cópia/sync** (spec 017). Favicon vira `faviconV2` data-URI + `applyFavicon()` em `brand.ts` (espelha logos `brandLogo*`); apps importam (site em build via `<link>`, SPAs via `applyFavicon()` no boot) — zero `faviconV2.png` em `apps/*/public`. Tema cross-subdomínio centralizado em `packages/ui/theme.tsx` (cookie único `artificio_theme` `Domain=.artificiorpg.com` + `applyTheme`/`setTheme`/`ThemeToggle`/`ThemeIcon`); `Header` ganha prop aditiva `showThemeToggle` (default false — só habilitar em módulo com CSS dark). Texto "presente da Artifício…" movido p/ `Footer.tsx` (+ espelho em `SiteFooter.astro`). Rename "módulo"→**"projetos"** (D-MARCA1) fica fora: depende de decisão de escopo própria | mantenedor: "o que é compartilhado tem que puxar de um lugar só e espalhar; sync duplica — quero dinâmica de importação de código já compartilhado". glossário/mesas sem dark CSS → toggle só habilitado onde dark funciona (accounts/site); dark de glossário/mesas = specs futuras | firme |
| D061 | 2026-06-12 | **Spec 015 — SSO + compat legado do glossário no ar (Gate D glossário fechado).** Glossário troca o auth próprio (email/senha BCrypt + JWT custom) por **Google OAuth via `accounts.` (D018)**: backend valida `artificio_session` com `@artificio/auth verifyToken` (`packages/auth` intocado); account-linking por email lower-case (`users.sso_user_id`); login/register de sessão legados → **410**. **Exceção autorizada e controlada à pétrea Google-only** (mantenedor, 2026-06-11), **restrita à migração**: `POST /api/migration/verify` aceita email+senha BCrypt legada **só para provar posse** de conta antiga **não-Google** → emite `migration_token` curto (10min, escopo `glossario-migration`, **não cria sessão**); `POST /api/migration/claim` (exige sessão Google + token) vincula `sso_user_id` e funde o auto-provisionado (repoint de FKs transacional, dedup de voto, só funde quem tem `password_hash=SSO_NO_PASSWORD`; 409 se o Google já é de outra conta legada real). BCrypt vive **só** no `/verify` (anti-enum: dummy-hash cost 10, resposta uniforme, rate-limit IP+email). Migration aditiva `migration_13_sso_link.sql` (online-safe). **Follow-up:** limpeza da coluna `password_hash` legada (futuro, aprovado). Prod live 2026-06-12 (promote `dev→main`, deploy glossario/accounts/mesas; smoke verde; WP raiz intocado). Hotfix #22: guards de rota esperam a sessão SSO resolver antes de redirecionar a `/login` | pétrea Google-only é inegociável, mas migrar contas não-Google sem perder termos/votos/comentários exige provar posse da conta antiga; exceção isolada num endpoint que nunca loga + limpeza futura | firme |
| D063 | 2026-06-12 | **Linguagem pública do produto usa "projetos"; "módulo" fica como termo técnico interno.** UI, navegação, rodapé, textos de onboarding e docs de retomada/produto devem falar em projetos quando o assunto for a experiência pública da suite. Documentação arquitetural, gates, isolamento, `apps/*`, workflows (`_deploy-module`) e nomes/identificadores técnicos preservam "módulo/module" | mantenedor pediu ajuste de terminologia + documentação antes do fluxo de prod; evita confundir usuário sem perder precisão operacional | firme |
| D064 | 2026-06-12 | **Laranja de marca canônico = `#FF5722`** (Material Deep Orange 500), supera o laranja `#FF9457` do D040. Mantenedor confirmou com certeza: a marca é o vermelho-laranja da UI (amostrado em `midias/telaprincipal.png` = `#FF5722`), não o pêssego `#FF9457` que o D040 amostrou do hexágono do logo (`Logo-PNG-Negativo-2.png`). `brandDeep` (hover/pressed/foco) = `#E64A19` (Deep Orange 700). Navy/ink `#020740` permanece (D040, não disputado). Fonte única `packages/ui` (`tokens.ts`/`styles.css`/`tailwind-preset.js`) corrigida p/ `#FF5722` + **paridade interna restaurada** (preset tinha drift `ink #10103A`/`brand #FC9054`, batendo com nada). Migração dos hexes locais duplicados (accounts `#ff9457`, mesas `#FF9457`, site) e do glossário (`#e85d26`) = fatias seguintes da Spec 020. Regra de contraste mantida: laranja = acento/marca, nunca texto de corpo sobre branco (AA) | mantenedor tem certeza da cor; D040 amostrou o logo, não o acento de UI; Spec 020 R9 | firme |
| D065 | 2026-06-12 | **Spec 020 R4/R8/R9 — piloto lua/sol = glossário ganha variante DARK; navy próprio migra junto p/ canônico.** Piloto único da habilitação de lua/sol (D-UX2) = **glossário** (não mesas): 21 telas, baixo blast radius (app de leitura), é a "base clara" da spec; mesas (+light) ficou de fora por ser app operacional em PROD com 134 telas dark-assumed. **Decisão de paleta dark do glossário** (alinhada à suite — site/mesas/accounts): canvas/page `#0F1830`, surface/cards `#1B2A4A`, surface-sutil `#16223E`, surface-forte `#22325A`, texto `#EEF1F8`, muted `#AAB3C7`, faint `#8A94AB`, borda `rgba(255,255,255,.12-.18)`, brand permanece `#FF5722` (`--artificio-brand`). **Navy migra junto** (R9/R2): `#1a2744`→**`#020740`** (canônico D040/D064) nos tokens light do glossário (`--color-brand-navy`/`--color-input-text`/`--color-text-primary`), pois o dark redefine superfícies de qualquer jeito — evita mexer 2x. **Técnica** (Tailwind v3, sem infra darkMode, ~500 utilities cruas): `darkMode:['selector','[data-theme="dark"]']` + **folha de remap** `[data-theme="dark"] .bg-white/.bg-gray-*/.text-gray-*/.border-gray-*` → tokens dark (especificidade vence, sem `!important`, sem tocar os 21 componentes) + override do bloco de CSS vars do `:root`. Backdrops `bg-black/NN` e `hover:bg-black` preservados (não remapeados). Habilitação do toggle = lua/sol via `setTheme`/`ThemeIcon` canônicos no `GlossarioHeader` (variant reativo p/ trocar logo navy↔negativo) + `applyTheme()` no boot + inline boot script p/ zero-flash. Gate: só fecha após checklist `dark-readiness-checklist.md` (T4) com AA medido. Mesas (+light) = fatia futura | mantenedor escolheu glossário+navy-junto (AskUserQuestion 2026-06-12); remap evita refactor dos 21 componentes; paleta dark reusa a escala navy já canônica da suite | firme |
| D066 | 2026-06-12 | **Spec 020 R4/R8 — mesas ganha variante LIGHT (lua/sol), com DEFAULT-DARK preservado (app operacional em PROD).** Segundo piloto (após glossário-dark D065). mesas = Tailwind v4, dark-only operacional; ~1873 ocorrências de utilities brancas (~28 classes `text-white`/`text-white/NN`/`bg-white/NN`/`border-white/NN`) + ~21 hexes hardcoded de superfície (`bg-[#13213f]`/`[#0F1A2E]`/`[#1B2A4A]`…) + tokens `--surface/--fg/--border/--fill`. **Técnica:** folha `[data-theme="light"]` em `index.css` (não toca os 134 componentes): (a) override dos tokens `:root` (fg branco→ink `#0B1220`, borders/fills branco→navy-alpha, surfaces dark→light `#FFFFFF/#EEF2F8/#F4F6FB`); (b) override `@layer base` (body, placeholder, select color-scheme, `.glass`/`.app-select`); (c) remap das utilities brancas → ink/navy-alpha proporcional à opacidade; (d) remap dos hexes hardcoded e gradientes → superfícies light; `text-[#020740]` (navy) preservado (bom contraste em light). Regras unlayered vencem utilities/`@layer` do Tailwind v4 sem `!important`. **DEFAULT-DARK (prod-safety):** mesas é operacional em produção no escuro; o boot NÃO pode virar light para quem nunca escolheu. Boot do mesas = light SOMENTE com **opt-in explícito do próprio mesas** = marcador local `mesas_theme==='light'` (gravado só ao alternar o tema dentro do mesas). O **cookie compartilhado `artificio_theme` NÃO conta** para o default do mesas — o `accounts` grava-o da preferência do SO no boot (review Codex PR #24), logo não é opt-in confiável. Ausência do marcador → **dark** (desvio deliberado do `resolveTheme` canônico). O toggle do mesas grava o cookie compartilhado (propaga cross-subdomínio) **e** o marcador `mesas_theme`. CSS default (sem atributo ou `[data-theme="dark"]`) = dark. Causa-raiz no accounts = backlog B9. **Validação local limitada:** mesas auth-gateia telas operacionais (redireciona a `accounts.`); smoke local cobre chrome/landing pública; AA das telas autenticadas (cards de mesa, painel, gestão, forms) = E2E do mantenedor. **Não habilitar nos dois em PROD ao mesmo tempo** sem cada um passar dark-readiness; hoje tudo LOCAL, sem commit/deploy | mantenedor pediu "parte para o mesas light"; remap evita refactor de 134 componentes; default-dark protege o app operacional de regressão visual em prod | firme |
| D067 | 2026-06-13 | **Tema lua/sol é COMPARTILHADO e persistente cross-subdomínio; cookie `artificio_theme` só é escrito em escolha EXPLÍCITA (revisa o mecanismo de default-dark do D066).** Mantenedor: "tem que ser compartilhado; os usuários não vão acessar todo o conteúdo, mas quando for acessar, tem que manter." A escolha de tema do usuário em qualquer módulo (glossário/site/accounts/mesas) vale em todos via o cookie único `artificio_theme` (`Domain=.artificiorpg.com`, 1 ano). **Correção de raiz (era débito B9 / review Codex):** `accounts` (`main.tsx`) e `site` (`Base.astro`) gravavam o cookie **no boot** a partir da preferência do SO, poluindo-o como falso opt-in. Agora **no boot todo app só LÊ+aplica** (`applyTheme`/`dataset.theme`), e o cookie só é escrito no **toggle explícito** (`setTheme`). Com isso o cookie vira opt-in confiável. **mesas volta a HONRAR o cookie** (substitui o marcador local `mesas_theme` do D066, que ficou desnecessário): `resolveMesasTheme` = `cookie ? cookie : 'dark'` (boot inline idem; toggle = `setTheme` só). Assim mesas herda a escolha compartilhada **e** propaga a sua. **Default sem nenhuma escolha:** glossário/site/accounts seguem a pref do SO (base clara); **mesas = dark** (operacional, ignora OS-prefers) — mesas só vira light se houver escolha explícita registrada no cookie. Toca apps `accounts`/`site`/`mesas` (frontend de tema; sem auth/contrato). Validação: `turbo build` + smoke de propagação cross-módulo | mantenedor exige persistência cross-módulo da escolha; a raiz (gravação implícita no boot) era o que forçava o workaround do D066; corrigida a raiz, mesas honra o cookie com segurança | firme |
| D068 | 2026-06-13 | **Comunicação padrão dos agentes = caveman ultra** (supera o nível implícito de D013). Português permanece obrigatório; nomes de arquivos, comandos, funções e identificadores permanecem literais. Usar modo normal só quando clareza/segurança exigir ou quando o mantenedor pedir explicitamente | economia máxima de tokens virou padrão operacional solicitado pelo mantenedor | firme |
| D069 | 2026-06-15 | **Contrato Real IP do ingress Cloudflare Tunnel.** Caminho confiavel = Cloudflare Tunnel -> `cloudflared` na `artificio_net` -> app. Subnet atual verificada = `172.18.0.0/16`. Apps nginx usam `TRUSTED_REAL_IP_FROM` + `real_ip_header CF-Connecting-IP` e repassam `$remote_addr`; apps Express diretos ou atras de nginx usam `TRUSTED_PROXY_CIDR` em `trust proxy`. Proibido contrato duravel com `$proxy_add_x_forwarded_for`, `trust proxy = 1` ou repasse cru de `$http_cf_connecting_ip` sem validar hop confiavel | evita spoofing de CF-Connecting-IP e balde unico de rate-limit/logs; padroniza todos os `*.artificiorpg.com` na VM | firme |
