# Dependências
node_modules/
.pnpm-store/
.venv/

# Build
dist/
dist-cjs/
build/
.turbo/
*.tsbuildinfo

# Ambiente / segredos — NUNCA versionar
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.crt
*.jks
*.keystore
id_rsa
id_rsa.*
*.keypublica.pub
*client_secret*.json
service-account*.json
secrets*.txt

# Backups (ficam fora do repo, em C:\projetos\artificiobackup)
*.dump
*.sql.gz
backup/
artificiobackup/

# Artefatos de deploy descartáveis (modelo tarball antigo — deploy canônico = git-pull, D039)
*.tgz
*.tar.gz
artificio-mesas-*.tgz

# Logs / temporários
logs/
*.log
tmp/
.tmp/
artifacts/
.DS_Store

# Docs internas de operação — NÃO publicar (recon de infra: containers, rotas, cofre)
# Mantidas só localmente + em C:\projetos\artificiobackup\docs-agents-internal-*
# Nota: usar /docs/agents/* (não docs/agents/) para permitir exceção de arquivo
# abaixo — padrão de diretório puro bloqueia negação (!) de arquivos dentro dele.
/docs/agents/*
# Exceção: fluxo Supervisor do OpenCode não tem recon de infra, é governança
# de agente pura — referenciado publicamente por AGENTS.md, precisa ser versionado.
!/docs/agents/opencode-supervisor-flow.md

# Editor
.vscode/
.idea/
.claude/launch.json
opencode.json

# Astro auto-generated types
.astro/

# Indice local legado (nao versionado)
.serena/
.codex/
/spec047-backup
# Amostras reais de import (dado real — nunca commitar). Ver specs/048 ambiente de teste.
temp/
/temp
# Added by code-review-graph
.code-review-graph/
