# Dependências
node_modules/
.pnpm-store/
.venv/

# Build
dist/
dist-cjs/
build/
.turbo/
*.tsbuildinfo

# Ambiente / segredos — NUNCA versionar
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
*.crt
*.jks
*.keystore
id_rsa
id_rsa.*
*.keypublica.pub
*client_secret*.json
service-account*.json
secrets*.txt

# Backups (ficam fora do repo, em C:\projetos\artificiobackup)
*.dump
*.sql.gz
backup/
artificiobackup/

# Artefatos de deploy descartáveis (modelo tarball antigo — deploy canônico = git-pull, D039)
*.tgz
*.tar.gz
artificio-mesas-*.tgz

# Logs / temporários
logs/
*.log
tmp/
.tmp/
artifacts/
.DS_Store

# Docs internas de operação — NÃO publicar (recon de infra: containers, rotas,
# subnet, nomes de secret, mapa de acesso). Mantidas só localmente + em
# C:\projetos\artificiobackup\docs-agents-internal-*
#
# `docs/agents/` PASSOU A SER VERSIONADO em 2026-09-03: o procedimento ali usa
# placeholder (`<CLONE_PROD>`, `<ACCOUNTS_DB>`) e a tradução vive só em
# `docs/agents-internal/values.md`, aqui embaixo. Publicar o "como" sem o "o quê".
/docs/agents-internal/

# Lock de ferramenta local do mantenedor (servidor MCP do opencode). Fora do
# workspace pnpm, do turbo e do CI — o repo não o instala. Versioná-lo só
# arrastava a árvore transitiva dele para o Dependency Review: 4 CVEs high em
# fast-uri reprovaram o check da PR #305 sem nada no produto usar o pacote.
/docs/agents/opencode-mcp/package-lock.json
/docs/agents/opencode-mcp/node_modules/

# Editor
.vscode/
.idea/
.claude/launch.json
opencode.json

# Astro auto-generated types
.astro/

# Indice local legado (nao versionado)
.serena/
.codex/
/spec047-backup
# Amostras reais de import (dado real — nunca commitar). Ver specs/048 ambiente de teste.
temp/
/temp
# Added by code-review-graph
.code-review-graph/
directMcpHost.js
/rtk

# Artefatos de sessao do Playwright MCP (snapshots, logs de console)
.playwright-mcp/
