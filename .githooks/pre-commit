#!/bin/sh
node scripts/api/pre-commit-verify.mjs
#!/bin/sh
# Installed by code-review-graph. Remove this file to disable pre-commit graph checks.
if command -v code-review-graph >/dev/null 2>&1; then
    PYTHONUTF8=1 code-review-graph update || true
    PYTHONUTF8=1 code-review-graph detect-changes --brief || true
fi
